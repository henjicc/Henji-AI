## 目标
- 页面背景改为 `#0a0b0d`，去除所有渐变背景
- 强调色统一为 `#007eff`，用于按钮、选中态、焦点环、进度与加载元素
- 删除相关操作保持现有红色，不做改动

## 全局更新
- 背景：将页面根容器背景替换为 `bg-[#0a0b0d]`
- 强调色：统一替换蓝紫混用为 `#007eff`；悬停采用 `hover:brightness-110` 保持层次
- 焦点态：`focus:ring-*` 统一为 `focus:ring-[#007eff]/50`
- 选中态：选项/标签等选中样式采用 `bg-[#007eff]/20 text-[#66b3ff]`（在深色背景上更清晰）

## 具体改动清单
- `src/App.tsx`
  - 背景：`min-h-screen bg-gradient-to-br from-gray-900 to-black ...` → `min-h-screen bg-[#0a0b0d] ...`（src/App.tsx:617）
  - 欢迎图标圆形底：`bg-gradient-to-r from-blue-500 to-purple-500` → `bg-[#007eff]`（src/App.tsx:816）
  - 加载指示：`border-blue-500`/`border-purple-500` → `border-[#007eff]`（src/App.tsx:725, 734）
  - 进度条：`bg-purple-500` → `bg-[#007eff]`（src/App.tsx:738）
  - 次级按钮（紫色）：`bg-purple-600/70 hover:bg-purple-600` → `bg-[#007eff] hover:brightness-110`（src/App.tsx:748）
  - 标签：`bg-blue-700/50` → `bg-[#007eff]/20 text-[#66b3ff]`（src/App.tsx:671）
- `src/components/SettingsModal.tsx`
  - 标题渐变：`bg-clip-text text-transparent bg-gradient-to-r from-blue-400 to-purple-500` → `text-[#007eff]`（src/components/SettingsModal.tsx:89）
  - 输入框焦点：`focus:ring-blue-500/50` → `focus:ring-[#007eff]/50`（src/components/SettingsModal.tsx:111, 134）
  - 主按钮：`bg-gradient-to-r from-blue-600 to-purple-600 ...` → `bg-[#007eff] hover:brightness-110 text-white`（src/components/SettingsModal.tsx:177）
- `src/components/MediaGenerator.tsx`
  - 焦点环统一：`focus:ring-purple-500/50`/`focus:ring-blue-500/50` → `focus:ring-[#007eff]/50`（多处：578, 640, 785, 835, 882, 930, 979, 1063, 1177）
  - 选中态：`bg-purple-500/20 text-purple-300` → `bg-[#007eff]/20 text-[#66b3ff]`（616, 816, 862, 912, 962, 1009）
  - 主操作按钮：`bg-gradient-to-r from-blue-600 to-purple-600 ...` → `bg-[#007eff] hover:brightness-110 text-white`（1188）
  - 开关/选项高亮：`bg-blue-500 text-white` → `bg-[#007eff] text-white`（725, 1036, 1046）
- 保留红色删除相关（不改动）
  - 例：`bg-red-600/60`, `bg-red-700/50`, 以及图片删除按钮（src/App.tsx:630, 709, 833–898；src/components/MediaGenerator.tsx:1101；src/components/WindowControls.tsx:44）

## Tailwind 配置（可选保持一致）
- 更新 `tailwind.config.js` 的暗色主题值（当前未广泛使用，但同步更清晰）：
  - `dark.bg: '#0a0b0d'`
  - `dark.accent: '#007eff'`
- 未来逐步将硬编码色替换为 `bg-dark-bg`、`text-dark-accent` 等，提升可维护性

## 视觉与细节
- 边框与分割线继续使用 `border-gray-700/50`，在新背景下对比度良好
- 滚动条维持当前灰色方案，视觉与暗背景协调
- 悬停/活跃态统一使用亮度提升而非色相变化，减少杂色

## 验证
- 启动现有开发服务后，检查：
  - 页面全局背景是否为纯色 `#0a0b0d`
  - 所有按钮/焦点环/进度与加载是否统一为 `#007eff`
  - 删除按钮与错误态红色是否保持
  - 黑暗背景下文本与边框对比度是否合规（WCAG AA 近似）

## 不改动说明
- 所有红色删除与错误提示保持原样，不做颜色替换

如确认，我将按上述清单执行改动，并在本地预览中核对效果。