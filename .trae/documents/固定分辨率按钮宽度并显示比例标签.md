## 目标
- 分辨率按钮固定宽度，避免内容变化导致面板位置移动
- 按钮文本显示选中的比例（如 16:9、1:1 或“智能”），不再显示具体像素数值

## 修改位置
- `src/components/MediaGenerator.tsx:730` 分辨率按钮的类名与文本
  - 类名增加固定宽度：`w-[120px]`
  - 文本：`selectedResolution === 'smart' ? '智能' : selectedResolution`

## 保留
- 面板内仍展示并可编辑具体尺寸（宽/高），不影响已有逻辑

## 验证
- 选择比例或修改尺寸后，按钮宽度不变，面板位置稳定
- 文本仅显示比例或“智能”，不显示像素数值