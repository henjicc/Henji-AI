## 目标
- 将主输入框背景调整为 `#1B1C21`
- 将历史记录内容展示区域的卡片背景统一为 `#1B1C21`（包含待处理/生成中/成功展示的容器）

## 改动范围与定位
- 主输入框（文本域）：`src/components/MediaGenerator.tsx:1177` 当前 `bg-gray-800/70` → 改为 `bg-[#1B1C21]`
- 历史记录展示卡片：`src/App.tsx`
  - 待处理卡片：`src/App.tsx:723` 当前 `bg-gray-800/50` → 改为 `bg-[#1B1C21]`
  - 生成中卡片：`src/App.tsx:732` 当前 `bg-gray-800/50` → 改为 `bg-[#1B1C21]`
  - 成功展示容器（图片/视频框）：`src/App.tsx:780, 806` 当前 `bg-[#131313]` → 改为 `bg-[#1B1C21]`
- 保持错误态红色卡片不变（增强可视反馈）

## 风格与可读性
- 边框与分割线继续使用现有灰色（如 `border-gray-700/50`），在新背景上保持良好对比度
- 不调整文案颜色，维持现有的可读性与层次

## 验证
- 本地预览检查：主输入框背景变为纯色 `#1B1C21`
- 历史记录各状态卡片背景统一为 `#1B1C21`
- 错误态红色卡片保留，不受影响

如确认，我将按上述行级改动执行并在本地预览验证效果。