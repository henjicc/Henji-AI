## 目标
- 去除下方输入区域内文本输入框（textarea）的描边，仅保留现有强调色焦点环

## 改动位置
- `src/components/MediaGenerator.tsx:1177` 文本域 `className`

## 修改方案
- 删除 `border` 与 `border-[rgba(46,46,46,0.8)]`，保留 `focus:ring-[#007eff]/50` 以维持可用性与可见焦点态

## 验证
- 文本输入框在非焦点态不再有描边；聚焦时仅显示蓝色焦点环

如确认，我将执行以上改动并在本地预览确认效果。