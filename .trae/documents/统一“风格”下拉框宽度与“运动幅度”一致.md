## 目标
- 将“风格”下拉框的显示宽度与“运动幅度”一致，避免视觉不齐。

## 定位
- “风格”容器：`src/components/MediaGenerator.tsx:879` 当前为 `w-auto min-w-[180px] relative`
- “运动幅度”容器：`src/components/MediaGenerator.tsx:927` 当前为 `w-auto min-w-[80px] relative`

## 修改方案
- 把“风格”容器的 `min-w-[180px]` 改为 `min-w-[80px]`，与“运动幅度”保持一致：
  - `src/components/MediaGenerator.tsx:879` → `w-auto min-w-[80px] relative`
- 其余内部元素不需要调整，宽度由容器和内部内容自适应即可。

## 验证
- 预览生成器顶部参数区，确认“风格”与“运动幅度”宽度一致，行内间距保持原有和谐。

如确认，我将按以上改动执行并在本地预览验证效果。