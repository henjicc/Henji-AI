## 目标
- 在 `API文档/派欧云/Minimax_Hailuo-02.md` 的 33-39 行补充清晰的联动规则与校验说明，确保使用者知道合法组合与客户端行为。

## 修改要点
- 明确合法组合：
  - 6 秒：`768P` 或 `1080P`
  - 10 秒：仅 `768P`
- 联动规则：
  - 当 `duration=10` 时，`resolution` 必须为 `768P`（客户端自动锁定）。
  - 当 `duration=6` 时，`resolution` 可选 `768P/1080P`（客户端仅显示这两项）。
- 校验说明：
  - 非法组合将导致请求被拒（400），适配器已规范化并在 UI 中限制。
- 示例提示：
  - 10 秒示例：`{"duration":10,"resolution":"768P"}`
  - 6 秒示例：`{"duration":6,"resolution":"1080P"}`

## 文件与范围
- 直接更新 `Minimax_Hailuo-02.md` 的 33-39 行，使用简洁的中文要点说明与两条示例。