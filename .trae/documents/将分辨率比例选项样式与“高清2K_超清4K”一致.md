## 目标
- 分辨率弹窗中的“比例”按钮恢复为填充样式，选中与未选中状态与“高清2K/超清4K”一致

## 对齐的样式规则
- 选中态：`bg-[#007eff] text-white`
- 未选中态：`bg-zinc-700/50 text-zinc-300 hover:bg-zinc-600/50`
- 内部比例矩形：统一使用白色描边（保证在不同填充背景上的可读性），不再根据选中态改变为强调色描边

## 修改位置
- `src/components/MediaGenerator.tsx`
  - 按钮容器类名（比例按钮）：约 `771–775` 行，将现有的描边高亮改为填充高亮（与 2K/4K 按钮一致）
  - 内部矩形边框逻辑：约 `780–792` 行，统一为 `border-white`

## 验证
- 打开分辨率设置弹窗，比例按钮的选中与未选中视觉与“高清2K/超清4K”完全一致
- 内部比例矩形在两种状态下均清晰可见

如确认，我将按以上修改执行并在本地预览确认效果。