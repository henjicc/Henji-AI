## 目标
- 不仅是文本域，而是将下方整块“输入区域”（图片预览、文本输入、生成按钮、操作按钮）统一使用背景 `#1B1C21`。

## 改动定位
- 输入区域容器：`src/components/MediaGenerator.tsx:1074` 当前为 `div className="relative"`

## 修改方案
- 为输入区域容器添加背景、圆角、边框与内边距，形成统一的深色卡片：
  - `className`: `relative bg-[#1B1C21] rounded-xl border border-zinc-700/50 p-4`
- 保留内部元素（图片预览、文本域、生成按钮、操作按钮）现有样式以保证交互与布局稳定；文本域已设置为 `#1B1C21`，与容器背景一致不会突兀。

## 验证
- 预览页面下方整块输入区域应呈现为一致的深色卡片背景，边界与内容间距协调，生成按钮定位正常。

如确认，我将按上述修改执行并在本地预览验证视觉效果。