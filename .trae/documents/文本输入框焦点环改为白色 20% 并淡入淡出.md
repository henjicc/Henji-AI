## 目标
- 将输入区域文本输入框的焦点环改为白色 20% 不透明度（ring 白色，透明度 0.2）
- 为焦点环加入淡入淡出的过渡动画

## 修改位置
- `src/components/MediaGenerator.tsx:1177` 文本域 `className`

## 调整内容
- `focus:ring-[#007eff]/50` → `focus:ring-white/20`
- 增加过渡：将 `transition-all` 调整为 `transition-shadow duration-300 ease-in-out`，使 ring（box-shadow）变化更平滑

## 验证
- 非聚焦时无 ring；聚焦时出现白色 20% 的 ring，并在获得/失去焦点时平滑淡入/淡出

如确认，我将执行以上修改并在本地预览。