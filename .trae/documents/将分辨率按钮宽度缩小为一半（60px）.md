## 目标
- 把分辨率按钮的固定宽度从 120px 调整为 60px（当前一半），保持面板位置稳定。

## 修改位置
- `src/components/MediaGenerator.tsx` 分辨率按钮类名：将 `w-[120px]` → `w-[60px]`

## 验证
- 按钮宽度缩至一半，文本仍显示比例或“智能”，面板位置不再移动。

如确认，我将进行修改并在本地预览确认 HMR 生效。