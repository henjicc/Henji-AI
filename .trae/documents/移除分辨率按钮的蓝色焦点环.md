## 目标
- 点击分辨率按钮后不再出现蓝色高亮描边（焦点 ring）

## 修改位置
- `src/components/MediaGenerator.tsx` 分辨率按钮类名处（约 730 行）
  - 移除 `focus:ring-2 focus:ring-[#007eff]/50`
  - 保留 `focus:outline-none`，避免浏览器默认 outline

## 验证
- 点击分辨率按钮不再出现蓝色 ring，面板交互不受影响