## 目标
- 点击“最大数量”输入框时不再出现蓝色或白色的焦点环（完全移除 ring/outline）

## 修改位置
- `src/components/MediaGenerator.tsx:1326` 输入框 `className`

## 变更内容
- 删除 `focus:ring-*` 类
- 显式禁用所有 ring/outline：`outline-none focus:outline-none focus-visible:outline-none ring-0 focus:ring-0 focus-visible:ring-0`

## 验证
- 点击“最大数量”输入框不再出现任何焦点环；其余交互与样式保持不变