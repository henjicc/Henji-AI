## 目标
- 将下方整块“输入区域”（图片预览、文本输入、生成按钮、操作按钮）的背景统一为 `#131313`，不透明度 70%

## 修改方案
- 输入区域容器：`src/components/MediaGenerator.tsx:1074`
  - 将 `bg-[#1B1C21]` 改为 `bg-[#131313]/70`
- 文本输入框：`src/components/MediaGenerator.tsx:1177`
  - 将文本域背景改为 `bg-transparent`，让容器统一背景透出，避免双层不透明度叠加
- 保持边框、圆角、内边距与按钮强调色不变

## 验证
- 整块输入区域呈现为统一的 `#131313/70` 背景；文本域内部不再出现与容器背景不一致的色块

如确认，我将执行以上调整并在本地预览确认效果。