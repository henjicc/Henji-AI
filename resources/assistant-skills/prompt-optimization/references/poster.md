# 文字海报

保留用户文字原文、语言和大小写。先确定标题、补充信息、品牌或标志的层级与位置；不要在提示词尾部加入与需求冲突的“无文字”。

字多时通过分组、留白和字号层级提高可读性，不把每行都写成主标题。背景和装饰避免挤占正文。需要精确排版时优先使用软件已有的可编辑文字/合成能力，不承诺生成模型百分百拼写正确。

修改已有海报只描述替换的文字及需要保持的字体、色彩和布局。字数变化较大时允许必要重排并说明约束，不能要求互相冲突的固定宽度与固定字号。

检查准确性需要实际看结果；当前助手不能识图时明确未核对文字，不把请求成功当作排版通过。
