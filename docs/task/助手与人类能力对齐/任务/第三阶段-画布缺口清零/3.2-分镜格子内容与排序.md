# 3.2 分镜格子内容与排序

## 任务状态

- 状态：待开始
- 所属阶段：第三阶段 · 画布缺口清零
- 前置任务：3.1
- 关联任务：无
- 是否可以并行：否（与 3.1 同在画布集合执行器上作业，避免冲突）

## 任务目标

让助手能改分镜格子的内容、能调整格子顺序。

烧掉最后 2 项画布 gap：`updateStoryboardFrame`、`reorderStoryboardFrame`。`GAP_BASELINE` 降 2。

**做完这一项，画布 5 项缺口全部归零**（配合 3.1）。

## 当前情况

### 缺口的成因：两侧都没有入口

分镜数据存在两个地方，但两边都不可写：

| 位置 | 现状 |
|---|---|
| `canvas.node` 的节点内部数据 | 分镜格子（frames）是 `storyboardNode` 类型节点 `data` 里的一个数组。`canvas.node` 只注册了 `display_name` 与 `position` 两条可写属性，**格子内容不在其中** |
| `storyboard.card` 实体 | 整个 `storyboard` 领域标着 `writeExclusion: '分镜是画布工程的只读摘要投影，写入请用 canvas.* 的通用动词。'`（`src/features/canvas/application/storyboardReflection.ts`） |

`storyboard` 的排除理由**指向了 `canvas.*`，但 `canvas.node` 那边并没有对应的写入路径**——两边互相指，形成闭环。这是本次盘点发现的一个真实设计漏洞。

### store 侧

- `updateStoryboardFrame(nodeId, frameId, data, options)`：改某个格子的内容
- `reorderStoryboardFrame(nodeId, draggedFrameId, targetFrameId)`：拖拽调整顺序

都在 `src/stores/canvasStore.ts`（约 1318 / 1377 行）。

### 相关既有能力

`update_canvas_node`（`src/core/assistant/capabilities/canvasMutationApplicationCapabilities.ts`）——执行时确认它的输入 schema 能不能表达「改某个格子」。若能，这一项可能只是没接上，而不是缺路径。

`list_storyboard_projects` / `get_storyboard_project`（只读）在 `toolboxApplicationCapabilities.ts`。

## 实施方案

先判断走哪条路，两个候选：

### 候选 A：在 `canvas.node` 上补格子属性

给 `storyboardNode` 类型的节点补一条可写属性（如 `canvas.node.storyboard_frames`，值类型 `json` 带 schemaRef）。

优点：与 `storyboard` 的排除理由一致（写入走 `canvas.*`），闭环解开。
缺点：`canvas.node` 的属性对所有节点类型共享，加一条只对 `storyboardNode` 有意义的属性，需要在可写性判定里按节点类型区分。

### 候选 B：把 `storyboard.card` 改为可写

给 `storyboard.card` 注册可写属性并配执行器，收窄 `storyboard` 的 `writeExclusion`。

优点：语义更直接，「改分镜格子」就是改 card。
缺点：与现有「分镜是只读投影」的设计取向相反，要重新论证；且 card 的稳定引用与 canvas.node 的关系需要理清。

**执行时必须先读代码判断，把结论和理由写进执行记录，不要凭直觉选。**

关键判断依据：分镜格子是否有独立于画布节点的身份？若 card 只是 node.data 的一个视图，候选 A 更正；若 card 有自己的生命周期，候选 B 更正。

## 执行步骤

1. 读 `src/stores/canvasStore.ts` 的 `updateStoryboardFrame` 与 `reorderStoryboardFrame`，确认格子数据结构与它在节点 data 里的位置。
2. 读 `src/features/canvas/application/storyboardReflection.ts`，确认 `storyboard.card` 的属性、引用格式、以及它与 `canvas.node` 的映射关系。
3. 读 `src/core/assistant/capabilities/canvasMutationApplicationCapabilities.ts` 的 `update_canvas_node` 输入 schema，确认它能否表达「改某个格子」。**若已经能，这一项就不是缺路径而是没接上，直接接上即可。**
4. **写下候选选择（A 或 B）与理由**，填进执行记录。判断依据是「分镜格子是否有独立于画布节点的身份」。
5. 按结论实现写入路径：
   - 候选 A：在画布 Fields 文件（1.3 产出）补属性 + 按节点类型的可写性判定 + 写入实现
   - 候选 B：给 `storyboard.card` 注册可写属性 + 执行器 + 收窄 `writeExclusion`
6. 处理 `reorderStoryboardFrame`：判断顺序是由数组下标还是由某个字段决定；能表达成属性写入则绑属性，否则考虑集合的 reorder 操作。
7. 更新 `src/features/canvas/application/canvasStoreLedger.ts`：两条 gap 改绑定。
8. `GAP_BASELINE` 降 2。
9. 新建或扩展测试：改格子内容后节点数据正确、顺序调整后格子排列正确、改动可撤销。
10. **验证闭环已解开**：确认 `storyboard` 的排除理由指向的路径**真实存在且可用**（这是本项要修的设计漏洞本身）。若采用候选 A，`storyboardReflection.ts` 的排除理由要更新成指向具体的属性 id。
11. 运行 `npx tsc --noEmit && npm run lint && npm run check:assistant-capabilities`。
12. 运行 `npx vitest run src/features/canvas src/features/assistant/applicationCapabilities`。
13. **实机验证交给用户**：让助手改某个分镜格子的描述文字，在画布上确认改动生效。

## 涉及内容

### 修改文件（视候选而定）

- 候选 A：画布 Fields 文件（1.3 产出）、`src/features/canvas/application/canvasReflection.ts`、`canvasMutationExecutor.ts`、`storyboardReflection.ts`（更新排除理由指向）。
- 候选 B：`src/features/canvas/application/storyboardReflection.ts`（注册可写属性、收窄排除）、新增 storyboard 写入执行器、`applicationControlRegistry.ts` 注册。
- 两者共同：`src/features/canvas/application/canvasStoreLedger.ts`、`storeActionCoverage.test.ts`。

### 可能新增

- 分镜写入的执行器与测试文件（候选 B 时）。

## 验收标准

- [ ] 候选选择（A 或 B）及理由已写入执行记录，依据是「分镜格子是否有独立身份」的代码事实。
- [ ] 助手能改分镜格子内容，有测试证明。
- [ ] 助手能调整格子顺序，有测试证明。
- [ ] **`storyboard` 的排除理由所指向的写入路径真实存在**——两边互相指的闭环已解开，理由文字更新为指向具体属性 id。
- [ ] 改动可撤销（走画布既有历史机制，未绕开）。
- [ ] `GAP_BASELINE` 已降 2；**画布 5 项缺口全部归零**（配合 3.1）。
- [ ] `npm run check:assistant-capabilities` 通过。
- [ ] 实机验证步骤已交给用户，并记录反馈。
- [ ] 总览任务状态已更新。

## 执行记录

<!-- 执行时按 00-任务总览.md 的规则填写 -->
