# 2.2 镜头卡状态捕获入口

## 任务状态

- 状态：待开始
- 所属阶段：第二阶段 · 三维缺口清零
- 前置任务：2.1
- 关联任务：无
- 是否可以并行：否（依赖 2.1 的镜头卡集合写入就位）

## 任务目标

判断并处理 `captureIntoSelectedShot` 这一项 gap：要么给助手一个对应入口，要么确认建模语义已经够用、改为 `excluded` 并写明理由。

烧掉 1 项 gap。`GAP_BASELINE` 从 13 降到 12。

**本任务的实质是一个判断，不是一次必然的实现。** 结论两种都合法，但必须有依据。

## 当前情况

### 这个动作是什么

`captureIntoSelectedShot(objectIds)`（`src/features/cameraStage/store/shotSlice.ts` 约 483 行）：把当前场景里对象的状态**记录进当前选中的那一张镜头卡**。这是简易模式的核心动作——用户摆好姿势/位置，点一下「记录到这张卡」。

### 为什么它成了缺口，但又不一定该补

助手侧已经有 `updateObjectAcrossShots`（`src/features/cameraStage/store/cameraStageStore.ts`），它是**建模语义**：改动同步进所有关键帧卡，不在单张卡上留点。

这个语义是上一轮为修一个真实 bug 引入的：助手放了个白色球体，播放时它一边移动一边变色。原因是新对象由 `syncAddedObjectToShots` 以默认状态写进所有卡，之后的 `updateObject` 在简易模式下只捕获进当前选中的那一张——于是球体在 1 张卡上是「新位置 + 白色」、其余卡上是「默认位置 + 默认颜色」，播放时插值。回归测试在 `src/features/cameraStage/store/modelingWrite.test.ts`。

所以：**给助手开放「只记录到某一张卡」，等于把当初修掉的那个坑重新打开一半。**

但反过来说，做动画本来就需要「这一刻是这样、下一刻是那样」，一律同步到所有卡则做不出任何动画——助手做动画目前走的是关键帧集合写入（`camera_stage.keyframe`），不走镜头卡。

### 判断需要的事实

执行时要确认：

1. 简易模式下，助手用关键帧集合写入能不能做出与「逐卡捕获」等效的动画？（读 `src/features/cameraStage/domain/shotCompiler.ts` 的 `compileShotsToAnimation`，确认镜头卡与关键帧轨道的关系）
2. 如果能，`captureIntoSelectedShot` 对助手就是冗余入口，应 `excluded('internal')`。
3. 如果不能（简易模式的卡状态与专业模式的轨道是两套数据），则助手在简易模式下确实缺一个入口，应补。

## 实施方案

### 分支 A：确认关键帧集合写入已覆盖 → 改为 excluded

账本条目改为：

```ts
captureIntoSelectedShot: {
  kind: 'excluded',
  category: 'internal',
  reason: '<写明简易模式卡状态如何由关键帧轨道表达，以及助手为什么用 camera_stage.keyframe 的集合写入即可；'
    + '并说明只往单张卡写会重现"新对象在其余卡上停留在默认状态、播放时插值"的问题>',
}
```

### 分支 B：确认确实缺入口 → 补一条属性或集合路径

优先考虑表达成属性写入而不是新增专用能力（`AGENTS.md`）。可能的形态：`camera_stage.shot` 上加一条「捕获对象状态」的写入属性，值为对象引用列表。若实在无法表达成属性，才考虑语义能力，并在执行记录里书面回答「为什么不能表达成属性写入」。

无论哪个分支，都要在 `重要记录.md` 的记录 004 里补上最终结论，把「待确认点」改为已确定。

## 执行步骤

1. 读 `src/features/cameraStage/domain/shotCompiler.ts`，弄清简易模式镜头卡（`StageShot.objectStates`）与动画轨道（`animation.tracks`）的关系：是编译产生的，还是两套并存的数据。
2. 读 `src/features/cameraStage/store/modelingWrite.test.ts`，理解上一轮修的那个 bug 的完整机制。
3. 读 `src/features/cameraStage/application/cameraStageKeyframeCollection.test.ts`，确认助手当前能做出哪些动画。
4. **写下判断结论**（分支 A 或 B）及依据，填进本文件的执行记录。
5. 按结论执行：分支 A 改账本 `excluded`；分支 B 补写入路径并加测试。
6. 更新 `重要记录.md` 记录 004 的待确认点。
7. `GAP_BASELINE` 13 → 12。
8. 运行 `npx tsc --noEmit && npm run lint && npm run check:assistant-capabilities`。
9. 运行 `npx vitest run src/features/cameraStage`。

## 涉及内容

### 修改文件

- `src/features/cameraStage/application/cameraStageStoreLedger.ts`：`captureIntoSelectedShot` 条目改绑定或改 `excluded`。
- `src/features/assistant/applicationCapabilities/storeActionCoverage.test.ts`：`GAP_BASELINE` 13 → 12。
- `docs/task/助手与人类能力对齐/重要记录.md`：记录 004 补最终结论。

### 分支 B 额外涉及

- `src/features/cameraStage/application/cameraStageReflection.ts`、对应 Fields 文件、写入执行器、新增测试。

## 验收标准

- [ ] 判断结论（分支 A 或 B）及依据已写入执行记录，依据引用了具体文件与代码事实，不是推测。
- [ ] 若为分支 A：`excluded` 理由说明了简易模式卡状态由什么表达、助手用什么替代，且通过 `weakExclusions` 校验。
- [ ] 若为分支 B：新增路径优先是属性/集合写入；若新增了专用能力，执行记录里书面回答了「为什么不能表达成属性写入」。
- [ ] 无论哪个分支，都不重新打开「新对象在其余卡上停留默认状态、播放时插值」这个已修问题——`modelingWrite.test.ts` 保持全绿。
- [ ] `重要记录.md` 记录 004 的待确认点已改为已确定。
- [ ] `GAP_BASELINE` 已降到 12。
- [ ] `npm run check:assistant-capabilities` 通过。
- [ ] 总览任务状态已更新。

## 执行记录

<!-- 执行时按 00-任务总览.md 的规则填写 -->
