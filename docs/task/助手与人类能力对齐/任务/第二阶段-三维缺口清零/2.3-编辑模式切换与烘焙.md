# 2.3 编辑模式切换与烘焙

## 任务状态

- 状态：待开始
- 所属阶段：第二阶段 · 三维缺口清零
- 前置任务：1.3
- 关联任务：2.4（专业模式是精细动画的前提）
- 是否可以并行：可与 2.1 / 2.4 / 2.5 并行

## 任务目标

让助手能在简易模式与专业模式之间切换，并能执行烘焙。

烧掉 2 项 gap：`setEditorMode`、`bakeToProMode`。`GAP_BASELINE` 降 2。

**这一项的实际价值可能高于表面**：用户提过「我们在用户界面上做了一些简化，所以有些操作在视图下面做不了」。专业模式才有完整的关键帧编辑，这条挡住了助手做精细动画。

## 当前情况

### 缺口现状

`camera_stage.project.editor_mode` 属性标着：

```
readOnly: '编辑模式只能通过正式烘焙操作切换。'
```

位置：`src/features/cameraStage/application/cameraStageReflection.ts` 的 `propertiesByEntity[ENTITY_TYPES.project]`（约 119 行）。

**这句理由只对「简易 → 专业」成立。** 烘焙（`bakeToProMode`）把镜头卡状态编译成关键帧轨道，是有损且不可逆的；但「专业 → 简易」和同模式内切换没有这个约束。现在一句话把三种情况一起挡住了。

### store 侧

- `setEditorMode(mode)`：直接切换模式
- `bakeToProMode()`：把简易模式的镜头卡烘焙成专业模式的关键帧轨道

两者都在 `src/features/cameraStage/store/cameraStageStore.ts`。**执行时必须先读懂 `bakeToProMode` 的实现**，确认：
- 烘焙是否真的不可逆（烘焙后原镜头卡数据是否丢失）
- `setEditorMode` 直接切到 `pro` 而不烘焙会发生什么（是否等价于丢掉所有简易模式内容）

这两个问题的答案决定了本任务的设计。**不要假设，去读代码。**

### 风险等级

烘焙若确实不可逆，属于 `AGENTS.md` 说的需要审批的操作。项目已有风险分级（R0–R4）与审批机制，`ApplicationCapabilityDefinition` 有 `risk` 字段，引擎在 `commit` 处按 `requiresApproval` 拦截。

## 实施方案

分两件事处理，因为它们的可逆性不同：

### 1. `editor_mode` 属性放开（可逆的那部分）

把属性从整体只读改为可写，但在写入执行器里对**不可逆方向**做额外处理：

- 若目标模式与当前相同 → 空操作
- 若「专业 → 简易」→ 直接切（确认是否有损；若有损同样要审批）
- 若「简易 → 专业」→ 这一跳等价于烘焙，见下

### 2. 烘焙（不可逆的那部分）

若确认不可逆，注册为带审批的语义操作能力，`risk` 设为 `R2` 或更高、`destructive: true`、`supportsUndo` 按实际情况填。

**注意**：这是本计划里少数几个「确实该新增专用能力」的地方之一——烘焙是一次数据形态转换（卡状态 → 关键帧轨道），不是设值。在执行记录里书面记下这个理由。

若读代码后发现烘焙**可逆**（原数据保留），则不需要专用能力，直接作为 `editor_mode` 的一次属性写入即可，`GAP_BASELINE` 同样降 2。

## 执行步骤

1. 读 `src/features/cameraStage/store/cameraStageStore.ts` 的 `setEditorMode` 与 `bakeToProMode` 实现，回答两个问题并写进执行记录：
   - 烘焙后原镜头卡数据是否保留？
   - `setEditorMode('pro')` 不经烘焙直接切，会不会丢内容？
2. 读 `src/features/cameraStage/domain/shotCompiler.ts`，确认卡状态与关键帧轨道的转换方向是否单向。
3. 根据结论选择设计：
   - 可逆 → 纯属性写入
   - 不可逆 → 属性写入（可逆方向）+ 带审批的语义能力（烘焙方向）
4. 修改 `cameraStageReflection.ts`：`editor_mode` 去掉整体 `readOnly`，或把理由收窄到只覆盖真正不可逆的那一跳。
5. 在对应 Fields 文件（1.3 产出）里补 `editor_mode` 的写入实现。
6. 若需要语义能力：在 `src/core/assistant/capabilities/cameraStage*ApplicationCapabilities.ts` 定义，在 `builtinApplicationCapabilityRegistry.ts` 注册，实现 `ApplicationSemanticOperationExecutor` 并在 `applicationControlRegistry.ts` 注册。
7. 更新账本：`setEditorMode` 与 `bakeToProMode` 两条 gap 改为对应绑定。
8. `GAP_BASELINE` 相应降 2。
9. 新建或扩展测试：切换模式生效、不可逆方向需要审批（若适用）、切换后场景内容符合预期。
10. 运行 `npx tsc --noEmit && npm run lint && npm run check:assistant-capabilities`。
11. 运行 `npx vitest run src/features/cameraStage src/core/assistant`。
12. **实机验证交给用户**（涉及界面观察）：让助手切到专业模式，确认时间轴面板出现完整关键帧编辑；再让助手切回，确认内容符合预期。

## 涉及内容

### 修改文件

- `src/features/cameraStage/application/cameraStageReflection.ts`：`editor_mode` 只读理由收窄或移除。
- 三维 project 的 Fields 文件（1.3 产出）：补 `editor_mode` 写入实现。
- `src/features/cameraStage/application/cameraStageStoreLedger.ts`：两条 gap 改绑定。
- `src/features/assistant/applicationCapabilities/storeActionCoverage.test.ts`：`GAP_BASELINE` 降 2。

### 可能新增（取决于步骤 1 的结论）

- `src/core/assistant/capabilities/` 下的烘焙语义能力定义。
- 对应的 `ApplicationSemanticOperationExecutor` 实现。
- 烘焙行为测试。

## 验收标准

- [ ] 烘焙可逆性的结论已写入执行记录，依据是实际代码而非推测。
- [ ] `editor_mode` 的只读限制已收窄到**只覆盖真正不可逆的那一跳**，可逆方向助手可写。
- [ ] 若新增了语义能力，执行记录里书面回答了「为什么不能表达成属性写入」，且 `risk` / `destructive` 与实际不可逆性一致。
- [ ] 若烘焙不可逆，未经审批的调用被拒绝，有测试证明。
- [ ] 切换模式后场景内容符合预期（有测试或用户实机确认）。
- [ ] `GAP_BASELINE` 已降 2。
- [ ] `npm run check:assistant-capabilities` 通过。
- [ ] 实机验证步骤已写清交给用户，并记录用户反馈。
- [ ] 总览任务状态已更新。

## 执行记录

<!-- 执行时按 00-任务总览.md 的规则填写 -->
