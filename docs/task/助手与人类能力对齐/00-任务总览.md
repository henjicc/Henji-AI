# 助手与人类能力对齐

## 任务目标

用户的要求是一句话：**「人能用这个软件做什么，智能助手就能做什么」**。

现状不是被权限挡住，而是**登记漏了**。一个界面功能要让助手可用，需要在四个地方分别登记（属性描述、读取、写入、账本），缺任何一处都是静默失效——没有报错，助手安静地少一块能力，只有用户实机撞上才发现。三维场景外观 24 项、`camera_stage.shot.time` 都是这么漏的。

前置工作（已完成，见提交 `ab25cd4` / `6524272` / `ab73d10`）已经建好三道门禁，把差集变成了一个可数的数字：**21 项**，播放控制 5 项在前置工作阶段已补齐，剩 16 项（三维 11、画布 5）。第二阶段（2.1–2.5）已把三维 11 项全部烧掉：镜头卡增删排序 3、镜头卡状态捕获 1、编辑模式与烘焙 2、姿态直接写入 2、清空轨道与轨迹控制点 3。**当前剩 5 项，全部是画布**。

本次任务要做两件事：

1. **降低登记成本**——把四处登记压成一处。这是根因：不改它，以后每加一个界面功能都要重走一遍四处，缺口会持续再生。
2. **把剩余缺口清零**——三维 11 项、画布 5 项，以及尚未建账的 15 个 store、生成页输入态、图片标注编辑器。

最终结果：`storeActionCoverage.test.ts` 里的 `GAP_BASELINE` 降到 0，`ASSISTANT_BLIND_FEATURES` 白名单清空，并且新增界面功能时助手同步接上的成本接近零。

### 属于本次任务

- 统一字段定义机制，收敛属性描述/读取/写入/账本四处登记。
- 三维运镜剩余 11 项缺口（镜头卡增删排序、编辑模式、姿态、轨迹、清空轨道）。
- 画布剩余 5 项缺口（清空、解散分组、重做、分镜格子改与排序）。
- 尚未建账的 15 个 store，以及修复门禁清点的 `src/stores/` 盲区。
- 受保护设置中**无安全理由**的两项松绑（`models.visibility`、`updates.configuration`）。
- 生成页输入态从 React 局部态提升为可寻址领域态并注册。
- 图片标注编辑器（imageMark）的领域层提升与注册。

### 不属于本次任务

- 新增业务功能。所有改动都是把**界面已有能力**接到助手能走的路径上。
- 删除数据分级（C0–C3）、暴露面（ui/assistant/local_adapter）、五档风险等级等既有安全机制。它们对单机应用偏重，但在跑且没添麻烦，拆除风险大于收益。
- 助手模型侧调优（提示词措辞、模型选择、路由策略）。
- 涉及真实密钥或系统文件选择器的 5 项受保护设置（技术上渲染层做不到，不是顾虑）。
- MCP、HTTP、插件等外部适配器。

## 当前进度

- 当前阶段：第三阶段 · 画布缺口清零
- 当前任务：3.1 画布清空解散与重做
- 整体状态：进行中
- 已完成任务数量：8
- 总任务数量：20
- 当前阻塞问题：无

## 阶段列表

| 阶段 | 阶段目标 | 状态 |
|---|---|---|
| 第一阶段 · 收敛登记成本 | 四处登记压成一处，门禁改为从统一定义派生 | 已完成 |
| 第二阶段 · 三维缺口清零 | 三维 11 项 gap 归零 | 已完成 |
| 第三阶段 · 画布缺口清零 | 画布 5 项 gap 归零 | 待开始 |
| 第四阶段 · 账本覆盖全部 store 与设置松绑 | 15 个未建账 store 全部归类，修门禁盲区，松绑 2 项设置 | 待开始 |
| 第五阶段 · 生成页输入态 | 生成页输入从 React 局部态提升为可寻址领域态并注册 | 待开始 |
| 第六阶段 · 图片标注编辑器 | imageMark 从全域失明到可读可写 | 待开始 |

## 任务清单

| 编号 | 任务名称 | 状态 | 前置任务 | 任务文件 |
|---|---|---|---|---|
| 1.1 | 设计统一字段定义机制 | 已完成 | 无 | [1.1](任务/第一阶段-收敛登记成本/1.1-设计统一字段定义机制.md) |
| 1.2 | 三维场景外观迁移到统一定义 | 已完成 | 1.1 | [1.2](任务/第一阶段-收敛登记成本/1.2-三维场景外观迁移到统一定义.md) |
| 1.3 | 其余领域迁移与门禁改造 | 已完成 | 1.2 | [1.3](任务/第一阶段-收敛登记成本/1.3-其余领域迁移与门禁改造.md) |
| 2.1 | 镜头卡集合写入与专用能力下线 | 已完成 | 1.3 | [2.1](任务/第二阶段-三维缺口清零/2.1-镜头卡集合写入与专用能力下线.md) |
| 2.2 | 镜头卡状态捕获入口 | 已完成 | 2.1 | [2.2](任务/第二阶段-三维缺口清零/2.2-镜头卡状态捕获入口.md) |
| 2.3 | 编辑模式切换与烘焙 | 已完成 | 1.3 | [2.3](任务/第二阶段-三维缺口清零/2.3-编辑模式切换与烘焙.md) |
| 2.4 | 姿态关节直接写入 | 已完成 | 1.3 | [2.4](任务/第二阶段-三维缺口清零/2.4-姿态关节直接写入.md) |
| 2.5 | 清空动画轨道与轨迹控制点 | 已完成 | 1.3 | [2.5](任务/第二阶段-三维缺口清零/2.5-清空动画轨道与轨迹控制点.md) |
| 3.1 | 画布清空解散与重做 | 待开始 | 1.3 | [3.1](任务/第三阶段-画布缺口清零/3.1-画布清空解散与重做.md) |
| 3.2 | 分镜格子内容与排序 | 待开始 | 3.1 | [3.2](任务/第三阶段-画布缺口清零/3.2-分镜格子内容与排序.md) |
| 4.1 | 修复门禁清点的 src-stores 盲区 | 待开始 | 无 | [4.1](任务/第四阶段-账本覆盖全部store与设置松绑/4.1-修复门禁清点的src-stores盲区.md) |
| 4.2 | 全局与项目类 store 建账 | 待开始 | 4.1 | [4.2](任务/第四阶段-账本覆盖全部store与设置松绑/4.2-全局与项目类store建账.md) |
| 4.3 | 三维视图态与生成筛选 store 建账 | 待开始 | 4.1 | [4.3](任务/第四阶段-账本覆盖全部store与设置松绑/4.3-三维视图态与生成筛选store建账.md) |
| 4.4 | 受保护设置松绑两项 | 待开始 | 4.2 | [4.4](任务/第四阶段-账本覆盖全部store与设置松绑/4.4-受保护设置松绑两项.md) |
| 5.1 | 抽出生成草稿纯领域层 | 待开始 | 无 | [5.1](任务/第五阶段-生成页输入态/5.1-抽出生成草稿纯领域层.md) |
| 5.2 | 参数联动提纯为纯函数 | 待开始 | 5.1 | [5.2](任务/第五阶段-生成页输入态/5.2-参数联动提纯为纯函数.md) |
| 5.3 | 生成草稿 store 化 | 待开始 | 5.2 | [5.3](任务/第五阶段-生成页输入态/5.3-生成草稿store化.md) |
| 5.4 | 注册生成草稿实体并放宽提交 | 待开始 | 5.3、1.3 | [5.4](任务/第五阶段-生成页输入态/5.4-注册生成草稿实体并放宽提交.md) |
| 6.1 | 标注编辑器领域层提升 | 待开始 | 无 | [6.1](任务/第六阶段-图片标注编辑器/6.1-标注编辑器领域层提升.md) |
| 6.2 | 注册标注文档实体 | 待开始 | 6.1、1.3 | [6.2](任务/第六阶段-图片标注编辑器/6.2-注册标注文档实体.md) |

## 重要说明

- **登记成本是根因，第一阶段不可跳过。** 先补缺口再收敛，等于用旧的高成本方式再写 16 遍。
- **`GAP_BASELINE` 是唯一的进度真相。** 位于 `src/features/assistant/applicationCapabilities/storeActionCoverage.test.ts`，当前 5（全部是画布）。每完成一项补齐就调小，只许降不许升。
- **专用能力与集合写入不并存。** 补 `collectionWrite` 的同一次提交必须删掉对应的专用能力（`AGENTS.md` 明令，且 `applicationControlCoverage.test.ts` 的迁移唯一性会红）。
- **每道新门禁必须做断牙验证**（故意破坏 → 确认变红 → 恢复 → 确认变绿）。断牙没跑过的门禁等于没有门禁。
- ~~待确认 1：姿态直接写入遇上已有关键帧的语义~~ 已在 2.4 确定：方案 C，见 [重要记录](重要记录.md) 记录 002。
- ~~待确认 2：`captureIntoSelectedShot` 是否应对助手开放~~ 已在 2.2 确定：开放，见 [重要记录](重要记录.md) 记录 004。

## 最近记录

### 2026-08-08（续二）

- **完成 2.5，第二阶段全部完成，三维 11 项缺口全部归零。** `clearTrack` 扩展 `remove_items` 支持轨道级引用（工程:对象:属性路径，3 段不带时间），同一批不能混用轨道级与关键帧级引用。轨迹编辑推翻了任务文档"新增 trajectory_knot 子实体"的建议——读 UI 发现拖任意控制点/切线手柄全部收敛到同一个 store 动作 `setShotSpatialPath`（整条路径替换），没有"改一个控制点"的原语，所以 `knots` 做成 json 数组属性整体读写（参考 `asset.tags` 先例），不新建子实体；同时发现 `setShotPathAnchor`（"拖端点"）实际改的是相邻镜头卡的对象位置快照，与 `knots` 无关，做成独立的 `start_position`/`end_position` 属性。`camera_stage.trajectory` 从整体 `writeExclusion` 变为正常可写实体（新增 `CameraStageMutationExecutor`）。`GAP_BASELINE` 8 → 5，`storeActionCoverage.test.ts` 现在只剩画布的 5 项。断牙验证实跑（两条门禁同时按预期变红）。新增 10 个测试用例（`clearTracks` 服务/执行器 6 个 + 轨迹编辑 6 个，注：含既有文件扩展）。`check:assistant-capabilities`（56 用例）、`cameraStage`+`applicationCapabilities`+`core/assistant` 全量（324 用例）、`electron-vite build` 全部通过。实机验证步骤已写入任务文档交给用户。

### 2026-08-08（续）

- 完成 2.4：姿态关节直接写入，方案 C（轨道无关键帧写静态值，有关键帧等价于当前时间点打点）。断牙验证实跑：临时改成无条件静态写（方案 B），确认「拖动播放头值跳回插值结果」的回归测试按预期变红，恢复后转绿。实现中发现比"要不要自动打点"更根本的问题：三维 object/camera 现有属性全部经建模语义（同步全部镜头卡、从不打点）提交，若动画属性复用同一路径会变相等于方案 B——解法是给 `CameraStageObjectDraft` 加两个独立累加字段，三种语义分流到三个落地方法，混用同批直接拒绝。63 条 `animatable.*` 由工厂批量生成；`pose_preset`（5 个命名预设）做成枚举属性而非专用能力。简易模式不支持（没有独立关键帧轨道，判断条件不成立），错误提示指向 2.2/2.3 的替代路径。`GAP_BASELINE` 10 → 8。重要记录 002 待确认点已解决。新增 `cameraStagePoseWrite.test.ts`（10 用例）。`check:assistant-capabilities`（56 用例）、`cameraStage`+`applicationCapabilities`+`core/assistant` 全量（312 用例）、`electron-vite build` 全部通过。实机验证步骤已写入任务文档交给用户。

### 2026-08-08

- 完成 2.3：读代码推翻了任务文档自己的假设——"专业→简易与同模式内切换没有约束"不成立，`setEditorMode` 第一行就把专业→简易直接拒绝，应用从未支持这个方向；它唯一的真实调用点是 `createNewProject` 对空场景设初始模式，已被 `create_camera_stage_project` 完全覆盖。`editor_mode` 维持整体只读，理由改写为准确描述。烘焙确认不可逆（原镜头卡结构永久丢失、清空撤销历史），注册为带审批的专用能力 `bake_camera_stage_to_pro`（`risk: R2`，`cameraStageControl('delete', ...)` 触发 `confirmation_required`），委托给界面同款的 `bakeCurrentProjectToPro()`，未重写业务逻辑。`setEditorMode` 改绑 excluded/internal，`bakeToProMode` 改绑新能力。`GAP_BASELINE` 12 → 10。新增 `cameraStageBake.test.ts`（3 用例）。`check:assistant-capabilities`（56 用例）、`cameraStage`+`applicationCapabilities`+`core/assistant` 全量（302 用例）、`electron-vite build` 全部通过。实机验证步骤已写入任务文档交给用户。

### 2026-08-07（续四）

- 完成 2.2：新增可写属性 `camera_stage.shot.capture_object_refs`，把列出的对象/摄像机当前实时状态记录进指定镜头卡，不依赖选中态。判断依据：`compileShotsToAnimation` 是单向编译，简易模式下任何镜头卡增删/挪时间点都会重新生成整份 `animation`，会把关键帧集合写入的直接编辑整体冲掉——所以"关键帧集合写入已覆盖"不成立，用户据此明确选择开放该入口。写入复用已有纯函数 `captureObjectsIntoShot`，只覆盖列出的对象、只落在指定卡上，不重新打开 `modelingWrite.test.ts` 守住的插值污染问题。`captureIntoSelectedShot` 账本绑到新属性，`GAP_BASELINE` 13 → 12。`重要记录.md` 记录 004 的待确认点已解决。新增 `cameraStageShotCapture.test.ts`（5 用例）。`check:assistant-capabilities`（56 用例）、`cameraStage`+`applicationCapabilities`+`core/assistant`+`canvas`+`assets` 全量（371 用例）、`electron-vite build` 全部通过。

### 2026-08-07（续三）

- 完成 2.1：镜头卡新建/删除/批量删除接到集合写入（`cameraStageShotService` + `CameraStageShotCollectionExecutor`，照抄关键帧集合执行器的结构）。验证「重排即改 time」的等价关系成立后，`reorderShot` 直接绑到已可写的 `camera_stage.shot.time` 属性，未新增集合 reorder 操作。同一次提交删掉专用能力 `add_camera_stage_shot`；`update_camera_stage_shot` 确认已被属性写入完全覆盖，一并下线。`GAP_BASELINE` 16 → 13。断牙验证（临时注释集合执行器注册，确认 `collectionCoverage` 变红后恢复转绿）已实跑。`check:assistant-capabilities`（56 用例）、`cameraStage`/`applicationCapabilities`/`core/assistant` 全量（294 用例）、`electron-vite build` 全部通过。

### 2026-08-07（续二）

- **第一阶段全部完成。** 1.3 把三维其余 29 项（object/camera/project/shot/keyframe/playback）、画布 3 项、素材 4 项可写属性全部迁到统一字段定义，`cameraStageWriterTables.ts` 因退化为纯转发而删除。过程中发现并修复了第二个类型陷阱：多个 store 动作共享同一批属性时（如 `updateObject` 一次改 4 个属性）账本条目会被覆盖，修法是把 `fieldLedgerEntries()` 改成按 action 分组累积。新增 `scripts/check-assistant-capabilities.cjs` 静态规则，禁止在 `*Fields.ts` 之外手写 `ApplicationPropertyWriterTable` 字面量，断牙验证已实跑。规则文档与两侧 skill 已同步。三道门禁与迁移前完全一致（56 测试不变），`npm test` 全量 1171 通过。

### 2026-08-07（续）

- 完成 1.2：三维场景 28 条属性（外观 25 + 时间轴 3）迁移到 `cameraStageSceneFields.ts` 试点。过程中发现并修复了任务文档预判的类型陷阱——账本展开 `fieldLedgerEntries()` 会丢失编译期完整性检查，修法是给 1.1 的 `ApplicationFieldDefinition` 加 `TAction` 类型参数保住字面量联合。断牙验证（删 `fog_distance` → tsc 与 storeActionCoverage 双双变红 → 恢复转绿）已实跑。三道门禁断言结果与迁移前完全一致（56 测试不变）。

### 2026-08-07

- 完成 1.1：新建 `src/core/application-control/fieldDefinition.ts`（78 行）与自测文件，一条 `ApplicationFieldDefinition` 声明可派生描述符/读取值/写入表项/账本条目四样产物，已在 `index.ts` 导出。三道既有门禁断言结果与迁移前完全一致（本任务不迁移任何现有属性）。`tsc` / `lint` / `check:colors` / `check:assistant-capabilities` 全绿。

### 2026-08-06

- 创建本计划。基于已完成的前置工作（三道门禁 + 21 项差集盘点 + 播放控制补齐）。
- 扫描发现三件事，已写入实施方案：
  1. 账本目前只覆盖 3 个 store，项目里共 18 个 zustand store。
  2. 门禁的领域清点只扫 `src/features/*`，**`src/stores/` 完全不在扫描范围内**——`canvasStore`、`settingsStore`、`projectStore` 等 6 个 store 是靠所属 feature 侥幸过关的。
  3. 一个属性要在 4 处登记（3 个文件），这是缺口持续再生的根因。
