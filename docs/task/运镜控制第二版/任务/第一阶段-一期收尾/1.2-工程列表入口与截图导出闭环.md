# 1.2 工程列表入口与截图导出闭环

## 任务状态

- 状态：待验证（代码完成，工程列表交互 + 截图闭环待用户在 Electron 窗口确认）
- 所属阶段：第一阶段-一期收尾
- 前置任务：1.1
- 关联任务：2.2（截图按钮最终落位到新工具栏时联动调整）
- 是否可以并行：可与 2.x 并行

> 本任务迁移自一期 3.2（`docs/task/运镜控制第一版/任务/第三阶段-持久化与产品接入/3.2-工具箱入口与结果回传.md`）。原任务中的"工具箱多工具首页"部分已在一期 2.1 反馈修复时提前落地（`src/workspaces/ToolboxWorkspace.tsx`，原 `ToolboxPlaceholder` 已删除），本任务只承接剩余范围。

## 任务目标

工具箱点开「运镜控制」后先看到工程列表页（新建/打开/重命名/删除），进入编辑器后可一键把当前机位取景画面导出为本地 PNG，导出的图片能在生成链路（对话或画布的图片输入位置）正常被选中使用，落地「摆场景 → 截图 → 喂给 AI 生成」完整闭环。

## 当前情况

- `src/workspaces/ToolboxWorkspace.tsx` 已是多工具卡片首页，点「运镜控制」卡片**直接进入** `CameraStageEditor`，中间没有工程列表页。
- 1.1 完成后已具备工程 list/get/upsert/rename/delete 命令桥。
- 截图未实现。三维视口是 `src/features/cameraStage/scene/StageScene.tsx` 里的 R3F `<Canvas>`；默认 WebGL 上下文未开 `preserveDrawingBuffer`，直接 `toDataURL` 会拿到空帧——需要在截图时主动渲染一帧再读，或开启 `gl={{ preserveDrawingBuffer: true }}`（执行时实测选择，注意后者的性能代价评估）。
- 图片落盘机制：`electron/main/services/ai-runtime/media-store.ts` + `henji-media://` 协议；媒体是否已上传的判断统一走 `src/utils/mediaPresence.ts` 共享函数（项目既有约束）。
- 机位视角渲染已可用（`viewMode === 'camera'` 时 `StageViewportCamera` 驱动真实 `PerspectiveCamera`）。

## 实施方案

在 `ToolboxWorkspace` 与 `CameraStageEditor` 之间插入一层「运镜控制工程列表页」：卡片式工程列表（名称、更新时间、对象数）+ 新建按钮 + 每项的重命名/删除操作，复用 `Ui*` 与项目现有列表/卡片样式。截图功能：编辑器内提供「截图导出」按钮（当前阶段放在视口浮层或顶栏，2.2 工具栏落地后迁移落位），点击时按当前机位视角渲染一帧、读出 PNG 数据，经 IPC 走 media-store 落盘，成功后给出可跳转/可复制的反馈提示。

## 执行步骤

1. 新建工程列表页组件（`src/features/cameraStage/projects/` 目录下，或等价位置），接 1.1 的命令桥实现列表/新建/重命名/删除。
2. 修改 `ToolboxWorkspace` 的 `renderTool('cameraStage')`：先渲染工程列表页，选中/新建工程后进入 `CameraStageEditor`（携带工程 id），编辑器内提供返回列表入口。
3. 实现截图管线：渲染层取当前机位一帧 PNG（实测 `preserveDrawingBuffer` 与"按需渲染后立即读帧"两种方式，择优）；若当前处于导演视角，截图仍按当前激活机位取景（无机位时禁用按钮并提示）。
4. 新增/复用 IPC 把 PNG 字节流交给 media-store 落盘，返回 `henji-media://` 引用。
5. 验证导出的图片能在生成 Tab 或画布节点的图片上传/参考图位置正常被选中使用，跑通完整闭环。
6. 视觉走查 + `npm run check:colors` + 原生控件检查无新增命中；把鼠标操作验证步骤清单交给用户。

## 涉及内容

### 新增文件

- `src/features/cameraStage/projects/`（工程列表页组件与列表项操作）。
- 截图相关：渲染层截图工具模块 + 主进程落盘 IPC（若 media-store 现有 IPC 不能直接复用）。

### 修改文件

- `src/workspaces/ToolboxWorkspace.tsx`：运镜控制入口改为先进工程列表。
- `src/features/cameraStage/CameraStageEditor.tsx`：接收工程 id、返回入口、截图按钮。
- `src/features/cameraStage/scene/StageScene.tsx`：暴露截图所需的 renderer 访问方式。

## 验收标准

- [ ] 工具箱进入运镜控制先显示工程列表，新建/打开/重命名/删除全部可用。
- [ ] 截图按钮能导出当前机位取景为本地 PNG（分辨率与视口一致或按约定尺寸，执行时确认）。
- [ ] 导出图片在生成链路图片输入位置正常可选可用，闭环打通。
- [ ] 无新增颜色硬编码、原生控件命中；lint 通过。
- [ ] 用户按操作清单完成真实窗口验证；`00-任务总览.md` 已同步。

## 执行记录

### 2026-07-03

- 完成内容：工具箱进入运镜控制先显示工程列表页（新建/打开/重命名/删除），编辑器加机位截图导出闭环。
- 实际改动：
  - 新增两级视图容器 `src/features/cameraStage/CameraStageApp.tsx`（工程列表 ↔ 编辑器），`ToolboxWorkspace.renderTool('cameraStage')` 改挂 `CameraStageApp`（原来直挂 `CameraStageEditor`）。
  - 新增工程列表页 `src/features/cameraStage/projects/CameraStageProjectList.tsx`：卡片列表（名称/对象数/更新时间）、新建工程、重命名（UiModal+UiInput）、删除（UiModal 确认）；全部 `Ui*` primitives。
  - service 补 `createNewProject`（重置空场景并入库）；store 补 `newScene`（清空场景与工程标识）。
  - 截图管线：新增 `scene/StageCaptureBridge.tsx`（Canvas 内经 `useThree` 注册"读当前帧为 PNG dataURL"）；`StageScene` 加 `captureRef` 参数并开启 `gl={{ preserveDrawingBuffer: true }}`（静态摆拍性能可忽略）；新增导出模块 `export/cameraStageScreenshot.ts`（持久化进 Media 得 `henji-media://` + 另存到系统下载目录，复用 `persistImageSource`/`toDisplaySrc`/`saveImageSourceToDownloads`，不新建存储方式）。
  - 编辑器：新增「截图」按钮（仅机位视角+有机位时可用，非机位视角禁用并提示）、「返回工程列表」按钮、截图结果提示条。
- 验证结果：渲染层 `tsc --noEmit` 仅剩 6 个基线错误（全在未改动的 `StageTransformControls.tsx`），本次零新增；`npm run lint` 通过；`npm run check:colors` 通过；原生控件检查仅命中 primitives（新代码未引入原生控件）。
- 关键设计点：截图只在「机位视角」下可用——机位相机 `makeDefault`，读 canvas 即得真实取景画面，避免离屏渲染的复杂度；卡片操作按钮改为绝对定位兄弟节点，规避 `<button>` 嵌套 `<button>` 的非法 DOM。
- 遗留问题：工程列表交互、截图导出、导出图片在生成 Tab 上传使用的完整闭环属运行时/鼠标操作验证，交用户在 Electron 窗口确认（步骤见下）。
- 用户验证步骤：① `npm run electron:dev` → 工具箱 → 运镜控制，进入工程列表页；② 新建工程 → 进编辑器 → 加对象/角色/机位；③ 顶栏切「机位视角」→「截图」→ 出现"已保存到下载文件夹：xxx.png"；④ 到系统下载文件夹确认 PNG；⑤ 到生成 Tab 的图片上传/参考图位置上传该 PNG，确认能正常作为参考图使用；⑥ 返回工程列表，验证新建工程已在列表、可重命名/删除、可重新打开恢复场景（并完成 1.1 的"重开恢复"验证）。
- 与原计划偏差：工程列表页做成 `CameraStageApp` 两级视图而非独立 workspace 组件（`ToolboxWorkspace` 保持通用，运镜控制自管列表/编辑器两级），比原任务文件设想更内聚；截图同时落 Media 与下载目录（下载目录是可驱动闭环，Media 副本为后续"应用内直接引用"预留）。
- 对后续任务影响：`ToolboxPlaceholder` 早已删除（一期 2.1 落地多工具首页时处理），本任务无需再处理；截图按钮当前在视口浮层，2.2 工具栏落地后迁入正式工具栏。
- 相关提交：见本次 commit。
