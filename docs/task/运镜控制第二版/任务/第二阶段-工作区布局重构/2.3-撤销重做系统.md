# 2.3 撤销重做系统

## 任务状态

- 状态：待开始
- 所属阶段：第二阶段-工作区布局重构
- 前置任务：无（作用于数据 store，与布局改造正交）
- 关联任务：2.2（Ctrl+Z/Ctrl+Shift+Z 快捷键接线）、3.1（关键帧操作必须进历史栈）
- 是否可以并行：可与 2.1/2.2 并行；建议在 3.1 动工前完成

## 任务目标

编辑器获得可靠的撤销/重做能力：对象增删改、变换（含 gizmo 拖拽）、姿态调整、颜色/显隐、场景设置等数据操作全部可撤销重做；选中态、视角模式、面板布局等界面态不进历史；gizmo 拖拽、滑杆连续调整等高频操作合并为单条历史记录。为第三阶段的关键帧编辑操作预留接入方式。

## 当前情况

- `src/features/cameraStage/store/cameraStageStore.ts` 是 zustand v5 单 store，无任何历史机制。
- 需要区分两类状态：**数据态**（`objects` 及未来的场景设置、动画轨道——进历史）与**界面态**（`selectedId`/`gizmoMode`/`viewMode`/`activeCameraId`——不进历史）。
- 高频写入点：gizmo 拖拽（`StageScene.handleGizmoChange` 拖动过程每帧回写 store）、`NumberInput` 的 `commitOnChange`/滚轮、姿态滑杆——若每次 set 都记一条历史，一次拖拽会产生上百条记录，必须节流/合批。
- 项目内没有其他撤销重做实现可复用（画布 `@xyflow/react` 场景另有自己的历史逻辑，不通用）。
- 候选方案：`zundo`（zustand 官方生态 temporal 中间件，支持 `partialize` 排除界面态、`handleSet` 节流合批）；若与 zustand v5 版本兼容性实测有问题，则自研轻量补丁栈（快照或反向补丁）。

## 实施方案

优先用 `zundo` 给 `cameraStageStore` 挂 temporal 中间件：`partialize` 只跟踪数据态切片；连续操作合批采用「操作会话」语义——gizmo 拖拽 start/end、滑杆按下/松开之间暂停记录，结束时记一条（zundo 的 pause/resume 或 handleSet 节流实现，执行时实测选择）；历史栈长度设上限（如 100 步）。对外暴露 `undo`/`redo`/`canUndo`/`canRedo` 给工具栏按钮与快捷键。工程加载（1.1 的 `loadScene`）后清空历史。若 zundo 兼容性验证失败，自研方案保持同一对外接口，内部用不可变快照栈实现（objects 数组本身是不可变更新，快照成本可接受）。

## 执行步骤

1. 安装 `zundo` 并做 zustand v5 兼容性最小验证（不行则走自研分支并更新本文件）。
2. 改造 `cameraStageStore`：挂 temporal 中间件、`partialize` 数据态、历史上限、`loadScene` 清史。
3. 处理合批：`StageTransformControls` 增加拖拽 start/end 事件接线，拖拽过程暂停历史；`NumberInput` 连续提交与姿态滑杆同理（按「获得焦点/按下 → 提交/松开」为一个会话）。
4. 工具栏/快捷键接入 undo/redo（与 2.2 的 `useCameraStageShortcuts` 汇合，先完成者预留接口）。
5. 自动化验证：脚本内连续执行「加对象 → 改位置 → 删除 → undo×3 → redo×3」断言状态正确；gizmo 合批部分写成用户手动验证步骤。
6. 回归 lint/tsc；输出手动验证清单。

## 涉及内容

### 新增文件

- （可能）`src/features/cameraStage/store/historyMiddleware.ts`：合批策略封装（若逻辑超出 store 文件体积约束则拆出）。

### 修改文件

- `package.json`：新增 `zundo`（若选用）。
- `src/features/cameraStage/store/cameraStageStore.ts`：temporal 中间件与历史动作。
- `src/features/cameraStage/scene/StageTransformControls.tsx` / `StageScene.tsx`：拖拽会话事件。
- `src/features/cameraStage/panels/`：滑杆/数值输入的会话边界接线（按实际触点最小改动）。

## 验收标准

- [ ] 对象增删改、变换、姿态、颜色、显隐、场景设置均可撤销/重做，行为正确。
- [ ] 一次 gizmo 拖拽 / 一次滑杆连续调整 = 一条历史记录。
- [ ] 选中态、视角切换、面板布局操作不产生历史记录。
- [ ] 工程加载后历史清空，undo 不会退回到上一个工程的数据。
- [ ] Ctrl+Z / Ctrl+Shift+Z 快捷键与工具栏按钮可用，`canUndo/canRedo` 驱动禁用态。
- [ ] lint/tsc 通过；自动化断言脚本通过；`00-任务总览.md` 已同步。

## 执行记录

（尚未开始）
