# 运镜控制第二版 · 任务总览

## 任务目标

一期（`docs/task/运镜控制第一版`）已经做出可交互的三维场景编辑器核心（对象增删改、角色姿态、多机位相机与双视角渲染），但一期规划中的持久化与产品闭环（原 3.1/3.2）尚未开始，且当前编辑器 UI 还是固定三栏的基础形态，缺少专业三维软件该有的工作区体验。本任务把一期未完成部分全部承接过来，并完成两块主体工作：

1. **工作区 UI 重构**：参照 C4D / Blender 的多面板工作区设计（用户已给参考图），把编辑器改造为「顶部工具栏 + 基础对象快速添加栏 + 中央主预览视口 + 右侧资源管理器 / 属性管理 + 底部时间轴」的可停靠布局——每个面板可拖拽重排、自由调整大小、折叠/展开，布局可记忆。
2. **时间轴与关键帧动画**：给场景对象的各个属性（位置/旋转/缩放/颜色/FOV/关节姿态等）加 AE 式关键帧能力（属性行码表打点 + 底部时间轴轨道），支持播放预览、关键帧编辑和速度（缓动）曲线，补上参考产品「只能出静态图」的短板。

属于本次任务：场景工程持久化（承接一期 3.1）、工程列表入口与截图导出闭环（承接一期 3.2 剩余部分）、可停靠面板布局框架、工具栏/快速添加栏/视口交互细节完善、撤销重做系统、关键帧数据模型与插值引擎、时间轴面板、速度曲线编辑器。

不属于本次任务：用户自定义模型导入（一期重要记录 007 已列为独立后续任务）、通用 IK 求解器、多人协作、云端同步、移动端适配。动画视频导出（逐帧渲染 + ffmpeg 编码）作为第四阶段列入本计划但**范围待确认**，见重要记录 004。

## 当前进度

- 当前阶段：第二阶段-工作区布局重构（2.1、2.3 开发完成）
- 当前任务：1.1/1.2/2.1/2.3 均待用户统一验证；剩 2.2 未做
- 整体状态：进行中
- 已完成任务数量：0（1.1、1.2、2.1、2.3 均待验证）
- 总任务数量：9
- 当前阻塞问题：无（4 项待确认事项已于 2026-07-03 全部确认，见 `重要记录.md`）

## 阶段列表

| 阶段 | 阶段目标 | 状态 |
|---|---|---|
| 第一阶段-一期收尾 | 完成一期遗留的场景工程持久化与「摆场景→截图→喂给 AI」产品闭环，让功能先整体可用 | 待开始 |
| 第二阶段-工作区布局重构 | 编辑器改造为 C4D/Blender 式可停靠多面板工作区，补齐工具栏/快捷键/撤销重做等专业编辑器基础体验 | 待开始 |
| 第三阶段-时间轴与关键帧动画 | 属性关键帧数据模型 + 插值引擎 + 时间轴面板 + 速度曲线编辑，能在编辑器内做出并播放属性动画 | 待开始 |
| 第四阶段-动画视频导出（待确认） | 关键帧动画逐帧渲染并编码导出为本地视频文件 | 待确认 |

## 任务清单

| 编号 | 任务名称 | 状态 | 前置任务 | 任务文件 |
|---|---|---|---|---|
| 1.1 | 场景工程持久化 | 待验证 | 无 | [任务/第一阶段-一期收尾/1.1-场景工程持久化.md](任务/第一阶段-一期收尾/1.1-场景工程持久化.md) |
| 1.2 | 工程列表入口与截图导出闭环 | 待验证 | 1.1 | [任务/第一阶段-一期收尾/1.2-工程列表入口与截图导出闭环.md](任务/第一阶段-一期收尾/1.2-工程列表入口与截图导出闭环.md) |
| 2.1 | 停靠式面板布局框架 | 待验证 | 无 | [任务/第二阶段-工作区布局重构/2.1-停靠式面板布局框架.md](任务/第二阶段-工作区布局重构/2.1-停靠式面板布局框架.md) |
| 2.2 | 工具栏、快速添加与视口交互完善 | 待开始 | 2.1 | [任务/第二阶段-工作区布局重构/2.2-工具栏快速添加与视口交互完善.md](任务/第二阶段-工作区布局重构/2.2-工具栏快速添加与视口交互完善.md) |
| 2.3 | 撤销重做系统 | 待验证 | 无 | [任务/第二阶段-工作区布局重构/2.3-撤销重做系统.md](任务/第二阶段-工作区布局重构/2.3-撤销重做系统.md) |
| 3.1 | 关键帧数据模型与插值引擎 | 待开始 | 1.1 | [任务/第三阶段-时间轴与关键帧动画/3.1-关键帧数据模型与插值引擎.md](任务/第三阶段-时间轴与关键帧动画/3.1-关键帧数据模型与插值引擎.md) |
| 3.2 | 时间轴面板 | 待开始 | 3.1、2.1 | [任务/第三阶段-时间轴与关键帧动画/3.2-时间轴面板.md](任务/第三阶段-时间轴与关键帧动画/3.2-时间轴面板.md) |
| 3.3 | 速度曲线编辑器 | 待开始 | 3.2 | [任务/第三阶段-时间轴与关键帧动画/3.3-速度曲线编辑器.md](任务/第三阶段-时间轴与关键帧动画/3.3-速度曲线编辑器.md) |
| 4.1 | 动画视频导出 | 待确认 | 3.2 | [任务/第四阶段-动画视频导出/4.1-动画视频导出.md](任务/第四阶段-动画视频导出/4.1-动画视频导出.md) |

可并行：1.1 与 2.1；2.3 与 2.1/2.2（撤销重做作用于数据 store，与布局改造正交）；1.2 与 2.x。3.x 内部按 3.1 → 3.2 → 3.3 串行。

## 重要说明

- **一期任务迁移**：一期的 3.1（子项目持久化）、3.2（工具箱入口与结果回传）未开始即迁移为本任务的 1.1、1.2；一期 2.2/2.3 的"用户交互验证"遗留项并入本任务各阶段的用户测试环节，一期任务整体收束，见重要记录 001。
- **面板布局库选型 dockview（待确认）**：调研结论建议引入 `dockview-react`（MIT、零依赖、v7 活跃维护），需用户确认新增依赖后 2.1 才动工，见重要记录 002。
- **关键帧引擎建议改自研（待确认）**：一期记录 001 选型 Theatre.js core，但复核发现其停更风险明显，建议改为自研轻量插值内核（cubic-bezier 求值 + 轨道采样），见重要记录 003。
- 涉及鼠标操作（面板拖拽、时间轴 scrub、关键帧拖动、视口交互）的验证一律交给用户在真实 `npm run electron:dev` 窗口完成，AI 只负责写清操作步骤和验证点（项目协作约定）。
- 一期已定且继续有效的约束：三维渲染留在渲染进程、资源走 `henji-media://` 协议（一期记录 006）、内置资源最小化（一期记录 007）、AE 式关键帧 UI 无需预先改造现有结构（一期记录 008）。

## 最近记录

### 2026-07-03（2.3 开发完成）

- 完成 2.3 撤销重做系统：`cameraStageStore` 接入 zundo temporal（partialize 只跟踪 objects、equality 引用去重、limit 100），会话式 handleSet 把「一次 gizmo 拖拽/滑杆连续调整」合并为一条记录；gizmo 走 dragging-changed 会话、数值/滑杆走 PropertyPanel focus/blur 会话；加载/新建工程后清史；编辑器加撤销/重做按钮 + Ctrl+Z/Ctrl+Shift+Z/Ctrl+Y。核心逻辑经 Node 断言脚本全部通过（离散增改删、界面态排除、会话合批）；tsc 零新增/lint/颜色通过。gizmo/滑杆合批手感待用户真实窗口验证。

### 2026-07-03（2.1 开发完成）

- 完成 2.1 停靠式面板布局框架：引入 dockview-react 7.0.2，编辑器改为「顶部控制栏 + 可停靠三面板（视口/资源管理器/属性）」，支持拖拽重排/调整大小/tab 分组/折叠 + 布局记忆（localStorage）+ 重置默认；主题 `--dv-*` 变量映射到项目 token（index.css）。tsc 零新增 / lint / 颜色 / model-i18n / vite build 全过（dockview+three 隔离在懒加载 chunk）。dockview 与 Canvas 共存、拖拽手感、主题观感待用户在 Electron 窗口验证。控制栏顺带为 2.2 工具栏落位打基础。

### 2026-07-03（1.2 开发完成）

- 完成 1.2 工程列表入口与截图导出闭环：工具箱进入运镜控制先显示工程列表页（新建/打开/重命名/删除，`CameraStageApp` 两级视图收口），编辑器加机位截图（`StageCaptureBridge` + `preserveDrawingBuffer`，机位视角下读当前帧为 PNG，落 Media 得 henji-media:// + 另存下载目录复用现有图片保存机制）。tsc（零新增）/lint/颜色/原生控件检查全过。工程列表交互与截图闭环待用户在 Electron 窗口验证，状态置为待验证。第一阶段 1.1+1.2 建议由用户一起测。

### 2026-07-03（1.1 开发完成）

- 4 项待确认事项全部确认：引入 dockview-react、关键帧引擎改自研、时间轴秒存储+30fps/5s、第四阶段视频导出纳入计划（重要记录 002/003/004 已更新为已确定）。
- 完成 1.1 场景工程持久化：照抄 storyboard-projects 五层结构落地 `camera_stage_projects` 持久化链路（建表/服务/IPC/契约/adapter/命令桥）+ 场景序列化模块（schemaVersion 1）+ store 工程状态 + 工程编排 service + 编辑器保存按钮与 Ctrl+S。lint/electron tsc/electron eslint/颜色检查全过，零新增渲染层类型错误。DB 落盘+重开闭环待用户在 Electron 窗口验证，状态置为待验证。
- 发现既有类型问题（非本次引入）：`StageTransformControls.tsx` 6 处 drei 事件类型报错，影响裸 `npm run build`，不影响 Electron 主链路，建议单独立小任务修复。

### 2026-07-03（计划建立）

- 建立本任务计划：分析一期遗留（3.1/3.2 未开始、2.2/2.3 待用户测试）与现有 `src/features/cameraStage/` 代码结构；调研可停靠面板布局库（dockview / flexlayout-react / react-mosaic / golden-layout）与关键帧引擎现状（Theatre.js core 停更风险）；按四阶段拆分 9 个任务，尚未开始任何实施。
- 一期 `docs/task/运镜控制第一版` 总览已标注 3.1/3.2 迁移至本任务并整体收束。
- 待确认事项 4 项：dockview 依赖引入、关键帧引擎自研替代 Theatre.js、动画帧率与时长默认值、第四阶段视频导出是否纳入本次范围。
