# 决策记录

## 2026-07-27 第一阶段开始

- 沿用 `重要记录.md` 记录 002：进度条改用 `transform` 是规范收敛，不表述为已确认的卡顿修复。
- 沿用 `重要记录.md` 记录 004：优先扩展现有公共组件，不新增与现有能力重复的 `Ui*` 组件。
- `Toggle` 滑块外壳的最终落点待完成调用点与 primitives 能力核查后确定，并同步回 `重要记录.md`。
- 所有涉及键盘、焦点、点击、主题切换和画布缩放的人工验收统一写入 `test-report.md`，本阶段实现过程中不要求用户中途测试。

## 2026-07-27 第一阶段结束

- `Toggle` 直接复用已经存在的 `UiSwitch`，不新增原语；决定已同步为 `重要记录.md` 记录 010。
- `UiModal` 和独立实现的 `AlertDialog` 通过 `useDialogFocusTrap` 共享同一个弹窗栈，确保嵌套弹窗只响应栈顶的 Esc。
- `UiModal` 打开后焦点落在弹窗语义容器，不抢占第一个输入框；关闭时还原到打开前的元素。
- 状态组件新增 `xs` 紧凑档，页面/区块状态统一收口；原生 `<option>` 占位必须是字符串，画布节点内部专属提示不属于本阶段结构改造，因此保留。
- 资产库首次加载保留网格骨架屏，它承担布局预留而不是普通加载文案，不替换成居中 spinner。
- `ProgressBar` 默认色改用运行时语义类 `bg-accent`，未被调用的 `color` prop 删除；2800ms 作为数据补间档独立于四档交互动效。

## 2026-07-27 第二阶段

- 设置弹窗把 156px 一级导航与 190px 二级导航合并为 208px 单列折叠导航；当前分类展开子项，其他分类保持单行入口。
- 设置弹窗标题改为横跨整条头部；底部关闭按钮与整条底部区域删除。原底部按钮只调用 `handleClose`，不存在统一提交逻辑，各设置仍由自身的 store、持久化函数或局部保存按钮负责。
- 设置弹窗高度按被删除的 72px 底部区同步收缩：960×640 时由 500px 降为 440px，1440×900 时由 684px 降为 612px。
- `SectionCard` 使用正式的 section 标题档，字段标签与说明分别消费 label/meta 令牌；资产库设置从嵌套 `UiPanel` 降级为零装饰分组。
- 工作区页面头统一复用既有 `UiPageHeader`，不新增组件；外层统一为 24px 内边距与 1152px 最大宽度。
- `UI_TEXT_TITLE_CLASS` 保持 `text-base`，以紧凑桌面应用的既有排版体系为准，不在单个页面绕开令牌。
- 资产库只在完整工作区模式显示统一页面头；浮动面板模式保持紧凑工具面板形态。
- 按项目约定未操作真实窗口，视觉与鼠标交互验收统一进入 `test-report.md`。

## 2026-07-27 第三阶段开始

- 先完成全项目选中态与字号用法普查，再定稿选中态令牌与排版迁移边界，避免机械替换。
- 沿用 `重要记录.md` 记录 006：模型显示管理保留“开启=高亮”，只把高亮面积从整行收回到既有 `Toggle` / `UiSwitch`。
- 沿用 `重要记录.md` 记录 004：本阶段不新增 `Ui*` 组件，优先扩展现有有限枚举变体和具名令牌。
- 任务 3.3 是否增加 ESLint 防回归规则，待完成存量清单与例外评估后写入 `重要记录.md`。
- 所有真实窗口观感、点击、筛选、开关和页面切换验收继续统一写入 `test-report.md`，本阶段实现过程中不要求用户中途测试。

## 2026-07-27 第三阶段结束

- 选中态定稿为四种语义：导航使用中性层底、强调文字与方向指示条；单选使用强品牌实底与白字；多选/标签使用强调描边、中性层底与强调文字；布尔状态只强调开关或复选框本体。
- `UiChipButton` 增加有限枚举 `selectionRole="navigation"`，没有新增组件；四类状态均由 `styleTokens.ts` 具名令牌驱动并通过 primitive 单测锁定。
- 模型显示管理保留“开启=高亮”，列表行改为无卡片的分隔行；高亮只落在复用的 `Toggle`，批量操作降为 muted，供应商与类型筛选使用多选词汇。
- 排版按设置、生成、画布/项目、助手、资产、工具/编辑、公共组件/参数七批迁移；直接字号类从 762 降至 509，五类令牌从 35 增至 398。
- 本阶段不增加全局字号 ESLint 禁令。剩余 509 处混合了紧凑控件、徽标、画布 LOD、富文本子选择器和时间轴等合法场景，立即全局禁止会制造大规模误报与豁免；保留现有任意像素字号禁令，后续在第五阶段评估针对新增代码或指定表面的窄规则。
- `check:ui-visual` 与真实窗口截图未执行，因为会启动并操作 Electron 界面；依项目约定统一交给用户最终人工验收。

## 2026-07-27 第四阶段开始

- 沿用 `重要记录.md` 记录 002、003、009：没有复现证据不做推测性性能优化，不恢复 `content-visibility`，成功图片保持自然高度并用宽高比预留空间。
- 4.2 的图片比例预留先独立实施；生成历史虚拟化必须等待 4.1 的真实复现或明确性能数据，不以“历史上限较大”作为单独实施依据。
- 项目约定禁止代理执行拖拽、快速滚动等鼠标验收，因此 4.1 的真实拖动复现与 4.2 的滚动观感、视觉零变化验收统一写入 `test-report.md`，待全部任务完成后由用户集中测试。
- 实现前先核对任务类型、尺寸信息和持久化边界，优先避免为显示层宽高比引入数据库迁移或跨层业务逻辑。

## 2026-07-27 第四阶段代码实施结束

- 成功图片保留原有 `w-full h-auto`，通过原生 `width` / `height` 提供解码前固有比例；没有增加固定槽位、`object-fit` 或裁切，因此代码层面不改变自然高度视觉。
- 尺寸学习写入 `GenerationTask.options.resultImageDimensions`，由稳定的 `useTaskState` 命令更新；历史保存继续复用 `useSaveTaskHistory`，没有让 `TaskCard` 直接访问数据库。
- 新任务已有的实际尺寸同步写入 `params.__dimensions`，补齐加载逻辑早已支持但保存逻辑遗漏的闭环；不新增数据库迁移。
- 已知比例的图片任务在非成功状态按 256px 结果宽度提前计算目标高度；未知尺寸的旧任务保留 `h-64` 降级，不为布局高度增加动画。
- `TaskCard.tsx` 因本阶段改动超过 500 行，按规范拆出只负责操作栏展示的 `TaskCardToolbar.tsx`；未新增任何 `Ui*` 原语。
- 4.1 尚未完成真实拖动复现，故不实施虚拟化及其它性能猜测。`react-window` 两项依赖确认源码零使用，仅记录清理建议。

## 2026-07-27 第五阶段开始

- 巡检能力保持两条职责分离的命令：`ui:tour` 只生成供人工查看的截图与索引，`check:ui-visual` 只输出可自动判定的规则结论。
- 两条命令共用场景、窗口尺寸、启动与导航基础设施，避免界面覆盖范围在两份脚本中再次漂移；规则实现优先拆成可单测的纯函数。
- 依据 4.1 执行记录，本阶段不新增 `ui:perf`：真实实例挂载、拖动测量和 tracing 尚未实际完成，当前没有被证明可复用的脚本可固化。
- 本阶段不扩大为全局字号 ESLint 禁令；页面标题一致性由真实 DOM 跨界面规则约束，保留记录 014 的增量治理决定。
- 自动化脚本可以实现完整交互场景配置，但遵守项目约定，不由代理执行需要点击、悬浮、拖拽或主观判断的真实窗口验收；统一交给最终人工测试。

## 2026-07-27 第五阶段代码实施结束

- `ui:tour` 与 `check:ui-visual` 共用 `scripts/lib/uiInspection.cjs` 的六类界面、16 个场景和窗口尺寸逻辑；场景清单不在两个入口重复维护。
- 截图巡检默认生成 16 场景 × 2 尺寸共 32 张截图和 `index.md`；规则审计不截图，只输出 `audit.json` 并在命中或场景失败时返回非零退出码。
- 十一条规则定稿为：表面叠层、文字对比度、内层圆角、非浮层阴影、CSS 隐藏定位、助手插入量逃逸、横向溢出、嵌套滚动、文本硬裁、过小命中区、页面标题字号一致性。
- `UiPageHeader` 增加 `data-ui-page-header` / `data-ui-page-title`，`TabContainer` 增加 `data-ui-workspace-inset-root`；这些是无样式、无业务状态的稳定审计锚点，不通过文案或脆弱 class 推断结构。
- 延续开始决定：不新增 `ui:perf`，不增加全局字号 ESLint 禁令；真实 UI 命令由用户在最终集中测试时执行。
