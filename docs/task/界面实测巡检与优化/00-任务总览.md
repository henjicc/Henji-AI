# 00 任务总览

## 任务目标

2026-07-27 用 Playwright Electron API 启动真实窗口，遍历主要页面与状态截图分析后发现：项目已经有一套完整的设计令牌与规范（`styleTokens.ts` / `motion.ts` / `henji-ui-surface` skill），**但令牌的实际消费率极低**——排版令牌全项目只被消费 19 次，手写字号 791 次；`UiEmpty` 只用了 4 次，手写空态 22 处。规范写在文档里，界面里没有落地。同时公共组件本身存在会诱导错误用法的默认形态（`Toggle` 用蓝色实心按钮表达布尔值、`UiModal` 无 Esc 关闭与焦点管理），以及一个真实的布局缺陷（智能助手侧栏打开时生成工作区底部输入面板被遮挡截断）。

本次任务要达到的结果：**让规范真正落到界面上，并修掉实测发现的具体缺陷**。做法是先修公共组件（改一处收益覆盖全项目），再修布局与信息架构，最后收敛视觉表达，并把这次的 Playwright 巡检脚本固化成可重复执行的项目命令。

### 属于本次任务

- 公共组件默认形态与可访问性缺陷修复（`UiModal`、`Toggle`、`ProgressBar`、状态组件）
- 实测发现的布局缺陷修复（助手侧栏遮挡、空态居中基准、设置弹窗信息架构）
- 选中态、页面标题、排版层级的表达统一
- 生成历史长列表的渲染治理
- 拖动助手面板卡顿问题的真实复现与定位
- 把 Playwright Electron 巡检固化为项目脚本

### 不属于本次任务

- 新增功能、新增页面、新增模型
- 配色方案本身的调整（accent 色值、主题预设配色）——上一轮 `UI设计系统统一` 已完成对比度治理，本次不再动色值
- 画布节点内部的交互重做
- 圆角档位大规模合并（沿用 `UI设计系统统一` 中 D-003 的搁置决定）
- macOS 端的真机验收

## 当前进度

- 当前阶段：第五阶段-巡检自动化固化
- 当前任务：5.1 代码已完成；所有真实界面与交互验收待最终集中执行
- 整体状态：五个阶段的计划内代码均已完成，等待按 `test-report.md` 统一实测
- 已完成任务数量：10（另有 4.2、5.1 代码完成、待人工验收）
- 总任务数量：13
- 当前阻塞问题：4.1 的真实拖动复现需要用户在最终集中测试时启动带调试端口的真实实例并自行发起生成；在此之前不实施推测性性能优化

## 阶段列表

| 阶段 | 阶段目标 | 状态 |
|---|---|---|
| 第一阶段-公共组件基座 | 修掉会被全项目继承的组件缺陷与歧义默认形态 | 已完成 |
| 第二阶段-布局与信息架构 | 修掉实测截图里可见的遮挡、错位、信息密度失衡 | 已完成 |
| 第三阶段-状态表达与排版收敛 | 让"选中/常态/强调"和标题层级全项目一致 | 已完成 |
| 第四阶段-性能定位与长列表治理 | 复现并定位拖动卡顿，治理无虚拟化的长列表 | 代码完成，待最终集中实测 |
| 第五阶段-巡检自动化固化 | 让这次的实测方法可以被随时重跑 | 代码完成，待最终集中实测 |

## 任务清单

| 编号 | 任务名称 | 状态 | 前置任务 | 任务文件 |
|---|---|---|---|---|
| 1.1 | UiModal 补齐键盘关闭与焦点管理 | 已完成 | 无 | [1.1](任务/第一阶段-公共组件基座/1.1-UiModal补齐键盘关闭与焦点管理.md) |
| 1.2 | Toggle 改为无歧义的开关形态 | 已完成 | 无 | [1.2](任务/第一阶段-公共组件基座/1.2-Toggle改为无歧义的开关形态.md) |
| 1.3 | 空/加载/错误状态收口到状态组件 | 已完成 | 无 | [1.3](任务/第一阶段-公共组件基座/1.3-空加载错误状态收口.md) |
| 1.4 | ProgressBar 改 transform 并登记动效档位 | 已完成 | 无 | [1.4](任务/第一阶段-公共组件基座/1.4-ProgressBar改transform并登记档位.md) |
| 2.1 | 生成工作区跟随助手插入量 | 已完成 | 无 | [2.1](任务/第二阶段-布局与信息架构/2.1-生成工作区浮动面板跟随插入量.md) |
| 2.2 | 设置弹窗信息架构重构 | 已完成 | 1.1 | [2.2](任务/第二阶段-布局与信息架构/2.2-设置弹窗信息架构重构.md) |
| 2.3 | 工作区页面头统一 | 已完成 | 无 | [2.3](任务/第二阶段-布局与信息架构/2.3-工作区页面头统一.md) |
| 3.1 | 选中态词汇表统一 | 已完成 | 无 | [3.1](任务/第三阶段-状态表达与排版收敛/3.1-选中态词汇表统一.md) |
| 3.2 | 模型显示与管理列表视觉重做 | 已完成 | 1.2, 3.1 | [3.2](任务/第三阶段-状态表达与排版收敛/3.2-模型显示与管理列表视觉重做.md) |
| 3.3 | 排版令牌落地 | 已完成 | 2.3 | [3.3](任务/第三阶段-状态表达与排版收敛/3.3-排版令牌落地.md) |
| 4.1 | 用真实生成任务复现并定位拖动卡顿 | 待最终集中实测 | 1.4 | [4.1](任务/第四阶段-性能定位与长列表治理/4.1-真实任务复现拖动卡顿.md) |
| 4.2 | 生成历史长列表渲染治理 | 代码完成，待集中验收 | 4.1（仅虚拟化部分依赖；图片高度预留已完成） | [4.2](任务/第四阶段-性能定位与长列表治理/4.2-生成历史长列表渲染治理.md) |
| 5.1 | Playwright Electron 巡检固化为项目脚本 | 代码完成，待集中验收 | 无 | [5.1](任务/第五阶段-巡检自动化固化/5.1-巡检脚本固化.md) |

## 重要说明

- **本次所有结论都来自真实 Electron 窗口的截图与实测，不是代码推测。** 复现方式见 `01-实施方案.md` 的「巡检方法」小节，任何任务开始前都可以按同样方式重跑一遍确认现状。
- **不要新增颜色值。** 调色只允许在 `src/index.css`、`tailwind.config.js`、`src/core/theme/colorTokens.ts` 三处，且本次任务范围内不需要新增。
- **不要新增 `Ui*` 组件。** 本次任务是让已有组件被正确消费，不是造新组件。确实需要新增时，先在 `重要记录.md` 里记一条并说明现有组件为什么覆盖不了。
- **布局定位不许藏在 CSS 里。** `position`/`top`/`left`/`right`/`bottom`/`z-index` 必须写在组件 className 上。任务 2.1 的根因就是 `scrollbar.css` 里藏了一句 `position: fixed`，靠 grep 搜不出来，只能逐层量祖先链宽度才发现。详见 `重要记录.md` 记录 008。
- **涉及鼠标操作的验证交给用户。** 拖拽、悬停、右键菜单、resize 这些场景，改完写清操作步骤和验证点，不要自己上手判断"手感对了"。
- **每次改完代码必须明确告知是否需要重启 `npm run electron:dev`。**
- 本任务与既有的 `docs/task/UI设计系统统一/` 是接续关系：那一轮做的是令牌体系建设与对比度治理，这一轮做的是令牌落地与实测缺陷修复。两边的记录不合并。

## 最近记录

### 2026-07-27（第五阶段代码实施完成）

- 新增 `npm run ui:tour`：共享配置覆盖生成、设置、画布、工具箱、资产库、助手六类界面，包含空态、下拉、聚焦、悬浮和右键状态；默认两档尺寸共 32 张截图并生成 Markdown 索引。
- `check:ui-visual` 扩展为同一场景清单、两档尺寸与十一条 DOM 规则，命中规则或场景失败时返回非零退出码。
- `UiPageHeader` 与工作区插入根只增加无视觉影响的 `data-*` 审计锚点；没有新增 UI 组件、颜色或业务状态。
- 两条命令使用 Playwright Electron API 与隔离 userData；`.ui-tour/`、`.ui-audit/` 均由 Git 忽略。
- 新增 6 条脚本单测；表面、颜色、Lint、TypeScript、模型 i18n、114 个测试文件与 Electron 构建全部通过。
- 依项目约定未执行会点击、悬浮和右键真实窗口的两条巡检命令；统一验收步骤已写入 `test-report.md` 的 M 组。
- 4.1 尚未形成经过真实实例验证的 CDP 性能脚本，因此没有新增 `ui:perf`。
- 第五阶段实现提交：`13f0cf8`（`feat: 固化界面巡检自动化`）。

### 2026-07-27（第四阶段代码实施完成）

- 生成结果图片继续使用 `w-full h-auto` 自然高度，不改变成功态视觉；渲染前通过实际结果尺寸、请求尺寸或历史学习尺寸写入 `width` / `height` 固有比例，避免等待解码后才撑开卡片。
- 新生成任务已有的实际 `dimensions` 现在会随历史记录持久化；旧记录首次图片加载时学习各图片真实尺寸，复用既有任务保存链路写回 `params`，无需数据库迁移。
- 已知图片比例的排队、准备、生成和失败状态提前使用目标结果高度；无尺寸的旧记录继续回退 `h-64`。
- `TaskCard` 修改后按文件体积约束拆出 `TaskCardToolbar`，行为与国际化文案保持不变。
- 未实施生成历史虚拟化：4.1 尚未完成真实拖动复现，没有证据证明长列表是根因；`content-visibility` 仍明确禁止。
- `react-window` 与 `@types/react-window` 仅存在于依赖清单，没有任何源码使用点，记录为后续依赖清理建议，本阶段不删除。
- 表面、颜色、Lint、TypeScript、模型 i18n、完整测试与 Electron 构建全部通过；真实拖动、快速滚动和视觉零变化统一记录在 `test-report.md`。
- 第四阶段代码提交：`1de18ee`（`refactor: 完成生成历史图片高度预留`）。

### 2026-07-27（第二阶段完成）

- 生成历史滚动容器移除藏在 `scrollbar.css` 中的视口固定定位，底部输入面板改以工作区容器绝对定位，助手插入量可沿既有布局链实时传递。
- 设置弹窗合并为 208px 单列折叠导航，标题横跨头部，删除底部关闭区；最小窗口内容宽度由约 518px 提升到约 656px，弹窗高度由 500px 降为 440px。
- 设置分区标题与字段标签恢复正确层级，资产库设置去掉内嵌卡片。
- 生成、画布项目、工具箱、资产库四个完整工作区统一消费 `UiPageHeader`，使用 24px 内容内边距与 1152px 最大宽度。
- 表面、颜色、Lint、TypeScript、模型 i18n、完整单测与 Electron 构建全部通过；真实交互验收统一留在 `test-report.md`。

### 2026-07-27（第一阶段完成）

- `UiModal` 与 `AlertDialog` 共用顶层弹窗栈，补齐 Esc、焦点接管/还原、Tab 循环与 ARIA 语义；新增 4 条单测覆盖关键时序。
- `Toggle` 复用现有 `UiSwitch`，21 个调用点无需改签名即可统一为滑块形态，位移动效收敛为 150ms。
- 状态组件扩展紧凑档，`UiEmpty` / `UiLoading` / `UiError` 使用量分别由 4/2/1 提升到 29/12/5；原生 option 文案和画布节点专属提示按边界保留。
- `ProgressBar` 改为 `scaleX`，移除编译期 accent 颜色，新增具名数据补间常量 `UI_DATA_TWEEN_MS`。
- `check:surface:strict`、`check:colors`、Lint、TypeScript、模型 i18n、完整单测和 `electron:build` 全部通过；需要真实交互的验收统一记录在 `test-report.md`。

### 2026-07-27（下午，用户反馈后修订）

根据用户反馈修订了 5 个任务文件和 `重要记录.md`：

- **2.1 根因下钻**：追加了祖先链实测，定位到真正的罪魁是 `src/styles/scrollbar.css:3` 的 `.app-scroll-container { position: fixed }`——一个叫「滚动条样式」的 class 把整个生成工作区钉死在视口上。不是"要不要避让"的设计选择，而是项目本来就在避让（画布是对的）但生成工作区漏了。改动量从"需要探测"降级为 **6-7 行**。同时算出复现临界值：**逻辑宽度 < 2160px 才会重叠**，解释了用户 3840×2160@150%（逻辑 2560）看不到、而 1080p 不缩放和 4K@200% 会撞。
- **3.2 方案修正**：用户澄清「开启=高亮」是有意设计，与设置→界面 的布尔项同一思路。**保留该语义映射**，改为把高亮从整行（约 600×36）收回到开关控件（约 44×24）——同一语义视觉面积差 15 倍才是问题所在。
- **4.1 解除阻塞**：改为挂载用户真实开发实例（`HENJI_ELECTRON_REMOTE_DEBUGGING_PORT=9222` + CDP），生成由用户自己发起，我只做只读测量。不再需要我持有 API Key。
- **4.2 方案定稿**：图片保持自然高度（用户确认当前视觉好），用 `aspect-ratio` 预留空间消除跳动，验收前提是截图逐像素确认视觉零变化。这部分**不依赖 4.1**，可先做。
- **2.2 决定落地**：底部「确定」及整条底部区域确定去掉。
- **新增通用规则**（记录 008）：样式表只做它名字承诺的事，布局定位不许藏在 CSS 里。这是第二次遇到同类问题（上一轮是 `storyboard.css` 藏全局主题变量）。

### 2026-07-27（上午，首次巡检）

- 用 Playwright Electron API（`_electron.launch()` + `firstWindow()`）巡检真实窗口，覆盖生成/画布/工具箱/资产库/设置/助手六个界面与 1440×900、960×640 两档尺寸，共 31 张截图。
- 实测确认：`UiModal` 无 Esc 关闭、无焦点转移、无 `role="dialog"`；生成工作区在助手侧栏打开时不避让；排版令牌消费率 19/791。
- 性能项：拖动助手侧栏在空历史与 30 张仿真卡条件下实测稳定 60fps（avg 16.7ms、0 次超 33ms 帧），**未复现用户报告的卡顿**，已拆为任务 4.1 继续定位。
- 建立本任务计划，5 个阶段 13 个任务。
