# 2.2 设置弹窗信息架构重构

## 任务状态

- 状态：已完成（人工验收待统一执行）
- 所属阶段：第二阶段-布局与信息架构
- 前置任务：1.1（弹窗键盘与焦点行为）
- 关联任务：1.2（布尔项改开关会直接改善设置页密度）、3.3（排版令牌落地）
- 是否可以并行：否（应在 1.1 和 1.2 之后做，否则会返工）

## 任务目标

让设置弹窗的宽度用在内容上而不是导航上，并修掉标题归属不清、分组标题层级倒置、底部按钮语义模糊三个问题。

## 当前情况

### 相关代码位置

- `src/components/Settings/index.tsx`：弹窗外壳、两级导航、底部按钮
- `src/components/Settings/` 下各分区组件
- `src/components/ui/primitives.tsx`：`UiModal`（`hideHeader` 模式）、`UiNavButton`

### 当前结构

```
┌─────────┬──────────────────────────────────────┐
│ 设置    │                                  [×] │  ← 标题格子有右边框
├─────────┼──────────┬───────────────────────────┤
│ 通用    │ 基础设置 │ 语言                      │
│ 密钥    │ 数据下载 │ 选择界面显示语言          │
│ 界面    │ 行为并发 │ 界面语言                  │
│ 模型    │ 更新维护 │ [自动 / Auto        ▾]    │
│         │          │                           │
│         │          │ 历史记录                  │
│  232px  │  285px   │ 保存数量限制              │
│         │          │ [50               ▴▾]     │
│         │          │                           │
│         │          │ （约 900px 空白）         │
├─────────┴──────────┴───────────────────────────┤
│                                        [确定]  │
└────────────────────────────────────────────────┘
```

### 实测确认的问题

1. **两级导航吃掉 60% 宽度**：一级 232px + 二级 285px = 517px，弹窗总宽 864px（960 窗口下）。内容区只剩 347px 且只有两个字段，下方约 500px 空白。
2. **标题归属不清**：左上角「设置」格子与一级导航列同宽、有右边框，视觉上是导航列的表头而不是弹窗标题。
3. **层级倒置**：分组标题（「语言」「历史记录」）是 12px 暗灰，字段标签（「界面语言」「保存数量限制」）是 14px 白色。分组比它的子项更弱，读不出结构。
4. **两级导航选中态不一致**：一级选中是「灰底 + **白字** + 右侧蓝竖条」，二级选中是「灰底 + **蓝字** + 右侧蓝竖条」。
5. **格线过多**：标题格右边框 + 一级导航右边框 + 二级导航右边框 + 头部下边框 + 底部上边框，把弹窗切成 6 块。
6. **底部「确定」语义模糊**：设置项是即时生效的，「确定」实际等于「关闭」。用一个 accent 实心按钮占据一整条 110px 高的区域来表达"关闭"，权重过高。
7. **小窗口下弹窗几乎撑满**：960×640 下弹窗 864×501，上下留白仅约 70px。

### 可以复用的部分

- `UiNavButton` 已经存在，一级/二级导航都可以用它，选中态由它统一提供（配合任务 3.1）
- `UiModal` 的 `widthClassName` / `overlayClassName` 是现成的尺寸控制入口
- 各分区组件内部结构不需要动（本任务只动外壳与分组层级表达）

## 实施方案

**优先级排序**：先做低风险高收益的 2、3、4、5、6，再评估 1（两级导航合并）——后者改动面最大。

### 必做（低风险）

- **标题归属**：去掉标题格子的右边框，让「设置」横跨整个头部宽度，和右侧关闭按钮同在一条头部栏里。
- **层级倒置**：分组标题改用 `UI_TEXT_SECTION_CLASS`（`text-sm font-medium text-text-dark`），字段标签改用 `UI_TEXT_LABEL_CLASS`（`text-sm font-medium text-text-soft`）。这样分组是亮的、字段标签是次亮的，层级正过来。说明文字用 `UI_TEXT_META_CLASS`。
- **导航选中态一致**：两级导航都走 `UiNavButton`，选中态统一（具体形态由任务 3.1 决定，本任务只保证两级用同一个）。
- **减少格线**：保留一级/二级导航的分隔线（它们确实是不同的区域），去掉标题格右边框和至少一条冗余边框。**判据**：同一层视觉深度只画一次边界。
- **去掉底部「确定」及整条底部区域**（已定，见 `重要记录.md` 记录 005）。设置项即时生效，「确定」实际等于「关闭」，而关闭已经有右上角 × 、Esc（任务 1.1 补齐）和点遮罩三条路径。连带去掉底部那条约 110px 高的容器——这对「960×640 下弹窗几乎撑满屏幕」是直接改善。
  - 执行时确认：`UiModal` 的 `footer` prop 在设置弹窗这处不再传值即可，不需要改 `UiModal` 本身
  - 确认没有任何设置项依赖"点确定才提交"（如果有，那个设置项本身要改成即时生效，不能靠保留按钮兜底）

### 需要评估（高风险）

- **两级导航合并**：把一级+二级压成单列可折叠导航（一级为分组标题，二级为其下的项），或者把二级导航改为内容区顶部的横向 Tab。前者省一整列宽度，后者把导航压到一行高。
  - 执行时先做一个改动量最小的原型截图对比，再决定是否推进。
  - 如果推进，需要重新分配弹窗宽度（当前 `widthClassName` 是多少要先查）。

### 不要做的

- 不要重新组织设置项的归属（哪个设置在哪个分区），那是产品决策
- ~~不要在设置弹窗里引入毛玻璃（它压在纯色 UI 上，按规范应该用不透明底色）~~
  **2026-07-31 推翻**：这条的判据是「背后是纯色 UI 还是用户内容」，而设置弹窗铺满 90vw，
  背后压的是画布 / 生成结果这类图片内容，不是纯色，符合准入条件。
  已改为 `UiModal surface="glass"`，内部走 `UI_GLASS_ADAPTIVE_*` 跟随毛玻璃开关退化。
  见下方执行记录 2026-07-31。

## 执行步骤

1. 读 `src/components/Settings/index.tsx` 完整实现，画出当前的容器层级和边框分布。
2. 做「必做」项，每项一次小提交：
   - 标题格右边框
   - 分组标题/字段标签的令牌替换（先只改设置弹窗内，全项目的排版令牌落地是任务 3.3）
   - 两级导航统一走 `UiNavButton`
   - 减少冗余格线
   - 去掉底部「确定」及整条底部区域（先确认没有设置项依赖"点确定才提交"）
3. 用巡检脚本在 1440×900 和 960×640 下各截一次图，和改前对比。
4. 评估两级导航合并：做原型 → 截图对比 → 在 `重要记录.md` 记决定 → 决定推进才实施。
5. 跑检查命令 + `npm run electron:build && npm run check:ui-visual`（改了层级色必须复跑对比度）。

## 涉及内容

### 修改文件

- `src/components/Settings/index.tsx`：外壳、导航、底部区域
- `src/components/Settings/` 下的分区组件：分组标题/字段标签的令牌替换
- `src/components/ui/primitives.tsx`：仅在 `UiNavButton` 需要支持二级形态时才改

### 可能废弃

- 设置弹窗底部的确认按钮区（如果决定去掉）

## 验收标准

- [x] 「设置」标题横跨头部，不再看起来像导航列表头
- [x] 分组标题在视觉上强于它下面的字段标签（层级正过来）
- [x] 一级和二级导航合并为单列；只有当前二级项使用统一选中态
- [x] 弹窗内的边框数量减少，同一层深度不重复画边界
- [x] 底部「确定」及整条底部区域已去掉；960×640 时由 500px 降为 440px，1440×900 时由 684px 降为 612px
- [ ] 关闭路径全部可用：右上角 ×、Esc、点遮罩
- [x] 不存在依赖底部按钮统一提交的逻辑；原按钮仅调用 `handleClose`，需要显式保存的局部功能保留自己的保存按钮
- [x] 960px 窗口下内容区由约 518px 增至约 656px，增加约 138px
- [ ] 960×640 下弹窗不撑满屏幕，上下留白合理
- [ ] `npm run check:ui-visual` 三屏四项仍为 0
- [x] `npm run lint`、`npx tsc --noEmit`、`npm test`、`npm run check:colors`、`npm run check:surface:strict` 通过
- [x] 任务总览已同步更新

## 需要用户手动验证的点

统一维护在 `TASK_DIR/test-report.md` 的“第二阶段 → F. 设置弹窗信息架构”，本文件不重复维护操作清单。

## 执行记录

### 2026-07-27

- 完成内容：标题横跨头部，两列导航合并为单列折叠结构，删除底部关闭区，修正分区标题与字段标签层级。
- 实际改动：导航由 156px + 190px 缩为 208px；最小高度由 500px 缩为 440px；`SectionCard` 改用 section 标题档；资产库设置移除嵌套 `UiPanel`。
- 验证结果：确认原底部按钮只有关闭语义；内容宽度与高度可由静态尺寸精确计算；Lint、TypeScript、表面、颜色、模型 i18n、486 条单测和 Electron 构建通过。
- 遗留问题：导航点击、三种关闭方式、即时生效、最小窗口观感和真实对比度等待用户最终统一验收。
- 与原计划的偏差：未做一次性静态原型截图；依据当前代码尺寸直接落地单列折叠方案，并将决定写入 `重要记录.md` 记录 011。
- 对后续任务的影响：3.1 只需统一叶子导航选中态；3.3 可继续扩大排版令牌覆盖面。
- 相关提交：`1d9870f`

### 2026-07-31

- 完成内容：三件事一起做——弹窗整块改毛玻璃；左侧改常驻目录（全展开）；每个大类改单页滚动 + 锚点定位 + scroll-spy；内容列限宽 `max-w-3xl`。
- 推翻的旧决定：「不要在设置弹窗里引入毛玻璃」，理由见上方「不要做的」。
- 实际改动：
  - `UiModal` 新增 `surface="glass"` 枚举变体；新增 `--ui-scrim-tint-soft` 档位——遮罩与玻璃 tint 相乘会把背后压到只剩 20% 透光率，两者必须一起改。
  - 弹窗内部改用既有的 `UI_GLASS_ADAPTIVE_REGION/SURFACE/DIVIDER`；中性控件本来就带 `ui-glass-adaptive-control`，只是缺一个 `.ui-glass` 祖先才没生效，所以调用点一行没动。
  - `UiNavButton` 的静息 hover 从 `hover:bg-surface-dark` 换成 `.ui-glass-adaptive-nav`（不透明灰压在玻璃上会变成实心贴片），顺带修好资产库侧栏的同一问题。
  - 各 tab 由「按分节条件渲染」改为「一整页连排 + `SettingsSection` 锚点」；分节内容太少（基础设置只有两个字段独占一屏）的问题因此消失，没有改动任何设置项的归属。
  - 新增 `useSettingsScrollSpy`：程序化滚动期间屏蔽 spy、尾部占位测量（否则最后一个分节滚不到顶）。
  - 模型大类只有一个分节，目录不再画只有一项的子列表；巡检场景同步改为点大类本身。
- 踩到的坑：切大类后的「回到顶部」原本放在 `requestAnimationFrame` 里，会和紧接着的同大类分节点击撞车，把已经开始的平滑滚动拽回顶部。改成在 effect 里同步执行，并给延迟补位加 token 判重。
- 验证结果：目录 6 次点击（含跨大类、含各大类最后一个分节）全部把目标分节停在内容区顶部 16px；手动滚动时高亮跟随正确；`check:ui-visual` 三个设置场景除既有的 `NumberInput` 步进器 16×24 命中外为 0，无对比度回归。
- 遗留问题：目录在 1440x900 下仍需滚动（17 行放不下）；`NumberInput` 步进器低于 24px 命中区是既有问题，未在本次处理。
