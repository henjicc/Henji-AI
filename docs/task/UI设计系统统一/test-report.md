# 测试与验证报告

本文件是**全部任务**的验证汇总，分两部分：

- **第一部分：自动化验证结果** — 由 AI 执行，记录真实输出
- **第二部分：待用户手动验证清单** — 由用户统一执行，AI 不代为断言

> 用户要求：手动测试工作全部留到最后统一执行，所有任务的手动验证项都汇总到本文件。

---

# 第一部分：自动化验证结果

## 第一阶段（1.1 / 1.2 / 1.3）

三个任务完成后的最终状态，均为实际命令输出：

| 检查项 | 结果 |
|---|---|
| `npx tsc -p tsconfig.json --noEmit` | 通过（仅存量 `src/core/imageEdit/worker/webgpuRuntime.ts` 的 `TS2206`，HEAD 上已存在，与本任务无关） |
| `npx eslint src --ext ts,tsx --max-warnings 0` | 通过，0 error |
| `npm run check:colors` | 通过 |
| `npm run check:model-i18n` | 通过 |
| `npm run check:surface` | exit 0（告警式）：表面问题 8 个文件 + 手写弹窗 15 处 |
| `npm run check:surface:strict` | exit 1（符合预期，存量未清零，第二阶段处理） |
| `npx vitest run` | **479 通过 / 11 跳过**，与阶段开始时一致 |

### 令牌收口量化结果

| 项 | 阶段开始 | 阶段结束 | 说明 |
|---|---|---|---|
| `rounded-[` | 25 | 19 | 剩余全部是 `var(--node-radius)` ×16 与 `inherit` ×3，均合法 |
| `z-[` | 24 | **0** | |
| `shadow-[` | 15 | **0** | |
| `border-[` | 40 | **0** | |
| `text-[` | 20 | 5 | 剩余全部是 `em` 相对值与 `length:var()`，均合法 |
| rgb/rgba 字面量 | 49 | **0** | |
| 静默失效的透明度类 | 8（此前未知） | **0** | 见下 |
| ESLint 视觉规则 | 1 类 | 6 类 | |

### 像素等价性验证（针对声称"纯登记、不改观感"的替换）

用 `npx tailwindcss` 实际生成 CSS 后逐条比对：

- `text-2xs/3xs/4xs/13/14/15` 均只产出 `font-size`，不带 `line-height` → 与原 `text-[Npx]` 等价 ✅
- `text-[12px] leading-6` → `text-xs leading-6`：实测生成的 CSS 中 `.leading-6` 位于 `.text-xs` **之后**，因此 `leading-6` 胜出，行高不变 ✅
- `shadow-panel` 取值刻意等于 Tailwind `shadow-2xl` → 换名无变化 ✅
- `rounded-[24px]` → `rounded-3xl`：Tailwind `rounded-3xl` = 1.5rem = 24px，精确相等 ✅
- 新令牌全部确认生成：`border-veil*`、`bg-veil*`、`from/to-veil*`、`shadow-node-selected/node-error/thumb/thumb-sm`、`rounded-hairline`、`border-1.5`、`z-*` 十档 ✅

### ESLint 规则反向验证

每条新规则都用临时探针文件确认**真的会触发**（正则写错会静默不生效）：

| 探针写法 | 是否拦截 |
|---|---|
| `"text-[13px]"` | ✅ |
| `"shadow-[0_0_0_1px_red]"` | ✅ |
| `"z-[9999]"` | ✅ |
| `"border-[rgba(255,255,255,0.5)]"` | ✅ |
| `"rounded-[7px]"` | ✅ |
| 模板串 `` `bg-[rgb(1,2,3)]` `` | ✅ |
| `"rounded-[var(--node-radius)]"` | ✅ 正确放行 |
| `"rounded-[inherit]"` | ✅ 正确放行 |

探针文件已删除，`git status` 干净。

规则上线当场抓出两处此前遗漏（因早期 grep 只扫了 `.tsx`）：
`createSuggestionRenderer.ts` 与其测试中的 `z-[1000]`，已改为 `z-dropdown`。

### 未达成项（已记录理由）

- **1.1 验收标准中"圆角只出现 `rounded-lg/xl/full` 三种写法"未达成**。原因见 `decisions.md` D-003：存量 `rounded`/`rounded-md`/`rounded-lg` 约 285 处，全部归档会产生 4px/6px→8px 的可见变化，风险与收益不匹配，改为"任意值归零 + ESLint 禁止新增"，档位收敛留待单独决策。
- **1.3 的 CI 接入未达成**。原因见 `重要记录.md` 记录 007：`.github/workflows/build.yml` 仍是 Tauri 时代产物（引用已删除的 `src-tauri/` 与 `tauri:build:ci`），本身已失效，不宜在其上叠加 UI 检查。已改为接入本地 `build` / `electron:build` 链路，满足实施方案"接入 CI **或**构建链路"的验收口径。

---

# 第二部分：待用户手动验证清单

> 说明：以下项目涉及鼠标交互、视觉观感、窗口行为，必须在真实 Electron 窗口（`npm run electron:dev`）中由用户操作确认。AI 不对这些项目的结果做任何断言。
>
> ⚠️ **A~E 组涉及 `tailwind.config.js` 改动，必须重启 `npm run electron:dev`。** F 组之后的改动只涉及组件与 className，热更新即可。
>
> 勾选方式：验证通过打 `[x]`，有问题请在该项后写明现象。

**分组与来源对照：**

| 组 | 来源任务 | 主题 |
|---|---|---|
| A~D | 1.1 / 1.2 | 令牌收口、z-index 层序、字号圆角 |
| E | 全体 | 回归底线 |
| F | 2.1 | 助手侧栏去卡片化 |
| G | 2.2 + 3.1 | 生成工作区任务卡 + 长列表 |
| H | 2.3 | 弹窗统一走 `UiModal` |
| I | 2.4 | 媒体查看器与播放器表面 |
| J | 2.5 | 日志面板与画布节点 |
| K | 3.3 | 装饰开销治理（观感影响最直接） |
| L | 3.2 | 助手 resize 手感（**需回传结论才能收尾**） |
| M | 1.4 | CI 迁移到 Electron（需在 GitHub 上触发） |
| N | 收尾 | 选项集合静息态去边框 |

## A. 优先验证：本阶段唯一"预期会看到变化"的地方

这三处此前因类名静默失效而**没有渲染出应有的效果**，修复后会首次显示出来。请重点确认观感是否可接受：

- [ ] **提示词优化预览遮罩**：生成页面点提示词优化按钮 → 预览浮层现在会有半透明深色底（`bg-app/70`），底下的编辑器内容应被遮住而不是透出来
- [ ] **上传缩略图边框**：生成页面上传 2 张以上图片 → 堆叠缩略图的白色边框现在会真正显示（此前退化成灰色默认边框）
- [ ] **模型选择器背景**：打开模型选择面板 → 面板与其搜索区的深色底是否正常

## B. 画布：本阶段改动最集中的区域

- [ ] 打开画布，各类节点（图片/视频/音频/上传/文本标注/分组/分镜/摄影台/数值源）外观与之前**一致**
- [ ] 点选节点 → 选中描边（蓝色 1px + 边框变色）正常
- [ ] 鼠标悬停未选中节点 → 边框略微变亮
- [ ] **改强调色后再看选中描边**：设置 → 界面 → 主题外观，把强调色改成明显不同的颜色（如橙色/绿色），回到画布点选节点 → 描边应跟着变色（此前会一直是蓝色，这是本次修复的缺陷）
- [ ] 生成失败的结果节点 → 红色描边正常
- [ ] 分组节点（GroupNode）外观正常，圆角与半透明底正常
- [ ] Alt 拖拽复制节点 → 副本盖在其他节点之上
- [ ] minimap 显示正常且在画布内容之上
- [ ] 节点逐行参数（模型行/媒体行/参数行）边框正常
- [ ] 分镜格子编辑器、分镜拆分卡片外观正常

## C. 浮层层序（1.2 的核心验证，改动风险最集中）

请按顺序验证层序关系，这些是 z-index 重排后最可能出问题的地方：

- [ ] 打开设置弹窗 → 能盖住助手侧栏与资产库浮层
- [ ] 设置弹窗内打开下拉（语言选择） → 下拉在弹窗之上，不被裁切
- [ ] 生成页面打开模型选择器 / 分辨率面板 / 预设面板 → 不被输入区遮挡
- [ ] 预设面板中点删除 → 删除确认浮层可见且在面板之上
- [ ] 同时打开资产库浮层 + 助手侧栏 → 层序合理
- [ ] 从历史记录或资产库拖拽文件 → **拖拽预览始终跟随鼠标且在最上层**（含拖过弹窗/侧栏时）
- [ ] 触发通知 Toast（生成完成）→ Toast 可见，且在弹窗之上
- [ ] 打开图片查看器 → 盖住其他所有浮层
- [ ] 打开视频查看器 / 音频查看器 → 同上
- [ ] 任意位置悬停出 tooltip → tooltip 在通知 Toast 之上
- [ ] 窗口标题栏始终可见可点，不被任何浮层盖住
- [ ] 画布中右键节点出菜单 / 节点下载菜单 / 节点参数气泡 → 都不被其他节点遮挡
- [ ] 画布连接提示（连线成功/失败的顶部提示）正常显示
- [ ] 摄影台时间轴：框选、缓动曲线编辑浮层正常显示
- [ ] 提示词编辑器输入 `@` 或 `/` 触发建议列表 → 列表正常弹出且不被遮挡（本次从 `z-[1000]` 改为 `z-dropdown`）
- [ ] 大文件上传选择弹窗、更新弹窗、重命名弹窗、清空历史确认 → 均正常显示在最上层
- [ ] 摄影台节点全屏弹窗（画布节点里打开摄影台）→ 正常全屏显示（本次移除了它的 z 覆盖）

## D. 字号与圆角（1.1 的排版部分）

- [ ] 设置弹窗左侧导航文字大小正常（`text-15`）
- [ ] 设置弹窗底部关闭按钮文字大小正常
- [ ] 画布节点标题文字大小正常（`text-14`）
- [ ] 模型选择列表项文字大小正常（`text-13`）
- [ ] 生成页面悬浮输入面板圆角正常（`rounded-3xl` = 24px，与之前相同）
- [ ] 上传缩略图圆角正常（11px → 12px，理论上几乎看不出）
- [ ] 摄影台时间轴关键帧小菱形标记显示正常
- [ ] 全局扫一眼各处文字：没有异常变大/变小或行距突变

## E. 回归底线

- [ ] 生成一次图片、一次视频，全流程正常
- [ ] 智能助手能正常对话并执行一次工具调用
- [ ] 设置各页签均能正常打开、修改并保存
- [ ] 资产库能正常打开、浏览、拖拽
- [ ] 日志窗口能正常打开

## F. 助手侧栏去卡片化（2.1）

- [ ] `Ctrl/Cmd + Shift + A` 打开助手侧栏 → 侧栏外观正常，边框阴影只有一层
- [ ] 发一条消息 → 用户消息与助手消息能清楚区分，且不再有"卡里套卡"的感觉
- [ ] 触发一次工具调用（如让助手改画布）→ 工具活动块、审批卡在消息内不再叠边框
- [ ] 切换到「历史」「记忆」视图 → 条目层级清晰，无多余边框
- [ ] 三种停靠模式（左/右/浮动）切换 → 外壳与内部层级都正常
- [ ] 拖拽侧栏标题栏移动、拖拽边缘 resize → 外壳换成 `UiPanel` 后定位与手柄仍正常
- [ ] 助手无历史记录时 → 空状态显示正常

## G. 生成工作区任务卡与长列表（2.2 + 3.1）

- [ ] 发起一次图片生成 → 依次观察"准备中 → 生成中（含进度条）"，确认卡片高度不跳动、进度条平滑到 100%
- [ ] 生成完成 → 结果图片容器外观正常，边界感足够但不刺眼
- [ ] 点击结果图片 → 查看器正常打开
- [ ] 右键结果图片 → 上下文菜单正常（复制/收藏/下载）
- [ ] 从结果图片拖拽到画布或外部 → 拖拽正常（`draggable` 透传是否生效的关键验证）
- [ ] 发起一次注定失败的生成（不填必需输入或断网）→ 错误状态正常，「问助手」「重试」可用
- [ ] 同时发起多个生成使其排队 → 排队状态显示正常
- [ ] 历史筛选栏输入匹配不到的关键词 → 空状态显示正常
- [ ] 视频结果卡片 → 缩略图、播放按钮、点击播放均正常
- [ ] 历史记录较多时上下快速滚动 → 流畅、**无白屏**、无跳动（`content-visibility` 的关键验证点）
- [ ] 发起新生成 → 列表自动滚到底部，能看到新任务
- [ ] 生成过程中滚动到列表中部 → 进度更新不会把视口强行拉走
- [ ] 删除一个任务 → 列表正确更新，滚动位置不乱跳
- [ ] 切换到其他工作区再切回 → 滚动位置与内容正常

## H. 弹窗统一走 UiModal（2.3）

每个改造过的弹窗都要实际打开：

- [ ] 项目重命名弹窗：打开 → 输入 → 回车确认 / Esc 取消 / 点遮罩关闭
- [ ] 创建预设弹窗：同上
- [ ] 添加自定义模型弹窗：表单输入、滚动、提交
- [ ] 大文件上传选择弹窗：三个选项可点，选择后行为正确
- [ ] 清空历史确认弹窗：确认与取消
- [ ] 删除任务确认弹窗：确认与取消
- [ ] 更新弹窗：检查更新 → 下载进度显示 → 关闭
- [ ] 设置内的数据迁移进度弹窗：迁移过程中显示正常，不被误关
- [ ] 助手侧栏打开时开弹窗 → 仍正确覆盖
- [ ] **在画布页开弹窗 → 仍然居中**（`UiModal` 用 portal 的原因，关键回归点）

## I. 媒体查看器与播放器（2.4）

- [ ] 生成一段音频 → 任务卡内播放器外观（应无多余边框）、播放/暂停/拖动进度/音量均正常
- [ ] 点击音频结果打开 `AudioViewerModal` → 播放器外观与改造前一致，功能正常
- [ ] 设置 → 语音克隆相关面板中的音频预览 → 播放器嵌在卡片内不再套娃
- [ ] 播放器上悬停查看时间 tooltip → 正常显示
- [ ] 打开视频查看器 → 控件条悬浮在视频上，播放/进度/音量/全屏均可用
- [ ] 视频查看器控件条在深色视频与亮色视频上都清晰可辨

## J. 日志面板与画布节点（2.5）

- [ ] 打开日志窗口 → 面板层级正常，无多余边框
- [ ] 日志窗口内切换到助手追踪视图 → 追踪列表滚动流畅（虚拟化未坏）、条目可展开
- [ ] 展开一条追踪详情 → 详情面板层级正常
- [ ] 打开画布 → 各类节点外观与改造前**完全一致**（本任务对节点只加注释，外观有变化说明误改）
- [ ] 生成页面的悬浮输入面板外观正常
- [ ] 打开快速标记/裁剪工具 → 拖放区提示正常

## K. 装饰开销治理（3.3，观感影响最直接）

移除了 10 处 `backdrop-blur`、改写了全部 `transition-all`、收敛了阴影档位。以下逐项确认观感是否可接受：

- [ ] 打开图片查看器 → 遮罩不再模糊（底色已 70~90% 不透明），观感是否可接受
- [ ] 打开视频查看器 → 同上，控件条在深色与亮色视频上都清晰
- [ ] 触发通知 Toast → 观感正常，文字清晰
- [ ] 画布：节点、分组节点、视频节点 → 内容在画布背景上仍清晰可读
- [ ] 资产库卡片 → 观感正常
- [ ] 各弹窗打开/关闭动画 → 过渡仍流畅（验证 `transition-all` 替换未破坏动画）
- [ ] 助手侧栏三种模式切换的过渡动画 → 仍流畅
- [ ] 按钮/选项的 hover 过渡 → 仍有过渡，不生硬
- [ ] 提示词优化流式输出时 → 遮罩不再模糊，是否可接受

## L. 助手 resize 手感（3.2，需要回传结论）

⚠️ **这一组决定 3.2 是关闭还是继续做。** 3.2 目前状态是「已分析，待测量」——首选手段 `contain: layout` 已被证据否决（工作区内有 9 个 `position: fixed` 元素），而 3.1 的 `content-visibility` 大概率已经覆盖了主要成本。

- [ ] 助手切到左停靠 → 拖右边缘 resize → 跟手，工作区跟随收缩，无明显延迟
- [ ] 助手切到右停靠 → 拖左边缘 resize → 同上
- [ ] **生成任务进行中（有进度条）时做上述 resize** → 这是你最初报告卡顿的场景，感受是否改善
- [ ] 历史记录很多时做上述 resize → 感受是否改善
- [ ] 浮动模式拖拽移动面板 → 仍然跟手
- [ ] 浮动模式拖拽右下角缩放 → 正常
- [ ] resize 到最小/最大边界 → 有正确钳制，工作区不被压到不可用
- [ ] 用键盘方向键操作 resize 手柄 → 仍正常

**回传方式**：
- 若**主观已不卡** → 直接说一句，3.2 关闭
- 若**仍卡** → 用 DevTools Performance 录制上述操作，回传 Layout / Recalculate Style 的耗时占比与帧率，再决定用哪个手段

## M. CI 迁移到 Electron（1.4，在 GitHub 上验证）

`.github/workflows/build.yml` 已从失效的 Tauri 残留重写为 Electron 的 `checks` + `build` 两个 job。**本机无法验证真实 runner 行为**：

- [ ] 在 GitHub 仓库页 `Actions → Build → Run workflow` 手动触发一次
- [ ] `checks` job 通过（颜色/表面/i18n/双侧 lint 与 tsc）
- [ ] `build` job 在 Windows / macOS 上都能装上原生模块（`better-sqlite3`、`sharp`）并产出安装包
- [ ] 若 `macos-13` 镜像已下线导致失败 → 改为 `macos-14`

## N. 选项集合静息态去边框（收尾改动）

把"容器内并列可点项"的静息态统一为无边框（`UiOptionButton variant="menu"`）。**预期会看到明显变化**，请判断观感：

**去掉边框、静息完全透明的地方：**

- [ ] 生成页提示词优化配置菜单 → 四个选项静息无框，hover 出底色，选中项实底
- [ ] 画布模型选择列表、生成页模型选择器 → 两边选中态现在是同一个蓝（此前一深一浅）
- [ ] 提示词编辑器输入 `@` 或 `/` 的建议列表 → 候选项静息无框
- [ ] 画布空白处双击的节点选择菜单 → 菜单项静息无框
- [ ] 画布节点右键的下载菜单 → 同上
- [ ] 分镜节点的「来图选择器」→ 同上
- [ ] 语言切换下拉、所有 `Dropdown` 下拉选项 → 静息无框
- [ ] 设置 → 大语言模型 → 供应商管理左栏列表 → 静息无框
- [ ] 提示词优化配置编辑器左栏列表 → 静息无框
- [ ] 设置 → 模型管理 → 模型可见性列表 → 隐藏的模型现在无框（仍是 40% 透明度）
- [ ] 摄影台左侧对象列表 → 静息无框
- [ ] **生成页任务卡上的 6 个图标按钮** → 静息完全透明，只有 hover 才出反馈

**去掉边框、静息留一层极淡底（4% 白）撑格子的二维网格：**

- [ ] 生成页模型选择器网格 → 格子边界是否还够清楚？糊的话可以提高底色档位
- [ ] 生成页比例 / 分辨率选择面板 → 同上
- [ ] 参数面板里的比例选择器、预设分辨率、画质档位 → 同上
- [ ] 语音选择面板的语音网格 → 同上

**⚠️ 选中态有一处颜色变化**：比例/分辨率格子此前手写 `bg-accent`（纯强调色），现在统一为公共的 `bg-brand-600`（略深一档）。这是为了和模型选择器、下拉、菜单保持一致。如果觉得变淡了不好看，可以改回去。

**刻意保留边框的地方**（判断是否认可，见 `decisions.md` D-009）：

- [ ] 筛选 chips、数值 marks、`CompositeRadio` 等纯文字小按钮 → 仍有框（去框会变裸文字）
- [ ] 工具箱卡片、摄影台工程列表卡 → 仍是卡片（是内容入口，不是选项）
- [ ] 参数面板的单选（`RadioInput`）→ 仍有框
- [ ] 「上传音频」「选择文件」等动作按钮 → 仍有框（是按钮不是选项）
