# 用户指令同步：连续完成全部任务交接

## 本次变更

- 用户已确认 2.1 方案：规范模型使用 `gpt-image-2`；首批支持 APIMart EXT/官方、KIE、Grsai VIP；Fal 在 1.3 补齐 2:1 后加入。
- 全景固定语义为 2:1、单张、原生 2K，支持质量参数时使用 `medium`；首版最多一张参考图，按是否有图自动切换文生图/图生图路由；产品名称暂用“720°全景”。
- 用户将“全景完成后暂停等待人工确认”更新为“自主连续完成全部 22 项任务”。`2.5` 保留编号，但职责改为自动路线审查和证据归档。
- 已更新任务总览、实施方案、`2.5` 任务文件和重要记录；未修改业务代码，也未替其他执行者修改 1.3/2.3 任务文件。

## 后续执行规则

1. 所有常规技术选型、可回退调整、测试范围和并行边界由团队根据仓库事实、官方证据和实际验证自主决定，不再向用户索要常规确认。
2. 发现方案与现实不符时，先在对应任务文件记录偏差，然后在可回退边界内选择修正、降级、更换方案或禁用问题能力。
3. 仅当同一问题无法跳过、无法降级、无替代方案且无法通过回退继续时，才把任务标为“已阻塞”并请求用户处理。
4. 每个任务仍必须独立验证、更新执行记录、同步总览并创建不混杂其他工作的独立提交。
5. `2.5` 不写新功能代码，只核对 2.1～2.4 证据并归档自主路线决定。未付费质量证据不阻止第三至第五阶段。

## 付费与外部写入边界

- 本轮用户确认的是模型/参数方案和连续实施授权，**不等于真实付费模型调用授权**。
- 不运行 2.1 设计的 60 次真实基准，不触发会消耗供应商额度的生成。
- 不将“接口支持 2:1”写成“已稳定生成无缝全景”。缺少付费真实样本时，画质、接缝、极点和供应商稳定性一律标为“待真实验证”。
- 允许静态资料核查、schema/请求构建测试、模拟响应、本地或已获授权样本、真实 Electron 界面和性能验收。

## 交接给后续执行者

- 1.3 和 2.3 已进入并行实施；任务文件状态与执行记录由各自负责人更新。
- 1.3 应以本交接的全景方案为权威输入，并将 Fal 限制为“补齐 2:1 与对应精确测试后才可执行”。
- 2.2 在 1.3 完成后可直接开始；2.3 可继续使用本地全景样本完成浏览器实现，不需要付费生成作为前置。
- 2.4 应完成元数据、持久化、本地样本与真实 Electron 验收；付费生成质量标为“待真实验证”。
- 2.5 完成归档后直接进入第三、第四阶段，不再发起用户决策请求。

## 提交信息

- 提交标题：`docs(canvas): 更新智能图像工具连续执行规则`
- 本提交只包含计划目录内的文档，不包含业务代码或其他并行工作。
