# 阶段同步：共享基础与全景调研交接

## 审计范围

本次只审计并归口以下四个已提交任务，不修改业务代码：

| 任务 | 提交 | 审计结论 |
|---|---|---|
| 1.1 建立图像能力目录与执行契约 | `2c6fbc00` | 边界清楚，能力声明和执行门槛可供 1.2 消费 |
| 1.2 实现工具条入口与相邻节点编排 | `5a229398` | 边界清楚，当前代码完成，等待真实 Electron 全景入口场景验证 |
| 1.4 建立结果语义与专用界面宿主 | `505f4e19` | 边界清楚，任务文件验收项完成 |
| 2.1 完成全景模型与提示词基准调研 | `1261a91c` | 非付费调研完成，真实质量、样本与用户决策仍待验证 |

逐提交文件清单未发现素材库、设置或助手附件等并行工作的文件。工作区现有 `src/features/assets/hooks/`、`src/i18n/locales/*/settings.json`、`src/stores/settingsStore.ts` 和 `src/stores/settingsStore.test.ts` 改动未被上述提交暂存或提交。

## 跨提交契约结论

1. 1.2 直接调用 1.1 的 `getExecutableCanvasImageCapabilitiesForSourceNode`，执行前还会从当前 Store 重取来源节点并再次查询；工具条没有复制能力发布状态、源媒体或模型编号判断。
2. 1.1 的 `planned + draft + defaultEnabled: false` 与 1.2 的执行门槛共同保证全景尚未实现时不能显示或强行执行；当前唯一可执行能力仍是既有宫格切分本地工具。
3. 1.4 的结果语义包含 `panorama`，但未把“有语义”等同于“球面浏览已实现”。`viewerSurfaceRegistry` 当前明确把 `panorama` 懒加载路由回落到平面查看器，避免出现空白界面。
4. 1.4 交接与 2.3 接入说明一致：2.3 应继续创建 `exportImage` 并写入 `resultKind: 'panorama'`，替换既有 `panorama` loader、清除回落标记，并通过统一查看请求打开；不得新建第二套弹窗 Store，也不得把 WebGL 常驻在 ReactFlow 节点中。
5. 1.2 当前创建型事务只负责相邻放置、连线、选中、持久化和回滚。2.2 若需要把能力编号、提示词模板版本或固定参数写入新节点，必须先扩展共享执行契约，不能在工具条按全景编号增加分支。

本轮未发现需要立即修复的跨提交代码缺陷。

## 联合验证

验证级别按已审计代码的共享契约、迁移和 Store 风险采用 L2 的精确联合验证，不运行真实供应商调用或付费生成。

- 精确 Vitest：能力注册表、1.2 的 3 个测试文件、1.4 的 6 个测试文件，共 10 个文件、40 项测试全部通过。
- 定向 ESLint：四个实现提交涉及的 TypeScript/TSX 文件全部通过，零警告。
- 双语 JSON：`src/i18n/locales/zh-CN/storyboard.json` 与 `src/i18n/locales/en-US/storyboard.json` 解析通过。
- 渲染层 TypeScript：未通过；只报告任务外既有助手附件 `file` 类型缺口，位置为 `electron/main/services/agent-runtime/runner/attachment-context.ts:58,90` 与 `src/core/assistant/attachments.ts:42`。四个审计任务没有新增类型错误；该阻断与各任务原交接一致。

## 当前状态

- 已完成：1.1、1.4，共 2 项。
- 待验证：1.2、2.1，共 2 项。
- 待开始：其余 18 项。
- 当前任务：等待用户确认全景模型方案，随后执行 1.3。
- 1.4 状态按仓库任务文件记为“已完成”。归口开始时收到的初始计数提示仍写“已完成 1 项”，与任务文件冲突；经总任务负责人确认后，已按真实状态纠正为 2 项。

## 用户确认闸门

开始 1.3 前需确认：

1. 规范模型是否保持 `gpt-image-2`，以及首批供应商是否采用 APIMart EXT、APIMart 官方、KIE、Grsai VIP，Fal 待 schema 补齐后加入。
2. 是否固定 2:1、单张、原生 2K；支持质量参数时默认 `medium`。
3. P04 场景参考图、P05 人物参考图及其可使用许可。
4. 产品名称采用“全景”“720°全景”或其他名称；技术契约仍固定为 360°×180° 等距柱状投影。
5. 首次真实基准是否授权 60 次、最高人民币 60 元；未授权时继续保持质量“待验证”，不得宣称模型稳定。

Fal 的静态结论仅为“API 支持自定义 2:1”；仓库当前 schema 缺 2:1，且真实无缝全景质量未验证，不能提前开放。

## 下游开始条件

- 1.3：用户确认上述模型范围和默认参数后开始；负责声明模型家族、供应商兼容组合、语义参数映射，并补齐 Fal schema 时所需资料与测试。
- 2.2：1.3 完成，且 2.1 的实现清单获用户确认后开始；可先完成结构，但真实质量基准通过前不得把未验证组合默认开放。
- 2.3：沿用 1.4 宿主实现，按用户确认的首版查看交互接入球面查看器；不依赖付费生成来完成组件和本地样本验证。
- 2.4：2.2、2.3 完成并取得真实付费授权后，执行生成质量、元数据、持久化和真实 Electron 验收。
- 2.5：2.4 证据齐全后交由用户确认路线；未确认前不启动第三至第五阶段。

## 提交信息

- 本交接与总览、重要记录使用独立文档提交。
- 提交标题：`docs(canvas): 同步全景试点实施进度`。
- 本提交不包含业务代码，也不包含工作区内其他并行改动。
