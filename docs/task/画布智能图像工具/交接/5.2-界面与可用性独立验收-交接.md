# 5.2 界面与可用性独立验收交接

## 验收范围与边界

- 独立验收基线：`f7be9422`；最终复跑使用 5.2 主验收代理完成全部门禁后的新鲜 Electron 产物。
- 只运行项目正式真实 Electron `test:reality` 场景、DOM 审计、截图目视和证据日志审查；本交接不修改业务代码、能力契约或测试夹具。
- 全程未点击任何模型“生成”动作，未使用 `--allow-paid`；全景、结果图、蒙版、宫格和图层均来自程序化本地样本或本地处理。

## 场景结果

| 正式场景 | 960×640 | 目视结论 |
|---|---|---|
| 统一图片能力工具条 | 通过 | 直达项、单一“更多”菜单、三个分组、实验标识与基础动作层级清晰；菜单底部需滚动但可达。 |
| 720°全景工具条/相邻节点 | 通过 | 源图、连线、固定 2:1 全景节点与工具条无遮挡。 |
| 720°全景沉浸式查看器 | 通过 | 球面、平面切换、复位、下载、全屏与关闭命令条完整；关闭后返回画布。 |
| 图片打光节点/可视化编辑器 | 功能场景通过 | 左侧单主预览、右侧模式/预设/补充要求和底部原子动作可达；先关闭再重开流程通过。 |
| 多角度节点/相机编辑器 | 通过 | 轨道、精度提示、方位列表和底部动作可达；关闭后 Three 画布卸载并可返回画布。 |
| 高清放大节点/保存重开 | 通过 | 源图→节点→本地模拟结果连线完整，参数行无裁切。 |
| 人像质感节点/保存重开 | 通过 | 模型近似、未做人脸检测和不保证身份稳定的精度边界在节点内可见。 |
| 元素编辑节点/唯一蒙版编辑器 | 通过 | 画笔、形状、撤销/重做、取消/完成与右侧参数均完整；取消不污染、关闭后可返回画布。 |
| 固定九宫格/本地宫格切分 | 通过 | 固定 3×3 、模型参数、素材组与 9 张本地切分语义可见，无节点叠压。 |
| 图层结果节点/图层界面 | 通过 | 图层 `ready` 结果、单层列表、透明度/显隐、单层/合成导出和底部原子动作均可达。 |

## DOM 审计与目视证据

- 960×640 对全部 10 个场景执行 11 类正式 DOM 规则审计：命中 0，场景失败 0。
- 首轮曾捕获打光编辑器选中态模型名 `GPT Image 2` 的 12px、2.73:1 对比度问题；主验收任务将副标题改为继承选项按钮激活文字令牌后，最终审计已清零。
- 三层表面、文字对比度、圆角、非浮层阴影、隐藏定位、工作区逃逸、横向溢出、双滚动、文本硬裁切、小命中区、页面标题不一致均为零命中。
- 已逐张打开最终 10 张成功截图目视。未发现遮挡、错位、空白、节点重叠或关键动作不可达；更多菜单和两个专用右栏在有限高度下使用明确内部滚动，不是硬裁切。
- 截图与报告仅保留在未跟踪的 `.artifacts/5.2-*`，不纳入 Git。

## 日志与证据可靠性

- 最终 UI 与 audit 均记录 10/10 `setupPassed=true`、10/10 `passed=true`、`setupError=null`；`browserErrors` / `logErrors` / `logWarnings` 均为空，日志查询未截断、无损坏行。
- 未观察到任何供应商生成请求、请求标识或付费记录；所有场景都停在创建节点、应用设置或本地结果模拟。
- 首轮发现场景 `setup` 失败时 `evidence.json` 仍可写入 `passed: true`。主验收任务已将 `setupPassed/setupError` 纳入证据契约，并强制 setup 失败时 `passed=false`；最终复跑的机读证据与 runner 退出码、`index.md` 完全一致。

## 证据索引

- 最终 UI 索引：`.artifacts/5.2-ui-independent-final-latest-960/index.md`。
- 最终 UI 机读证据：`.artifacts/5.2-ui-independent-final-latest-960/evidence.json`。
- 最终 DOM 审计：`.artifacts/5.2-ui-audit-final-latest-960/audit.json`。
- 首轮缺陷证据仍保留在 `.artifacts/5.2-ui-independent-960/`、`.artifacts/5.2-layer-stack-960-rerun/` 和 `.artifacts/5.2-ui-audit-960/`，用于说明修复前后差异。

## 对 5.2 主任务的交接

1. 本独立验收发现的图层场景离开画布保存竞态、打光选中态对比度、菜单角色定位和 setup 证据语义已由 5.2 主任务修正，最终复跑无界面阻塞。
2. 画布工具条、全景、打光、多角度、高清、人像、元素编辑、图层和九宫格的窄屏功能与视觉结论均为“通过”。
3. 真实模型质量、实际账户权限、账单、图层 alpha/bbox 以及人物/商品保真度未获付费授权，仍是“待真实验证”，不得宣传为已验证。

## 本交接提交

- 提交标题：`test(canvas): 补充图片能力界面独立验收`
- 提交哈希：见 Git 历史中上述标题的唯一提交。
