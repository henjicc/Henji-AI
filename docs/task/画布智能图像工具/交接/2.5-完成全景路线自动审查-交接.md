# 2.5 完成全景路线自动审查—交接

## 路线结论

决定：**按当前架构继续第三至第五阶段**。

没有发现需要暂停、回退或改造共享基础的致命证据。真实模型画质、供应商稳定性、实际耗时和费用仍为“待真实验证”，不能宣传为已通过；这些未付费项不阻塞后续实现。若未来真实基准失败，优先禁用问题供应商组合、暂停默认开放或更换模型，不回退已经验证的能力目录、节点编排、结果语义、查看器和项目兼容基础。

## 证据结论表

| 审查维度 | 状态 | 结论与证据 |
|---|---|---|
| 模型与提示词静态契约 | 达标 | `1261a91c` 完成官方资料、候选矩阵、两版提示词、五类样本、评分与预算；默认语义已供 1.3/2.2 使用。 |
| 真实模型生成质量 | 待真实付费验证 | 未执行 60 次基准；接缝、极点、地平线、主体重复、文字伪影、清晰度、提示遵从和跨渠道稳定性均无付费证据。 |
| 样本复现材料 | 部分达标 | 纯文本样本和评分契约已定义；P04/P05 参考图许可、受管路径和 SHA-256 尚未归档。 |
| 节点复现与模型限制 | 达标 | `6b6497d8`：`panoramaGenNode`、固定 2:1/2K/单张/medium、最多一张参考图、版本化提示词、模型策略、失败与恢复测试完成；42 项精确测试通过。 |
| 工具条与图事务 | 达标 | 1.2 真实 Electron 场景确认点击“720°全景”后只创建一个相邻节点、一条来源连线并选中新节点。 |
| 结果语义与球面浏览 | 达标 | `cbe58b2c`：`exportImage + resultKind: panorama`、惰性 Three/R3F 查看器、球面/平面、拖动、缩放、复位、全屏、关闭和降级完成。 |
| 性能与资源释放 | 达标（本地） | `frameloop="demand"`、纹理/几何体/材质显式释放；正式场景实际关闭、切换并重开，无浏览器或日志错误/警告。 |
| 外部文件兼容 | 达标 | `21afc2e5`：PNG iTXt、JPEG APP1、WebP XMP chunk 无像素重编码写入最小 GPano；普通图、非法 XMP、非 2:1 和不支持格式有明确状态。 |
| 保存、重开与项目包 | 达标 | 正式 Electron 场景覆盖项目保存重开、下载元数据读取和真实 `.henjiproj` 导出导入；结果语义、媒体引用和 GPano 保留。 |
| 桌面视觉与日志 | 达标 | 1440×900、960×640 的工具条/查看器共四个场景通过；浏览器错误、日志错误和警告均为 0，11 项 DOM 审计零命中，截图已目视。 |
| 实际成本 | 待真实付费验证 | 本轮供应商调用次数为 0、实际模型费用为 0；2.1 的 ¥60 只是首次完整基准预算上限，不是已发生费用。 |

## 证据索引

- [2.1 任务](../任务/第二阶段-全景试点/2.1-完成全景模型与提示词基准调研.md) / [2.1 交接](2.1-完成全景模型与提示词基准调研-交接.md) / 提交 `1261a91c20659932e91146ff2fa0cd4103f92577`。
- [2.2 任务](../任务/第二阶段-全景试点/2.2-实现全景生成功能节点.md) / [2.2 交接](2.2-实现全景生成功能节点-交接.md) / 提交 `6b6497d893c46b67672cf6cfddac5f1f78a5f1a7`。
- [2.3 任务](../任务/第二阶段-全景试点/2.3-实现全景结果语义与沉浸式查看器.md) / [2.3 交接](2.3-实现全景结果语义与沉浸式查看器-交接.md) / 提交 `cbe58b2cfe5324ad0017176c119be6b968a2879e`。
- [2.4 任务](../任务/第二阶段-全景试点/2.4-补齐全景元数据与真实验收.md) / [2.4 交接](2.4-补齐全景元数据与真实验收-交接.md) / 提交 `21afc2e504c168ffbebc20b17157e521f7d96e71`。
- 视觉证据：`.ui-tour/panorama-2.4/`；DOM 审计：`.ui-audit/panorama-2.4/audit.json`。二者是本地验证产物，不纳入 Git 提交。

## 授权、成本与对外表述边界

- 用户授权团队连续完成全部计划任务，但没有授权真实付费模型调用；两种授权不可混同。
- 本轮没有调用 APIMart、KIE、Grsai、Fal 或其他供应商，实际模型费用为 0。
- 不得使用“已验证无缝全景”“已验证 GPT Image 2 全景质量”“已验证渠道稳定性”等表述。
- 可以准确表述为：“全景的节点、参数、查看、导出和项目往返链路已通过本地自动化与真实 Electron 验收；真实模型画质待授权验证。”
- 未来获得付费授权后，应按 2.1 的五类样本与评分表执行，不用单一成功样本替代稳定性结论。

## 已知非全景阻塞

截至 `21afc2e5` 的验收记录：

- `tsc` 受助手附件新增 `file` 模态但既有 `AgentInputModality`/配额表未同步的三处错误影响。
- `check:assistant-capabilities` 受 `src/features/maskEditor` 缺 Reflection/StoreLedger 和 `specialEditorController.ts` 未建账影响。

这些问题没有出现在全景精确测试、`electron-vite build` 或正式 Electron 场景中，不构成全景路线回退理由；后续整体验收需按当时仓库状态重新检查，不能永久豁免。

## 后续任务边界

- 2.1 与 2.4 保持“待验证”，只表示真实模型样本和画质/成本证据未完成，不表示本地实现未完成。
- 2.2、2.3 与 2.5 为“已完成”；1.2 已由正式工具条场景推进为“已完成”。
- 3.1 与 4.3 已按连续执行授权提前完成静态契约，技术结论继续有效。
- 3.2 正在由并行任务归口；4.1 已由对应任务完成并同步任务记录。本任务没有修改它们的任务文件或业务代码。
- 后续能力仍遵循“能力入口统一、功能节点可复现、复杂界面节点外专用化、结果语义驱动消费方”的路线，不强制不同能力使用同一模型。

## 文件与提交

本任务只修改：

- `00-任务总览.md`
- `01-实施方案.md`
- `任务/第二阶段-全景试点/2.5-完成全景路线确认闸门.md`
- `重要记录.md`
- `交接/2.5-完成全景路线自动审查-交接.md`

提交标题：`docs(canvas): 完成全景试点路线审查`。本交接与上述文件位于同一独立提交；未修改业务代码、3.2/4.1 任务文件或其他代理文件。
