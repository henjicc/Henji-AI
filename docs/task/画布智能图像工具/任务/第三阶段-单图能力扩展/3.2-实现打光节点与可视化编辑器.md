# 3.2 实现打光节点与可视化编辑器

## 任务状态

- 状态：已完成
- 所属阶段：第三阶段——单图能力扩展
- 前置任务：3.1
- 关联任务：1.2、1.4、5.1、5.2
- 是否可以并行：可以；可与不修改专用界面宿主和能力目录同一文件的其他能力任务并行

## 任务目标

实现可从图片工具条创建的打光节点，以及与 3.1 控制契约一致的节点外可视化编辑器。用户确认控制后回写节点并生成普通图片结果，重新打开项目时仍能恢复参数。

## 当前情况

- 标准节点已有模型行、媒体输入、参数行和生成动作，可保留为节点基础。
- 1.4 将提供专用界面宿主，复杂灯光预览不应常驻 ReactFlow 节点。
- 参考界面同时存在手动与智能模式，但最终字段必须以 3.1 的模型能力为准。
- 当前没有灯位图形控件、打光参数版本或模式互斥迁移。

## 实施方案

节点展示源图、当前打光摘要、模型状态和生成按钮；点击摘要打开节点外编辑器。编辑器以一块主预览表面承载灯位交互，手动/智能模式共享同一层级，不再套多层卡片。参数先在草稿中编辑，确认后通过应用服务一次性写回；取消不污染节点。

## 执行步骤

1. 按 3.1 契约定义版本化打光参数、默认值、校验、迁移和请求映射纯函数。
2. 注册打光节点并复用标准模型、媒体输入和生成状态；增加打光摘要行和打开编辑器动作。
3. 实现手动界面：光向图、亮度、颜色、轮廓光及重置；只显示模型真实支持的控制。
4. 实现智能界面：提示词、参考光照图和已确认预设；处理模式切换与参数保留规则。
5. 构建无障碍键盘操作、脏状态关闭确认、节点删除/项目切换关闭和错误反馈。
6. 将参数映射到生成请求，输出普通图片并保留能力来源和参数版本。
7. 添加参数纯函数、组件、持久化、生成失败和真实 Electron 场景测试。

## 涉及内容

### 新增文件

- 预计在 `src/features/canvas/nodes/` 增加打光节点、参数模块和测试。
- 预计在专用界面目录增加打光编辑器、预览控件和测试。

### 修改文件

- 能力目录、`canvasNodes.ts`、`nodeRegistry.ts`、节点类型映射和迁移。
- 专用界面宿主、生成请求语义映射、中英文文案和图标。
- SDK catalog/presentation 仅在 3.1 确认需要时修改。

### 可能废弃

- 无。若 3.1 取消智能或手动模式，对应界面不创建。

## 验收标准

- [x] 从图片节点创建打光节点并正确连接，源图和参数可保存重开。
- [x] 编辑器的每个控件都能映射到已确认模型能力，近似控制有明确说明。
- [x] 确认、取消、模式切换、脏状态和节点删除行为一致；契约不提供单独“重置”，默认值由新节点和迁移器统一恢复。
- [x] 无付费请求模拟锁定普通 image 结果、失败占位和多结果提交契约；未在本任务发起真实付费生成。
- [x] 编辑器按需加载，预览是静态主表面，关闭时由统一宿主释放。
- [x] 单元/组件测试和真实 Electron 场景通过。
- [x] 执行记录和交接已更新；总览与重要记录由总任务归口同步。

## 执行记录

### 2026-08-29

- 完成内容：完成 Fal IC-Light v2 SDK 适配、打光 V1 契约、专用节点、手动/智能编辑器、保存迁移与普通图片输出链路。
- 实际改动：手动模式固定 Fal IC-Light v2 和五档方向；智能模式固定 GPT Image 2 系列并允许最多一张光照参考图；两模式不静默互退。
- 验证结果：14 个定向测试文件 80 项通过，后续内部写回回归 21 项通过；SDK 构建、Electron 构建、模型文案/颜色/表面/图标门禁通过；1440×900 真实 Electron 场景通过并目视截图。
- 遗留问题：真实人物、商品、室内生成质量与价格仍待后续显式授权付费验证；TypeScript 全局检查仍受与本任务无关的 `file` modality 三处既有错误阻挡。
- 与原计划的偏差：`GenerationNodeShell` 不能同时合并连线源图与编辑器光照参考图，因此使用复用标准媒体行、生成服务和输出提交内核的专用节点执行器。
- 对后续任务的影响：其他需“连线主源+专用参考”的节点可复用 `prepareRelightGenerationInput` 的请求排序形状，不应绕回通用提示词合并。
- 相关提交：待本次独立提交后回填。
