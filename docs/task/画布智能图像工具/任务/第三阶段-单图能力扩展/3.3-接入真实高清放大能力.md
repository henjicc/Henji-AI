# 3.3 接入真实高清放大能力

## 任务状态

- 状态：已完成
- 所属阶段：第三阶段——单图能力扩展
- 前置任务：2.5
- 关联任务：1.2、1.3、5.1、5.2
- 是否可以并行：可以；可与 3.1、3.4、4.1、4.3、4.5 并行调研，实施时需协调能力目录

## 任务目标

接入以提高分辨率并尽量保持原内容为目标的高清能力，而不是默认用生成模型重绘。用户可选择经过验证的放大倍率/质量模式，结果仍是普通图片。

## 当前情况

- 项目已有图片处理、Sharp、生成运行时和普通图片结果节点，但没有统一的 AI 放大能力。
- Real-ESRGAN 是可参考的开源方案；本地内置会影响安装体积、硬件兼容和首次下载，云端方案则涉及价格与隐私。
- GPT 图片重绘可能改变人脸、文字和细节，不能直接命名为无损高清。
- 当前选择本地还是云端、支持倍率和最大像素均待本任务用证据确定。

## 实施方案

任务内先完成轻量选型，再实施标准处理节点。对本地与云端从质量、速度、安装体积、GPU/CPU、价格、隐私和失败恢复比较；由团队在已有授权下自主选择并记录证据与回退方案。节点只暴露真实可执行参数，并在预计像素、文件大小或成本过高时预检。

## 执行步骤

1. 调查现有供应商是否已有超分模型，并核对 Real-ESRGAN 等本地方案的许可证、模型体积和平台支持。
2. 用人像、插画、文字、低清压缩和大图样本比较内容保持、伪影、速度和资源占用。
3. 提供本地/云端选择与预算，获得确认后确定倍率、质量模式、最大像素和默认值。
4. 如需模型适配，按官方资料更新 SDK；如需本地长任务，放入主进程服务并加入取消、进度和统一日志。
5. 注册高清能力与标准节点，连接源图，运行前展示预计输出尺寸/费用，结果写入普通图片节点。
6. 覆盖超大图、透明通道、方向信息、取消、磁盘不足、模型不可用和项目重开测试。
7. 在真实 Electron 中检查结果尺寸、视觉保持、内存峰值和错误提示。

## 涉及内容

### 新增文件

- 选定云端时：相应 SDK 供应商模型资料、目录和测试。
- 选定本地时：Electron 图片/模型服务、进度契约和测试；具体路径执行时遵循架构规则。
- 画布高清能力节点或标准能力适配器及测试。

### 修改文件

- 能力目录、节点注册表、模型/参数映射、生成或图片处理命令链路。
- 中英文文案、计价/尺寸预检和项目媒体持久化相关代码。

### 可能废弃

- 无。不得用已有普通图片生成按钮替代真实放大。

## 验收标准

- [x] 选型有官方资料、许可证、体积/费用和自主决策记录；未授权付费样本明确保留待验证。
- [x] 提交前按倍率精确计算输出像素；透明图明确拒绝，EXIF 方向按视觉尺寸修正。
- [ ] 人脸、文字和主要结构的改变在可接受范围；非无损模式说明已完成，真实质量待付费验证。
- [x] 超大图、超大文件、透明通道和非法倍率在上传/付费前安全失败；供应商失败复用现有生成恢复链路。
- [x] 普通图片结果可连接、保存、下载和重开，原图片不被覆盖。
- [x] 精确测试、真实 Electron 编排/持久化/日志/DOM 审计通过；真实模型质量、耗时与供应商资源峰值仍待付费验证，但不阻塞实现任务完成。
- [x] 执行记录和总览已同步。

## 执行记录

### 2026-08-29

- 完成内容：选择 Fal Topaz，接入唯一模型目录、高清能力、标准节点、2×/4×/模式/人脸增强参数、48MP/20MiB/Alpha/EXIF 付费前预检、普通图片结果与保存重开场景。
- 实际改动：扩展既有图片信息契约返回方向与 Alpha；标准生成壳增加通用的“无提示词工具”和异步运行前参数准备能力；新增 Topaz schema、展示补丁、能力策略、节点、迁移、回归测试和正式 Electron 场景。
- 验证结果：84 项定向测试、精确 ESLint、模型 i18n/图标/表面检查、SDK 101 模型打包门禁、`electron-vite build`、真实 Electron UI 与 UI Audit（11 条规则零命中）通过；截图已目视。完整 `electron:build` 被仓库既有助手能力台账缺口阻断，不是本任务回归。
- 遗留问题：未授权真实付费调用；画质、伪影、真实耗时、供应商稳定性和资源峰值保持待验证。
- 与原计划的偏差：未等待人工选择；依据官方价格、协议与安装体积自主选择云端 Topaz。透明输入因官方默认 JPEG 不承诺保留，而是在上传前拒绝。
- 对后续任务的影响：5.1 可直接把高清作为已实现能力展示，但不得宣称真实模型质量已验证；未来离线模式可单独评估 Real-ESRGAN。
- 相关提交：`4b2ab268`（`feat(canvas): 接入真实高清放大能力`）；最终跨能力回归见 5.2 交接。

### 2026-08-29（5.2 归口验收）

- 完成内容：在统一工具条、普通图片结果、保存重开和跨能力回归中重新验证高清能力，并把任务状态从“待验证”收口为“已完成”。
- 实际改动：未改变 Topaz 模型或节点契约；仅同步任务状态。真实付费画质仍单独保留为外部验证边界。
- 验证结果：高清节点在 1440×900 与 960×640 正式 Electron 场景均通过；双尺寸 UI audit 零命中；全量单元测试、类型检查和完整 Electron 构建通过。
- 遗留问题：本轮没有付费授权，未验证真实画质、真实耗时、供应商稳定性与账单；不将这些内容写成通过。
- 与原计划的偏差：原验收项把付费质量和实现完成状态绑在一起；5.2 按全计划统一边界将“代码与非付费验收完成”和“真实模型质量待验证”分开记录。
- 对后续任务的影响：不再阻塞本计划收口；未来真实质量问题优先禁用供应商组合或调整发布状态，不回退共享节点与媒体契约。
- 相关提交：5.2 最终集成提交（见 5.2 交接）。
