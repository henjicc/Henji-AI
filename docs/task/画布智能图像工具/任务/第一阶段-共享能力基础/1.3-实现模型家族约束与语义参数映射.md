# 1.3 实现模型家族约束与语义参数映射

## 任务状态

- 状态：已完成
- 所属阶段：第一阶段——共享能力基础
- 前置任务：1.1
- 关联任务：2.1、2.2、3.1、3.3、3.4、4.3、4.5
- 是否可以并行：可以；可与 1.2、1.4 并行

## 任务目标

使功能节点能声明允许的规范模型家族、供应商/渠道兼容条件和固定语义参数，并把“2:1、2K、单张、参考图”等产品语义可靠映射到各供应商 schema，而不在 UI 中按模型编号分支。

## 当前情况

- `ModelInputRow.tsx` 和 `useModelPickerList.ts` 主要按媒体类型和 `ModelTag` 过滤，粒度不足。
- GPT Image 2 在多个供应商目录中存在，但相同比例或分辨率可能使用不同字段与值。
- `analyzeRatioResolutionParams` 已能识别比例/分辨率字段，可作为语义映射基础。
- 项目规则要求可移植模型行为进入 SDK schema，痕迹AI专属展示补丁进入 `src/models/presentation/`，禁止 UI 硬编码模型差异。

## 实施方案

扩展模型选择输入以接受声明式约束对象，而不是单个布尔开关。增加纯函数预检和语义参数解析：输入能力要求与候选模型 schema，输出可用模型、默认值、不可用原因和实际请求参数。供应商特定能力缺口优先修正 SDK schema；产品文案和显示顺序才进入展示层。

## 执行步骤

1. 基于模型适配技能形成确认清单，明确规范模型编号、平台矩阵、参数来源和价格来源。
2. 设计模型约束类型，至少支持规范模型家族、媒体能力、必需比例/分辨率、参考图数量和输出数量。
3. 扩展模型列表查询与模型行，使节点只展示通过预检的候选并能解释无候选原因。
4. 建立语义参数映射器，复用 ratio/resolution 分析，不复制供应商字段名到节点组件。
5. 对缺失 2:1 或错误默认值的模型，按模型适配流程先更新官方资料和 SDK schema，再生成目录。
6. 添加 APIMart、KIE、Grsai、Fal 等差异场景的纯函数与组件测试。

## 涉及内容

### 新增文件

- `src/features/canvas/capabilities/modelCompatibility.ts`：能力模型候选解析、拒绝原因与语义参数映射纯函数。
- `src/features/canvas/capabilities/modelCompatibility.test.ts`：四家 GPT Image 2 的平台组合、参数映射和请求契约测试。
- `src/features/canvas/params/useModelPickerList.test.tsx`：模型列表旧调用兼容与能力约束筛选测试。

### 修改文件

- `src/features/canvas/capabilities/types.ts`、`builtInCapabilities.ts`、`index.ts`：扩展模型策略并登记全景已确认组合、固定语义和双提示词模板版本。
- `src/features/canvas/params/ModelInputRow.tsx`、`NodeModelParamsControls.tsx`、`ModelPickerList.tsx`、`useModelPickerList.ts`：可选接收能力模型策略；无兼容候选时展示稳定原因，省略策略时行为不变。
- `packages/ai-sdk/src/catalog/fal/gpt-image-2.model.ts`：只为 GPT Image 2 增加 2:1 与 2K 自定义尺寸映射。
- `src/models/presentation/fal/part-1.ts`：增加 Fal GPT Image 2 的 2:1 与 2K 展示文案。
- `packages/ai-sdk/docs/model-adaptation/GPT-Image-2/GPT-Image-2_Fal.md`、`README.md`：同步资料与更新日期。
- `packages/ai-sdk/tests/catalog/fal-targets.test.ts`：验证 2688×1344 请求与不扩散到共享比例的边界。
- `src/i18n/locales/zh-CN/storyboard.json`、`src/i18n/locales/en-US/storyboard.json`：补充无兼容模型说明。

### 可能废弃

- 无。现有 `requiredTags` 保持兼容，待全部调用迁移后再评估是否收敛。

## 验收标准

- [x] 功能节点可以只展示指定规范模型家族和真正满足语义要求的供应商组合。
- [x] 2:1、分辨率、参考图和数量由纯函数映射，UI 不包含供应商字段名分支。
- [x] 不兼容候选有稳定原因，且不会被选中或发送请求。
- [x] 模型 schema 改动有官方来源、确认清单、参数测试和目录/i18n 检查。
- [x] 历史标准节点的模型列表行为无回归。
- [x] 任务执行记录与独立交接已更新；任务总览由阶段归口同步。

## 执行记录

### 2026-08-29

- 完成内容：建立声明式模型约束、候选/拒绝结果与语义参数映射；把全景策略固定为 `gpt-image-2`、已确认平台组合、2:1、2K、单张、最多一张参考图及存在质量参数时 `medium`；补齐双提示词模板版本。
- 实际改动：模型行与 picker 可选消费能力策略；Fal GPT Image 2 单独加入 2:1 和 2K，自定义尺寸映射为 2688×1344；资料、展示补丁和精确测试同步更新。
- 验证结果：L2 精确测试 7 个文件 146 项通过；定向 ESLint、`gen:catalog`、`check:model-i18n` 通过；渲染层 `tsc` 仅剩任务外既有的助手附件 `file` 模态 3 项错误，本任务新增错误为零。未执行真实付费生成。
- 遗留问题：模型真实无缝全景质量、延迟和成本仍须后续付费基准验证；本任务只证明 API 与项目契约可表达。
- 与原计划的偏差：Fal 2K 以官方允许的自定义尺寸实现，不把 2:1 加入所有 Fal 模型的共享常量。
- 对后续任务的影响：2.2 必须从能力定义读取 `modelPolicy`，用 `mapCanvasCapabilityModelParams` 做创建默认值和提交前预检；不得自行复制供应商参数 ID。
- 相关提交：`feat(canvas): 建立图片能力模型约束与语义映射`（哈希以本次独立提交为准）。
