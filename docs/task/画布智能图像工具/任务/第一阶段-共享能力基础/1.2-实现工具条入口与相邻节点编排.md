# 1.2 实现工具条入口与相邻节点编排

## 任务状态

- 状态：已完成
- 所属阶段：第一阶段——共享能力基础
- 前置任务：1.1
- 关联任务：2.2、3.2～3.5、4.2、4.4、4.6、5.1
- 是否可以并行：可以；1.1 完成后可与 1.3、1.4 并行，但需避开共享注册表文件

## 任务目标

让图片节点工具条从能力目录读取入口，并能在点击后原子地创建、放置、连接和选中对应功能节点。未实现、无兼容模型或源媒体不满足的能力必须隐藏或说明不可用原因。

## 当前情况

- `NodeActionToolbar.tsx` 已包含多种基础操作，再平铺九个长标签会明显拥挤。
- `getNodeToolPlugins` 只返回本地工具；生成型能力尚无入口。
- `canvasStore.findNodePosition`、`useCanvasNodeMenu` 和 `canvasApplicationService` 已有相邻放置与连线能力，可复用。
- 节点创建、连线、选择和项目保存若分散执行，失败时可能留下孤立节点或重复历史记录。

## 实施方案

工具条只直接展示少量高频能力和一个“更多”入口，其余按生成变换、结构化输出、本地处理分组。点击能力调用共享应用服务完成创建和连线；特殊能力创建后打开专用界面，普通能力只选中新节点。工具条只消费能力可用性结果，不自行判断模型或节点类型。

## 执行步骤

1. 确认工具条现有操作的优先级和最大可用宽度，制定直接展示与更多菜单规则。
2. 增加能力入口渲染和键盘/读屏语义，保证关闭状态不会出现可点击空壳。
3. 在应用服务中新增“从源节点创建能力节点”的事务：解析源端口、找空位、创建节点、连接、选中并持久化。
4. 处理重复点击、无可用位置、端口不兼容、项目不存在和创建失败，记录统一结构化日志。
5. 特殊编辑器只通过事件或控制器打开，避免工具条直接依赖打光/多角度组件。
6. 添加工具条渲染、能力筛选、相邻创建、连线、撤销/重做和错误回滚测试。

## 涉及内容

### 新增文件

- 预计在 `src/features/canvas/application/` 增加能力节点编排服务及测试；具体文件名执行时按现有服务命名确认。
- 预计在 `src/features/canvas/ui/` 增加能力菜单组件；若需要新增共享 UI 原语，必须先向用户说明并获确认。

### 修改文件

- `src/features/canvas/ui/NodeActionToolbar.tsx`：读取能力入口并加入更多菜单。
- `src/features/canvas/application/canvasApplicationService.ts`：复用或扩展原子创建、连接、持久化能力。
- `src/stores/canvasStore.ts`：仅在现有事务无法覆盖时增加最小状态动作。
- 对应中英文画布文案和测试。

### 可能废弃

- 无。现有编辑、宫格切分等工具先保留，4.2 再决定入口是否合并。

## 验收标准

- [x] 兼容图片节点能看到能力入口，小节点或窄视口不会因九个按钮溢出。
- [x] 点击可用的创建型能力只创建一个相邻节点和一条正确连线，并自动选中新节点。
- [x] 不可用能力不会触发创建；错误有用户可见反馈和结构化日志。
- [x] 创建、连线、撤销、重做和项目保存是可恢复的一致操作。
- [x] 未破坏生成、编辑、复制、下载、删除和现有本地工具。
- [x] 组件测试、应用服务测试及真实 Electron 工具条场景通过。
- [x] 任务执行记录已更新；任务总览按并行分工由主代理统一归口。

## 执行记录

### 2026-08-29

- 完成内容：2.4 增加正式 Electron 场景 `canvas-panorama-toolbar`，从兼容图片节点点击“720°全景”，验证只创建一个相邻 `panoramaGenNode`、一条来源连线并自动选中新节点；1440×900 与 960×640 均通过。
- 实际改动：本任务代码无追加改动；真实场景和证据由 2.4 统一维护。
- 验证结果：正式 Electron UI 场景两种窗口尺寸通过，结构化日志、浏览器错误和警告均为零；DOM 规则审计 11 项零命中。原“缺少真实工具条场景”的唯一遗留已解除。
- 遗留问题：无。
- 与原计划的偏差：真实场景在全景能力开放后补齐，符合原执行记录约定。
- 对后续任务的影响：后续能力可继续复用已通过真实桌面验收的能力入口与原子编排事务。
- 相关提交：随 2.4 独立提交。

### 2026-08-29

- 完成内容：工具条已直接消费可执行能力查询；少量能力直显，超过三项时保留前两项并将其余按目录分组收入“更多”；当前只显示唯一可执行的宫格切分。
- 实际改动：新增图片能力工具条组件与布局函数；新增图片能力应用服务；将既有批量写入的快照、回滚、单历史和持久化逻辑提取为可复用事务内核；补充中英文工具条文案。
- 验证结果：精确 Vitest 3 个文件共 12 项通过；定向 ESLint、表面、颜色、图标检查和双语 JSON 解析通过。渲染层 TypeScript 全量检查仅被并行的助手附件 `file` 模态错误阻断，本任务文件无类型错误。
- 遗留问题：尚无专门的真实 Electron 图片能力工具条场景；直接展示数量需在 5.1 结合真实使用再收口。
- 与原计划的偏差：宫格切分是已有本地工具，点击后复用现有对话框，不会为它创建空的功能节点；创建型能力的原子节点路径已完整实现并测试。
- 对后续任务的影响：2.2 只需将全景能力切换为 `implemented + canvas-node`，工具条即会自动出现，点击后会进入本任务的统一事务入口。
- 相关提交：`feat(canvas): 接入图片能力工具条编排`（最终哈希以交接对应提交为准）。
