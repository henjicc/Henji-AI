# 2.4 补齐全景元数据与真实验收

## 任务状态

- 状态：待验证
- 所属阶段：第二阶段——全景试点
- 前置任务：2.2、2.3
- 关联任务：2.5、5.2
- 是否可以并行：否；需要完整生成与浏览链路

## 任务目标

确保全景文件不仅在当前会话可看，还能保存、重开、导出并被支持 Photo Sphere 的外部工具识别；以真实 Electron 场景完成质量、交互、日志、兼容和性能验收。

## 当前情况

- 当前 `png-metadata.ts` 只写入分镜专用文本块，不是通用 GPano/XMP 实现。
- 画布项目持久化保存节点/连线快照，媒体导入导出还需确认新增全景语义和元数据不会丢失。
- 全景浏览可由节点结果语义驱动，但外部软件识别通常依赖尺寸和 GPano 字段，文件格式支持需要实测。
- 真实生成可能收费；未授权时只能验证预置样本和录制响应。

## 实施方案

先确认项目最终保存的图片格式、Sharp 对 XMP 的支持和目标外部查看器要求，再建立独立全景元数据服务，不修改分镜元数据语义。项目内以结果语义为主、文件元数据为辅助；外部导出在无法可靠嵌入时必须明确说明而不是伪造成功。

## 执行步骤

1. 追踪生成结果下载、落盘、复制、导出和项目打包链路，确认 PNG/JPEG/WebP 的实际格式与转码点。
2. 根据 Google Photo Sphere 规范确定最小 GPano 字段：投影类型、完整宽高、裁剪宽高和偏移；记录不同格式支持结论。
3. 在 Electron 图片服务增加独立嵌入/读取能力及 IPC/平台封装，保持 PAL 边界和统一日志。
4. 验证下载、另存、复制、项目导出导入和重启后全景语义与文件均可恢复。
5. 使用 2.1 固定样本执行真实 Electron 场景：创建、生成或载入样本、球面浏览、平面切换、下载、重开。
6. 检查左右接缝、极点、地平线、主体重复、交互帧率、纹理释放和失败提示；保存截图、日志和结果表。
7. 如果执行真实付费生成，先再次确认授权并记录准确费用；未授权则将模型质量项标记为待验证。

## 涉及内容

### 新增文件

- 预计在 `electron/main/services/image/` 增加全景元数据模块及精确测试。
- 预计增加平台契约、适配器与命令层薄封装。
- 预计增加真实 Electron 全景场景和固定测试样本清单。

### 修改文件

- `electron/main/ipc/image.ts`、`src/commands/image.ts` 及对应平台层：增加全景元数据读写入口。
- 项目导入导出媒体收集代码：仅在测试证明语义或文件被遗漏时修改。
- `electron/main/services/image/png-metadata.ts`：原则上不混入 GPano；若抽通用块函数必须保持分镜测试通过。

### 可能废弃

- 无。分镜元数据格式必须保持兼容。

## 验收标准

- [x] 全景项目保存、关闭、重开后仍以全景模式打开且源文件可读。
- [x] 导出文件包含经过读取测试验证的全景元数据；PNG/JPEG/WebP 支持，其他格式明确拒绝。
- [x] 项目打包导入后媒体引用、结果语义、文件字节和查看模式不丢失。
- [x] 真实 Electron 场景覆盖工具条创建、确定性样本载入、球面浏览、平面切换、下载、项目重开和项目包往返；未触发付费生成。
- [ ] 接缝、极点、地平线、主体重复与真实模型稳定性待付费样本验证；本地交互、资源释放、费用边界和日志已有实际记录。
- [x] Electron 主进程改动已经 `electron-vite build` 后再验收，相关精确测试通过；完整 `electron:build` 仅被仓库既有助手能力台账阻塞。
- [x] 执行记录与交接已更新；总览由计划统筹任务统一同步。

## 执行记录

### 2026-08-29

- 完成内容：追踪图片落盘、另存、快速下载、剪贴板、项目保存与项目包链路；依据 Google Photo Sphere 规范实现最小 GPano；完成 PNG/JPEG/WebP 无像素重编码写入/读取、正式 Electron 下载和项目/项目包往返验收。
- 实际改动：新增全景元数据容器服务、Electron IPC/preload/PAL/commands 薄封装；全景下载前按 `resultKind: panorama` 嵌入 XMP；扩展项目包精确测试与正式 Electron 工具条/查看器场景；修正全景结果入口按钮的内外层圆角审计问题。
- 验证结果：全景元数据及下载/迁移/项目包精确测试通过；PNG/JPEG/WebP 解码像素写前写后一致，普通图不误判、非法字段返回 `invalid`、GIF 返回 `unsupported`；`electron-vite build` 通过；正式 Electron UI 在 1440×900、960×640 覆盖创建、连线、选中、球面/平面、下载、保存重开、项目包导入，浏览器错误/日志错误/警告均为零；11 项 DOM 审计零命中；截图已逐张目视。
- 遗留问题：未执行真实供应商付费生成，因此模型输出的左右接缝、极点连续、地平线、重复主体、清晰度和跨渠道稳定性仍为“待真实验证”；剪贴板传递的是系统位图，不承诺保留文件容器 XMP。
- 与原计划的偏差：为避免像素重编码，没有直接依赖 Sharp 输出链路，而是使用 Sharp 读取尺寸/格式并对 PNG iTXt、JPEG APP1、WebP XMP chunk 做容器级写入；完整 `electron:build` 被仓库既有 `maskEditor`/`specialEditorController` 助手能力账本问题阻塞，已以实际 `electron-vite build` 和正式 Electron 场景完成本任务可归因验收。
- 对后续任务的影响：2.5 可据此确认共享能力、结果语义、专用查看器、外部元数据和项目往返路线均闭环；真实模型画质缺口不得被解释为实现缺陷或已验证结论。
- 相关提交：`feat(canvas): 完成全景元数据与真实验收链路`（最终哈希见交接）。
