# 2.1 完成全景模型与提示词基准调研

## 任务状态

- 状态：待验证
- 所属阶段：第二阶段——全景试点
- 前置任务：1.1
- 关联任务：1.3、2.2、2.3、2.4、2.5
- 是否可以并行：可以；可与 1.2～1.4 并行，但真实付费测试须单独授权

## 任务目标

用官方资料和固定样本回答“哪些 GPT Image 2 供应商组合能稳定生成可浏览的 2:1 等距柱状全景、需要什么提示词和参数、成本是多少”，形成可供 2.2 直接实现的确认清单。

## 当前情况

- APIMart、KIE、Grsai 的 GPT Image 2 目录可见 2:1 能力；Fal 当前比例候选未直接暴露 2:1，需要核查官方接口和项目 schema。
- GPT Image 2 能理解图片生成/编辑任务，但官方资料不保证左右无缝、极点正确或完整球面一致性。
- PanFusion、Text2Light 可提供全景质量评价思路，但不代表它们适合作为当前产品模型。
- 用户要求尽量不消耗 Liblib 额度；本任务不会点击参考站生成。

## 实施方案

按模型适配流程先收集官方页面、字段和价格，再建立统一样本、提示词版本和评分表。静态资料完成后已向用户报告预计调用次数和预算；用户已确认模型与参数实施方案，但本轮未授权真实付费生成。允许按确认方案实现与默认配置，但不得把未经真实验证的组合宣称为“已稳定生成无缝全景”。

## 执行步骤

1. 写清确认清单：规范模型、首批供应商、渠道、比例、分辨率、参考图限制、张数、价格和错误协议。
2. 以官方资料核对 APIMart、KIE、Grsai、Fal；标注抓取日期、原文字段和项目映射，无法确认项写“待确认”。
3. 定义提示词版本，至少要求等距柱状投影、360×180 球面、严格 2:1、左右无缝、地平线水平、极点连贯、无边框/文字，并区分纯文本和参考图编辑。
4. 建立五类固定样本和评分标准：接缝、极点、透视、主体重复、提示遵从、耗时、费用。
5. 计算最小测试矩阵和预算，记录本轮付费测试未获授权。
6. 不执行真实付费调用；将质量、接缝、极点和供应商稳定性结论标为“待真实验证”，不将该缺口作为 1.3、2.2 或后续能力的实施阻塞。
7. 给出首选、候选、排除组合和原因，更新 1.3/2.2 所需约束及重要记录。

## 涉及内容

本任务主要处理官方资料、SDK 模型目录、全景样本和任务记录。若发现 schema 不准确，按模型适配规则更新 `packages/ai-sdk/docs/model-adaptation/` 的资料后才能修改 `packages/ai-sdk/src/catalog/`。测试图片应放现有测试资源位置或临时目录，不得把无必要大图提交进仓库。

## 特殊风险与授权

- 任何真实供应商调用都会产生费用，必须在列明总次数和估算成本后获得用户显式授权。
- 不得用单一成功样本宣称模型稳定；无法完成足量测试时结论必须标注置信度。
- 第三方开源项目的许可证、权重许可和部署要求只作参考，不默认纳入产品。

## 验收标准

- [x] 每个候选组合都有官方来源、能力矩阵、价格来源和核查日期。
- [ ] 提示词、固定参数、测试样本和评分表可复现（P04/P05 的参考图文件与 SHA-256 待确认）。
- [x] 首选/候选/排除结论有证据；未真实测试项明确标为待验证。
- [x] 真实付费调用如发生，已有用户授权、完整日志和成本记录（本轮未发生真实付费调用）。
- [x] 形成可直接供 2.2 使用的默认模型、固定参数和降级策略确认清单。
- [x] 任务总览、执行记录和重要决定已同步。

## 执行记录

### 2026-08-29

- 完成内容：核查 OpenAI、APIMart、KIE、Fal、Grsai 官方/平台一手资料和仓库现有 GPT Image 2 定义；形成平台矩阵、两个提示词版本、五类固定样本、评分门槛、最小调用矩阵、预算和精简确认清单。
- 实际改动：新增 `调研/全景模型与提示词基准.md` 与本任务交接；本文件状态调整为“待验证”。没有修改 `.model.ts`、供应商运行时、画布代码或模型展示配置。
- 验证结果：静态资料与仓库路径已核对；确认 APIMart、KIE、Grsai 已有 2:1，Fal API 支持自定义 2:1 但仓库当前比例集合不含 2:1。没有执行真实生成，因此不能判断无缝质量或宣称模型稳定。
- 遗留问题：供应商范围、默认 2K/medium、参考图数量和产品名称已确认；Fal schema 修正属于 1.3。P04/P05 可复现样本文件和 SHA-256 由 2.4 使用本地或已获授权资源补齐；本轮不执行真实付费测试，质量结论保持“待真实验证”。
- 与原计划的偏差：原计划把真实付费调用列在 2.1 内；由于尚未获得明确授权，本次按要求只完成非付费部分并保持“待验证”。
- 对后续任务的影响：1.3/2.2 按已确认清单实施约束和默认组合，同时保留“画质待真实验证”证据边界；2.4 负责元数据、本地/模拟样本、持久化和真实 Electron 验收。
- 相关提交：`docs(canvas): 完成全景模型与提示词基准调研`（最终哈希见交接或 Git 历史）。
