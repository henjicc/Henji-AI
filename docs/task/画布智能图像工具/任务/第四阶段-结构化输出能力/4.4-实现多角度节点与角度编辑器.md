# 4.4 实现多角度节点与角度编辑器

## 任务状态

- 状态：已完成
- 所属阶段：第四阶段——结构化输出能力
- 前置任务：4.1、4.3
- 关联任务：1.2、1.4、5.1、5.2
- 是否可以并行：可以；可与 3.2、3.3、3.4、3.5、4.2、4.6 在文件边界明确时并行

## 任务目标

实现多角度功能节点和节点外球形相机编辑器，使用户以模型实际支持的预设或参数选择视角，生成带角度标签的单图或图片组，并可保存、重开和继续连接。

## 当前情况

- 项目已有 Three/R3F 和相机舞台交互经验，但不应直接复用沉重的相机舞台业务模块。
- 1.4 提供专用界面宿主，4.1 提供多结果落图和素材组。
- 参考界面采用球体/立方体/图片平面、水平/垂直角度和景别，实际可用范围由 4.3 决定。
- 若编辑器自由拖动却只能映射离散模型角度，必须在交互上吸附或明确近似。

## 实施方案

节点保持紧凑，只显示输入图、当前视角摘要、模型/输出数量和生成状态。编辑器使用轻量 Three 场景说明相机相对主体的位置；精确 API 支持连续控制时保存数值，否则吸附到 4.3 确定的离散预设。生成结果由 4.1 组织并保存角度标签。

## 执行步骤

1. 定义版本化相机参数、预设、校验、迁移和模型请求映射纯函数。
2. 注册多角度节点，复用标准媒体输入、模型行、提示词和生成状态。
3. 实现节点外编辑器：主体参考、相机轨迹、方位/垂直控制/距离控制、预设以及确认/取消。两个官方端点都没有 prompt 入参，因此不展示伪提示词开关。
4. 根据 4.3 契约实现连续限制或离散吸附，并为近似控制给出明确提示。
5. 调用生成链路并通过 4.1 创建带顺序和角度标签的图片组；单图模式保持普通结果。
6. 处理源图失效、输出数量不符、部分失败、取消、重试、项目切换和资源释放。
7. 添加参数、交互、输出标签、持久化、资源生命周期和真实 Electron 场景测试。

## 涉及内容

### 新增文件

- 多角度节点、相机参数模块、专用编辑器和相应测试。

### 修改文件

- 能力目录、节点注册、专用界面宿主、模型映射、多结果落图服务、中英文文案和图标。
- SDK catalog/presentation 仅按 4.3 确认结果修改。

### 可能废弃

- 无。不得复制相机舞台完整编辑器；只复用通用 Three 交互/样式能力。

## 性能与回退要求

- 编辑器关闭后必须停止帧循环并释放几何体、材质和纹理。
- Three 初始化失败时保留预设列表作为可用降级，不阻断节点数据读取。

## 验收标准

- [x] 工具条能创建并连接多角度节点，节点参数可保存重开。
- [x] 相机控件与 4.3 模型能力一致，离散模型不会呈现虚假连续精度。
- [x] 单图或图片组按契约创建，顺序和角度标签正确，部分失败可恢复。
- [x] 确认、取消、离散吸附、无伪提示词和资源释放行为正确。
- [x] 结果可连接、素材组可展开，项目导入导出不丢失标签。
- [x] 单元/组件测试和真实 Electron 场景通过。
- [x] 执行记录和中文交接已更新；总览按并发边界留给主任务统一更新。

## 执行记录

### 2026-08-29

- 完成内容：完成连续模型控制与九档离散方位两个 profile、最多 6 视图/并发 2 的批次执行、失败项恢复、结果组原子落图、专用节点及轨道编辑器。
- 实际改动：新增 Fal 可选多角度 SDK tool pack、隐藏执行模型解析、版本化角度/批次契约、节点/特殊编辑器/迁移/中英文案、正式 Electron 验收场景及测试。
- 验证结果：SDK 可移植、IIFE、模块化包、构建与 72 个相关用例通过；Electron 1440×900 与 1280×720 真实场景通过，UI 审计 11 项规则零命中。
- 遗留问题：未触发付费请求；两个 Fal 端点的主体一致性、视角质量和实际计费仍待后续小额真机矩阵验证。
- 与原计划的偏差：4.3 官方契约确认两个端点都不接收 prompt，因此不实现提示词开关；模型作为节点隐藏执行依赖，不进入普通模型选择器。
- 对后续任务的影响：5.1 可直接使用 `image.multi-angle`、`multiAngleGenNode` 和专用编辑器入口；真实质量只影响实验状态，不阻塞静态实现。
- 相关提交：`feat(canvas): 实现多角度节点与相机编辑器`（哈希见最终交接回报）。
