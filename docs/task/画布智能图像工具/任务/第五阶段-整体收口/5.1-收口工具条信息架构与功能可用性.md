# 5.1 收口工具条信息架构与功能可用性

## 任务状态

- 状态：已完成
- 所属阶段：第五阶段——整体收口
- 前置任务：3.2、3.3、3.4、3.5、4.2、4.4、4.6
- 关联任务：1.2、5.2
- 是否可以并行：否；需要基于全部已实现能力的真实状态统一收口

## 任务目标

在所有目标能力已经有真实入口和成本信息后，最终确定工具条直接入口、更多菜单、分组、禁用说明、键盘操作和不同节点宽度下的布局，使功能可发现但不挤压画布基础操作。

## 当前情况

- 1.2 只建立通用入口和初始分组，不应在缺少真实能力前过度设计最终排序。
- 参考产品平铺多个功能文本，但本项目已有生成、编辑、复制、下载和删除等操作，布局密度不同。
- 不同能力有模型费用、输入限制、特殊界面和本地处理差异，需要一致但不过度打扰的状态表达。
- UI 规则要求横向条带数量受控、最多一个分隔、图标动作低层级、选中态不依赖鼠标悬浮。

## 实施方案

以实际使用路径和节点宽度测试确定最多 3～4 个直接能力，其余进入单一更多菜单。入口按“生成/变换、结构化输出、本地处理”分组，但菜单不做多层嵌套。模型或源媒体不满足时，根据可恢复性选择隐藏或禁用并解释。基础操作和危险操作保持现有层级。

## 执行步骤

1. 汇总每项能力的适用节点、使用频率假设、费用、特殊界面和不可用原因。
2. 用小/中/大节点、100%及常用缩放、中文/英文和窄窗口测试当前工具条。
3. 确定直接入口、更多菜单、分组、排序、标签长度、图标、分隔和危险操作位置。
4. 统一可用、禁用、实验、付费、本地、加载中和错误状态的视觉及辅助说明。
5. 补齐键盘导航、焦点管理、读屏名称、菜单关闭、工具提示和触控命中区域。
6. 检查工具条出现/消失不改变节点尺寸，不触发全画布重渲染，不与 ReactFlow 拖拽冲突。
7. 添加响应式、国际化、能力状态和真实 Electron 视觉审查场景；由执行者打开截图目视检查。

## 涉及内容

### 新增文件

- 原则上不新增共享 UI 原语；若现有菜单/按钮无法满足，必须在任务记录中说明原因、替代方案和影响范围，再按用户已给予的常规实现授权自主决定。

### 修改文件

- `src/features/canvas/ui/NodeActionToolbar.tsx` 及 1.2 建立的能力菜单。
- 能力目录中的分组/排序/展示元数据、中英文文案和图标映射。
- 工具条组件测试、真实 Electron 场景与视觉审查配置。

### 可能废弃

- 经统一入口完全覆盖的重复工具条入口可合并；底层执行能力与历史项目兼容保留。

## 验收标准

- [x] 九项能力均可从清晰路径到达，直接入口数量受控且无二级嵌套迷宫。
- [x] 小节点、常用缩放、窄窗口和中英文下无裁切、遮挡或跳动。
- [x] 禁用/隐藏规则一致，可恢复问题给出原因，付费动作在目标节点执行前可见成本。
- [x] 键盘、焦点、读屏、工具提示和点击命中符合项目 UI 约束。
- [x] 工具条不改变节点高度，不引入热路径扫描或拖拽掉帧。
- [x] 组件测试与真实 Electron 视觉审查通过，截图已由执行者目视检查。
- [x] 执行记录与交接已更新；总览和跨任务重要决定由 5.2 统一收口。

## 执行记录

### 2026-08-29

- 完成内容：完成九项图片能力审计、响应式直达区、单层更多菜单、三分组、禁用原因、实验标识、键盘与焦点闭环，并补齐正式 Electron 双尺寸场景。
- 实际改动：1440 宽屏直达“元素编辑、高清、打光、720°全景”，960 宽屏直达前两项；其余统一进入“生成与变换、结构化输出、本地处理”。空图片节点保留可聚焦禁用项；基础动作与能力区以一处分隔线隔开。
- 验证结果：36 项定向 Vitest、20 项场景注册测试、定向 ESLint、中英文 JSON、Electron 构建全部通过；正式 Electron 1440×900/960×640 场景通过，UI audit 两档 11 规则零命中，截图已目视检查。
- 遗留问题：所有云模型质量、实际耗时和真实账单仍待用户统一付费测试；工具条本身没有触发供应商调用。仓库既有 assistant attachment file modality 三项 TypeScript 欠账不属于本任务。
- 与原计划的偏差：4.6 因未付费烟测暂时关闭图层分离；结合用户已明确要求全部功能完成后统一测试，5.1 将其改为可达、可执行的实验入口，并保留实验标识与节点内费用信息，不把质量写成已验证。
- 对后续任务的影响：5.2 可直接按本交接中的九项入口矩阵、双尺寸截图和审计报告执行全局收口。
- 相关提交：`feat(canvas): 收口图片能力工具条信息架构`。
