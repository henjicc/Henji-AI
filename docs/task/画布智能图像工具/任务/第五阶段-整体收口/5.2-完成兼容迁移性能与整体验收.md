# 5.2 完成兼容迁移性能与整体验收

## 任务状态

- 状态：已完成
- 所属阶段：第五阶段——整体收口
- 前置任务：5.1
- 关联任务：全部任务
- 是否可以并行：否；这是最终集成验收任务

## 任务目标

完成旧项目兼容、项目导入导出、媒体持久化、撤销重做、性能、日志、国际化和真实 Electron 全流程回归，清理临时兼容层，并形成可发布或明确待验证的最终状态。

## 当前情况

- 本计划会新增能力声明、功能节点、结果语义、专用界面、多结果和可能的主进程图片服务，跨越多个持久化边界。
- 各任务的精确测试不能替代旧项目、多个能力组合、长时间打开/关闭专用界面和项目打包往返。
- 项目规定真实应用视觉验收必须使用真实 Electron 窗口和正式场景，浏览器不能代替。
- 真实模型调用可能收费，发布前必须区分“代码验证通过”和“付费模型质量已验证”。

## 实施方案

先建立跨能力验收矩阵，再逐项执行自动化和真实 Electron 场景。发现缺陷回到所属任务修复并补测试，不在本任务堆临时模型分支。只有所有强制项通过、待验证项有明确授权/风险说明、计划状态一致时才完成。

## 执行步骤

1. 核对 1.1～5.1 的任务状态、执行记录、提交、遗留问题和用户授权/团队决策记录，找出未闭环项。
2. 准备旧版画布、普通图片、分镜、素材组、全景、打光、多角度、元素编辑、九宫格、高清、人像和图层项目样本。
3. 验证新建、连接、生成/处理、取消、失败、重试、撤销/重做、复制、删除、保存重开和项目切换。
4. 验证项目打包导出/导入、媒体收集、本地路径解析、缺失文件、元数据和跨版本迁移。
5. 按任务改动范围运行精确测试、lint/类型/目录/i18n/主进程构建等规定检查，失败回到根因修复。
6. 运行正式真实 Electron 场景和视觉审查，检查不同缩放、节点数量、专用界面反复开关、大图/多图资源占用和日志链路。
7. 核对付费测试授权、实际费用、模型质量和未验证供应商；未授权项不得标为已验证。
8. 删除只服务开发的临时开关、重复入口和无引用兼容代码；保留必要迁移和降级。
9. 更新全部计划状态、重要记录和最终用户可见结论，按任务边界检查提交未混入他人改动。

## 涉及内容

### 新增文件

- 必要的跨能力真实 Electron 场景、固定项目夹具和回归测试；只加入可维护、无敏感信息和体积合理的资源。

### 修改文件

- 根据验收缺陷回到对应模块修改；本任务不预设新的业务架构。
- 项目迁移、导入导出、媒体收集、日志或测试基础仅在实际缺口有证据时调整。
- 本计划目录中的总览、任务记录和重要决定。

### 可能废弃

- 临时实验开关、重复入口、未引用适配器和已完成迁移的短期桥接代码；删除前必须证明无历史项目和调用依赖。

## 验收标准

- [x] 九项能力的入口、节点、参数、输出和专用界面符合各自契约。
- [x] 全景可载入、保存、重开、下载并进入真正球面浏览；付费生成画质单独保留在 2.1/2.4 待验证。
- [x] 单图、多图素材组和图层栈在保存、导入导出、缺失媒体和旧项目场景中行为稳定。
- [x] 普通图片生成、分镜、素材组、编辑、下载、复制和宫格切分无回归。
- [x] 取消、失败、重试和部分失败不会留下孤儿节点、泄漏运行态或丢失源媒体。
- [x] 工具条、专用界面和大图/多图场景满足本轮功能压力与资源释放要求。
- [x] 必需的精确测试、静态检查、构建和正式真实 Electron 视觉场景全部通过。
- [x] 付费模型质量的已验证/待验证边界、费用和风险清楚；本轮真实付费次数和费用均为 0。
- [x] 所有计划文件状态一致，执行记录完整，每个完成边界有独立且不混杂的提交。

## 执行记录

### 2026-08-29

- 完成内容：完成九项图片能力、旧项目迁移、单图/素材组/图层栈事务、项目包、恢复取消、双尺寸界面、静态门禁和功能压力矩阵；把仓库既有 P0 构建阻塞及验收中新发现的 P1 根因全部清零。
- 实际改动：收窄助手附件为真实支持的 image/video/audio；为专用编辑器补 StoreLedger 并登记蒙版编辑边界；修复全景下载临时文件泄漏、多结果媒体所有权、视频/音频并发落盘、图层来源媒体打包、项目包原子导入导出、跨项目生成/恢复迟到写入；修正打光对比度和正式 Electron 场景/evidence/stress 夹具。
- 验证结果：全量 Vitest 486 文件、2844 项通过（44 项按仓库既定条件跳过）；渲染层与 Electron ESLint、双端 TypeScript、完整 `electron:build`、SDK/模型/i18n/颜色/表面/图标/主进程导入/助手能力门禁全部通过。正式 Electron 在 1440×900 与 960×640 共 20 个能力场景通过，双尺寸 220 条 UI 审计规则零命中；smoke 为 61 帧且零页面/控制台错误。134 节点压力场景完整加载，50 个闲置生成节点没有常驻编辑器，退出码 0；空闲/缩放约 60/61 FPS，往返平移探针 21 FPS，堆内存 64→90 MB（该脚本不是专业平移基准）。
- 遗留问题：仅 2.1 与 2.4 的真实全景五类付费样本、画质、渠道稳定性、耗时和账单待授权验证；本轮没有真实模型调用，费用为 0。其他能力的真实模型质量也不被本轮非付费证据夸大为已验证，但不构成计划任务状态阻塞。
- 与原计划的偏差：验收过程中额外发现并修复六类受管文件/项目切换/项目包 P1，以及正式场景的假绿与竞态；没有引入模型分支、第二套媒体服务或跳过门禁。
- 对后续任务的影响：本计划的实现与非付费验收已完成；未来真实质量问题应通过发布状态、供应商组合或模型策略回退，不回退共享能力目录、节点编排和结果/媒体契约。
- 相关提交：最终集成提交见本任务交接；独立架构验收 `3a5cf6fb`，独立界面验收 `758f5f27`。
