# 3.4 整体回归与验收

## 任务状态

- 状态：自动验收完成，等待用户手测
- 所属阶段：第三阶段-多机位硬切
- 前置任务：全部（1.1~3.3）
- 关联任务：无
- 是否可以并行：否

## 任务目标

对整个"简易模式时间轴重做"任务做一次收口回归：全套自动检查通过、治理约束复查通过、用户按完整手测清单在真实 Electron 验收通过，任务整体标记完成。

## 当前情况

- 各任务已各自跑过快速检查；本任务跑完整链路并复查治理约束。
- 已知遗留：上一个任务计划（`docs/task/运镜控制简易模式/`）的 `npm run electron:smoke` 资源 403 阻塞未解决（断言在 `scripts/electron-phase4-smoke.cjs:214`）。若本次回归同样命中，属既有问题，不算本任务引入，但需在执行记录中写明现象是否一致。
- 手测约定：鼠标交互类验证不自行操作（CLAUDE.md 协作约定），汇总各任务清单交用户执行。

## 实施方案

自动回归 + 治理复查 + 用户手测三段收口；发现问题按所属任务回改并在对应任务执行记录补记，本任务只做收口不做新功能。

## 执行步骤

1. 自动回归：`npm run test`、`npm run gen:model-manifest`、`npm run check:colors`、`npm run check:model-i18n`、`npm run lint`、`npx tsc -p tsconfig.electron.json --noEmit`、`npx eslint electron --ext ts --report-unused-disable-directives --max-warnings 0`、`npm run electron:build`、`npm run electron:smoke`（403 若复现按上文处理）。
2. 治理复查：原生控件命中仅 `primitives.tsx`；无新增颜色硬编码；无新增裸 any（对 `src/features/cameraStage/simple/`、`domain/renderCameraSchedule.ts` 等本任务改动面 grep）；新文件 ≤400 行。
3. 汇总用户手测总清单（合并 1.2/1.3/2.1/2.2/2.3/3.2/3.3 各清单，按场景组织）：
   - 单机位基础流：建卡→摆画面→scrub 预览→trim→重排→缩放→过渡气泡调参→硬切→撤销/重做→保存重开。
   - 多机位流：双摄像机建卡→切换点预览→机位徽标改机位→跨机位硬切呈现→导出 MP4 对比预览。
   - 兼容流：旧简易工程（无 cameraId）打开行为不变；专业模式时间轴不受影响；烘焙为专业工程仍正常。
4. 用户验收通过后：更新 00-任务总览（全部已完成）、清理各任务遗留标记、最终 Git 提交。

## 涉及内容

- 无新增代码（回归中发现的问题回到所属任务修复）。
- 更新 `docs/task/简易模式时间轴重做/` 下各文档状态。

## 验收标准

- [x] 自动回归通过（`electron:smoke` 仅复现已知资源 403，已记录为既有阻塞）。
- [x] 治理复查四项全部通过。
- [ ] 用户手测总清单三条流全部通过。
- [ ] 00-任务总览与各任务文件状态已同步，最终提交完成。

## 执行记录

- 2026-07-11：已按完整链路运行 `npm run test`（11 个文件、97 条用例）、`npm run gen:model-manifest`、`npm run check:colors`、`npm run check:model-i18n`、`npm run lint`、`npx tsc -p tsconfig.electron.json --noEmit`、`npx eslint electron --ext ts --report-unused-disable-directives --max-warnings 0` 与 `npm run electron:build`，均通过。构建只产生被忽略的 manifest/seeds/产物，未纳入本任务变更。
- `npm run electron:smoke` 在 `scripts/electron-phase4-smoke.cjs:214` 复现 `Failed to load resource: the server responded with a status of 403 (Forbidden)`；报错位置、现象均与本计划列明的上一任务既有阻塞一致，不归因于本任务，未改动功能代码。
- 治理复查通过：`git diff --check` 无空白错误；原生控件搜索在 `src/` 中仅命中 `src/components/ui/primitives.tsx`；本任务改动面无颜色硬编码和新增裸 `any`；新增文件均不超过 400 行。
- 已复核提交 `24c728e`（3.1）、`17c3f43`（3.2）、`0cbc7d3`（3.3）：机位时间表/预览派生/导出逐帧 seek 共用同一渲染链路；记录 007 的首摄像机画幅锁定与导出规则、记录 008 的 Ctrl+点击三态时间码均已纳入回归事实。

### 用户手测总清单（待执行）

1. **单机位基础流**：新建镜头卡并摆放画面；拖动标尺 scrub 预览；右边缘 trim、块身重排、Alt+滚轮缩放；打开过渡气泡调参并将过渡拖为硬切；依次撤销/重做，保存后重开工程确认时间轴、时间码 Ctrl+点击三态切换均正确。
2. **多机位流**：新建摄像机 A/B，为相邻镜头卡通过机位徽标分别指定 A/B；播放和 scrub 跨切换点，确认视口与画幅遮罩即时跳至 B；确认跨机位段为剪切线、时长不可编辑，改回同机位后恢复过渡；导出 MP4 并逐帧对照预览切换点。首摄像机改画幅时，确认其他机位同步且导出尺寸跟随首机位。
3. **兼容流**：打开无 `cameraId` 的旧简易工程，确认播放/编辑不变；确认专业模式时间轴无回归，并可正常烘焙为专业工程；单机位导出、取消导出正常。可选：旧数据机位画幅不一致时，确认提示不阻断且日志中心存在 `camera_stage.video_export.aspect_ratio_inconsistent`。

未执行上述真实 Electron 鼠标交互前，不得将本任务标记为人工验收完成。
