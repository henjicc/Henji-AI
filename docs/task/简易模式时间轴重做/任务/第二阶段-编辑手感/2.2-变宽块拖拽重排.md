# 2.2 变宽块拖拽重排

## 任务状态

- 状态：已完成
- 所属阶段：第二阶段-编辑手感
- 前置任务：1.2
- 关联任务：2.1（手势约定：块身 = 重排，边缘 = trim）
- 是否可以并行：可与 2.1 并行——实际执行时因改动同一批文件，与 2.1 合并编码、拆成两次独立提交（详见 decisions.md D4）

## 任务目标

静止块支持按住块身拖拽重排（过渡跟随其前面的静止卡一起移动，数据上天然如此，见重要记录 001）；拖拽过程有明确的插入位指示；与点击（选卡）、双击（重命名）、边缘拖拽（trim）手势不冲突。

## 当前情况

- 旧实现用 `useReorderDrag`（`src/components/ui/fileUploader/useReorderDrag.ts`），1.2 已随卡片布局移除接线。该 hook 假设**等宽项**（旧面板硬编码 204px 偏移），比例块变宽后其偏移模型不适用——这是新写时间轴专用重排 hook 的理由（CLAUDE.md"拖拽排序统一复用 useReorderDrag"针对的是文件上传场景，此处语义不同，任务文件记录此偏差即可）。
- store 现成 action：`reorderShot(id, toIndex)`（`store/shotSlice.ts:129`，含重编译，一次调用一条历史）。
- 块布局：`buildClipLayout` 输出每块 `x` / `width`，插入位可由拖拽点 x 与各静止块中线比较得出。
- 点击 vs 拖拽区分：`useReorderDrag` 内部有移动阈值模式可参考（阈值内算点击）。

## 实施方案

新建 `simple/timeline/useClipReorder.ts`：

- pointerdown 在静止块身（非边缘命中区）→ 记录起点；超过移动阈值（约 4px）进入拖拽态，未超过在 pointerup 时触发点击回调（选卡）。
- 拖拽态：被拖块半透明跟随指针水平移动；根据指针 x 与各静止块中线计算目标插入 index，在目标间隙渲染插入指示线；其余块不做实时挪位动画（变宽块做占位动画复杂度高、收益低，一期用指示线即可——若实现时发现简单可再加）。
- pointerup → 调一次 `reorderShot(shotId, toIndex)`；Escape 取消。
- 过渡块不可拖拽重排（它跟随前卡）。
- 重排提交补 `logger.debug`（event 如 `simple_mode.shot.reordered`）。

## 执行步骤

1. 新建 `useClipReorder.ts`（阈值区分点击/拖拽；插入 index 计算纯函数拆出，便于单测）。
2. `ShotClipTrack` 接 hook：被拖块浮起样式、插入指示线渲染；点击回调走 `selectShot`（替换 1.2 的直接 onClick）。
3. `ShotTimelinePanel` 接 `reorderShot` 提交。
4. 插入 index 计算单测（拖到最左/最右/自身位置/相邻位置）。
5. 自检：vitest、lint、前后端 tsc、check:colors。
6. 写用户手测清单：拖块重排顺序正确且过渡跟随、拖到自身位置无变化、点击仍选卡、双击仍重命名、边缘拖拽仍 trim、一次重排一条撤销记录、Escape 取消。

## 涉及内容

### 新增文件

- `src/features/cameraStage/simple/timeline/useClipReorder.ts`：变宽块重排 hook（含插入位纯函数）。
- `src/features/cameraStage/simple/timeline/useClipReorder.test.ts`：插入 index 计算单测。

### 修改文件

- `src/features/cameraStage/simple/timeline/ShotClipTrack.tsx`：接重排 hook 与指示线。
- `src/features/cameraStage/simple/ShotTimelinePanel.tsx`：提交接线。

## 验收标准

- [ ] 静止块可拖拽重排，过渡跟随前卡，插入指示线位置正确。
- [ ] 点击 / 双击 / 边缘 trim / 块身重排四种手势互不误触。
- [ ] 一次重排只产生一条撤销记录；插入 index 单测通过。
- [ ] vitest / lint / 前后端 tsc / check:colors 通过；用户手测清单已提供。
- [ ] 任务总览已同步更新。

## 执行记录

- 2026-07-11：与 2.1 合并编码完成（同一批文件），提交为 `完成：2.1 块边缘拖拽调时长 + 2.2 变宽块拖拽重排`。
- 新建 `src/features/cameraStage/simple/timeline/useClipReorder.ts`：导出纯函数 `computeInsertIndex(
  midpoints, pointerX)`（排除被拖块自身后的静止块中线数组 + 指针 x → 插入下标，语义与 store 的
  `reorderShot(id, toIndex)` 完全一致，可直接传参）；hook 部分 `beginReorder` 记录起点与
  `fromIndex`，`handlePointerMove` 超过 4px 阈值才判定为拖拽（`state.moved = true`）并持续重算
  `insertIndex`，`handlePointerUp` 未超阈值走 `onSelect(shotId)`（点击语义），超阈值且
  `insertIndex !== fromIndex` 才调 `onReorder(shotId, insertIndex)`（拖回原位不提交、不产生撤销历史）。
  提交时补 `logger.debug('simple_mode.shot.reordered', ...)`。Escape 取消。
- 单独新建 `useClipReorder.test.ts`：6 条用例覆盖 `computeInsertIndex` 的最左/最右边界、相邻中线之间、
  恰好等于某中线、空候选数组（只剩 1 张卡时排除自身后为空）、单候选左右两侧共 6 种情况。
- `ShotClipTrack.tsx` 接入：`staticBlocks` 从当前 `layout` 派生（`{shotId,x,width}[]`），`trackRef` 挂在
  轨道内容容器上供指针坐标换算；拖拽中渲染 `pointer-events-none` 的插入指示线（`bg-accent`
  竖条），位置按目标插入下标对应的相邻块坐标计算。过渡块本身不接入重排 hook（跟随前面的静止卡，数据
  层面天然如此，无需额外代码）。
- `StaticClipBlock.tsx` 块身 wrapper 从原来的 `onClick={onSelect}` 改为挂 `reorderHandlers`
  （pointerdown/move/up/cancel），拖拽中加 `cursor-grabbing`/`opacity-70`/`shadow-lg`/`translateX` 视觉
  跟手；键盘 Enter/Space 仍走独立的 `onSelect` prop（不受指针手势影响）。删除按钮与重命名输入框补充
  `onPointerDown={stopPropagation}`，避免点击它们时误触发块身拖拽/选中。
- 手势互斥验证（联合 2.1）：右边缘 trim 命中区 `beginTrim` 内 `stopPropagation`，不会冒泡到块身触发
  `beginReorder`；块身拖拽不会进入 trim 命中区（两者是不重叠的 DOM 区域）；双击重命名走独立
  `onDoubleClick`，与 pointerdown/up 序列互不干扰。
- 自检：`npm run test`（76/76，新增 6 条 `useClipReorder` 用例，见 test-report.md）、`npm run lint`
  （0/0）、`npx tsc -p tsconfig.electron.json --noEmit`（0 报错）、`npm run check:colors`（通过）、
  原生控件检查（新增/改动文件零命中）全部通过。

### 用户手测清单（真实 Electron 窗口，`npm run electron:dev`）

1. 按住某张静止块块身（非边缘）拖动超过约 4px：应出现半透明跟手效果与蓝色插入指示线；松开鼠标后
   镜头卡顺序按指示线位置调整，紧随其后的过渡块（若有）应跟随该卡一起移动。
2. 拖动某张卡到它自己原来的位置再松手：顺序应不变，且不应产生新的撤销历史（Ctrl+Z 应跳过这次操作，
   直接回到更早的状态）。
3. 单击（不拖动）某张静止块：应正常选中该卡（视口应用其快照、播放头跳到该卡起点），与拖拽重排互不
   冲突。
4. 双击某张静止块名称：应正常进入重命名编辑态，不会被块身拖拽手势拦截。
5. 拖拽重排的同时验证边缘 trim 仍可用：拖右边缘应正常触发调时长而不是被误判成块身重排。
6. 完成一次重排后按一次 Ctrl+Z：应完整撤销回重排前的顺序，且只产生一条撤销记录。
7. 拖拽过程中按 Escape：应取消本次重排，卡片顺序保持不变。

✔️无需重启（仅渲染层 `src/` 组件与 hook 改动，Vite HMR 可热更新；若页面表现异常可手动刷新渲染层作为兜底）。
