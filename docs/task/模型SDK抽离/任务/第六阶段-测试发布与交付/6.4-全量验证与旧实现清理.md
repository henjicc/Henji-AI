# 6.4 全量验证与旧实现清理

## 任务状态

- 状态：已完成（非 Electron 真宿主验收由 6.5 继续）
- 所属阶段：第六阶段 测试发布与交付
- 前置任务：6.3、3.4、6.1
- 关联任务：6.5（非 Electron 真宿主验收）、6.6（需新增授权的供应商残余真实性验证）
- 是否可以并行：否

## 任务目标

对整个迁移做一次完整验证，清理全部残留的旧实现与过渡代码，更新项目规则文档指向新结构，并逐条核对 [01-实施方案.md](../../01-实施方案.md) 第九节的最终验收条件。原计划预期本任务完成即整个计划完成；实际只读审计确认非 Electron 真宿主与需新增授权的外部真实性边界不能在 6.4 内诚实满足，分别由 6.5、6.6 继续。

## 当前情况

前 20 个任务完成后，预期仍会残留以下几类需要收尾的内容（执行时逐项核实，不得假设已处理）：

### 应已删除但需确认的文件

- `scripts/generate-model-manifest.cjs`、`electron/main/services/ai-runtime/js-runtime.ts`、`manifest.ts`、`resources/model-manifest.json`（任务 3.3）
- `scripts/migrate-model-definitions.cjs`（任务 3.2）
- `src/core/defineModel.ts`（任务 3.4，若已无引用）
- `packages/ai-sdk/src/llm/task-registry.ts`、`llm/sdk/provider-error.ts`（任务 4.3）

### 过渡代码

- 任务 2.2 在 `request-builder-dsl.ts` 引入的 `BuilderEvaluator` 参数（任务 3.3 应已移除，需确认）
- 任务 1.2 在主进程与渲染层写的临时引用（应已删除）
- 各任务执行记录中标注的「遗留问题」——本任务统一收口，能修的修，不能修的转为新任务或写进 README 的已知限制

### 规则文档需要更新的位置

| 文件 | 需要更新的内容 |
|---|---|
| `AGENTS.md` | 规则索引表中「新增供应商/模型」一行的路径（现指向 `docs/model-adaptation/README.md`，已迁至 SDK）；全局架构边界第 1 条「模型行为写在 `src/models/**.model.ts`」已不成立；第 2 条的生成链路描述已变 |
| `docs/rules/model-adaptation.md` | 适配流程的文件路径全部变化 |
| `docs/rules/architecture.md` | 生成链路描述、跨层导入规则中 SDK 包的定位 |
| `docs/rules/testing.md` | 第四节「模型 / 参数 schema」小节里的 `npm run gen:model-manifest` 已删除 |
| `.claude/skills/henji-model-adaptation/SKILL.md` 与 `.codex/skills/` 同名文件 | 全部路径与流程，**两份必须同步** |
| `.claude/skills/canvas-node-builder/SKILL.md` 与 `.codex/` 副本 | 若引用了模型 schema 路径 |
| `CLAUDE.md` | 若涉及 |

### 遗留的规则冲突

`AGENTS.md` 全局架构边界第 1 条写「模型行为写在 `src/models/**.model.ts` 的 schema 里」——迁移后模型定义在 `packages/ai-sdk/src/catalog/`。这不是笔误修正，而是架构描述变更，需要重写该条并说明新的配置驱动边界。

## 实施方案

按「先验证、再清理、最后改文档」的顺序，避免文档先改导致验证时对不上。

全量验证按 `docs/rules/testing.md` 的 **L3** 级别执行——本任务符合「无法可靠界定影响范围的跨目录跨层重构」与「合并前的高风险检查」两条。

清理时对每个待删文件先跑一次全仓库引用搜索，确认无引用再删。

## 执行步骤

1. **全量验证**（按 testing.md L3）：
   ```
   npm run lint
   npx eslint electron --ext ts --report-unused-disable-directives --max-warnings 0
   npx tsc -p tsconfig.json --noEmit
   npx tsc -p tsconfig.electron.json --noEmit
   npx vitest run
   npm run check:sdk
   npm run electron:build
   npm run electron:smoke
   ```
2. 跑真实性测试：`npm run test:reality -- --suite integration`，以及 `--suite ui` 与 `--suite ui-audit`。
3. 跑助手验证：按 `docs/rules/testing.md` 第四节完成 L-A 静态不变量与 L-B 剧本 harness。
4. 逐项核对上述「应已删除但需确认的文件」，对每个先搜引用再删：
   ```
   rg -n "generate-model-manifest|js-runtime|getManifestStore|model-manifest|BuilderEvaluator|migrate-model-definitions"
   ```
5. 收口全部任务执行记录中的「遗留问题」：能修的修，不能修的转为新任务（编号在所属阶段末尾追加）或写进 `packages/ai-sdk/README.md` 的已知限制。
6. 更新规则文档（上表 7 处），重点是 `AGENTS.md` 的规则索引表与全局架构边界第 1、2 条。
7. 同步两份 skill（`.claude/skills/` 与 `.codex/skills/`），逐字比对确认一致。
8. 核对文档中提到的每个命令、路径、脚本名真实存在（testing.md L0 的要求）。
9. 逐条核对 [01-实施方案.md](../../01-实施方案.md) 第九节的 12 项最终验收条件；未满足项保持未勾并转为正式后续任务，不得以替代证据冒充通过。
10. 更新 [00-任务总览.md](../../00-任务总览.md)：全部任务标记已完成，整体状态改为已完成。
11. 按 `AGENTS.md` 完成标准第 3 条检查并报告开发环境状态。

## 涉及内容

### 修改文件

- `AGENTS.md`：规则索引表路径、全局架构边界第 1、2 条。
- `docs/rules/model-adaptation.md`、`architecture.md`、`testing.md`：路径与流程。
- `.claude/skills/henji-model-adaptation/SKILL.md`、`.codex/skills/henji-model-adaptation/SKILL.md`：两份同步。
- `.claude/skills/canvas-node-builder/SKILL.md` 与 `.codex/` 副本：若涉及模型 schema 路径。
- `CLAUDE.md`：若涉及。
- `packages/ai-sdk/README.md`：已知限制。
- `docs/task/模型SDK抽离/00-任务总览.md`：最终状态。

### 可能废弃

- 前述「应已删除但需确认的文件」清单中仍存在的任何文件。
- `src/models/` 若只剩空目录则删除（保留 `presentation/`）。

## 验收标准

- [x] L3 全量验证 8 条命令全部通过
- [x] `npm run test:reality -- --suite integration` / `--suite ui` / `--suite ui-audit` 全部通过
- [x] 智能助手 L-A + L-B 验证通过
- [x] 待删文件清单逐项确认：要么已删除，要么记录了保留理由
- [x] `rg -n "generate-model-manifest|js-runtime|getManifestStore|model-manifest|BuilderEvaluator"` 在业务代码中无命中（计划文档除外）
- [x] 全部任务的「遗留问题」已收口：修复、转为新任务、或写入已知限制，无悬空项
- [x] `AGENTS.md` 的规则索引表与全局架构边界第 1、2 条已按新架构重写
- [x] `docs/rules/` 三份规则文档路径已更新，且提到的命令与路径经搜索确认存在
- [x] 两份 `henji-model-adaptation` skill 逐字一致
- [ ] [01-实施方案.md](../../01-实施方案.md) 第九节的 12 项最终验收条件全部满足
- [x] [00-任务总览.md](../../00-任务总览.md) 已更新为 6.4 完成、6.5 可开始的真实状态
- [x] 开发环境状态已按 `AGENTS.md` 完成标准第 3 条检查并如实报告

## 执行记录

### 2026-08-27

- 完成内容：完成旧实现逐项引用审计与清理、规则/skill/文档路径收口、最终 L3/Reality/助手验证、Electron 截图目视、`0.1.1` 私有补丁发布与远端干净安装。
- 实际改动：删除 5 个 LLM 重新导出壳与占位测试/API；保留有真实应用职责的 `src/core/defineModel.ts`；修复真实 UI 资产标签对比度；追加 6.5 真 Tauri 零付费验收。
- 验证结果：L3 8/8；Reality integration 9 文件/35 测试，UI 56/56，audit 56 场景×11 规则 0 命中；L-A/L-B 通过；SDK prepublish 44 文件/490 测试；发布后干净安装与 99 模型/五入口无网络探针通过。
- 遗留问题：01 第九节第 10 项仍未满足，已正式转为 6.5；四 override 模型 E2E 与 provider connection/余额真实验证转为需新增授权的 6.6；其余外部/真机边界按 README 已知限制保留，6.4 未再次付费。
- 与原计划的偏差：本机有可用 Tauri 2 宿主但尚未接线 SDK/fixture，UXP 无可启动宿主，不能把“可实施”写成“已通过”；因此 6.4 自身完成但整体计划仍为 21/22。
- 对后续任务的影响：6.5 已解锁；6.6 等待 6.5 与用户新增授权；两者完成前不得宣布整个计划完成。
- 相关提交：未提交。共享工作区包含用户与其他任务的大量未提交改动，无法证明独立提交不会混入，故按任务授权保持原状。
