# 6.7 计划外收口检查点交接

## 结论

- 计划外检查点完成。原计划仍为 23/23；6.7 不改写既有任务计数。
- Fal Z-Image Turbo 唯一一次真实生成 completed，证明 6.6 的 `status_url` 修复不仅适用于 Seedream，也适用于另一条 Fal 模型链路。
- 全量代码、配置、测试与文档审计完成；4 个可离线闭环的缺陷已修复并补精确回归，UXP/Fal 官方上传的既有边界按用户要求继续后置，不阻断本检查点。
- SDK 抽离主体已提交：`26a23136 refactor(ai-sdk): 完成模型 SDK 抽离`，713 个文件，34,449 行新增、15,338 行删除。

## 授权与真实请求

用户授权边界：Fal Z-Image 只允许 1 次真实 create；禁止失败后自动重提；单次预估费用必须不超过 US$0.05；同一任务必要的 poll/result 允许；不得测试其他付费模型。

零网络预检结果：

- project model ID：`fal-ai-z-image-turbo`
- canonical model ID：`z-image-turbo`
- provider / route：`fal` / `fal-ai/z-image/turbo`
- 本地唯一资料源价格：`$0.005 / megapixel`
- 本次 1024×1024 精确目录估价：`$0.00524288`，低于 `$0.05` 授权上限
- 最小请求：text-only、1:1、1024×1024、1 张、8 steps、regular acceleration、prompt expansion=false、safety checker=true
- 凭据：正式 real profile 中 Fal 凭据可用；仅在内存解密，未输出或落盘
- 预检网络请求：0

真实链路结果：

| 项目 | 结果 |
|---|---|
| create | 1 次，唯一一次，无重试 |
| poll | 1 次，只轮询 create 返回的同一 task |
| result | 1 次，只读取同一 task 的结果元数据 |
| blocked / unknown | 0 |
| 终态 | completed |
| 耗时 | 5,284 ms |
| 估算费用 | US$0.00524288 |
| status URL | host `queue.fal.run`，path 仅保存 SHA-256 前 12 位摘要 `9df7a76a782f` |
| result URL | 1 个，host `v3b.fal.media`，path 仅保存 SHA-256 前 12 位摘要 `58d0f249af75` |
| task ID | 仅保存 SHA-256 前 12 位摘要 `342d5d0dfa36` |

未下载生成结果、未保存完整 URL、未做 HEAD/GET 媒体下载、未发起其他供应商或模型请求。

## 审计范围与结论

逐类核对了任务 2.4～6.6 交接、任务总览与 Git diff，并覆盖：

1. 发布包：五个 exports、显式 dependencies、175 模块 ESM+DTS 构建、sideEffects、私有 registry、README/CHANGELOG、prepublish 与仓库外 Vite 五入口。
2. SDK portable 边界：无 Electron、`@/`、Node builtin/全局量或动态代码生成依赖；`Transport`/凭据/媒体读取/日志/trace 宿主契约保持窄接口。
3. 99 catalog：49 图片、49 视频、1 音频；ID、canonical ID、aliases、参数 ID 与迁移前一致；展示补丁仍留在应用侧。
4. provider/runtime：8 个供应商的 upload、create、poll、chat、连接与 fixture；Fal `status_url` 保留和复用已由 Seedream 与本次 Z-Image 两类真实模型证明。
5. Electron：ai-runtime/llm 只保留日志、IPC、落盘、取消、进度等宿主职责；preload/IPC 输入输出边界存在；renderer 继续经 command/platform/PAL 消费。
6. 文档/示例/测试：模型和 LLM 资料迁入包内；Electron/Tauri/UXP/域名/错误指南、三个示例、fixture provenance 与任务历史边界保持可追溯。
7. 工程配置：npm workspace、package lock、catalog 生成、旧 manifest/VM 清理、CI/build/smoke 接线一致。
8. 安全与归属：staged diff 无 `.npmrc`、`.env`、私钥、真实 token、日志、压缩包、安装包或生成媒体；新增行高熵 token/Bearer 扫描为 0。

### 审计发现并修复

| 发现 | 修复 | 精确证据 |
|---|---|---|
| SDK tsconfig 注释要求禁止 Node 类型，但根 workspace 的 `@types/node` 会自动提升注入 | 增加 `types: []` | `tsc --listFilesOnly` 中无 `@types/node` |
| portability gate 只拦 `node:`，会漏掉 `fs`/`path` 等裸 builtin 与 `Buffer/process` | 用 Node builtin 清单同时拦带/不带 `node:` 的 specifier，并加入内建负向自检 | `npm run check:sdk` 扫描 175 文件通过 |
| polling 每轮正常 timeout 后未移除 AbortSignal listener | 正常完成与 abort 两路都显式移除 listener | 20 轮 add/remove 数量相等，abort 错误契约保持 |
| SDK README 快速安装仍写 0.1.1 | 同步为 0.1.3，并在 prepublish 断言 README 与 package version 一致 | 最终 prepublish 通过 |
| Electron `continuePolling` 的 SDK/落盘/pending 异常缺 failed 闭环 | 补 `ai_runtime.poll.start/result/failed`，保持首个异常原样上抛 | 成功与 pending 保存失败 2 项精确测试通过 |

### 明确保留边界

- UXP 本轮按用户要求暂不处理，不以未验收阻断。
- Fal 官方上传仍由 `@fal-ai/client` 自身网络栈执行，尚未证明会完全走宿主 `Transport`；这是既有 UXP 可移植性边界。本轮不做真实上传，也不扩大网络授权。
- 6.1 fixture 中只有 1/8 来源于真实日志，其余来自文档；该 provenance 边界未被改写。

## 验证命令与结果

| 验证 | 结果 |
|---|---|
| Fal provider + fixture + catalog 精确回归 | 3 文件、85 项通过 |
| polling + Electron continuePolling 精确回归 | 2 文件、9 项通过 |
| `node scripts/check-model-id-parity.cjs` | 99/99 通过 |
| `npm run gen:catalog` + loader/consumer tests | 99 imports；3 文件、13 项通过 |
| `npm run prepublishOnly -w packages/ai-sdk` | 45 文件、493 项；175 模块；仓库外 Vite 五入口通过 |
| `npm run lint` | 通过，0 warning |
| Electron ESLint | 通过，0 warning |
| renderer / Electron TypeScript | 两套均通过 |
| SDK TypeScript Node 类型隔离 | 通过；无 `@types/node` |
| 仓库全量 `npm test` | 396 文件通过、9 跳过；2342 项通过、44 跳过 |
| `npm run electron:build` | 通过 |
| `npm run electron:smoke` | 通过；8 provider，0 page error，0 console error |
| `git diff --cached --check` | 主体提交前通过 |

## Git 边界与排除项

主体提交前二次复核结论：

- 没有 camera 路径变更。
- 52 个 agent-runtime 文件与 8 个 renderer assistant core 文件主要是 1 行旧 LLM 类型/执行层 import 迁入 `@henjicc/ai-sdk`，属于 4.1/4.2 必要消费端迁移；`agent-utility.ts` 的较大 hunk 也只合并迁移后的 SDK 导入并删除旧执行层导入。
- application-control 只改 2 个文件，作用是从已删除的 `model-manifest.json/src/models` 真相源切换到 SDK catalog。
- assets 只改 `AssetCard.tsx` 1 行，是 6.4 正式 UI audit 已明确归属的对比度修复。

6.7 主体提交时未纳入代码提交、仍保留在工作区的真实证据：

- `交接/6.5-Tauri窗口.png`
- `交接/6.5-机器证据.json`
- `交接/6.6-ui-evidence/` 整目录
- `交接/6.6-ui-final/` 整目录

后续 6.8 经用户授权逐文件审计：8 个关键证据文件已脱敏归档；1 张与最终恢复截图 SHA-256 完全相同的重复截图、1 个未被引用且可再生的最终轮自动索引已删除。6.7 临时联网 harness 已从 `/tmp` 删除，无仓库内临时产物。

## 发布与开发环境

- 本轮未发布 npm 新版本；0.1.3 仍是当前私有发布版本。
- 6.7 修复属于后续仓库提交，若要让外部消费方获得这些加固，需另行评估版本与发布授权；本轮未擅自发布。
- 交付前确认仓库原无 `electron:dev` 实例，已启动 1 棵当前仓库专属进程树；main/preload/renderer 构建成功，Electron 窗口进程持续运行且启动输出无错误。最终状态：`🟢 开发环境已启动`。
