# 13.3a SDK 消费项目清单交接

日期：2026-08-28

范围：只读扫描 `/Users/henji/Documents/VibeCode` 下项目；只修改 `Henji-AI` 文档与索引。

## 结论

已建立唯一清单：`packages/ai-sdk/docs/consumers.md`，并从 `packages/ai-sdk/README.md` 的接入索引链接。

扫描确认 3 个独立实际宿主：

1. `Henji-AI`：SDK 主仓库与 Electron 首发验证宿主，workspace 源码版本 `0.2.4`。
2. `henji-ai-ps`：Photoshop UXP 受限宿主，精确锁 `0.1.8`。
3. `say-it`：Tauri/Rust 管理 QuickJS 宿主，精确锁 `0.2.3`。

另有 3 个 SDK 仓内可运行示例，manifest 都精确声明 `0.1.1`，没有 lockfile。它们是内部消费验证面，不是额外外部仓库，但 13.3b 仍应升级和执行零网络验证。

所有其他一级项目均未发现 package manifest、lockfile、源码 import 或构建脚本引用。历史任务/README 引用单独归为非消费者证据，没有扩大消费者数量。

## 13.3b 精确输入

### Henji-AI 应用宿主

- 当前 SDK 源码已经是 `0.2.4`，无需修改依赖版本或 lockfile。
- 需检查 GLM 区域身份落点：
  - `src/services/llm/LlmConfigService.ts`
  - `src/services/llm/LlmConfigService.test.ts`
  - `src/services/llm/llmDiscoveryService.ts`
  - `src/components/Settings/hooks/useLlmSettings.ts`
  - `src/components/Settings/sections/LlmProviderDialog.tsx`
  - `src/components/Settings/sections/llmSettingsSectionHelpers.ts`
  - `electron/main/services/llm/runtime.ts`
  - `electron/main/ipc/llm-runtime.ts`
  - `electron/main/services/ai-runtime/sdk-runtime.ts`
  - `electron/preload/api.d.ts`
  - `src/platform/contracts/llmRuntime.ts`
- 目标不是在 UI 写模型分支，而是消费 `@henjicc/ai-sdk/llm/bigmodel` preset/discovery，完整保留 `providerFamilyId`、`endpointProfile`、`credentialId`。国内 `bigmodel` 与国际 `bigmodel-global` 必须分凭据坐标。
- 建议验证：BigModel SDK 精确测试、`LlmConfigService`/discovery/IPC/runtime 相关测试；若修改主进程，执行 `npm run electron:build`。

### henji-ai-ps

- 精确升级文件：
  - `/Users/henji/Documents/VibeCode/henji-ai-ps/package.json`
  - `/Users/henji/Documents/VibeCode/henji-ai-ps/pnpm-lock.yaml`
- 从 `0.1.8` 跨到 `0.2.4` 后重点核对：
  - `src/infrastructure/ai-sdk/application-profile.ts`
  - `src/infrastructure/ai-sdk/runtime.ts`
  - `src/infrastructure/ai-sdk/generation-client.ts`
  - `scripts/check-uxp-ai-sdk.mjs`
  - `smoke/uxp/entry.ts`
  - `smoke/uxp/llm-entry.ts`
- Photoshop 当前消费生成 pack/LLM streaming；除非产品另行要求，不增加 BigModel/GLM 入口。必须确认既有模型子路径仍存在、IIFE 不引入 Node/动态代码生成、凭据和媒体仍由 UXP 宿主注入。
- 建议验证：`pnpm typecheck:uxp-smoke && pnpm check:uxp-sdk && pnpm smoke:uxp:build && pnpm check:uxp-smoke`，再执行完整 `pnpm check`。

### say-it

- 精确升级文件：
  - `/Users/henji/Documents/VibeCode/say-it/package.json`
  - `/Users/henji/Documents/VibeCode/say-it/package-lock.json`
- 从 `0.2.3` 升到 `0.2.4` 后重点核对：
  - `scripts/build-ai-sdk-runtime.mjs`
  - `sdk-runtime/capability-entry.ts`
  - `sdk-runtime/groq-entry.ts`
  - `sdk-runtime/llm-modules-entry.ts`
  - `sdk-runtime/sdk-bootstrap.ts`
  - `src-tauri/src/providers/sdk_runtime/bundle_tests.rs`
  - `src-tauri/src/providers/sdk_runtime/online.rs`
- Say-It 当前只打包 Bailian ASR/translation、Groq、LLM modules；简单版本同步不得把 generation 或 BigModel 带进现有独立 bundle。若以后确实消费 GLM，应另建按需 BigModel entry/bundle，并通过统一 LLM 路由注册。
- 建议验证：`npm run sdk-runtime:typecheck && npm run sdk-runtime:build`、`npm run test:rust`、完整 `npm run check`，并核对 bundle 隔离测试。
- 扫描时 Say-It 工作区存在其他任务未提交改动；13.3a 没有修改该仓库。

### SDK 仓内 3 个示例

- 精确升级文件：
  - `packages/ai-sdk/examples/minimal-node/package.json` 与 `README.md`
  - `packages/ai-sdk/examples/llm-chat/package.json` 与 `README.md`
  - `packages/ai-sdk/examples/form-renderer/package.json`
- 三者从 `0.1.1` 精确升级到 `0.2.4`，不新增长期 workspace alias；分别执行 `dry-run` 或 build/start 的零网络路径。
- 示例没有 lockfile。13.3b 若安装产生 lockfile，应先决定是否把它作为可复现示例资产纳入，而不是无意提交生成文件。

## 扫描与版本证据

- 全盘固定字符串扫描排除了 `.git`、`node_modules`、`dist`、`target`、`build`、`coverage` 与缓存目录。
- `henji-ai-ps` manifest/lock 均为 `0.1.8`，lock integrity 已记录到唯一清单。
- `say-it` manifest/lock 均为 `0.2.3`，lock integrity 已记录到唯一清单。
- `Henji-AI` 根 lock 将 `node_modules/@henjicc/ai-sdk` 链接到 workspace `packages/ai-sdk`，包版本为 `0.2.4`。
- 13.2 发布证据：`@henjicc/ai-sdk@0.2.4` integrity 为 `sha512-k/k7bFj2b0vXc4nDd7VIHJblpp67gD8mivXY9K+Lu5u21BhqSs51vnDfQgG79tQe1qOhboVjrdVDZl1Yak56Gg==`。

## 边界与未做项

- 未修改、安装、构建或提交任何消费项目。
- 未把绝对个人路径写入 SDK 运行时代码；它只存在于开发协调文档。
- 未把文档引用误算为消费者，也未把 SDK 自身测试/fixture 当成独立项目。
- 版本升级与业务接入均留给 13.3b；本任务只提供准确文件级输入。
