# 14.6c SDK 示例升级 0.2.5 交接

日期：2026-08-28

范围：只升级并验证 `Henji-AI/packages/ai-sdk/examples` 下三个独立示例，同步消费清单的三条示例行；不修改外部 `say-it`、`henji-ai-ps`、清单顶部发布版本或 Henji Electron 运行时。

## 结论

- `minimal-node`、`llm-chat`、`form-renderer` 均从 `@henjicc/ai-sdk@0.2.4` 精确升级到已发布的 `0.2.5`，并同步各自 README 的精确版本。
- 三个目录均从 GitHub Packages 远端包正式安装；安装使用 `--no-package-lock`，没有生成示例根级 lockfile，manifest 也没有 `workspace:` / `file:` / `link:` 旁路。
- 三个示例实际解析的远端包元数据一致：
  - version：`0.2.5`
  - tarball：`https://npm.pkg.github.com/download/@henjicc/ai-sdk/0.2.5/b3b80f250d943ac2b23ac4c95b5837dacc2d93dd`
  - integrity：`sha512-6WCgQEWM+EenbBT7njFQEx21jlwrO9KVIdJHHeXula8vImZ3fgcLjnEs+B1W6HaYqv2Cd5DCMk+Ug0wO0eNrLA==`
- 保留 13.3b3 建立的按需入口设计，没有因 `0.2.5` 的供应商 setup 新契约改回包根入口或扩大产物。

## 按需入口与打包边界

### minimal-node

- 仍只使用 `@henjicc/ai-sdk/generation` 与 `@henjicc/ai-sdk/runtime`。
- 零网络 dry-run 仍经过真实 catalog、builder、预处理与 KIE provider；结果为 `networkCalls=0`、`interceptedRequests=1`、`pending`、`dry-run-task`，估价 `$0.004`。
- 关闭 tree-shaking 的依赖图为 158 modules / 637,071 bytes；包含 2 个 generation 模块和 115 个 catalog 模块，LLM / BigModel / provider setup 无关模块均为 0。

### llm-chat

- 仍只使用 `@henjicc/ai-sdk/llm/streaming`，不回退到包根 client。
- 零网络 SSE fixture 输出 `SDK OK`，`networkCalls=0`、`interceptedRequests=1`。
- 正式 `check:bundle` 门禁通过：37 modules / 79,442 bytes / `forbiddenModules=0`。generation、catalog、包根 client、capabilities、Groq、LLM modules 与 BigModel preset/models/pricing 均未进入；仅保留通用端点身份解析所需 profiles。

### form-renderer

- 仍只使用 `@henjicc/ai-sdk/generation`、`@henjicc/ai-sdk/catalog` 与 `@henjicc/ai-sdk/runtime`。
- TypeScript build 和 start 通过，真实输出 5 个差异模型的参数、媒体与 composite 表单契约。
- 关闭 tree-shaking 的依赖图为 159 modules / 637,659 bytes；包含 2 个 generation 模块和 115 个 catalog 模块，LLM / BigModel / provider setup 无关模块均为 0。

## 验证

本任务按 L2 执行：升级精确发布依赖并重新验证三个独立消费面的远端包、类型构建、零网络运行与按需 bundle 边界；未改 SDK 协议、Electron 或应用层，不扩大到仓库全量测试。

1. 三个目录分别执行带 GitHub Packages 进程级认证的 `npm install --no-package-lock`：通过；均解析到远端 `0.2.5`、元数据与 14.5 发布记录一致、0 漏洞。
2. `minimal-node`：`npm run dry-run` 通过（内含 TypeScript build）。
3. `llm-chat`：`npm run dry-run && npm run check:bundle` 通过（内含 TypeScript build）。
4. `form-renderer`：`npm run build && npm start` 通过；`start` 内部也从头执行 build。
5. `minimal-node` 与 `form-renderer` 的临时 bundle 静态图门禁通过（`treeShaking=false`），确认不靠摇树隐藏错误导入。
6. 三个示例 manifest/README 无 `0.2.4` 残留，三个示例根级无 `package-lock.json` / `npm-shrinkwrap.json`，`git diff --check` 通过。

安装过程中保留了两次非代码失败的事实：首次未指定私有 registry 时请求 npm 公共仓库得到 E404；随后仅设 `NODE_AUTH_TOKEN` 但没有 registry auth 坐标时得到 GitHub Packages E401。最终通过 npm 进程参数显式设置 scope registry 与 auth 坐标后完整重跑成功；凭据未写入仓库、manifest、lock 或交接。

## 边界

- 未发起真实或付费模型请求。`minimal-node` 和 `llm-chat` 的 `npm start` 是付费 live 入口，本任务不执行；`form-renderer` 的零网络 `npm start` 已执行。
- 未修改、安装或提交 `say-it` 和 `henji-ai-ps`；`packages/ai-sdk/docs/consumers.md` 仅修改三条示例表格行，顶部发布版本、外部消费者行和示例段导语保留给 14.7 统一收口。
- 未修改 SDK 源码、发布包、Henji Electron 主进程或渲染层，因此本任务不需要 Electron 重启来加载代码。
