# 8.6 双仓最终审计交接

## 最终判定

SDK 与两个宿主的代码、发布物和自动化门禁已经完成；唯一未取得通过证据的是 Photoshop UXP 真机面板。
该项状态保持 `waiting_user`，不能把 Node/fixture 结果冒充真实 UXP 验收，也不再自动反复操作 UDT。

| 验收项 | 状态 | 证据 |
|---|---|---|
| 图片生成/编辑，视频生成、文生/图生/参考生/编辑，音频生成 | 完成 | `ModelCapabilityProfile.operations` 与组合查询精确测试 |
| LLM 按 text/image/video/audio 输入筛选 | 完成 | 真实 LLM catalog 派生画像；单项与 `allOf` 组合测试 |
| 供应商与能力组合过滤 | 完成 | `providerIds`、output、operation、input、feature、tag 的 AND/OR 查询 |
| 按需单模型、供应商包和工具集合 | 完成 | 99 个单模型 pack、8 个 provider pack；bare/单模型/供应商/工具 bundle 门禁 |
| ASR/OCR 等新类型扩展 | 完成（扩展协议） | 开放 `CapabilityModule` 注册、发现、执行、取消、注销；当前是 fixture，不宣称已有真实供应商模型 |
| Fal 三个消除工具 | 完成 | 默认 99 之外 optional 3；单模型与 `fal-image-edit-tools`；上传、轮询、失败与取消 fixture |
| Henji-AI 本体显式选择 | 完成 | 8 个 provider pack、99 generation + 14 LLM；optional erase 默认 0 |
| Photoshop 精选模型迁移 | 完成 | 36 generation + 3 erase 全部 SDK；39 个显式 pack、7 provider、legacy 执行 0 |
| 旧模型与旧执行层清理 | 完成 | GPT Image 1.5、Seedream 4.0/4.5 等 8 个退役 ID 仅留持久化迁移；provider 执行目录与 PPIO 清空 |
| UXP LLM 窄入口 | 完成 | `@henjicc/ai-sdk/llm/streaming`，IIFE/ESM 无 Zod、JIT、Node 填充或 global fetch |
| 上传与 Abort | 自动化完成 | KIE/Fal MediaReader→上传→create/poll/result、LLM SSE 与生成 Abort 均有离线 fixture；真实 UXP 仍待下项 |
| Photoshop UXP 真机面板 | 等待用户 | 独立 smoke 已构建且生产隔离；尚无面板 `passed` JSON 或 `.uxp-smoke/result.json` |

## 发布与提交链

- SDK 当前版本 `@henjicc/ai-sdk@0.1.7`；GitHub Packages version ID `1179686525`，integrity
  `sha512-VGFbXa7swy0W6Xx6HWz5yk0mD7cGSy4ZBCNdZHyBkJy6mh0JMQaknca5NBwjagzcKYcVmoCLgqszoGOgQWaXUg==`，
  shasum `66e3d13abc5e3a36b80eb2bd9149f18e5e552f01`。
- Photoshop lockfile 指向 GitHub Packages 的 0.1.7 tarball，并锁定同一 integrity；当前安装物版本也是 0.1.7。
- Henji-AI 主链：`17f07f95`（按需/开放扩展）→ `d164a38d`/`46b2224d`（Fal optional）→ `5b7c72b6`
  （应用显式选择）→ `3edf8d0c`/`cb11592b`（0.1.6）→ `e65d3d98`（0.1.7 UXP LLM）。
- Photoshop 主链：`9ac6d44` → `b150248` → `adc4a65` → `86ff677` → `dcd2be2` → `32c0b5f`。
- 当前环境没有 GitHub Packages 读取 token，因此额外的 `npm view` 返回 401；这不否定既有发布证据，审计没有重发包。

## 本次最小复验

Henji-AI：

- 能力发现、开放模块、模块化 generation、应用选择：4 文件 / 29 项通过。
- `check:sdk` 通过：326 个 SDK 源文件可移植；默认 generation 99 / network 0 / risk 0；bare、单 KIE、KIE pack、
  单 Fal erase、Fal 工具集合、默认 generation、LLM-only 静态边界均通过。
- UXP LLM streaming：IIFE 32,658 bytes、ESM 32,194 bytes，32 modules，生命周期 network 0，SSE 与 Abort fixture 通过。

Photoshop：

- application profile、架构门禁、Fal erase、UXP fixture：4 文件 / 19 项通过。
- 生产与 smoke TypeScript 均通过。
- 按项目锁定的 Node 22.23.2 重建并复验：generation IIFE 161,797 bytes，39 models/packs、7 providers、
  network 0、risk 0；LLM IIFE 30,796 bytes、risk 0；生产隔离、loopback-only manifest 均通过。
- 当前 shell 默认 Node 24 首次被 `check:uxp-sdk` 的 Node 22 门禁正确拒绝；切换到一次性 Node 22 后通过，属于环境版本保护而非产品失败。

## 安全、产物与工作区归属

- 两个目标提交的新增/修改文件未命中 GitHub token、OpenAI key、AWS key或私钥特征；Photoshop `.npmrc` 只提交
  `${GITHUB_PACKAGES_TOKEN}` 环境占位，不含明文 token。
- Photoshop `.uxp-smoke/`、`dist/`、`node_modules/` 均被忽略；smoke 结果、UDT 缓存和 Photoshop 日志未提交。
- Photoshop 跟踪工作区干净。Henji-AI 的 `package*.json`、`scripts/lib/uiInspectionScenes.cjs`、
  `src/core/imageEdit/**`、`src/features/imageEdit/**`、`src/core/theme/icons.ts`、`.tmp/` 与 VGPU Glow 文件属于同时进行的
  图片编辑任务；本审计未修改、暂存、清理或回退它们。
- 原 SDK 抽离总览的 23/23 早已完成；8.1～8.6 是用户追加的分发与 UXP 适配链，由各交接承载，现有总览没有遗漏的
  8.x checkbox，因此不修改原任务清单。

## 唯一剩余人工步骤

在 fixture server 保持运行、`/Users/henji/Documents/VibeCode/henji-ai-ps/.uxp-smoke/dist/manifest.json` 已准备好时：

1. UXP Developer Tool 点击 **Add Plugin**，选择上述 `manifest.json`，然后点击 **Load**。
2. Photoshop 打开 **插件 → 痕迹AI SDK Smoke**，等待约 10 秒；复制面板 JSON，或确认
   `/Users/henji/Documents/VibeCode/henji-ai-ps/.uxp-smoke/result.json` 已生成。

只有结果中全部检查为 `passed`，才能把 `waiting_user` 改为真机完成；若 `Response.body` / `ReadableStream` 不兼容，必须保留
`llmCompatibility` 的真实错误，不能用 polyfill 掩盖。
