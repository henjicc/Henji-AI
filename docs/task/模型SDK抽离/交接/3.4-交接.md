# 3.4 渲染层回接 SDK 目录——交接

## 完成范围

- 以 `catalog + modelPresentations + composeModelDefinition + defineModel` 为唯一应用侧回接链，验证 99 个静态模型全部合成、注册成功。
- 为 `ModelRegistry` 的 10 类公开查询补充真实 99 模型回归，覆盖递归 panel 参数名称、选择项 label、默认值、供应商/类型/标签筛选、动态计价、模型信息与统计。
- 从迁移前 Git `HEAD` 与迁移后合成结果生成规范化结构摘要，固化 10 个代表模型的 SHA-256 基线；函数统一记为 `[Function]`，对象键排序、数组保序，避免把运行环境差异误当结构差异。
- 补充 ModelScope 自建模型单实例链路测试，覆盖应用服务添加、SDK registry 读取、应用面板显示、动态 `inputLimits` 与真实运行时 builder 请求体。
- 修复 3 个 3.2 后仍读取已删除 `src/models/**/*.model.ts` 的测试：模型描述完整性、画布比例/分辨率、KIE Z-Image 生成准备。
- 未改界面结构、样式、按钮层级或产品行为；未把展示字段移入 SDK 运行时定义。

## 8 项界面回归

| 项目 | 自动化/结构证据 | 结论 |
|---|---|---|
| 模型选择器 | `loadAllModels()` 99/99；`getModelsByProvider/Type/Tag`；模型描述与 i18n 全量检查；Electron 模型面板与 3 个合并/渠道场景 | 通过；两种窗口尺寸已目视 |
| 参数面板 | 99 模型递归 `ParamDef.name` 与 dropdown/radio `label`；`ParameterPanel` 8 项；Electron Midjourney 设置/参考图 | 通过；标签、开关、分组、参考图入口与价格显示均正常 |
| 参数联动 | 10 模型摘要含 linkages；Grsai、APIMart Midjourney、KIE Hailuo 显式非空检查；catalog/presentation 回归 | 通过 |
| 价格显示 | `ModelRegistry.calculatePrice` 与模型原 calculator 对照；生成准备与应用服务回归；Electron 生成页/Midjourney 截图显示 `¥0.30` | 通过 |
| 进度估算 | 10 模型完整结构摘要包含 `meta.polling/progress/progressLearning`；99 模型注册无失败 | 通过 |
| 画布节点 | `inputLimits`、媒体连接规划、素材组图结构与模型选择器单测；Electron Midjourney 节点/设置；canvas visual 强制 24/24 注册节点 | 通过；图片输入端口、比例端口与参数面板已目视 |
| 设置页模型管理 | `modelDefaultsManager`、ModelPicker 选项/宽度/列表回归；Electron 显示管理/别名 | 通过；8 供应商数量与统一别名列表正常 |
| 智能助手 | L-A 静态不变量 152 项、L-B harness 35 项、生成能力 handler 6 项 | 通过 |

> 正式 UI 场景不会枚举 99 个模型逐项打开。99 项的完整性由 loader、registry、ID parity、i18n 与 catalog 测试覆盖；Electron 自动化只提供真实宿主中的代表性界面回归，不把代表场景伪写成 99 项人工验收。

## 10 个模型结构比对

一次性探针分别在迁移前 `HEAD` 临时 worktree 与当前 `catalog + presentation` 合成链上运行；两边使用同一规范化规则，以下摘要全部精确一致。探针完成后已删除，长期基线固化在 `src/core/loaders/modelLoader.test.ts`。

| 模型 | 供应商 | SHA-256 | params / linkages / presentation groups |
|---|---|---|---:|
| `volcengine-seedream-5.0-lite` | Volcengine | `5b060b0992c57ff55750651ec2c8cc138dd952f2c466ab291a489e765d73501f` | 3 / 0 / 0 |
| `bailian-qwen-image-3.0` | Bailian | `55fa864900323f9f66ee4fba37e60fe3e364af06c8192c9d0d3e4140ac029ccf` | 4 / 0 / 0 |
| `grsai-nano-banana-pro` | Grsai | `e52830a6cb080856d94df10c4a533a0af900aa7a18b625d68d9a16080fbfecf8` | 3 / 2 / 0 |
| `ppio-minimax-speech` | PPIO | `d18e7aa8953803b053a28e11c25964647267271efc54aeb6231e157da9261823` | 6 / 0 / 0 |
| `modelscope-custom` | ModelScope | `049cf4b1a0ca9645dba4ec864a70987e3d752d36b37dbd5754bf2262539a3a6f` | 5 / 0 / 0 |
| `apimart-midjourney` | APIMart | `d9bf786e99f81885a6d8a3a108368cf2a62578a35a4fa4b7e24f69ae70a7c422` | 23 / 11 / 1 |
| `apimart-midjourney-video` | APIMart | `c93fe905c5c068a4861127084cd1cdaa92bdc856cb7cca09377b04bf96f87370` | 6 / 2 / 1 |
| `kie-hailuo-02` | KIE | `5c5094dad33b8e0f7f8f2ae2a5d83fe0670181cfa581b8412e3df4a0c397c38f` | 3 / 2 / 0 |
| `fal-ai-bytedance-seedream-v4` | Fal | `e6b4de37bf188bb20170d6623d5f559fa694a3c535210aaa27f990d38ec5d224` | 3 / 0 / 0 |
| `fal-ai-veo-3.1` | Fal | `5b0a294db374a4bda194baf589cb7b30e7c4bfd80b8ddd406d38c37111f03222` | 8 / 2 / 0 |

其中 3 个样本显式校验 `linkages`，2 个样本显式校验 `ParamPresentationGroup.sections`，满足任务要求的最少 2+2 覆盖。

## 验证命令与结果

### 已通过

- `node scripts/check-model-id-parity.cjs`：99 项通过；`meta.id`、`canonicalModelId`、`aliases`、`params[].id` 未变化。
- `npm run check:model-i18n`：通过，无缺失公共/模型专属 key。
- `npm run lint`：通过。
- `npx vitest run src/core/loaders/modelLoader.test.ts src/core/modelCatalog/generationModelDescriptions.test.ts src/core/params/canvas-ratio-resolution.test.ts src/services/modelscopeCustomModels/ModelscopeCustomModelService.test.ts src/features/generation/application/generationPreparationService.test.ts src/features/canvas/application/mediaConnectionPlanner.test.ts src/features/generation/application/generationApplicationService.test.ts`：7 文件、24 项通过。
- `npx vitest run src/components/MediaGenerator/components/ParameterPanel.test.ts src/features/canvas/application/assetGroupGraph.test.ts src/features/canvas/params/useModelPickerOptions.test.tsx src/features/canvas/params/ModelPickerWidth.test.tsx src/features/canvas/params/ModelPickerList.test.tsx src/features/settings/modelDefaultsManager.test.ts src/features/assistant/applicationCapabilities/registerGenerationCapabilityHandlers.test.ts`：7 文件、33 项通过。
- `npx vitest run packages/ai-sdk/tests/catalog src/models/presentation src/core/params/canvas-ratio-resolution.test.ts src/core/params/canvas-ratio-resolution-requests.test.ts`：15 文件、258 项通过；两份根测试已迁出 `src/models/`，请求测试直接消费 SDK `catalogIndex`。
- `npm run check:application-control-invariants`：27 文件、152 项通过。
- `npm run test:assistant-harness`：9 文件、35 项通过。
- `npx tsc -p tsconfig.json --noEmit`：SDK dist 重建后与测试迁移后各复跑一次，均通过。
- `npm run electron:build`：由并行 3.3 在释放验收窗口前完成，完整构建通过；本任务直接使用该最新稳定产物执行真实 Electron 验收，未在其后修改产品代码。
- `npm run electron:smoke`：由 3.3 对同一构建产物执行并通过。
- `npm run test:reality -- --suite ui --out .ui-tour/sdk-3-4-2026-08-27`：72 场景（36 场景 × 1440×900/960×640）、0 失败、0 运行时错误，临时隔离数据。
- `npm run test:reality -- --suite ui-audit --out .ui-audit/sdk-3-4-2026-08-27`：72 场景、11 条规则、0 命中、0 场景失败。
- `VISUAL_OUT=.canvas-visual/sdk-3-4-2026-08-27 npm run check:canvas-visual`：`ok: true`，默认模型选择器夹具通过；repeat 为 0 像素差，滚动条上下间距差 0.46px。
- `VISUAL_OUT=.canvas-visual/sdk-3-4-all-types-2026-08-27 VISUAL_FILL_ALL_TYPES=1 VISUAL_REQUIRE_ALL_TYPES=1 npm run check:canvas-visual`：`ok: true`，注册节点 24/24、missing 0、repeat 为 0 像素差。
- `find src/models -mindepth 1 -maxdepth 1 -print`：只剩 `src/models/presentation`；`rg "import.meta.glob|src/models/.+\\.model\\.ts" src/models src/core/loaders` 无结果。

## 截图与证据

- UI 总索引：`/Users/henji/Documents/VibeCode/Henji-AI/.ui-tour/sdk-3-4-2026-08-27/index.md`；结构化证据：同目录 `evidence.json`（72 场景、0 运行时错误）。
- 模型选择器：`1440x900-generation-model-panel.png`、`960x640-generation-model-panel.png`。Agent 已打开目视：供应商/类型/功能筛选、模型名称/供应商/媒体类型、收藏按钮均存在；宽窗口 5 列、紧凑窗口 4 列，滚动区域完整，无横向溢出或硬裁切。
- 参数面板：`1440x900-generation-midjourney-settings.png`、`960x640-generation-midjourney-reference.png`。Agent 已打开目视：模型版本、风格分组、开关联动、参考控制与高级参数均有正确标签和控件；紧凑窗口使用可滚动面板，未与底部生成器重叠，价格显示为 `¥0.30`。
- 设置模型管理：`1440x900-settings-models.png`、`960x640-settings-models-alias.png`。Agent 已打开目视：8 供应商筛选、各供应商模型计数、显示/隐藏操作及跨供应商统一别名列表均排版正常，无缺项或截断。
- 画布：`1440x900-canvas-midjourney-node.png`、`960x640-canvas-midjourney-settings.png`。Agent 已打开目视：Midjourney 图片媒体端口、比例连接端口、价格、标准参数行与 MJ 特殊面板均存在；面板滚动和紧凑尺寸无异常覆盖。
- 画布全类型视觉：`/Users/henji/Documents/VibeCode/Henji-AI/.canvas-visual/sdk-3-4-all-types-2026-08-27/canvas-visual-1787816669963/off.png` 与同目录 `result.json`。Agent 已打开截图目视：可见的图像/视频/音频模型选择器排版清晰，minimap 显示完整全类型夹具；结构化结果确认 24/24 注册类型在 DOM 中、0 缺失，节点框/minimap/连线路径重复采集完全一致。
- UI audit 只产出结构化报告、不产截图：`/Users/henji/Documents/VibeCode/Henji-AI/.ui-audit/sdk-3-4-2026-08-27/audit.json`。截图目视使用同一批正式 Electron UI tour 产物完成，没有用浏览器、Chrome、ego-browser 或裸 Vite 代替。

## 未做、失败项与遗留风险

- 未执行真实付费生成、真实网络上传或写入用户资料；ModelScope 的“生成”验证止于 SDK 真实 builder 请求体，不能称为服务端端到端成功。
- 正式 Electron UI 不具备 99 个模型逐项展开的现成场景；全量字段完整性由代码级检查覆盖，UI 仅做代表模型/界面验收。
- 10 模型摘要把函数标准化为 `[Function]`，证明字段结构、配置值和函数存在位置一致，不证明函数源码文本逐字相同；请求 builder 行为另由 catalog/provider fixture 回归覆盖。
- 本任务只验证既有助手模型访问链路，没有改变助手能力“通/不通”、没有新增欠账，也没有推翻已完成结论，因此未更新 `assistant-status.md`。
- 工作区包含多任务与用户既有未提交改动；本任务未 reset、checkout、清理或提交。

## 5.3 输入

- 外部消费方应从 SDK 根入口读取纯运行时 `catalog`；痕迹AI 展示补丁、I18n 与 `composeModelDefinition` 属于应用层，不能作为 SDK 公共目录契约泄漏出去。
- 如 5.3 需要提供可筛选目录接口，应复用 catalog/meta 的 provider、type、tags、canonical ID，不依赖 `ModelRegistry` 的界面副作用。
- ModelScope 自建模型继续只使用 SDK 导出的 `replace/register/getModelscopeCustomModel(s)` 单实例注册表；持久化 CRUD 仍在宿主。

## 6.4 输入

- 保留 `modelLoader.test.ts` 的 99 注册、10 查询与 10 模型历史摘要，作为最终旧实现清理后的应用回接门禁。
- 复跑 258 项 catalog/presentation/canvas、i18n、ID parity、L-A/L-B 与正式 Electron UI/audit/canvas visual。
- 明确 99 项逐个 UI 展开和真实付费生成仍不是自动化覆盖；不得用 fixture、结构测试或代表性截图冒充。
- 扫描并删除任何重新出现的 `src/models/**/*.model.ts` 或模型目录 glob。

## 工作区与开发环境状态

- Git：未提交；保留多任务混合现场。
- 一次性结构探针与临时迁移前 worktree：均已删除。
- 开发环境：🔄 开发环境已重启。当前仓库唯一的 `npm run electron:dev` PID 89686、进程组 89686；`cwd` 已用 `lsof` 核实为 `/Users/henji/Documents/VibeCode/Henji-AI`，main/preload 构建成功、renderer dev server 为 `http://localhost:5173/`、Electron 主进程 PID 89978 与 renderer 子进程均存在。
