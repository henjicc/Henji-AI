# 14.7 供应商密钥设置与 SDK 同步收口交接

更新时间：2026-08-28

## 结论

- `@henjicc/ai-sdk` 当前发布版本与主仓 workspace 版本均为 `0.2.5`。
- `/Users/henji/Documents/VibeCode` 下实际消费面仍为 3 个独立宿主与 3 个 SDK 仓内示例，没有发现第 7 个实际消费者。
- 两个外部消费者均精确锁定 `0.2.5`，且 lockfile 使用与 14.5 发布记录一致的远端 tarball 与 integrity；没有 `workspace:`、`file:`、`link:` 或浮动版本。
- 14.2 至 14.6 的实现、发布、消费端升级提交均存在于正确仓库。14.7 只回写消费清单与审计证据，没有改动运行时代码。
- 14.3 的两张正式 Electron 截图已再次目视核验，供应商表单、密钥输入与窄窗口布局符合交接边界。

## 提交链核对

| 阶段 | 仓库 | 提交 | 核对结果 |
|---|---|---|---|
| 14.2 供应商凭据原子事务 | `Henji-AI` | `918e582e` | 存在 |
| 14.3 供应商管理 UI | `Henji-AI` | `24704703` | 存在 |
| 14.4 设置收口审查 | `Henji-AI` | `6327af4a` | 存在 |
| 14.5 SDK 0.2.5 发布准备 | `Henji-AI` | `b34e62d5` | 存在 |
| 14.5 SDK 0.2.5 发布记录 | `Henji-AI` | `1140f48d` | 存在 |
| 14.6a Say-It 升级 | `say-it` | `77643d0` | 存在 |
| 14.6b Photoshop UXP 升级 | `henji-ai-ps` | `4f61d9a` | 存在 |
| 14.6c 仓内示例升级 | `Henji-AI` | `4b3f8acc` | 存在 |

## 消费矩阵

扫描范围为 `/Users/henji/Documents/VibeCode`，排除 `.git`、`node_modules`、`dist`、`out`、`target`、`build`、`coverage` 与缓存目录；同时核对 manifest、lockfile、源码 import 和构建脚本。README 与历史交接中的文字引用不计为运行消费者。

| 消费面 | 版本与来源 | 入口 / 边界 | 证据 |
|---|---|---|---|
| `Henji-AI` | workspace `0.2.5`；根 lock 指向 `packages/ai-sdk` | SDK 生产者与 Electron 首发验证宿主 | `b34e62d5`、`1140f48d` |
| `henji-ai-ps` | manifest/lock 精确 `0.2.5` | 39 个图片 generation/erase packs；不带 BigModel、LLM modules、provider setup | `4f61d9a` |
| `say-it` | manifest/lock 精确 `0.2.5` | 百炼 ASR/translation、Groq 与插件 LLM modules；不带 BigModel、generation、provider setup | `77643d0` |
| `minimal-node` | manifest 精确 `0.2.5`，无 lock | generation/runtime；不带 LLM/BigModel | `4b3f8acc` |
| `llm-chat` | manifest 精确 `0.2.5`，无 lock | 通用 LLM streaming；不带 generation、BigModel preset/models/pricing、Groq、LLM modules | `4b3f8acc` |
| `form-renderer` | manifest 精确 `0.2.5`，无 lock | generation/catalog/runtime 与五模型表单；不带 LLM/BigModel | `4b3f8acc` |

两个外部 lockfile 的共同远端证据：

- tarball：`https://npm.pkg.github.com/download/@henjicc/ai-sdk/0.2.5/b3b80f250d943ac2b23ac4c95b5837dacc2d93dd`
- integrity：`sha512-6WCgQEWM+EenbBT7njFQEx21jlwrO9KVIdJHHeXula8vImZ3fgcLjnEs+B1W6HaYqv2Cd5DCMk+Ug0wO0eNrLA==`

未发现额外消费者；`ai-roundtable`、`fast-install`、`henji`、`henji-dev`、`henjicc.github.io`、`mySkills`、`test` 均无 manifest、lockfile、源码 import 或构建脚本引用。

## 验证结果

### 精确测试

运行 14.2 至 14.5 相关测试并通过：

- 供应商凭据、IPC、keystore、设置服务与 LLM 配置测试；
- `ApiKeyInput` 与 `LlmProviderDialog` 组件/交互测试；
- SDK BigModel 与 provider preset 测试；
- 画布文本处理回归测试。

结果：`11` 个测试文件、`77` 个测试全部通过。

### 正式构建门禁

`npm run electron:build` 已执行到正式 TypeScript 门禁：

- catalog 导入 `99`、模型 pack `99`、provider pack `8` 生成成功；
- 颜色、surface、图标、模型 i18n、主进程导入边界通过；
- SDK portability、generation IIFE、模块化 bundle 与 UXP LLM streaming 门禁通过；
- 助手能力覆盖 `4/4`，相关 `27` 个文件、`152` 个测试通过；
- SDK `364` 个模块构建成功。

构建随后仍被 14.2/14.3 已记录的附件模态类型错误阻断，未进入 `electron-vite`：

- `electron/main/services/agent-runtime/runner/attachment-context.ts:58`
- `electron/main/services/agent-runtime/runner/attachment-context.ts:90`
- `src/core/assistant/attachments.ts:42`

三处均为 `AgentInputModality` 没有 `file` 成员的并行任务问题；14.7 未越界修改。该阻断不推翻本次文档回写、SDK 门禁和精确测试结果。

## 正式截图复核

- `/Users/henji/Documents/VibeCode/Henji-AI/.ui-tour/14.3-provider-manager/960x640-settings-llm-provider-manager.png`
- `/Users/henji/Documents/VibeCode/Henji-AI/.ui-tour/14.3-provider-manager/1440x900-settings-llm-provider-manager.png`

目视结果：两种尺寸均为扁平的新建自定义供应商表单；字段标签位于控件上方，API Key 眼睛按钮内嵌输入框右侧，没有“当前编辑”头块、重复推荐说明或卡片套卡片。`1440×900` 完整显示密钥管理地址与启用项；`960×640` 使用弹窗内部纵向滚动承载下方字段，底部动作区固定，无横向溢出、文本截断或控件遮挡。

## 仓库状态与边界

- `Henji-AI`：审计开始时干净；14.7 仅产生消费清单与本交接改动。
- `henji-ai-ps`：工作区与暂存区均干净。
- `say-it`：暂存区为空；保留其他任务的 `37` 个已跟踪文件改动与 `1` 个未跟踪 schema 文件，主要分布于 Rust 音频/桌面上下文/窗口/文本对齐、SDK runtime/plugin package 与 floating-orb UI。14.7 没有修改或暂存这些文件。
- 本任务没有真实供应商请求或付费网络。14.5 的远端发布/回装和 14.6 各消费端的离线、fixture、bundle 验证边界沿用对应交接，不把它们写成真实模型成功。
- 本次不改变智能助手能力的“通/不通”状态，不需要更新 `assistant-status.md`。

## 后续维护入口

后续 SDK 发布继续以 `packages/ai-sdk/docs/consumers.md` 为唯一消费清单：先在 `Henji-AI` 完成 SDK 首发验证与发布，再按实际使用入口逐个升级消费者、核对精确版本和 integrity，并将提交、验证结果与未引入能力边界回写清单。
