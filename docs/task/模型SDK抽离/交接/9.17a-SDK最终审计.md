# 9.17a SDK 最终发布审计

> 状态：通过
> 审计基线：`443d9988` (`feat(ai-sdk): 新增外部 LLM 模块边界`)
> 最终消费版本：`@henjicc/ai-sdk@0.2.2`
> 验证级别：L3（最终发布审计，用户明确要求完整门禁）
> 副作用边界：没有重新发布、没有改版本或功能，没有发起真实模型/付费请求

## 最终结论

`@henjicc/ai-sdk@0.2.2` 的本地源码、远端元数据、远端 tarball 和仓库外安装物一致。Node ESM、strict TypeScript、标准 Vite、无 `TextEncoder` / `TextDecoder` / WebSocket 全局的受限宿主均通过。23 个公开入口可解析，99 个 generation、9 个 ASR、3 个翻译、Groq 和外部 LLM module 的数量、唯一性、按需隔离、冲突拒绝与释放语义均有通过证据。

`0.2.0` 仍可被精确安装，但它有已知 Node ESM 目录导入缺陷，不得使用。GitHub Packages 不支持对它写 npm deprecation；当前 `latest` 明确指向 `0.2.2`，CHANGELOG 和 9.11 交接已明确标注不建议/不得使用 `0.2.0`。

## 发布坐标与完整性

- workspace `packages/ai-sdk/package.json`：`0.2.2`。
- 根 `package-lock.json` workspace 版本：`0.2.2`。
- GitHub Packages version ID：`1180522976`。
- dist-tag：`latest -> 0.2.2`。
- shasum：`7b221cfde8c8e278378680eb3154207448e92f43`。
- integrity：`sha512-6VMyZwxz/oVTKmGAlfMQyAjVxLgjJISDdE5df2sQhvFpxAp4cXJvB3kvMWKIjI9HIl+Y7CsOqfpA9lvz2MM6QA==`。
- 远端 tarball：515,316 bytes，解压后 2,042,555 bytes，846 个文件。远端 metadata、`npm pack @henjicc/ai-sdk@0.2.2` 与 9.15a 发布前精确 tarball 的 shasum/integrity 一致。

## 远端全新安装

使用独立临时目录从 GitHub Packages 精确安装 `@henjicc/ai-sdk@0.2.2`，未使用 workspace link：

- Node ESM 实际执行通过：ASR 5+4、Qwen-MT 3、Groq 1、外部 LLM module 1，统一发现 13 个内置 ID 且唯一。
- strict TypeScript 消费方通过。
- 标准 Vite dev 从已安装包的 `dist` 解析全部 23 个 exports。
- 受限 VM 没有 `TextEncoder`、`TextDecoder` 和 WebSocket 全局；ASR、实时 ASR、翻译、Groq 和外部 LLM module 均完成 import/创建/执行/释放 fixture，生命周期网络请求为 0。
- 远端包的外部 LLM module 实际执行成功，对内置 Groq 坐标的插件遮蔽被拒绝，错误同时指出新旧 source namespace。

## 完整门禁与穷举结果

`npm run prepublishOnly -w packages/ai-sdk` 全链通过：

- 可移植性扫描：359 个 SDK 源文件，无 Node/Electron 导入、Node 全局、仓库别名或动态代码生成。
- generation IIFE：99 个模型、157 modules、349,538 bytes，风险计数 0，import/create/catalog/dispose 网络请求 0。
- modular bundle：默认 generation 严格 99；单模型、provider pack、工具 pack、ASR、翻译、Groq、外部 LLM modules 入口互不带入禁止的 generation/catalog/其他能力或具体供应商。
- 按需体积（IIFE / ESM / modules）：非实时 ASR `15,708 / 15,242 / 11`，实时 ASR `16,897 / 16,426 / 10`，翻译 `14,932 / 14,456 / 11`，Groq `366,033 / 365,641 / 118`，外部 LLM modules `15,439 / 14,981 / 12`。
- UXP 窄 LLM streaming：无文本编解码器依赖、无生命周期网络，fixture transport 与 Abort 各调用 1 次。
- SDK 全量：60 个测试文件、601 条测试全部通过。
- 构建：359 个 ESM 模块及对应声明生成。

穷举与资源语义由同一轮全量测试保护：

- 默认 generation catalog 逐项派生与客户端查询都锁定 99，Fal 的 3 个可选消除工具不污染默认目录。
- 百炼非实时 ASR 5 + 实时 ASR 4 = 9；Qwen-MT 3；Groq 默认 1，坐标全部唯一。
- `capability-id-contract.test.ts` 覆盖 module ID、provider/kind/model 坐标、跨类型发现冲突和 `unregisterSource` 只释放所有来源。
- `llm/modules.test.ts` 覆盖外部 LLM ID/坐标/Groq 冲突、Abort/cancel/timeout、`drainSource`、`unregisterSource`、dispose 幂等、流式背压和日志脱敏。
- 实时 capability 的 cancel/dispose 关闭 driver/session，默认 generation 与模块化 generation 的 dispose 都使客户端失效且不遗留注册。

## 安全、代码生成与网络审计

对远端 0.2.2 tarball 解包后独立扫描：

- 真实 token/key/私钥特征：0。
- `.npmrc`、`.env`、log、source map、压缩包/安装包等非预期文件：0。
- 运行时 Node/Electron 导入：0。
- `eval` / `new Function` / dynamic import：0。
- WebSocket 全局参照：0；实时 ASR 只使用宿主 `RuntimeContext.realtime`。
- 组件导入、客户端创建、目录读取和释放的隐式网络请求：0。

扫描如实检出两处**显式兼容 fallback** 会在调用方未注入 transport 且实际执行请求时读取 `globalThis.fetch`：

1. `dist/llm/sdk/provider.js`：`createModelStepLanguageModel` 的可选 transport 兼容；宿主没有 fetch 时在发网前报 `[TRANSPORT_REQUIRED]`。
2. `dist/upload/fal-legacy.js`：只为兼容 0.1.3 两参 `uploadToFal`；新链路使用 `uploadToFalWithTransport`。

这两处不会在 import/create/catalog/dispose 阶段自动发网，也不在 Say-It 的 0.2.2 ASR/translation/Groq/插件 LLM 路径上；Say-It 依然注入 HTTP/实时 transport。它们是已有、受限定、不阻断 0.2.2 消费的兼容边界，不是隐式生命周期网络。后续若发布新的破坏性版本，可以删除两个 fallback，把 transport 统一收紧为必填；本任务不修改已发布功能和版本。

## Say-It 最终锁定

- `/Users/henji/Documents/VibeCode/say-it/package.json` 已精确声明 `"@henjicc/ai-sdk": "0.2.2"`，没有版本范围。
- Say-It `package-lock.json` 已锁定到 `0.2.2` 的 GitHub Packages tarball，resolved URL 中的 shasum 为 `7b221cfde8c8e278378680eb3154207448e92f43`，integrity 与本审计一致。
- Say-It 插件 LLM 统一接入提交已在 `371545e`；SDK 最终结论是继续精确锁定 `0.2.2`，不回退 `0.2.1`，不使用 `0.2.0`，不改成 semver 范围。

## 遗留边界

- 本审计验证的是发布物、fixture、scripted transport 和宿主契约，没有发起百炼、Groq、Fal 或任何 generation 的真实付费请求。
- 两处可选 global fetch fallback 如上所述；按需入口和 Say-It 正式路径均继续使用宿主注入。
- `0.2.0` 由于 GitHub Packages API 限制无法添加 registry deprecation，因此仍可被精确安装；防误用依赖 `latest=0.2.2`、文档警示与消费方精确锁定。
