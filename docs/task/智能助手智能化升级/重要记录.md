# 重要记录 —— 智能助手智能化升级

## 记录 001：本任务采用增量优化而非重写

- 日期：2026-07-24
- 状态：已确定
- 背景：现有智能助手已经具备唯一 Runner、工具网关、utility process、宿主命令面和安全边界。
- 最终决定：只围绕智能性、上下文、记忆、验证和可解释性做增量改造，不替换现有 Agent 主架构。
- 选择原因：降低回归风险，保留第七阶段已经完成的能力。
- 影响任务：全部任务

## 记录 002：第七阶段手动测试清单作为继承基线

- 日期：2026-07-24
- 状态：已确定
- 背景：第七阶段代码已完成，但真实 Provider、Electron 鼠标交互、日志窗口和长稳仍需要手动测试。
- 最终决定：原清单 D:/VibeCode/Henji-AI/docs/task/智能助手/第七阶段-最终手动测试清单.md 不删除、不覆盖；在 1.1 中完整继承，并作为后续回归的基础清单。
- 选择原因：避免新任务开始后丢失已有验收范围。
- 影响任务：1.1、4.2

## 记录 003：唯一 Runner 继续有效

- 日期：2026-07-24
- 状态：已确定
- 背景：Claude Code 的多入口共用执行内核对 Henji-AI 同样适用。
- 最终决定：不引入第二套 SDK 自动循环；所有计划、工具、审批、预算、验证和恢复继续由 AgentRunner 或其明确的运行时模块统一控制。
- 选择原因：避免消息、审批、检查点和停止条件分裂。
- 影响任务：2.1、2.2、2.3、3.1、3.2

## 记录 004：模型通用描述与供应商适配继续分离

- 日期：2026-07-24
- 状态：已确定
- 背景：同一通用模型可能由多个供应商接入，供应商模型文件需要保持独立适配。
- 最终决定：定性描述只维护在 D:/VibeCode/Henji-AI/src/core/modelCatalog/generationModelDescriptions.ts；tags、schema、请求构建、轮询和供应商可用性仍留在各自模型文件和运行时。
- 选择原因：描述只写一次，同时不破坏已有供应商适配。
- 影响任务：2.1、4.2

## 记录 005：用户偏好继续使用自然语言

- 日期：2026-07-24
- 状态：已确定
- 背景：用户希望直接用自然语言表达供应商、模型和工作方式偏好。
- 最终决定：继续使用 user-instructions.md 和已确认记忆，不新增偏好参数面板；模型只能在安全和真实能力约束内遵循这些偏好。
- 选择原因：更符合 Agent 记忆系统，也避免偏好配置碎片化。
- 影响任务：2.1、3.3

## 记录 006：不直接展示原始思维链

- 日期：2026-07-24
- 状态：待确认
- 背景：模型可能提供 reasoningText，但原始推理内容并不等同于可靠执行事实。
- 最终决定：方案优先展示结构化计划、工具原因、证据和下一步；是否在设置中展示受限 reasoning 摘要，待真实模型和产品交互验收后确认。
- 选择原因：让用户知道助手在做什么，同时避免把未经验证的内部推理当成事实。
- 影响任务：4.1、4.2

## 记录 007：复杂多 Agent 延后

- 日期：2026-07-24
- 状态：已确定
- 背景：Claude Code 的 swarm、mailbox 和 teammate 权限桥复杂度较高，当前没有真实瓶颈证据。
- 最终决定：本任务先建设单 Agent 的计划、验证和后台摘要/验证侧任务；不直接建设 swarm/team。
- 选择原因：先解决当前测试中已经观察到的智能性问题。
- 影响任务：2.3、3.1、4.2

## 记录 008：推荐字样只作为兼容候选的软优先级

- 日期：2026-07-24
- 状态：已确定
- 背景：部分通用模型描述由用户明确标注“推荐使用”，需要让智能助手在未指定模型时优先考虑。
- 最终决定：只有在安全、真实能力、参数 schema 和供应商可用性均满足，且用户没有明确指定具体模型时，才优先选择描述中带“推荐使用”字样的候选；用户明确的供应商要求或偏好继续用于限定或排序候选，多个推荐候选再结合任务目标、质量、速度和成本比较。
- 选择原因：落实用户推荐策略，同时避免推荐文案覆盖硬能力约束或用户当前明确要求。
- 影响任务：1.2、2.1、4.2
