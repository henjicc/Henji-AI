# 2.1 图像处理命令主进程优先

## 任务状态

- 状态：已完成
- 所属阶段：第二阶段-媒体与运行时收口
- 前置任务：1.2
- 关联任务：2.3
- 是否可以并行：否

## 任务目标

让 Electron 桌面端图像处理命令优先且可靠地在主进程执行，移除或限制会造成 UI 线程卡顿的静默前端 canvas/data URL fallback。

## 当前情况

- 主进程图像服务位于 `electron/main/services/image/ops.ts`，已覆盖加载、持久化、分割、裁剪、合并分镜、读取/写入分镜元数据、保存到下载目录等能力。
- 渲染层命令桥 `src/commands/image.ts` 接近 1000 行，包含主进程调用和大量浏览器 fallback 双实现。
- 画布图像工具 `src/features/canvas/application/toolProcessor.ts` 和 `src/features/canvas/application/imageData.ts` 依赖这些命令，并在部分场景自行使用 canvas。
- 标注类工具有交互和矢量绘制需求，可暂留渲染层；裁剪、分割、合并、持久化更适合主进程。

## 实施方案

按 1.2 的策略改造 `src/commands/image.ts`：Electron 环境主进程失败时抛出明确错误，不再静默执行前端重活。保留必要的 web-only fallback 时，应通过运行时判断和日志隔离。对画布工具中可直接调用主进程命令的裁剪、拆分逻辑，优先复用已有命令；标注等强交互绘制类逻辑可暂留渲染层，但不得作为主进程裁剪/拆分失败后的默认兜底。

## 执行步骤

1. 读取 `src/commands/image.ts`、`electron/main/services/image/ops.ts`、`electron/main/ipc/image.ts`、`src/platform/contracts/image.ts`，确认主进程能力是否覆盖所有目标函数。
2. 将 `src/commands/image.ts` 中 Electron 路径的静默 `catch` 改为明确错误处理；仅在非桌面运行时保留前端 fallback，并在日志中区分 `desktop` 与 `web-only`。
3. 对 `mergeStoryboardImages`、`cropImageSource`、`prepareNodeImageSource`、`prepareNodeImageBinary`、`saveImageSourceTo*` 分别验证返回值兼容。
4. 检查 `src/features/canvas/application/toolProcessor.ts` 是否仍重复实现主进程已覆盖的裁剪和分割逻辑，必要时改为调用命令桥。
5. 运行类型检查和相关 lint；涉及画布交互的部分写清楚用户手动验证步骤。

## 涉及内容

### 新增文件

- 待确认：如需拆分 `src/commands/image.ts`，可新增 `src/commands/image/*` 子模块，但应先确认不扩大任务范围。

### 修改文件

- `src/commands/image.ts`：限制 Electron 下静默前端 fallback，必要时拆分或提取 helper。
- `src/features/canvas/application/toolProcessor.ts`：减少重复的本地 canvas 裁剪、拆分 fallback。
- `src/features/canvas/application/imageData.ts`：对齐新 fallback 策略。
- `electron/main/services/image/ops.ts`：仅在发现主进程能力缺口时补齐。
- `src/platform/contracts/image.ts`、`electron/main/ipc/image.ts`：仅在需要新增主进程能力时修改。

### 可能废弃

- `src/commands/image.ts` 中仅服务早期迁移的前端重型实现：在 Electron 路径不再需要时废弃或限制为 web-only。

## 验收标准

- [x] Electron 桌面端下图像裁剪、分割、合并、保存不会静默落回浏览器重型实现。
- [x] 主进程调用失败时有明确错误和日志，便于定位。
- [x] 画布分镜拆分和裁剪仍走主进程命令，返回路径或结果类型与原命令保持兼容。
- [x] 通过 `npx tsc -p tsconfig.electron.json --noEmit`。
- [x] 本任务未改动 Electron 主进程，无需运行 Electron 侧 eslint。
- [x] `00-任务总览.md` 和本任务执行记录已同步更新。

## 执行记录

### 2026-07-01

- 完成内容：收紧 `src/commands/image.ts` 桌面端 fallback；Electron 主进程图像命令失败时记录函数名、耗时、输入来源并抛出错误，web-only 路径继续保留浏览器实现。同步收紧画布 `crop`、分镜拆分和节点图预处理调用方，避免它们在桌面端自行 catch 后回到 canvas/data URL。
- 实际改动：修改 `src/commands/image.ts`、`src/features/canvas/application/toolProcessor.ts`、`src/features/canvas/application/imageData.ts`；更新任务总览和本任务记录。
- 验证结果：`npx tsc -p tsconfig.electron.json --noEmit` 通过；`npm run lint` 通过。
- 遗留问题：标注工具仍保留在渲染层，这是交互绘制能力，不作为主进程裁剪/拆分失败后的兜底；缩略图 data URL 治理继续放在 2.3。
- 与原计划的偏差：未拆分 `src/commands/image.ts`，本次只做边界收紧，避免扩大范围。
- 对后续任务的影响：2.3 可基于新的错误传播策略继续治理缩略图路径化；2.2 可独立处理提示词优化附件后端化。
- 用户手动验证步骤：启动 `npm run electron:dev` 后，在真实 Electron 窗口中对画布图片执行裁剪、分镜拆分、合并/保存分镜图；确认结果节点正常生成、图片可打开、失败时出现明确错误而不是长时间卡住。
- 相关提交：`3fe1294` 收紧图像命令桌面端回退策略。
