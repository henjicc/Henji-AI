# 1.2 桌面端回退策略基线

## 任务状态

- 状态：已完成
- 所属阶段：第一阶段-边界盘点与基线确认
- 前置任务：1.1
- 关联任务：2.1、2.3
- 是否可以并行：否

## 任务目标

定义 Electron 桌面端下主进程能力失败后的统一处理策略，避免图像、媒体、文件保存等任务静默回退到浏览器线程执行重活。

## 当前情况

- `src/commands/image.ts` 在 `splitImage`、`splitImageSource`、`mergeStoryboardImages`、`prepareNodeImageSource`、`prepareNodeImageBinary`、`cropImageSource`、`persistImageSource`、`saveImageSourceToDirectory` 等函数中保留前端 fallback。
- `src/features/canvas/application/toolProcessor.ts` 在裁剪、标注、分镜拆分中使用前端 canvas，部分逻辑是显式 fallback。
- `src/features/canvas/application/imageData.ts` 在桌面端优先调用图像命令，但失败后仍会回到 data URL 和 canvas 路径。
- 裸 Vite 调试允许保留 web-only fallback，但 Electron 生产路径不允许静默回退到浏览器重活。

## 实施方案

先设计统一策略，再在后续代码任务中落地。建议区分三种行为：桌面端生产路径直接抛错并记录日志；裸 Vite 或明确 web 模式允许 fallback；测试环境可通过开关或 mock 保持兼容。

## 执行步骤

1. 根据 1.1 的边界清单，列出所有当前 fallback 入口。
2. 为每类入口定义处理方式：禁止 fallback、允许轻量 fallback、允许 web-only fallback。
3. 明确日志要求：Electron 路径命中 fallback 时是否记录 warning、是否带函数名和输入来源类型。
4. 更新 `01-实施方案.md` 的“新旧实现兼容方式”和“风险控制方式”。
5. 将后续需要改代码的位置同步到 2.1 和 2.3 的任务文件。

## 涉及内容

### 新增文件

- 无。

### 修改文件

- `docs/task/桌面端架构收口/01-实施方案.md`：补充 fallback 基线。
- `docs/task/桌面端架构收口/任务/第二阶段-媒体与运行时收口/2.1-图像处理命令主进程优先.md`：同步实际改造范围。
- `docs/task/桌面端架构收口/任务/第二阶段-媒体与运行时收口/2.3-缩略图与媒体预处理收口.md`：同步实际改造范围。
- `docs/task/桌面端架构收口/00-任务总览.md`：更新任务状态和最近记录。

### 可能废弃

- 无代码废弃，本任务只定规则。

## 验收标准

- [x] 每个当前图像和媒体 fallback 入口都有明确处理策略。
- [x] 已确认 Electron 生产路径是否允许浏览器重活 fallback。
- [x] 后续任务的实施范围已按确认策略更新。
- [x] `重要记录.md` 中“桌面端主进程失败不应静默回退到浏览器重活”已更新。
- [x] `00-任务总览.md` 已同步更新。

## 执行记录

### 2026-07-01

- 完成内容：定义桌面端 fallback 基线：Electron 生产路径主进程失败不允许静默回退到浏览器重活；裸 Vite 调试可保留 web-only fallback；测试脚本通过 mock 或显式测试分支兼容。
- 实际改动：更新实施方案、重要记录、2.1/2.3 任务说明和总览；未修改业务代码。
- 验证结果：静态确认 `src/commands/image.ts`、`src/features/canvas/application/toolProcessor.ts`、`src/features/canvas/application/imageData.ts`、缩略图缓存和视频预览中存在需要分级治理的 fallback。
- 遗留问题：2.1 需把 Electron 图像命令 `catch` 从静默 fallback 改为明确错误；2.3 需把缓存缩略图主路径改为文件路径或展示 URL。
- 与原计划的偏差：无。
- 对后续任务的影响：2.1 和 2.3 的验收口径已明确，轻量 UI 预览可保留，重型处理和长期持久化不得依赖浏览器 data URL。
- 相关提交：本次文档提交。
