# 3.2 浏览器本地存储与遗留入口清理

## 任务状态

- 状态：已完成
- 所属阶段：第三阶段-数据与通用能力治理
- 前置任务：1.1
- 关联任务：3.1、2.2
- 是否可以并行：可以与 2.1 并行；若同时修改设置或模型配置，需协调文件冲突。

## 任务目标

对 localStorage、旧 provider 工具、旧上传工具和反向依赖进行分级治理，清理会误导后续开发继续走网页前端路径的入口。

## 当前情况

- API Key 主路径已迁到 safeStorage/keystore：`electron/main/services/keystore.ts`、`src/commands/aiRuntime.ts`、`src/commands/llmRuntime.ts`。
- 旧接口 `src/core/providers/base/utils.ts` 仍有 `getApiKey`、`setApiKey`、`getFalApiKey`，会读写 localStorage。
- `src/utils/falUpload.ts` 仍导入 `@fal-ai/client` 并在渲染层上传，当前搜索未发现主路径引用，但依赖仍存在。
- `src/models/modelscope/modelscope-custom.model.ts` 和相关面板仍从 localStorage 读取自定义模型。
- `src/config/providers.ts`、`src/components/Settings/hooks/useSettings.ts`、`src/utils/updateConfig.ts`、`src/utils/dataPath.ts` 等保存 UI 偏好或设置到 localStorage。
- `src/core/panels/registerDefaultPanels.ts` 从 core 反向 import UI 组件，属于分层治理问题。

## 实施方案

先建立 localStorage 分类清单：轻量 UI 偏好可暂留，业务配置和模型扩展迁移到 SQLite 或 app data JSON，密钥类旧入口删除或改为明确不可用。对未引用的上传/provider 遗留文件和依赖，先确认引用和打包影响，再删除或标记废弃。

## 执行步骤

1. 搜索 localStorage、`@fal-ai/client`、`ali-oss`、`axios`、`getApiKey`、`setApiKey` 的真实引用。
2. 将 localStorage 项分为：可保留 UI 偏好、需迁移业务配置、已迁移兼容数据、应删除敏感入口。
3. 对 `src/core/providers/base/utils.ts` 的 API Key 函数确认是否仍被导出引用；如无主路径依赖，改为废弃或删除。
4. 对 `src/utils/falUpload.ts` 确认是否未引用；如未引用，删除文件并移除无用依赖。
5. 为 ModelScope 自定义模型设计迁移方式：优先复用 SQLite `custom_models` 或现有 custom model service；迁移旧 localStorage 数据。
6. 评估 `src/core/panels/registerDefaultPanels.ts` 迁出 core 的低风险方案，必要时拆成单独后续任务，不在本任务强行扩大。

## 涉及内容

### 新增文件

- 实际处理：ModelScope 自定义模型定义读取内存 registry，不再直接读取 localStorage；旧 localStorage 数据会一次性迁移到 SQLite `custom_models`，并已通过 `customModels` 领域 IPC 下沉到 Electron 主进程。

### 修改文件

- `src/core/providers/base/utils.ts`：处理旧 localStorage API Key 入口。
- `src/utils/falUpload.ts`：未引用时删除；仍需兼容时标记废弃并改为主进程路径。
- `package.json`、`package-lock.json`：仅在确认依赖无用后移除 `@fal-ai/client`、`ali-oss` 或 `axios`。
- `src/models/modelscope/modelscope-custom.model.ts`：移除模型定义中直接读取 localStorage 的逻辑。
- `src/components/MediaGenerator/components/ModelscopeCustomModelManager.tsx`、`src/components/params/panels/ModelscopeCustomModelPanel.tsx`：迁移自定义模型数据来源。
- `src/config/providers.ts`、`src/components/Settings/hooks/useSettings.ts`：按分类结果决定是否保留或迁移。

### 可能废弃

- `src/utils/falUpload.ts`：如果确认无引用且后端上传已覆盖。
- `src/core/providers/base/utils.ts` 中 API Key localStorage 函数：如果确认无主路径依赖。

## 验收标准

- [x] localStorage 使用清单已分类，敏感入口和业务配置有处理结论。
- [x] 未使用的前端上传/provider 遗留入口已删除或明确标记废弃。
- [x] ModelScope 自定义模型不再依赖模型定义直接读 localStorage，或已记录待确认阻塞原因。
- [x] 如删除依赖，`npm install` 后 lockfile 与依赖树一致。
- [x] API Key 主路径仍通过 Electron keystore 工作。
- [x] 通过 `npm run lint`。
- [x] `重要记录.md` 和 `00-任务总览.md` 已同步更新。

## 执行记录

### 2026-07-01

- 完成内容：完成本轮 localStorage 与遗留入口分级治理；敏感 API Key 旧入口不再读写 localStorage；未引用的前端 Fal 上传入口和相关依赖已删除；ModelScope 自定义模型定义不再直接读取 localStorage。
- localStorage 分类结论：轻量 UI 偏好暂留，包括隐藏供应商/模型、语言、折叠、快速下载、并发数、价格展示、测试模式和提示词优化按钮行为；业务配置中 ModelScope 自定义模型旧 localStorage 仅作为一次性迁移来源，迁移后主存储走 SQLite `custom_models`；敏感密钥入口统一走 Electron keystore，旧 `getApiKey/setApiKey/getFalApiKey` 改为明确抛错。
- 实际改动：删除 `src/utils/falUpload.ts`、`src/types/ali-oss.d.ts`；移除 `@fal-ai/client`、`ali-oss`、`axios` 直接依赖并更新 lockfile；新增 `src/models/modelscope/customModelRegistry.ts`；调整 ModelScope 自定义模型面板/管理器同步 registry；封死 `src/core/providers/base/utils.ts` 中的 localStorage API Key helper。
- 验证结果：`rg` 确认 `@fal-ai/client`、`ali-oss`、`axios`、`falUpload`、`uploadToFalCDN`、`uploadMultipleToFalCDN` 无命中；`npx tsc -p tsconfig.json --noEmit`、`npx tsc -p tsconfig.electron.json --noEmit`、`npm run lint`、`npx eslint electron --ext ts --report-unused-disable-directives --max-warnings 0` 通过。
- 遗留问题：`core/panels/registerDefaultPanels.ts` 的反向依赖已治理，默认面板注册器已迁到 UI 参数面板目录；ModelScope 自定义模型持久化已走 SQLite `custom_models` 和 Electron 领域 IPC。
- 与原计划的偏差：未一次性迁移所有 localStorage 项，按已确认分级策略保留轻量 UI 偏好。
- 对后续任务的影响：桌面端架构收口计划的 7 个任务均已完成；后续可按新任务继续扩展 history、presets、settings 等数据领域 IPC。
- 相关提交：`f8e1414` 完成桌面端架构收口。
