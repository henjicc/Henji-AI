# 3.1 数据服务领域接口收口

## 任务状态

- 状态：待开始
- 所属阶段：第三阶段-数据与通用能力治理
- 前置任务：1.1
- 关联任务：3.2
- 是否可以并行：可以与 2.1、2.2 并行，但不建议与其他数据存储任务并行。

## 任务目标

减少渲染层直接持有 SQL 和数据库 schema 的范围，逐步将历史、预设、设置、自定义模型、画布项目、故事板项目等数据操作收口为 Electron 主进程领域 IPC。

## 当前情况

- `electron/main/services/db.ts` 已统一持有 SQLite 连接和基础 schema。
- `src/platform/contracts/db.ts` 暴露通用 `execute/select`。
- `src/services/database/DatabaseService.ts` 在渲染层创建 history、presets、settings、custom_models 表并拼 SQL。
- `src/services/canvasProjects/CanvasProjectService.ts` 在渲染层创建和操作 canvas_projects 表。
- `src/commands/projectState.ts` 使用 `DbPlatform` 操作 storyboard_projects。
- 这种方式迁移成本低，但长期会让渲染层像后端，且事务、迁移和数据兼容散落。

## 实施方案

先新增领域级 IPC，不立即删除 raw SQL。优先选择变更面较小的一组数据作为试点，例如 canvas_projects 或 settings。确认模式后再迁移 history/presets/custom_models。迁移完成前保留 `DbPlatform`，但新增业务不再直接使用 raw SQL。

## 执行步骤

1. 选择第一批试点数据域：建议从 `canvas_projects` 或 `storyboard_projects` 开始，因为接口集中、范围清晰。
2. 在 `electron/main/services/` 中新增或扩展对应领域服务，封装 SQL 和 row mapping。
3. 在 `electron/main/ipc/` 中新增领域 IPC handler，并在 preload/platform/commands 中提供稳定前端命令。
4. 将试点渲染层服务改为调用领域命令，不再直接拼 SQL。
5. 保留旧 raw SQL 通道并增加迁移备注，避免一次性影响 history/presets。
6. 验证数据读取、创建、更新、删除和旧数据兼容。

## 涉及内容

### 新增文件

- 待确认：可能新增 `electron/main/services/canvas-projects.ts`、`electron/main/ipc/canvas-projects.ts`、`src/commands/canvasProjects.ts` 或同类领域命令文件。

### 修改文件

- `electron/main/services/db.ts`：保留基础连接和 schema，必要时迁移 schema 初始化职责。
- `src/services/canvasProjects/CanvasProjectService.ts`：试点迁移后改为调用领域命令。
- `src/commands/projectState.ts`：如选择 storyboards 作为试点，则改为调用领域命令。
- `src/platform/contracts/db.ts`、`electron/main/ipc/db.ts`：只在需要标记废弃或限制使用时修改。

### 可能废弃

- 渲染层服务中的 `CREATE TABLE` 和 raw SQL 拼接：对应领域迁移完成后废弃。

## 验收标准

- [ ] 至少一个数据域完成领域 IPC 试点，渲染层不再直接拼 SQL。
- [ ] 旧数据可正常读取，新增数据写入同一 SQLite 数据库。
- [ ] `DbPlatform` 的后续保留范围和废弃策略已记录。
- [ ] 通过 `npx tsc -p tsconfig.electron.json --noEmit`。
- [ ] 通过与修改范围匹配的前端 lint。
- [ ] `重要记录.md` 中数据库 raw SQL 决策已更新。
- [ ] `00-任务总览.md` 和本任务执行记录已同步更新。

## 执行记录

### YYYY-MM-DD

- 完成内容：
- 实际改动：
- 验证结果：
- 遗留问题：
- 与原计划的偏差：
- 对后续任务的影响：
- 相关提交：

