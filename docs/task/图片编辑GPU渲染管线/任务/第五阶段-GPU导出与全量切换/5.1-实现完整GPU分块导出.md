# 5.1 实现完整 GPU 分块导出

## 任务状态
- 状态：已完成
- 所属阶段：第五阶段-GPU导出与全量切换
- 前置任务：3.2、4.1
- 关联任务：5.2
- 是否可以并行：否

## 任务目标
按瓦片和 halo 在 GPU 执行完整 RenderGraph，只在导出阶段回读，并继续流入现有主进程编码器覆盖全部格式。

## 当前情况
- `src/features/imageEdit/v3/export/` 与主进程输出 sink 已支持 JPEG、WebP、PNG、TIFF、AVIF、BigTIFF 的分块编码。
- 当前导出像素由 CPU 生成；GPU scene 尚无 export tile 协议实现。

## 实施方案
复用现有导出规划、halo 和编码服务，GPU Worker 仅生产有界 output tile；按顺序回读并维持取消、背压、元数据和临时文件清理语义。

## 执行步骤
1. 实现 export 请求、瓦片调度、halo 和过期任务取消。
2. 复用 RenderGraph 渲染每个输出瓦片并做唯一允许的 readback。
3. 对接现有输出 sink，不改格式和编码职责。
4. 验证边界接缝、HDR位深、取消和失败清理。
5. 与 CPU 整图/分块 golden 对比全部格式。

## 涉及内容
### 修改文件
- GPU Worker export、现有导出调度和精确格式测试。

## 验收标准
- [x] 全部现有格式可由GPU瓦片输入编码。
- [x] tile边界无可测接缝且颜色元数据保持。
- [x] 预览热路径仍无readback，导出内存有界。
- [x] 取消/失败不残留临时文件或GPU资源。
- [x] 5.1 交接已更新；总览由主任务集成时统一更新。

## 执行记录
### 2026-09-05
- 完成内容：在唯一 GPU Scene Worker/device/vGPU Context 内实现 export job、tile/halo、背压 ACK、取消、过期代次防护、有界多尺度效果与唯一导出 readback，并接回现有主进程编码/sink 合同。
- 实际改动：新增 GPU export plan/session/runtime/residual；预览与导出共享 256 MiB 总账；四个生产物化入口优先复用活动 GPU Scene；新增真实 Electron 分块导出巡检场景。
- 验证结果：24 个精确 Vitest 文件 187/187，其中包含真实 WebGPU 效果、HDR/alpha 与全格式真实编解码；L2 精确阶段 61/61，显式 ESLint/tsc/脚本检查通过；1440×900、960×640 Reality 通过并目视截图。
- 遗留问题：`vitest related` 对导入 `?raw` WGSL 的源文件仍报 Rollup `PARSE_ERROR pos:7`；同一最小命令在干净基线 `main@8f600cbe` 同样失败，非本分支回归。
- 与原计划的偏差：大 support 不分配整图 target，改为最大 2048 的全局低频分析、256 halo 局部块与全局坐标固定权重的 3×3 overlap-add，以消除输出 tile 局部相位。
- 对后续任务的影响：5.2 只需做全量切换/L3 总验收，不得新建设备、编码器或效果语义；需处理或精确裁剪现有 `vitest related` WGSL 门禁。
- 相关提交：见本任务的 `feat(image-edit): 实现完整GPU分块导出` 提交。
