# 4.2 实现 GPU 直接呈现与设备恢复

## 任务状态
- 状态：已完成
- 所属阶段：第四阶段-大图色彩与直接呈现
- 前置任务：2.2、4.1
- 关联任务：5.2
- 是否可以并行：可与3.2并行

## 任务目标
使用 OffscreenCanvas + vGPU Surface 直接呈现，实时预览零 GPU 回读，并在初始化失败或 device lost 时无黑帧切换稳定 Canvas2D fallback。

## 当前情况
- 现有最终展示使用 ImageBitmap、Canvas2D atlas 或 DOM 栈；GPU 基础路径仍要把帧交回主线程。
- session 已有最后稳定输出和现有 CPU 管线，可作为双表面交接基础。

## 实施方案
将可见 canvas 控制转交 GPU Worker 的 OffscreenCanvas；保留隐藏 Canvas2D 稳定表面，只有新后端首帧 ready 后才原子切换。

## 执行步骤
1. 建立 OffscreenCanvas/Surface 初始化、resize、DPR 和 camera 更新。
2. 实现直接呈现和 ImageBitmap 中间路径降级。
3. 监听 device lost，销毁关联资源并启动 CPU fallback。
4. 恢复后只按最新权威 snapshot 重建一次。
5. 在双尺寸 Electron Reality 注入失败与恢复场景。

## 涉及内容
### 修改文件
- 预览表面组件、GPU Worker/session、设备恢复与 Reality 场景。

## 验收标准
- [x] 正常预览每帧零 GPU readback。
- [x] resize/DPR/缩放不产生旧帧覆盖或画面跳变。
- [x] 初始化失败和device lost无黑帧且可继续编辑。
- [x] 恢复不复制会话或重复上传无关资源。
- [x] 交接已更新；总览由主任务集成时统一更新。

## 执行记录
### 2026-09-04
- 完成内容：完成可见 canvas 的单次 OffscreenCanvas 转移、vGPU Surface 直接呈现、三级降级、双表面首帧交接、四类代次门控及有界设备恢复。
- 实际改动：复用唯一 GPU Scene Worker/device/vGPU Context、3.1 RenderGraph 与 4.1 atlas/色彩/预算；正常 Surface 帧不创建 ImageBitmap、不经过 Canvas2D、不回读。device lost 清理旧 registry/compositor/context/Surface，只按最新权威 scene 恢复一次；恢复首帧前继续显示稳定 CPU 帧，连续失败停在 CPU。
- 验证结果：精确 Vitest 11 文件 58/58、Runtime 8/8、Reality 计划 20/20、renderer/electron TypeScript 与本任务 ESLint 通过。Electron Reality 在 1440×900、960×640 双档完成 8192、复杂图 100 次拖动、初始化失败、真实 `GPUDevice.destroy()` 恢复成功与恢复失败；Surface 路径 `ImageBitmap=0`、Canvas2D direct draw=0、readback=0，截图逐一目视无黑屏、旧帧或内部状态泄露。
- 遗留问题：`verify:changed` 的 `vitest related` 会把未配同名测试的 TS 依赖图中的 `?raw`/声明输入交给 Rollup 并报 `PARSE_ERROR`；精确测试、ESLint、双 tsc 与 Reality 均独立通过。该验证器问题不在 4.2 产品范围内。
- 与原计划的偏差：初始化失败 Reality 采用只在正式 UI Inspection 环境开放的只读 preload 布尔门控和隐藏 DOM 事件；正式运行不暴露命令或任意参数。真实 pre-device acquire 失败由单测证明 acquire 为 0，Reality 则在已出现 direct 首帧后注入相同 fallback 事件，以便截图验证稳定帧与继续编辑。
- 对后续任务的影响：3.2 继续只提供 RenderGraph 内容语义；5.2 可直接复用 Surface 状态机、稳定帧和恢复诊断，不得新建 Worker/device/context。
- 相关提交：`feat(image-edit): 实现WebGPU直接呈现与设备恢复`。
