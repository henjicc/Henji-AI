# 3.2 接入画笔标注与效果图

## 任务状态
- 状态：已完成
- 所属阶段：第三阶段-完整图层与效果链
- 前置任务：3.1
- 关联任务：5.1
- 是否可以并行：可与4.2并行

## 任务目标
让稀疏画笔、标注缓存、模糊、扩散和辉光在同一 GPU 场景中执行，消除效果中间结果的 GPU→CPU→GPU 往返。

## 当前情况
- 画笔和蒙版已有 Float32 稀疏瓦片；标注由 Canvas 光栅化后进入 CPU 管线。
- `previewCustomEffectsV3.ts` 可将部分效果临时交给 vGPU，但仍需返回 CPU tile。

## 实施方案
按内容版本增量上传画笔/蒙版瓦片；标注缓存为纹理；把三类效果改成 Target 输入输出并复用稳定 ping-pong 资源。

## 执行步骤
1. 接入稀疏画笔和蒙版瓦片增量更新。
2. 建立标注纹理缓存与失效边界。
3. 迁移模糊、扩散、辉光到同一 RenderGraph。
4. 处理 halo、透明边缘和任务取消。
5. 用 SSIM、边缘和连续笔画 Reality 验证。

## 涉及内容
### 修改文件
- GPU RenderGraph、现有效果执行器、画笔/标注资源桥与专项测试。

## 验收标准
- [x] 笔画只上传变化瓦片且撤销/重做正确。
- [x] 三类效果热路径无 CPU readback。
- [x] SSIM≥0.999且透明边缘无黑白边/接缝。
- [x] 取消和失败能回退并释放资源。
- [x] 交接与任务记录已更新；总览由主任务集成时统一更新。

## 执行记录
### 2026-09-05
- 完成内容：画笔、稀疏蒙版、标注和 fast blur/diffusion/glow 已接入唯一 GPU Scene 与 3.1 RenderGraph；效果 Target 在线性预乘 alpha 域内执行，复用常驻 ping-pong/halo 资源，不再经过 CPU region executor、ImageBitmap→Float32 或诊断读回。
- 实际改动：新增不可变 `resourceKind` 协议并从权威节点/descriptor 编译，bridge 不再按 GPU format 猜测资源来源；画笔/蒙版按 contentVersion、generation 和 tile key 增量上传，支持跨 512 边界；标注复用唯一光栅入口并按内容、字体、布局指纹缓存；full profile 复用既有 raster-brush/eraser。补齐 Target liveness、256 MiB 预算回退和可恢复源纹理等待竞态。
- 验证结果：最终精确组 24 文件 113/113、双 TypeScript、变更范围 ESLint、脚本测试 28/28、Surface 检查与 diff-check 全绿。真实 vGPU golden：blur 最小/默认/最大半径与大小图最低 SSIM `0.9999938793`，diffusion `0.9996398243`，glow 最低 `0.9999979470`；halo 左右边界最低 `0.9999961249`。Electron Reality 双尺寸画笔、撤销/重做、非 live 标注、三效果、透明边缘/halo、预算不足 CPU 回退及 4.2 恢复均通过，正常 Surface 路径 ImageBitmap/readback 为 0。
- 遗留问题：无 3.2 阻塞项。导出仍归 5.1；直接 Surface/device 恢复继续归 4.2。临时 Reality 日志和截图完成目视后已从 worktree 删除，不入库。
- 与原计划的偏差：为落实连续笔画真实验收，将既有 raster-brush/eraser 最小接入 full profile；没有新建工具、按钮或画笔模型。总览不在并行分支改动，由主任务集成统一更新。
- 对后续任务的影响：5.1 应消费 RenderGraph/Target 语义，不得从 Surface/atlas 回读；4.2 合并后继续保持 `webgpu-surface` 首选呈现与既有恢复状态机。
- 相关提交：`e70691f3 feat(image-edit): 接入GPU画笔标注与效果链`；后续修复提交见本分支最新提交。
