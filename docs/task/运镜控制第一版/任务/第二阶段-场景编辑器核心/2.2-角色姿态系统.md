# 2.2 角色姿态系统

## 任务状态

- 状态：开发完成，待用户交互验证
- 所属阶段：第二阶段-场景编辑器核心
- 前置任务：2.1、1.2
- 关联任务：2.3（可并行）
- 是否可以并行：是（与 2.3 并行，均基于 2.1 的对象框架独立扩展）

## 任务目标

在场景编辑器里支持添加人形角色对象，支持体型变体切换、预设姿势一键应用、逐关节滑杆精细调节，达到实测参考产品观察到的姿态控制能力（身体前倾/转身/侧倾、躯干前倾/扭转/侧倾、头部点头/转头/歪头、左右肩/肘/腕的前举/外展/扭转等）。

## 当前情况

- 1.2 应已验证 Mixamo 人形骨架可转换为 glTF 并在 three.js 中正确显示骨骼、动作片段可播放（具体结论见 1.2 执行记录）。
- 2.1 已提供场景对象增删改的基础框架（对象类型、变换属性、对象列表/属性面板），角色对象类型目前是占位，需要在本任务里接入真实实现。
- 实测 LibLib「3D导演台」确认姿态控制是 FK 滑杆（每个滑杆对应关节欧拉角的一个分量）+ 预设姿势库，不是通用 IK；体型变体（男性/女性/宽厚/健壮/纤细/少年/儿童/二头身）是同一套骨架的比例/网格变体，不是完全独立的模型。

## 实施方案

用 1.2 验证过的骨骼模型作为角色基础网格，姿态调节采用 FK 方式——每个滑杆直接写对应骨骼的局部欧拉角分量，不引入通用 IK 求解器（见重要记录 002 和实测结论）。预设姿势用一组"骨骼名称→欧拉角"的 JSON 数据描述，具体来源可以是从 Mixamo 静态姿势帧提取，也可以是手工调好的几组常用姿势，本任务只需要覆盖代表性的 3-5 个，不追求覆盖参考产品的全部预设。体型变体通过对基础骨骼网格做整体/局部缩放实现，一期先做 2-3 种代表性变体，不需要覆盖全部体型。

## 执行步骤

1. 在 2.1 的场景对象类型里新增角色对象类型（引用骨骼模型资源 + 当前姿态数据 + 体型变体参数）。
2. 实现角色对象的三维渲染：加载 1.2 验证过的 glTF 骨骼模型，开发调试阶段用 `SkeletonHelper` 辅助确认关节位置。
3. 实现姿态调节面板（复用 `Ui*` 滑杆组件），按实测观察到的分组结构（身体/躯干/头部/左右手臂-肩/肘/腕）逐个绑定到对应骨骼的欧拉角分量。
4. 实现预设姿势库（先做 3-5 个代表性预设，如站立、坐姿、走路静止帧、挥手），一键应用后所有滑杆同步更新为对应数值。
5. 实现体型变体切换（2-3 种代表性变体），验证切换后姿态滑杆仍然可用、网格不错位、不穿模。
6. 验证多个角色对象同时存在场景中时姿态互不干扰，各自独立可调。

## 涉及内容

### 新增文件

- `src/features/cameraStage/` 下角色姿态相关组件（姿态调节面板、预设姿势选择器）、姿态预设数据文件（骨骼名称到欧拉角的映射），具体文件路径执行时按单一职责拆分确定。

### 修改文件

- 2.1 产出的场景对象类型定义（`sceneTypes.ts` 或等价文件）：扩展支持角色对象类型。
- 2.1 产出的对象列表面板/属性面板：扩展支持角色类型的专属属性展示（切换到姿态调节面板）。

## 验收标准

- [x] 能添加人形角色对象并正确显示骨骼网格，无明显穿模或比例错误。（打包产物内自动化截图确认：骨骼模型正确渲染为纯色站立姿势；细节观感待用户交互验证）
- [x] 预设姿势库至少 3-5 个可一键应用，应用后模型姿态正确。（5 个预设：站立/交谈/行走/坐姿/蹲伏，自动化验证坐姿/蹲伏截图姿态正确、滑杆数值同步回写）
- [x] 逐关节滑杆可以精细调节且实时生效，滑杆分组与实测参考产品的分组结构基本一致。（身体/躯干/头部/左右臂-肩肘腕/左右腿-髋膝踝 7 组 15 关节 45 滑杆；轴语义标签待用户实际手感验证后再决定是否细化）
- [x] 至少 2-3 种体型变体可切换，切换后姿态调节仍然正常可用。（标准/健壮/纤细/儿童 4 种，儿童变体自动化截图确认小身体大头、姿态滑杆继续可用）
- [x] 多个角色对象同时存在场景中时互不干扰，各自独立调节。（自动化验证：角色1 改髋关节后角色2 应用蹲伏，回选角色1 数值仍为改后值）
- [x] `00-任务总览.md` 已同步更新任务状态和进度。

## 执行记录

### 2026-07-03 开发完成（Claude）

**内置角色资源与加载链路（落实重要记录 006/007）**

- `resources/camera-stage/UAL1_Standard.glb`（7.6MB，Quaternius UAL Standard 无根运动版）+ `LICENSE.txt`（CC0）随仓库提交、随应用分发（`electron-builder.yml` 增加 extraResources 条目）。
- 新增内置资源解析链路：`electron/main/services/media/bundledResources.ts`（解析 resources/ 相对路径 → 绝对路径 + 注册 henji-media 白名单根，开发态/打包态行为一致）→ `media:getBundledResourcePath` IPC → preload → PAL `media.getBundledResourcePath` → `desktopApi.getBundledResourceUrl()`。协议层补充 `.glb/.gltf` MIME。
- 渲染层 `useCharacterModelUrl` 模块级缓存解析结果；非桌面运行时（裸 Vite）或资源缺失时返回 null，角色回退占位渲染。

**姿态系统（FK，不引入 IK）**

- 数据模型：`domain/poseTypes.ts` 定义 15 个受控关节（骨盆/脊柱/头 + 左右肩肘腕 + 左右髋膝踝，UE Mannequin 骨骼命名映射），姿态 = 关节 → 相对绑定姿态的欧拉偏移（角度制 XYZ 序）+ 可选骨盆平移偏移（坐姿/蹲伏需要整体降低身体，纯旋转表达不出来）。
- 渲染应用：`scene/CharacterModel.tsx`，`bone.quaternion = rest × offset`；每角色实例 `SkeletonUtils.clone` 独立骨架（多角色互不干扰）；GLB 材质整体替换为单一 `MeshStandardMaterial` 纯色（选中态 emissive 增亮，与几何体一致）；蒙皮网格关 frustumCulled 防误裁剪。`three-stdlib` 升为显式依赖。
- 预设姿势：**离线采样生成**而非手工调参——`scripts/generate-camera-stage-poses.cjs`（`npm run gen:camera-stage-poses`）直接解析 GLB 的 JSON/BIN chunk，在指定动画片段/时间点采样受控骨骼局部旋转，与绑定姿态求差输出 `domain/posePresets.gen.ts`（自动生成、随仓库提交）。当前 5 个预设：站立(Idle_Loop)/交谈(Idle_Talking_Loop)/行走(Walk_Loop 步中帧)/坐姿(Sitting_Idle_Loop)/蹲伏(Crouch_Idle_Loop)。调整预设清单改脚本内 `PRESET_SPECS` 后重跑。
- 体型变体：`domain/bodyVariants.ts`，同骨架比例变体（整体非均匀缩放 + 头部骨骼缩放）：标准/健壮/纤细/儿童。骨骼结构不变，切换后滑杆全部继续可用。
- 面板：`panels/CharacterPoseSection.tsx`（体型 chips + 预设 chips + 7 分组手风琴逐关节 XYZ 滑杆，滑杆 + NumberInput 双输入实时提交），挂在 PropertyPanel 角色分支。新角色默认应用"站立"预设（避免生硬 T-pose）。

**自动化验证（打包产物内）**

- 脚本操作真实 UI：添加角色 → 骨骼模型加载渲染（非占位）→ 应用坐姿（截图姿态正确、左腿髋 X 滑杆回写 -85）→ 滑杆改 30 实时生效 → 切换儿童体型（截图小身体大头）→ 添加第二角色应用蹲伏 → 回选角色1 髋 X 仍为 30（无串扰）。控制台除存量 403（历史记录引用 E 盘白名单外视频，1.1 已记录的环境问题）外零报错。
- lint / 前端 tsc / electron tsc / check:colors 全过；新文件全部 < 250 行（posePresets.gen.ts 433 行为自动生成数据）。
- `npm run electron:smoke` 失败原因为上述存量 403 环境问题（与 1.1 时相同），非本次改动引入。

**遗留给用户的交互验证点**：预设姿势观感、滑杆手感与轴向直觉（如需给 X/Y/Z 配"前倾/扭转/侧倾"语义标签，验证后再定）、体型变体比例观感、儿童体型下姿态是否有明显穿模。
