# 01 迁移方案：Tauri → Electron

> 本文件记录当前相对稳定的迁移后架构与剩余收尾边界。历史调研与决策见 `记录/重要决定.md`、`记录/技术调研-Electron-2026.md`，逐任务细节见 `任务/`。

## 一、当前技术栈（迁移后基线）

- 外壳：**Electron 42.5.0**（Node/TS 主进程 + 打包 Chromium）
- 前端：React 18 + TypeScript 5 + Vite 4 + Tailwind CSS 3
- 构建：electron-vite 3.1.0 + electron-builder 26.15.3
- 更新：electron-updater + GitHub Releases（本地 generic feed E2E 已验证；真实 GitHub 发布待凭据）
- 状态：Zustand 5；国际化：i18next / react-i18next
- 画布：`@xyflow/react`（ReactFlow 12）为主画布；`konva` / `react-konva` 用于图片编辑与标注工具
- 数据库：SQLite + `better-sqlite3`
- 密钥：Electron `safeStorage`，加密文件位于旧本地数据根
- 图像处理：`sharp`
- 项目包：`archiver` + `yauzl`
- 旧壳：Tauri 2.0 / Rust 仍保留到 4.3，作为回退、对照和资源目录历史落点

## 二、当前架构

### Electron 主进程（`electron/main/`）

- `index.ts`：主进程入口，初始化窗口、协议、IPC、updater
- `window.ts`：无边框透明窗口、自定义标题栏行为、受控最大化/还原
- `protocol.ts`：`henji-media://` 自定义媒体协议，支持本地媒体、MIME、HEAD、Range、路径安全校验
- `ipc/`：所有 `ipcMain` handler，按能力域拆分
- `services/`：
  - `db.ts`：`better-sqlite3` 数据层，复用旧 `henji.db`
  - `keystore.ts`：`safeStorage` 密钥加密存储
  - `system.ts`：fs/dialog/shell/path/http/logging 基础能力
  - `ai-runtime/**`：模型生成主链路，含 manifest、DSL、provider、上传、轮询、trace、进度学习
  - `llm/**`：LLM 流式 runtime 与取消
  - `image/**`：图片加载、持久化、裁剪、切分、合并、元数据
  - `clipboard.ts` / `drag.ts`：剪贴板与原生拖拽
  - `project-package.ts`：项目包 zip 导入导出
  - `updater.ts`：electron-updater 集成

### Preload（`electron/preload/`）

- 使用 `contextBridge` 暴露窄白名单 `window.henjiNative.*`
- 保持 `contextIsolation: true`、`nodeIntegration: false`、`sandbox: true`
- 渲染层不能直接接触 Node、Electron `ipcRenderer` 或主进程服务

### 渲染层（`src/`）

- `commands/`：前端命令桥；对外签名稳定，内部走 PAL
- `platform/`：平台抽象层（PAL），包含 contracts 与 electron/tauri adapters
- `core/`：模型定义/注册、请求构建、`GenerationService`、联动引擎
- `features/canvas/`：画布主实现
- `models/`：`*.model.ts` 模型定义，仍保持配置驱动

核心思想：**业务层只认 `src/commands/*` 与 `src/platform/*`，不关心桌面外壳实现。Electron 是新主路径，Tauri adapter 仅为旧壳兼容。**

## 三、生成主链路

迁移后模型生成路径：

```text
GenerationService
  -> src/commands/aiRuntime.ts
  -> src/platform/adapters/electron/aiRuntime.ts
  -> electron/preload/index.ts (window.henjiNative.ai)
  -> electron/main/ipc/ai-runtime.ts
  -> electron/main/services/ai-runtime/**
```

约束：

- 业务 UI 禁止直接调用 provider API
- provider 鉴权、路由、请求格式、轮询、结果解析全部落在 Electron 主进程
- 模型特定行为仍优先通过 `src/models/{provider}/*.model.ts` 配置表达
- `src/core/providers/` 继续作为兼容层/类型基类，不作为真实 provider 执行层

## 四、已完成迁移范围

- Electron 工程骨架与构建链路
- 无边框窗口与自定义标题栏
- preload 安全 IPC 桥
- PAL 接入与渲染层 Tauri 直连收口
- SQLite 数据层
- safeStorage 密钥存储
- fs/dialog/shell/path/http/logging 系统能力
- `henji-media://` 资源协议与媒体加载
- AI Runtime：PPIO / Fal / KIE / ModelScope、上传、轮询、取消、trace、进度学习
- LLM 流式 Runtime：SSE 增量、reasoning token、取消、错误终止
- 图像处理命令：load/persist/save/info/prepare/split/crop/merge/metadata
- 剪贴板与原生拖拽
- 项目包 zip 导入导出
- electron-builder 打包配置、electron-updater 主链路、发布脚本切换
- Electron smoke、画布压测、DPI 检查、Windows NSIS/MSI、本地 updater E2E

## 五、当前剩余事项

### 不阻塞日常开发，可作为发布前验收

- macOS 真实设备验收：DMG、safeStorage、拖拽/剪贴板、透明窗口、媒体播放
- 代码签名与公证：Windows 证书、Apple Developer 账号、notarization
- 真实 GitHub Release 自动更新：需要发布凭据、旧版本包和线上 release feed
- 真实旧用户数据回归：旧 `henji.db`、Media、历史项目包、真实 API key
- 真实 provider 端到端：四家 provider 真实 API key 下至少各跑通代表模型
- 图像像素级对比：旧 Rust 水印/分镜文字样张与 `sharp` 输出对比

### 4.3 最终清理项

- 打 `tauri-final` tag 标记最后一个含 Tauri 的可回退点
- 删除 `src-tauri/` Rust 旧壳与构建配置
- 移除 `@tauri-apps/*`、`@crabnebula/tauri-plugin-drag`、`@tauri-apps/cli`
- 删除 `tauri:*` 脚本与 Tauri 压测脚本
- 删除 `src/platform/adapters/tauri/*`，`runtime.ts` 简化为 Electron-only
- 将 manifest/seeds 生成目录从 `src-tauri/resources/` 收口到 Electron 专用资源目录
- 全仓清理 `Tauri`/`tauri` 命名、注释、文档与旧兼容说明

## 六、数据兼容方式

- 数据库：Electron 显式使用与旧 Tauri identifier 对齐的数据目录（Windows：`%LOCALAPPDATA%/com.henji.ai/Henji-AI/henji.db`），复用同一 `henji.db`
- 媒体：沿用旧本地数据根下的媒体/上传目录，Electron 通过 `henji-media://` 暴露本地文件给渲染层
- 密钥：Tauri `keyring` 与 Electron `safeStorage` 不自动互通；Electron 设置页已提示用户重新填写
- 资源：manifest/seeds 当前仍生成到 `src-tauri/resources/`，Electron 打包时复制到随包 `resources/`

## 七、打包与更新

- `electron-builder.yml` 配置：
  - `appId: com.henji.ai`
  - Windows：NSIS + MSI
  - macOS：DMG x64/arm64
  - `extraResources`：manifest/seeds
  - `asarUnpack`：`better-sqlite3`、`sharp`、`@img`、`@napi-rs`
  - `publish`：GitHub Releases
- 当前验证：
  - `npm run electron:build` 通过
  - `npm run electron:smoke` 通过
  - Windows NSIS/MSI 静默安装并启动验证通过
  - 本地 generic feed 模拟 updater 检测、下载、校验、downloaded 通过
- 当前限制：
  - 未签名安装包会触发系统信任提示
  - macOS 公证与真实 GitHub Release 升级需要凭据与设备

## 八、总体策略状态

原迁移策略是“抽象先行、双壳共存、按域迁移、最后清理”。目前状态：

1. **抽象先行已完成**：PAL 已接入业务调用面。
2. **双壳共存仍保留**：Tauri 未删，便于回退和对照。
3. **按域迁移已完成**：第三阶段各能力域均已落地 Electron 实现。
4. **最后清理暂缓**：4.3 等真实发布条件更稳后执行。

## 九、全局开发约束

1. 遵循 `AGENTS.md` 全部解耦红线：配置驱动、生成主链路边界、UI primitive 单点、颜色令牌三处入口、文件体积治理、`any` 增量零新增、导出函数显式返回类型。
2. 渲染层禁止新增对 `@tauri-apps/*`、Electron `ipcRenderer`、Node 内置模块的直接 import，一律经 `src/platform/*` 或既有 `src/commands/*`。
3. Electron 主进程禁止导入渲染层组件。
4. 主进程与 preload 走安全白名单 IPC，所有路径/媒体/zip/剪贴板输入都必须做校验。
5. 新增模型优先改模型定义与 Electron AI Runtime，不在 UI 写 provider 特判。
6. 4.3 之前不要随手删除 Tauri 文件；需要删除时按 4.3 计划整批验证。

## 待确认清单（更新）

- ~~Rust 后端迁移策略~~ → 已确定并实施：全量 Node/TS 重写，无 sidecar。
- ~~构建工具链~~ → 已实施：electron-vite + electron-builder。
- ~~自动更新方案~~ → 已实施：electron-updater + GitHub Releases；真实线上发布待凭据。
- ~~数据库方案~~ → 已实施：better-sqlite3，复用旧 `henji.db`。
- Tauri 残留删除时机：建议在无需回退旧壳、并完成发布前关键验收后执行。
- 代码签名/公证证书：待用户具备证书与 Apple 账号后配置。
- macOS 真机验收：待真实设备。
- manifest/seeds 资源目录重命名：留给 4.3，避免当前扩大改动面。
