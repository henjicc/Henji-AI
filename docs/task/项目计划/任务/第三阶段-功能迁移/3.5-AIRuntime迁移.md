# 3.5 AI Runtime 迁移（全量 Node 重写）

## 任务状态
- 状态：进行中
- 所属阶段：第三阶段-功能迁移
- 前置任务：1.2、2.3、3.2
- 关联任务：3.4、3.7、4.1
- 是否可以并行：体量大，建议独立推进；可在 3.1/3.2/3.3/3.4 完成后开工

## 任务目标
将 Rust `ai_runtime`（模型生成主链路）全量重写为 Electron 主进程的 Node/TS 实现，等价覆盖 `ai_generate`/`ai_continue_polling`/`ai_cancel_task`/`ai_reload_model_manifest`/`ai_get_progress_estimate`/`ai_record_progress_sample` 等命令，保持 `GenerationService → 命令桥 → 后端` 主链路边界不变。

## 当前情况（Rust 现状，约定迁移范围）
- `ai_runtime/`：`commands.rs`(10 命令)、`providers/{fal,kie,modelscope,ppio}.rs`、`polling.rs`、`trace.rs`、`task_registry.rs`、`media_store.rs`、`model_manifest.rs`、`request_builder_dsl.rs` + `js_runtime.rs`(boa_engine)、`request_normalizer.rs`、`progress_learning.rs`(921 行)、`upload/`(827 行，见 3.x 上传)。
- 模型清单：`src-tauri/resources/model-manifest.json`（由 `gen:model-manifest` 生成），运行时据此构建请求。
- 前端契约：`src/commands/aiRuntime.ts`（DTO 已定义，签名保持不变）。
- 关键子能力：请求构建 DSL（JS，原 boa_engine 执行）、请求归一化、轮询、trace、进度学习与采样。
- **重要（1.1 已核实）**：顶层 `src-tauri/src/modelscope.rs`（`modelscope_submit_task`/`modelscope_check_status`）在全仓 `src/` 中**无任何前端调用**，已被 `ai_runtime::providers::modelscope.rs`（经通用 `ai_generate`/`ai_continue_polling` 路由）取代。**本任务只需迁移 `ai_runtime/providers/modelscope.rs`，顶层 `modelscope.rs` 直接作废，不迁移**。

## 实施方案（1.2 已确认具体实现路径，详见 `任务/第一阶段-准备工作/1.2` 执行记录的对照表）
按子模块在主进程重写：
- HTTP：`undici`/`fetch` 重写 4 家 provider 的鉴权/路由/请求格式/轮询/结果解析。
- **请求构建 DSL（已确认两段式，非全 JS）**：①`request_builder_dsl.rs` 中**大部分是纯声明式 JSON DSL**（字段映射 `fields`(支持 `a.b.c` 点路径取值)/`transforms`(trim/string/number/boolean/join)/`constants`/`removeEmpty`、`endpoints.rules` 条件选路由）——直接写一个 ~150 行纯函数解释器照搬即可，**不需要任何 JS 引擎**；②仅当模型用 `builder_js`/`selector_js`（JS 函数字符串）时才需要执行 JS——用 `node:vm` 建一个预置 `JS_PRELUDE`（`js_runtime.rs` 中 9 个分辨率计算等纯函数，原样复制为 JS 字符串）的沙箱 context 执行，语义与原 boa_engine 实现逐行对应。
- **任务注册与取消（已确认极简）**：`task_registry.rs` 仅是 `Set<string>`（取消协作式——轮询/流式循环内部定期检查，不是硬中断）。Node 下用模块级 `Set<string>` 即可；**建议叠加 `Map<taskId, AbortController>`** 让取消同时中断底层 fetch，比原实现更快生效（增强而非偷工减料）。
- **轮询**：`polling.rs` 本身只是 sleep 封装，真正循环在 `commands.rs::ai_generate`/`ai_continue_polling`，实施时需先读这两个函数体确认提交/续轮询的状态机边界，再用 `async function + setTimeout` 循环复刻（每轮检查取消、超过 maxAttempts 报 timeout）。
- **进度学习**：移植 `progress_learning.rs` 算法（`TimeBucket` night/day/evening 三分桶 + 全局/分桶两级截尾均值估时 + seed 文件兜底），逐函数对齐。**关键协同点**：`progress_samples` 表由 `progress_learning.rs` 独立创建在同一个 `henji.db` 里（不在 `DatabaseService` 四表中），3.1 已补充该表 DDL——本任务直接复用 3.1 暴露的数据库连接/查询入口，不要自己单独开一个 better-sqlite3 连接。
- trace：对齐 `AiRuntimeTrace` 结构，经 IPC 返回。
- 模型清单：主进程读取同一 `model-manifest.json`，`ai_reload_model_manifest` 重新加载。

## 执行步骤
0. **（前置，1.2 遗留）** 在现有 Tauri 应用上跑通 1.2 待生成的图像像素一致性验证基线——本步骤实际属于 3.7（图像处理）范畴，此处仅提示：若 3.5 与 3.7 并行推进，AI Runtime 中涉及"生成结果含水印/分镜渲染"的端到端验证需等 3.7 基线就位后再做。
1. 先读 `src-tauri/src/ai_runtime/commands.rs` 中 `ai_generate`/`ai_continue_polling` 的完整循环体，确认状态机边界（1.2 未逐行读出，留到此步）。
2. 搭 `electron/main/services/ai-runtime/` 骨架：provider 接口、请求归一化、声明式 DSL 解释器、JS DSL 执行器（`node:vm`）、轮询器、任务注册表（`Set` + 可选 `AbortController`）、trace。
3. 实现声明式 DSL 解释器与 JS DSL 执行器，用现有 manifest 跑通 1~2 个模型的请求构建，逐字段对照 Rust 版输出比对。
4. 逐家 provider 重写（ppio→fal→kie→modelscope，**只迁移 `providers/modelscope.rs`，不迁移顶层 `modelscope.rs`**）：鉴权、endpoint 选择、请求体、提交、轮询、结果解析、错误归一。
5. 实现 `ai_generate`/`ai_continue_polling`/`ai_cancel_task`：含轮询参数（interval/maxAttempts）与取消。
6. 移植媒体上传（原 `ai_runtime/upload/`，827 行：fal/kie/ppio/bizyair）——图生图/图生视频等模型需先把本地媒体上传到对应平台，重写其鉴权与上传协议（含 `ali-oss` 等可在 Node 直接复用）。
7. 移植 `progress_learning` 与 `ai_get_progress_estimate`/`ai_record_progress_sample`，复用 3.1 的数据库连接，校验估时与采样持久化。
8. 暴露 IPC 通道 + PAL `aiRuntime` 适配；`commands/aiRuntime.ts` 改走 PAL。
9. 端到端：每家 provider 至少跑通 1 个图像 + 1 个视频/音频模型（含需上传输入媒体的链路），比对 trace 与产物；验证取消与续轮询。

## 涉及文件
### 新增
- `electron/main/services/ai-runtime/**`（providers / dsl / polling / trace / progress / task-registry）。
- `electron/main/ipc/ai-runtime.ts`。
### 修改
- `src/platform/adapters/electron/aiRuntime.ts`。
- `src/commands/aiRuntime.ts`：改走 PAL（DTO 不变）。
### 可能废弃
- Tauri 移除后：整个 `src-tauri/src/ai_runtime/`（含 `upload/`、`progress_learning.rs`、`js_runtime.rs` 等全部子模块）；`src-tauri/src/modelscope.rs` 若被本任务覆盖一并废弃（否则归入 3.x 其他）。

## 验收标准
- [ ] 4 家 provider 各至少 1 个模型端到端生成成功，产物与现状一致。
- [ ] 请求构建 DSL 输出与 manifest 预期一致（含联动后的参数）。
- [ ] 轮询、续轮询、取消行为等价；trace 结构对齐。
- [ ] 进度估时与采样与现状偏差在可接受范围。
- [ ] `ai_reload_model_manifest` 可重载清单。
- [ ] 主链路边界未破坏（业务 UI 不直连模型 API）；无新增 `any`；类型检查通过。

## 执行记录
### 2026-06-26
- 完成内容：新增 Electron 主进程 AI Runtime Node/TS 骨架，覆盖 manifest 加载/重载、声明式请求 DSL、`node:vm` JS DSL 沙箱、runtime constraints 归一化、trace 脱敏、任务取消注册表、媒体结果下载落盘、进度估时/采样持久化；实现 PPIO/Fal/KIE/ModelScope 四家 provider 的提交与续轮询基础逻辑；新增 `ai:generate`、`ai:continuePolling`、`ai:cancelTask`、`ai:reloadModelManifest`、`ai:getProgressEstimate`、`ai:recordProgressSample` IPC，preload 与 Electron PAL `aiRuntime` 接入真实通道。同步修正 Electron `progress_samples` DDL 为 Rust 等价的 `id INTEGER PRIMARY KEY AUTOINCREMENT`。
- 追加完成内容：补齐媒体上传预处理链路，按 Rust 现状递归扫描媒体字段，支持本地路径、`file://`、旧 `asset/tauri localhost` URL 与 data URI；Fal 转 data URI，PPIO 按 route/field 改写为 data URI、裸 base64 或公网 URL，KIE/ModelScope 优先 KIE 上传、回退 BizyAir、最后 data URI；BizyAir 上传已移植 token→OSS PUT(HMAC-SHA1)→commit 流程，KIE 上传已移植 multipart 流程；上传策略从原始 params 的 `__upload_provider` / `__upload_fallback` 读取。
- 实际修改文件：`electron/main/services/ai-runtime/**`、`electron/main/ipc/ai-runtime.ts`、`electron/main/index.ts`、`electron/main/services/db.ts`、`electron/preload/api.d.ts`、`electron/preload/index.ts`、`src/platform/adapters/electron/aiRuntime.ts`、`docs/task/项目计划/00-项目总览.md`、本任务文件。
- 验证结果：`npx tsc -p tsconfig.electron.json` 通过；`npx eslint electron --ext ts --report-unused-disable-directives --max-warnings 0` 通过；`npx eslint src\platform\adapters\electron\aiRuntime.ts --ext ts,tsx --report-unused-disable-directives --max-warnings 0` 通过；`npm run electron:build` 通过（仅既有 Rollup chunk 警告）；Playwright `_electron` 实测 `window.henjiNative.ai` 新增方法存在、manifest 重载返回 58、provider key status 可读、进度估时与采样写入可用、测试样本已清理，`pageErrors=[]`、`consoleErrors=[]`。追加验证：上传链路补齐后再次通过 Electron TypeScript、Electron ESLint、增量 `any` 检查、`npm run electron:build` 与 Playwright `_electron` AI IPC 回归；新增 AI Runtime 文件均未超过 400 行。
- 遗留问题：四家 provider 尚未用真实 API key 做端到端产物生成验收；进度学习当前覆盖样本窗口、截尾均值、meta/default 兜底，尚未接入 `progress-seeds.json` seed 兜底与复杂 profile segment。
- 与计划的偏差：按任务体量先落地可编译可调试的主链路骨架与基础 provider 状态机，再补上传链路；真实 provider 端到端验收留在后续具备 API key/网络条件时完成。
- 相关提交：`f49c8e7 推进 Electron AI Runtime 迁移`；本次追加上传链路提交待提交。
