# 4.2 打包、签名与自动更新

## 任务状态
- 状态：进行中
- 所属阶段：第四阶段-测试与收尾
- 前置任务：4.1
- 关联任务：4.3
- 是否可以并行：否

## 任务目标
用 electron-builder 产出 Windows/macOS 安装包，配置代码签名，并以 electron-updater + GitHub Releases 实现真正的增量自动更新（替代现状的 Release 轮询 + 手动下载）。建立「跟随 Electron 大版本升级」的维护惯例（决定 008）。

## 当前情况
- 现状打包：Tauri bundler（`tauri.conf.json` bundle，Windows WiX/zh-CN，macOS infoPlist，资源含 `model-manifest.json`/`progress-seeds.json`）。
- 现状更新：`src/services/updateChecker.ts` 走 GitHub Release API 比较版本，手动下载 msi/exe；`scripts/release.cjs` 做版本发布自动化。
- 目标（决定 005/006）：electron-builder + electron-updater + GitHub Releases。

## 实施方案
配置 electron-builder（appId 对齐 `com.henji.ai`、产物目标、图标、资源打包），处理 better-sqlite3/sharp/@napi-rs/canvas 等**原生模块的跨平台打包与 rebuild**；接入 electron-updater 做后台检查 + 下载 + 安装；改造 `UpdateDialog`/`updateChecker` 走新更新流程。

## 执行步骤
1. electron-builder 基础配置：appId、productName（痕迹AI）、icons、win(nsis/msi)/mac(dmg) targets、`extraResources`（manifest/seeds）。
2. 处理原生模块：确保 better-sqlite3、sharp、@napi-rs/canvas 在目标平台正确打包（`asarUnpack`/`@electron/rebuild`/prebuild）。
3. 代码签名：Windows（证书/EV，若具备）、macOS（签名 + 公证 notarize，若具备 Apple 账号）。【待确认：是否具备签名证书】
4. 自动更新：接入 electron-updater，发布渠道指向 GitHub Releases；实现后台检查、下载进度、就绪后提示重启安装。
5. 改造前端更新入口：`UpdateDialog.tsx`/`updateChecker.ts` 走 electron-updater 事件（替代 Release 轮询）。
6. 调整发布脚本：`scripts/release.cjs` 适配 electron-builder 产物与版本发布流程。
7. 记录「跟随大版本升级」惯例到收尾文档（4.3）。
8. 全流程演练：从版本号变更 → 构建 → 发布 → 旧版本自动检测并升级成功。

## 涉及文件
### 新增
- electron-builder 配置（`electron-builder.yml` 或 package.json `build` 段）。
- 主进程自动更新逻辑（`electron/main/services/updater.ts`）。
### 修改
- `package.json`：打包/发布脚本与依赖（electron-updater）。
- `src/components/UpdateDialog.tsx`、`src/services/updateChecker.ts`：走新更新流程（经 PAL）。
- `scripts/release.cjs`：适配新产物。
### 可能废弃
- Tauri bundler 配置（4.3 移除）；旧 Release 轮询更新逻辑。

## 验收标准
- [ ] Windows 与 macOS 安装包可正常安装运行。（Windows `--dir` 产物已自动化启动验证；安装器与 macOS 待真实环境验收）
- [x] 原生模块（sqlite/sharp/canvas）在打包产物中可用。（`better-sqlite3` 与 `sharp` 已进入 `app.asar.unpacked`）
- [ ] 代码签名完成或明确记录为「暂缺证书」的已知限制。
- [ ] 自动更新端到端成功：检测→下载→安装→重启到新版本。（IPC/UI 已接入，真实 Release 升级待发布凭据与旧版本包）
- [x] 资源（manifest/seeds）正确随包分发。
- [ ] 升级惯例已写入收尾文档。

## 执行记录
### 2026-06-26
- 完成内容：接入 electron-builder 基础配置与 Electron 自动更新主链路。新增 `electron-builder.yml`，配置 `appId=com.henji.ai`、产品名、Windows NSIS/MSI、macOS DMG、GitHub Releases 发布通道、`model-manifest.json`/`progress-seeds.json` 资源分发，以及 `better-sqlite3`/`sharp`/`@img`/`@napi-rs` 的 `asarUnpack`。新增 `electron:pack`、`electron:dist`、`electron:publish` 脚本与 `electron-updater` 依赖。主进程新增 updater 服务与 IPC，preload/PAL 暴露 `updater` 能力；前端 `updateChecker` 在 Electron 下走 electron-updater，旧 Tauri 环境保留 GitHub Release 轮询兜底；更新弹窗支持下载中与重启安装状态。`scripts/release.cjs` 已改为 Electron 构建/发布流程。
- 实际修改文件：`electron-builder.yml`、`package.json`、`package-lock.json`、`electron/main/services/updater.ts`、`electron/main/ipc/updater.ts`、`electron/main/index.ts`、`electron/preload/api.d.ts`、`electron/preload/index.ts`、`src/platform/contracts/updater.ts`、`src/platform/contracts/index.ts`、`src/platform/adapters/electron/updater.ts`、`src/platform/adapters/electron/index.ts`、`src/platform/adapters/tauri/updater.ts`、`src/platform/adapters/tauri/index.ts`、`src/services/updateChecker.ts`、`src/workspaces/GenerationWorkspace/hooks/useUpdateCheck.ts`、`src/components/UpdateDialog.tsx`、`src/i18n/locales/zh-CN/ui.json`、`src/i18n/locales/en-US/ui.json`、`scripts/release.cjs`、本文件。
- 验证结果：`npm run check:colors` 通过；`npm run check:model-i18n` 通过；`npx tsc -p tsconfig.electron.json --noEmit` 通过；本次改动文件定向 ESLint 通过；`npm run electron:build` 通过（仅既有 Rollup chunk/circular re-export 警告）；`npm run electron:smoke` 通过，manifest=58，provider status=5，画布 2560x1360，rAF 62fps，`pageErrors=[]`、`consoleErrors=[]`；`npx electron-builder --dir` 通过并生成 `release/win-unpacked`；未安装版产物在干净环境中可启动，CDP 验证 `window.henjiNative` 存在、updater 状态为 `not-available`（无 `app-update.yml` 时不误报错误）、页面错误为空；资源文件与原生模块位于预期包内路径。
- 遗留问题：真实 Windows 安装器、macOS DMG、代码签名/公证、GitHub Release 自动更新端到端仍需要证书、发布 token、旧版本包与真实环境验收；`electron-builder --dir` 提示 `package.json` 缺少 author、`@electron/rebuild` 与 builder 内置 rebuild 存在重复建议，暂不阻断。
- 与计划的偏差：本机目录打包产物不会生成 `app-update.yml`，因此 updater 在无发布元数据时显式降级为 `not-available`；自动更新端到端需等真实 release 产物验证。
- 相关提交：本次提交。
