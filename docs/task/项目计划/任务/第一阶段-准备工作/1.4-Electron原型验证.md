# 1.4 Electron 原型验证（spike）

## 任务状态
- 状态：已完成
- 所属阶段：第一阶段-准备工作
- 前置任务：1.1
- 关联任务：2.1、2.2、2.3
- 是否可以并行：可与 1.2、1.3 并行

## 任务目标
用最小代价验证「Electron + 现有 Vite 前端」可跑通的关键风险点，把不确定性前移。原型只验证可行性，**不接入业务、不动现有 `src-tauri/`**，可在独立分支或临时目录完成。

## 当前情况
- 前端为 Vite 4 + React 18，画布用 `@xyflow/react` + `konva`，均为纯 Web 技术，预期可直接在 Electron 渲染层运行。
- 需提前验证的高风险点（来自 2026-06 调研）：
  - electron-vite 加载现有前端（含 `@/` 别名、Tailwind、i18n）。
  - 无边框 + 透明窗口在 Windows/macOS 的表现（决定方案待确认项）。
  - `protocol.handle()` 加载本地媒体且视频可正常二次播放（Range 支持）。
  - better-sqlite3 在目标 Electron 版本下 `@electron/rebuild` 成功打开 `henji.db`。
  - `webContents.startDrag()` 可把本地文件拖入系统/外部应用。

## 实施方案
搭建一个最小 Electron 工程，逐项点亮上述风险验证，产出结论与样例代码片段供第二、三阶段复用。**实际执行时未引入 electron-vite**（那是 2.1 工程骨架任务的范围），而是手写 `main.js`/`preload.js`/`index.html` 直接用 `electron .` 启动——因为 1.4 的目的是验证 Electron 运行时本身的能力（窗口/协议/数据库/拖拽），不涉及前端构建链路，手写原型更快拿到结论；electron-vite 与现有 Vite 前端栈的集成验证留给 2.1 实测。用 **Playwright 的 `_electron` API** 驱动原型（而非人工点击/截图猜测），可自动获取截图、读取 DOM、执行脚本、捕获 console/异常，验证过程可重复、有据可查。

## 执行步骤
1. ~~用 electron-vite 脚手架建最小工程~~ → 改为：在 `prototype/electron-poc/` 手写 `main.js`/`preload.js`/`index.html`，验证 Electron 运行时能力（与 2.1 的前端构建链路验证解耦）。
2. ~~验证加载现有前端构建产物~~ → 本任务不覆盖，留给 2.1。
3. 验证无边框 + 透明窗口（`frame:false` + `transparent:true`），用 Playwright 截图确认渲染效果。
4. 验证 `protocol.handle('henji-media', ...)`：加载真实生产数据中的一张图片 + 一个视频，并模拟「播放→跳转中段→跳回开头→重新播放」确认**不报 FFmpegDecodeError**。
5. 验证 better-sqlite3：`npm i` 后用 `@electron/rebuild` 针对 Electron 42 的 Node ABI 重新编译，打开一份**真实 `henji.db` 副本**并查询。
6. 编写 `webContents.startDrag` 的 IPC 通路代码（`henji:start-drag` handler），**但本次未做端到端的真实 OS 拖拽验证**（Playwright 无法可靠模拟拖出到桌面的原生拖放，留待 3.8 用人工方式验证）。
7. 汇总结论，回填执行记录；据结论更新 `01-迁移方案.md` 与 3.1 的表清单（新发现的 `canvas_projects` 第 7 张表）。

## 涉及文件
### 新增
- `prototype/electron-poc/`：原型工程（不并入 `src/` 主代码，保留为参考与回归依据）。
  - `package.json`、`main.js`、`preload.js`、`index.html`：最小 Electron 应用。
  - `test-driver.js`：Playwright `_electron` 驱动脚本，可重复运行复验。
  - `.gitignore`：排除 `node_modules/`、`test-data/`（含真实用户数据副本，不入库）、截图与日志产物。
### 修改
- `docs/task/项目计划/任务/第三阶段-功能迁移/3.1-数据层SQLite迁移.md`：补充真机验证发现的第 7 张表 `canvas_projects`。
### 可能废弃
- 原型工程本身（迁移完成后可整体删除，或保留作为 Electron 运行时能力的纯净参考）。

## 验收标准
- [x] ~~electron-vite 能加载并热更新现有前端栈~~ → **范围调整**：electron-vite 集成验证移至 2.1（理由见「实施方案」），1.4 改为验证 Electron 运行时本身。
- [x] 无边框透明窗口效果确认（Windows 下 `frame:false`+`transparent:true` 正常渲染，自定义拖拽标题栏可用；**macOS 差异未验证**，当前开发机为 Windows，留待有 Mac 设备时补充）。
- [x] 本地图片与视频经 `protocol.handle` 正常加载，**视频二次播放/跳转/重新播放全部正常**（无 FFmpegDecodeError，Range 支持生效）。
- [x] better-sqlite3 在 Electron 42.5.0 下经 `@electron/rebuild` 成功打开**真实 `henji.db` 副本**并查出 276 条历史记录。
- [ ] `startDrag` 可拖文件到外部 → **未验证**：IPC 通路已写好，但 Playwright 无法可靠驱动真实 OS 级拖放，需人工验证（留给 3.8）。
- [x] 结论与样例代码片段已记录（见执行记录），`01-迁移方案.md`/3.1 已据此更新。

## 执行记录
### 2026-06-26
- 完成内容：
  1. 在 `prototype/electron-poc/` 搭建最小 Electron 42.5.0 应用（手写 main/preload/renderer，未用 electron-vite），用真实生产数据验证：
     - **无边框 + 透明窗口**：`frame:false`+`transparent:true` 在 Windows 下正常渲染，自定义 `-webkit-app-region:drag` 标题栏可用。
     - **`protocol.handle` 媒体协议**：注册 `henji-media://` scheme（`stream:true`），实现 Range 请求解析（206 分片响应）。用真实生产图片（1536×2048 PNG）与视频（5.04s/1280×720 MP4）测试，**图片/视频元数据均正常加载**；自动化模拟「播放 0.3s→跳转到中段→跳回开头→重新播放」全链路，**未出现 FFmpegDecodeError**，验证了 2026 调研中标记的已知坑确实可以通过实现 Range 规避。
     - **better-sqlite3 + Electron ABI**：首次 `npm install` 装的预编译二进制是按**系统 Node 22.12（NODE_MODULE_VERSION 127）**编译的，在 Electron 42 下启动报 `NODE_MODULE_VERSION 146` 不匹配（`ERR_DLOPEN_FAILED`）——**真实复现了决定 007 标注的已知风险**。执行 `npx electron-rebuild -f -w better-sqlite3` 后问题解决，无需手动配置 MSVC 环境变量（该版本走预编译路径，没有触发本机 C++ 编译）。重建后用**真实 `henji.db` 文件副本**验证：`SELECT name FROM sqlite_master` 查出 8 个对象（7 张表 + 1 个 sqlite_sequence），`history` 表查出 276 条真实记录。
     - **发现新表 `canvas_projects`**：之前 1.1/1.2/1.3 的静态代码搜索只找到 `progress_samples`（progress_learning.rs）与 `storyboard_projects`（projectState.ts）两张"隐藏表"；本次对真实数据库跑 `sqlite_master` 查询又发现第 7 张表 **`canvas_projects`**，来自 `src/services/canvasProjects/CanvasProjectService.ts`（第 3 个独立 `Database.load` 连接）。核查确认 `storyboard_projects` 与 `canvas_projects` **均为活跃代码**（分别被项目包导入导出与 `App.tsx`/`projectStore.ts` 引用），不是新旧替代关系，迁移需保留两张表。已回填 3.1 任务文件。
     - **`webContents.startDrag` IPC 通路**：已编写 `henji:start-drag` handler 与 preload 暴露，但本次仅做了代码层面的接线，未做真实 OS 拖放的端到端验证（Playwright 这块能力有限），标记为遗留给 3.8 人工验证。
  2. 用 **Playwright `_electron` API**（而非人工截图/点击）驱动整个验证过程：`test-driver.js` 启动应用、等待自动测试脚本跑完、截图存档、读取 DOM 文本结果、捕获 console/pageerror。期间定位并修复了一个真实 bug：自动测试脚本里 `video.addEventListener('loadedmetadata', ...)` 存在**「错过事件」竞态**（视频在监听器挂上前 metadata 已加载完成，导致 `await` 永久挂起），修法是先判断 `video.readyState >= 1` 再决定是否需要监听事件——这个坑本身对后续 2.4/3.4 的实际业务代码迁移也是一个有价值的提醒（凡是用 Promise 包装 DOM 事件等待，都要先检查事件是否已经发生过）。
- 实际修改文件：新增 `prototype/electron-poc/{package.json,main.js,preload.js,index.html,test-driver.js,.gitignore}`；修改 `docs/task/项目计划/任务/第三阶段-功能迁移/3.1-数据层SQLite迁移.md`（补充第 7 张表）。
- 验证结果：见上方「完成内容」，核心风险点（窗口/协议/数据库）均有真实数据验证通过的截图与 JSON 结果存档（`screenshot.png`/`auto-test-result.json`，已 `.gitignore` 排除不入库）。
- 遗留问题：①`startDrag` 真实拖放未验证，留给 3.8；②macOS 下透明窗口表现未验证（无 Mac 设备），留给 4.1 跨平台验收；③CSP 安全警告（Electron 默认开发期警告，未设置 Content-Security-Policy），2.1 搭建正式工程骨架时需补上 CSP meta 标签。
- 与计划的偏差：未使用 electron-vite 脚手架（原计划步骤 1-2），改为手写最小应用直接验证运行时能力，electron-vite 与现有前端栈的集成验证推迟到 2.1；`startDrag` 验收项从「确认可用」降级为「IPC 通路已接好，真实拖放待 3.8 人工验证」。
- 相关提交：随当前分支提交。
