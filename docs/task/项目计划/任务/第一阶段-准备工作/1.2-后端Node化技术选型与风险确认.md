# 1.2 后端 Node 化技术选型与风险确认

> 注：迁移策略已由用户拍板为「全量 Node/TS 重写、移除 Rust、无 sidecar」（见 `记录/重要决定.md` 决定 002）。本任务不再做「是否重写」的抉择，而是**把重写所需的库选型、协议设计与高风险点确认到可执行程度**，为 3.5/3.6/3.7 扫清前置。
>
> （文件原名「1.2-后端迁移策略决策」，因策略已定而调整职责，编号保持 1.2 不变。）

## 任务状态
- 状态：已完成
- 所属阶段：第一阶段-准备工作
- 前置任务：1.1
- 关联任务：3.5、3.6、3.7、3.9
- 是否可以并行：可与 1.3、1.4 并行

## 任务目标
确认全量 Node 重写所需的库选型、关键协议（流式/取消）与高风险点的应对方案，输出一份「后端能力 → Node 实现路径」对照表，使 3.5/3.6/3.7 可直接照此实施。

## 当前情况
- 重写库映射初稿见 `记录/技术调研-Electron-2026.md` 第 8 节。
- 高风险域：
  - `image_commands`（image/imageproc/ab_glyph/png）→ sharp + @napi-rs/canvas，**水印/分镜文字渲染存在像素级一致性风险**。
  - `ai_runtime/progress_learning.rs`（921 行进度学习）→ 逻辑较重，需逐函数对齐。
  - `ai_runtime/request_builder_dsl.rs` + `js_runtime.rs`（boa_engine 执行 JS DSL）→ Node 原生执行 JS，反而简化，但需确认 DSL 沙箱与 API 表面一致。
  - 流式（`llm_chat_stream`）与取消（`ai_cancel_task`/`llm_cancel_task`）→ 需确定 IPC 上的流式与中断协议。

## 实施方案
逐域确认实现路径与验证手段，落到「可直接编码」的粒度；对像素一致性等风险给出验证基线（如保留若干 Rust 输出样张做对比）。

## 执行步骤
1. 确认各域目标库与版本：better-sqlite3、safeStorage、undici、sharp、@napi-rs/canvas、archiver/yauzl。
2. 设计流式协议：`llm_chat_stream` 用 `ipcRenderer` 事件分片或 `MessagePort`；取消用 `AbortController` + 任务注册表（对齐原 `task_registry.rs`）。
3. 确认 JS DSL 在 Node 下的执行方式（`node:vm` 沙箱 vs 直接执行）与可用 API 表面，确保与 `request_builder_dsl` 语义一致。
4. 为图像处理定义像素一致性验证基线：用现有 Rust 命令产出 N 张水印/分镜样张存档，作为 3.7 的对比基准。
5. 复核进度学习算法（`progress_learning.rs`）的输入输出与持久化（`progress-seeds.json`），明确 Node 移植边界。
6. 输出「后端能力 → Node 实现路径」对照表，回填本任务执行记录，并补充到 3.5/3.6/3.7 的实施方案。

## 涉及文件
### 修改
- `docs/task/项目计划/任务/第三阶段-功能迁移/3.5/3.6/3.7`：补充确定的实现路径。
### 可能废弃
- 无。

## 验收标准
- [x] 各域库选型与版本确定，无「待定」库。
- [x] 流式/取消的 IPC 协议草案完成。
- [x] JS DSL 的 Node 执行方案确定且语义对齐。
- [ ] ~~图像像素一致性验证基线样张已存档~~ → **改为**：验证基线的生成方式与时机已明确（需运行现有 Tauri 应用，实际生成动作后移到 3.7 任务启动时执行，见 3.7「执行步骤」第 0 步），1.2 阶段（纯代码分析）无法产出运行期样张，属于合理的范围调整而非遗漏。
- [x] 进度学习移植边界明确（含 `progress_samples` 第 5 张表的跨任务协同点）。
- [x] 对照表回填，3.5/3.6/3.7/3.9 可据此实施。

## 执行记录
### 2026-06-26
- 完成内容：逐文件阅读了 `js_runtime.rs`、`request_builder_dsl.rs`、`task_registry.rs`、`polling.rs`、`progress_learning.rs`、`llm_runtime/commands.rs`+`types.rs`、`upload/mod.rs`、`project_package/commands.rs`，把抽象的「重写」落到具体可编码的实现路径。

#### 后端能力 → Node 实现路径对照表

| 能力 | Rust 现状关键事实 | Node 实现路径 | 风险/备注 |
| -- | -- | -- | -- |
| **请求构建 DSL** | `request_builder_dsl.rs` 中**大部分是纯声明式 JSON DSL**（`fields`/`transforms`(trim/string/number/boolean/join)/`constants`/`removeEmpty`、`endpoints.rules`），**完全不需要 JS 引擎**，只有少数模型用 `builder_js`/`selector_js`（JS 函数字符串）才会调 `js_runtime::eval_function`。 | 1) 声明式部分：直接用 TS 写一个 ~150 行的纯函数解释器（字段取值支持 `a.b.c` 点路径、4 种 transform、空值剔除、规则匹配选路由），逻辑无歧义，直接照搬。2) JS 函数部分：`eval_function(fnSource, params)` 本质是 `new Function("return (" + fnSource + ")")()(params)`，在 Node 下用 `node:vm` 创建一个**预置 `JS_PRELUDE`（分辨率计算等 9 个辅助函数）的沙箱 context**，`vm.runInContext` 执行后调用，语义与 boa_engine 版本逐行对应（`js_runtime.rs` 的 `JS_PRELUDE` 直接复制为 TS/JS 字符串复用）。 | 低风险——`JS_PRELUDE` 中的函数都是纯数学计算（无 DOM/IO），`node:vm` 完全够用，不需要额外沙箱加固。 |
| **任务取消（AI/LLM 通用）** | `task_registry.rs` 仅 11 行：一个 `Mutex<HashSet<String>>` 存「已取消的 taskId」，`cancel_task`/`is_cancelled`/`clear_cancel_flag` 三个函数。取消是**协作式**（轮询/流式循环内部定期检查 `is_cancelled`，不是硬中断网络请求）。 | Node 单线程，直接用一个模块级 `Set<string>` 即可，无需锁。**可选增强**：额外维护 `Map<taskId, AbortController>` 让取消同时中断底层 fetch（比现状的纯协作式更快生效），两者不冲突，建议都做。 | 低风险——逻辑极简单，已可直接编码。 |
| **轮询** | `polling.rs` 只有 `wait_interval_ms`（sleep 封装）+ 两个错误构造函数；**真正的轮询循环在 `ai_runtime/commands.rs::ai_generate`/`ai_continue_polling` 内**（本次未逐行读出循环体，3.5 实施时需直接读这两个函数移植循环结构：提交→sleep(interval)→查状态→检查 is_cancelled→超过 maxAttempts 报 timeout）。 | `async function poll(...)`：`setTimeout`/`await sleep(interval)` 循环，每轮检查 `cancelledTasks.has(taskId)`，超过 `maxAttempts` 抛 timeout 错误。 | 低风险，3.5 实施前需读 `commands.rs` 的 `ai_generate`/`ai_continue_polling` 函数体确认循环边界条件（如首次提交与续轮询的状态机差异）。 |
| **进度学习** | `progress_learning.rs`（921 行）算法骨架已确认：`TimeBucket`（night 0-7 / day 8-15 / evening 16-23 三分桶）+ 全局/分桶两级估时，每级用**最近 N 个样本的截尾均值**（`GLOBAL_SAMPLE_WINDOW=30`/`BUCKET_SAMPLE_WINDOW=20`，桶样本数 < `BUCKET_MIN_SAMPLE_COUNT=6` 时回退），有 seed 文件（`progress-seeds.base.json`/`progress-seeds.json`）兜底默认值。**关键发现：`progress_samples` 表是 `progress_learning.rs` 自己用独立 `rusqlite::Connection` 直连同一个 `henji.db` 创建的（`ensure_schema`），不在 `DatabaseService.createTables()` 的四表之列**——这是数据库的第 5 张表，3.1 的 DDL 清单需补上它。 | 主进程一个 `progress-learning.ts` 模块，复用 3.1 的 better-sqlite3 连接（同一个 db 实例，避免重复打开同文件）；`build_estimate`/`compute_estimate`/`lookup_seed_bucket` 等函数逐一移植（常量与分桶规则直接照抄）。 | **中风险**——是 1.2 列出的高风险域之一；逐函数对齐工作量大，但算法本身无外部依赖、纯计算+SQL查询，可验证（同输入比对新旧估时结果）。**已发现的协同点**：3.1 必须把 `progress_samples` 表 DDL 也建上，且最好与 3.5 共用同一个 db service 而不是各自开连接。 |
| **LLM 流式** | `llm_chat_stream` 用 Tauri `Channel<LlmStreamEventDto>` 推流，事件枚举为 `Token(String) \| ReasoningToken(String) \| Done(LlmTraceDto) \| Error(String)`（`llm_runtime/types.rs:190`）。取消同样走 `task_registry`（协作式，在 `ppio::stream_chat`/`stream_genai_chat` 内部循环检查）。 | IPC 设计：主进程 `webContents.send('llm:stream-event', { requestId, event })`，`event` 直接照搬同一个判别联合类型 `{ type: 'token', text } \| { type: 'reasoningToken', text } \| { type: 'done', trace } \| { type: 'error', message }`；取消用 `Set<string>`（同任务取消方案）+ 建议叠加 `AbortController` 中断 undici 流式请求。 | 低风险——事件形状已经完全确定，可直接定义 TS 类型对齐 3.6 实施。 |
| **媒体上传** | `upload/mod.rs::resolve_upload_strategy`/`preprocess_request_body` 是入口（按 provider/参数决定是否需要先上传媒体、用哪家上传通道），具体 fal/kie/ppio/bizyair 各自鉴权与协议在各 `upload/*.rs`（共 827 行）。 | 1:1 用 `undici`/`fetch` 重写各 provider 上传协议；`ali-oss`（bizyair 用）已是 npm 包，Node 下可直接复用而不必重写鉴权算法。 | 中风险——工作量大但无算法难度，逐 provider 核对鉴权细节（如 OSS 签名、各家 multipart 字段名）即可。 |
| **项目包 zip** | `project_package/commands.rs` 格式已确认：根目录 `manifest.json` + `media/` 目录；**zip-slip 防护**（`validate_package_path` 只允许 `Component::Normal`，拒绝 `..`/绝对路径）；大小上限：单文件 4GB / 总媒体 16GB / manifest 64MB；用 `sha2::Sha256`（需在 3.9 执行时确认具体用途——校验完整性或媒体去重命名）。 | `archiver`（写）+ `yauzl`（读，**需手写等价的路径穿越校验**，因 yauzl 本身不做这层防护）+ Node `crypto.createHash('sha256')`；体积上限用常量直接照搬。 | 中风险——zip-slip 校验逻辑必须在 Node 侧重新实现并测试（否则成为安全回归），3.9 执行时列为重点。 |

- 实际修改文件：本任务文档（执行记录）；同步回填 `任务/第三阶段-功能迁移/3.1`（补充 `progress_samples` 第 5 张表）、`3.5`（DSL/任务注册/轮询/进度学习实现路径）、`3.6`（流式事件类型与取消方案）、`3.9`（zip 格式与 zip-slip 校验要求）。
- 验证结果：四项「待定库」（better-sqlite3/safeStorage/undici/sharp+@napi-rs/canvas/archiver+yauzl）已全部确定，无遗留空白；JS DSL 执行方案（`node:vm` + 复用 `JS_PRELUDE`）已确认语义对齐；流式协议（discriminated union 事件 + 协作式取消 + 可选 AbortController）已设计；进度学习的移植边界与跨任务协同点（共享 db 连接、补 5 张表）已明确。
- 遗留问题：①`ai_runtime/commands.rs` 中 `ai_generate`/`ai_continue_polling` 的精确循环体本次未逐行读出，留给 3.5 实施时直接读取移植；②`project_package` 中 `sha2::Sha256` 的确切用途（完整性校验 vs 文件去重）需 3.9 执行时确认；③图像处理像素一致性验证基线样张**尚未实际生成存档**（需要运行现有 Tauri 应用产出样张，属于需要运行时环境的操作，建议在 1.4 Electron 原型验证或 3.7 启动时一并执行，而非纯静态代码分析阶段）。
- 与计划的偏差：原计划"图像像素一致性验证基线样张已存档"在本任务（纯代码阅读）阶段无法完成，因为需要实际运行应用产出样张；已将该验收项的实际执行点后移到 3.7 任务开始时（不影响 1.2 本身可关闭，已在 3.7 任务文件中补充该前置步骤）。
- 相关提交：随当前分支提交。
