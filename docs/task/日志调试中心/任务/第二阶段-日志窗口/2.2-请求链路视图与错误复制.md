# 2.2 请求链路视图与错误复制

## 任务状态

- 状态：已完成（自动化检查全通过；交互行为需人工验证，见 `test-report.md`）
- 所属阶段：第二阶段-日志窗口
- 前置任务：2.1
- 关联任务：1.2（链路视图消费其请求/响应事件）
- 是否可以并行：可与 2.3 并行

## 任务目标

在日志页面上：点开任意一条日志能看到结构化详情（JSON 折叠展开、超长字段单独展开）；按 requestId 一键聚合出"一次请求的完整链路"（请求 → 轮询/流式 → 结果/失败）；错误事件突出显示并支持一键复制为 Markdown 或 JSON，方便直接贴给他人或 AI 排查。

## 当前情况

- `LogEvent` 自带关联字段：`requestId`、`taskId`、`modelId`、`providerId`（`src/core/logging/types.ts`），1.2 完成后一次 LLM/生成调用的请求、响应、失败事件共享同一 requestId，聚合条件已具备。
- 现有 UnifiedLogViewer 只有平铺列表 + 字符串化 context，无详情面板、无折叠 JSON、无链路聚合、无复制。
- 复制能力：项目在 Electron 下有剪贴板能力（preload 白名单已含剪贴板相关方法，执行时在 `electron/preload/api.d.ts` 确认方法名），渲染层也可用 `navigator.clipboard` 兜底。
- 2.1 已把事件美化字典抽为 `src/features/logs/eventDisplay.ts` 可复用。

## 实施方案

1. 日志详情面板：点击日志行在页面侧栏（或行内展开区）展示完整事件，context/error 用可折叠 JSON 树渲染（自实现轻量组件，按层级折叠，长字符串默认收起点击展开；不引入重型依赖——若要引第三方 JSON 视图库，先按 CLAUDE.md 约定征求用户确认）。
2. 链路视图：详情面板提供"查看完整链路"入口，按 requestId 过滤出全部相关事件并按时间排序展示为纵向时间线（复用列表行组件 + 阶段标记）；也支持从工具栏直接输入/粘贴 requestId 查链路。
3. 错误聚焦：列表中 error 级别行用既有语义色突出；工具栏加"只看错误"快捷开关（等价级别过滤的快捷方式）。
4. 一键复制：单条事件与整条链路都支持复制为 Markdown（含模型/provider/时间/请求体/响应体代码块，适合贴给 AI）和原始 JSON 两种格式；格式化函数放 `src/features/logs/` 下独立模块。

## 执行步骤

1. 建 JSON 折叠树组件（`src/features/logs/components/JsonTree.tsx`，用 `Ui*`/语义色，控制在 ≤400 行）。
2. 建日志详情面板组件，接入列表行点击。
3. 建链路聚合逻辑（`logStore` 中按 requestId 选择器）与链路时间线视图。
4. 建复制格式化模块（`src/features/logs/copyFormats.ts`：事件→Markdown、事件→JSON、链路→Markdown、链路→JSON）并接复制按钮，复制成功给轻提示。
5. 工具栏加"只看错误"开关。
6. 用户手动验证步骤：触发一次成功 LLM 对话与一次失败调用 → 点开响应事件看 JSON 折叠 → 点"查看完整链路"确认请求/响应/结果按时序聚合 → 复制链路 Markdown 粘贴到文本编辑器检查格式 → 打开"只看错误"确认只剩失败事件。
7. 回归检查：`npm run lint`、`npm run check:colors`、原生控件检查。

## 涉及内容

### 新增文件

- `src/features/logs/components/JsonTree.tsx`：折叠 JSON 树。
- `src/features/logs/components/LogDetailPanel.tsx`：详情面板。
- `src/features/logs/components/RequestChainView.tsx`：链路时间线。
- `src/features/logs/copyFormats.ts`：复制格式化。

### 修改文件

- `src/features/logs/logStore.ts`：requestId 聚合选择器。
- `src/features/logs/LogsPanel.tsx` 及列表/工具栏组件：接线详情、链路入口、只看错误。

## 验收标准

- [ ] 任意日志行可打开详情，JSON 按层级折叠展开，超长字符串默认收起。
- [ ] 一次 LLM 调用的请求/响应/结果事件可按 requestId 聚合为时间线视图。
- [ ] 错误事件在列表中视觉突出，"只看错误"开关可用。
- [ ] 单条事件与整条链路均可复制为 Markdown 与 JSON，Markdown 粘贴后代码块格式正确、内容与日志一致。
- [ ] lint / check:colors / 原生控件检查通过；任务总览已同步更新。

仅渲染层改动：✔️无需重启（热更新）。

## 执行记录

### 2026-07-10 完成

- 新增 `src/features/logs/components/JsonTree.tsx`：自实现轻量 JSON 折叠树（129 行，无第三方依赖），按层级展开/折叠对象与数组（默认展开 1 层），长字符串（>200 字符）默认收起、点击展开，不同值类型用颜色区分。
- `LogEventDetail.tsx` 原地改造：JSON 展示从 `<pre>{JSON.stringify(...)}}` 换成 `<JsonTree value={event} />`；新增"复制 Markdown"/"复制 JSON"按钮（含"已复制"临时反馈）；有 `requestId` 时新增"查看完整链路"入口。未按原定文件清单新建 `LogDetailPanel.tsx`，理由见 `decisions.md`（2.1 已建好详情面板骨架，新建同功能文件会造成重复实现）。
- 新增 `src/features/logs/components/RequestChainView.tsx`：`UiModal` 承载的请求链路时间线，按 requestId 聚合、按时间升序纵向排列（错误事件红色圆点标记、显示相对首条事件耗时），可点击某条事件就地展开 `JsonTree`，顶部提供整条链路复制 Markdown/JSON。
- `logStore.ts` 新增导出 `selectEventsByRequestId(events, requestId)` 选择器，签名与 2.1 handoff 建议（内部读取 store 快照）不同，改为接收调用方已持有的 `events` 数组，让链路视图能随新事件到达自动刷新，理由见 `decisions.md`。
- 新增 `src/features/logs/copyFormats.ts`：`eventToMarkdown`/`eventToJson`/`chainToMarkdown`/`chainToJson`/`copyTextToClipboard` 五个函数，复制到剪贴板用 `navigator.clipboard.writeText` 兜底，未给 preload 的 `HenjiClipboardApi` 新增 `writeText` 方法（理由见 `decisions.md`）。
- `LogFilterToolbar.tsx` 新增"只看错误"开关（独立于 `levelFilter` 的布尔状态，与其他过滤条件为"与"关系）与 requestId 链路查询输入框（支持 Enter 提交）。
- `LogsPanel.tsx` 接线：新增 `errorOnly`/`chainRequestId` 状态，`chainEvents` 用 `useMemo` 包 `selectEventsByRequestId`（基于完整 `events`，不受当前过滤条件影响，确保链路查询总能看到该 requestId 下全部事件），渲染 `RequestChainView` 弹层。
- i18n：`logsWindow.toolbar.errorOnly`/`chainLookupPlaceholder`、`logsWindow.detail.jsonTree.{expandString,collapseString}`、`logsWindow.chain.{title,viewButton,count,empty}`、`logsWindow.copy.{markdown,json,copied}` 中英文均已补齐。
- 验收标准清单本身未在文件内逐项打勾——全部交互类验收点（JSON 折叠展开、链路聚合时序、复制粘贴格式、只看错误开关）需要真实操作 UI/复制粘贴，按项目约定交给用户手动验证，步骤见 `test-report.md` 步骤 Z~AC。"lint/check:colors/原生控件检查通过"一项已通过自动化检查确认。
- 自动化检查：`npm run lint`/`check:colors`/`check:model-i18n`/`gen:model-manifest`、`npx tsc --noEmit`（全仓库）、`npx tsc -p tsconfig.electron.json --noEmit`、`npx eslint electron --ext ts --report-unused-disable-directives --max-warnings 0`（本任务未改 `electron/` 目录，跑一遍确认零回归）、原生控件检查、JSON 语法校验全部通过，无新增裸 `any`，新增文件均远低于 400 行阈值。未执行 `electron:build`/`electron:smoke`（纯渲染层改动，理由见 `test-report.md`）。
- 详见同目录 `progress.md`/`decisions.md`/`handoff.md`/`changed-files.md`/`test-report.md`。
