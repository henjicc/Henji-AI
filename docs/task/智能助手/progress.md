# 智能助手任务进度

## 当前状态

- 当前阶段：第七阶段 · 内置能力全面接入
- 阶段状态：实现完成，待最终统一验证
- 已完成实现：1.1～6.5（6.3 因无真实第三方需求取消；第二、第四至第六阶段真实模型与鼠标交互并入最终统一验收）
- 开始日期：2026-07-23
- 当前任务：等待第七阶段真实 Electron、真实 Provider、鼠标交互和长稳统一验收
- 阻塞问题：无代码阻塞；真实 Provider、鼠标交互、长稳和日志窗口/JSONL 抽查并入最终测试清单

## 阶段进度

| 任务 | 状态 | 说明 |
|---|---|---|
| 1.1 整体架构与执行框架定稿 | 已完成 | `agent-contract/v1`、事件、往返、状态机和进程边界已冻结 |
| 1.2 工具能力清单与权限分级 | 已完成 | MVP 8 工具、画布第二批、7.x 能力目录和 R0～R4 已完成 |
| 1.3 提示词与上下文工程方案 | 已完成 | P0～P8、路由、预算、压缩和 offload 已完成 |
| 1.4 安全边界与威胁模型 | 已完成 | C0～C3、T01～T26、S01～S26 和验收门槛已完成 |
| 2.1 全局导航状态收口 | 实现完成 | 导航、素材库和工具箱子工具已收口；待用户交互复核 |
| 2.2 宿主命令面与前端动作工具桥 | 实现完成 | context/command/query、frontend 往返和可见生成任务命令已落地 |
| 2.3 模型适配层支持工具调用 | 实现完成 | AI SDK 单步文本/tool/object/usage/cancel 路径与测试已完成 |
| 2.4 智能助手模型配置与能力验证 | 实现完成 | Agent Profile、能力编辑、真实 smoke 入口和 fallback 判定已完成；待真实模型验证 |
| 3.1 运行时骨架与事件流 | 已完成 | 唯一 Runner、状态机、预算、事件流、控制 IPC 与单 thread 并发已落地 |
| 3.2 工具注册表与工具网关 | 已完成 | 权威注册表、固定网关管线、审批、幂等及首批 8 工具已落地 |
| 3.3 上下文构建与意图路由 | 已完成 | revision 上下文、确定性优先路由、渐进目录、压缩与 offload 已落地 |
| 4.1 侧边栏容器与停靠悬浮拖拽 | 待验证 | 真实宽高逐帧排版、局部工作区避让与离屏内容隔离已实现，待鼠标复验 |
| 4.2 对话与执行可视化 | 待验证 | 事件重放、流式对话、工具/审批卡、运行控制和结果跳转已实现 |
| 4.3 错误诊断入口 | 待验证 | 受限日志证据查询、诊断提示规则和全局/生成错误入口已实现 |
| 5.1 首用例端到端联调 | 待验证 | 生成/诊断闭环、C2 审批、日志与结果跳转已接线；待真实 Provider 验收 |
| 5.2 护栏与验收 | 待验证 | 确定性护栏测试与清单已完成；待真机取消、审批、重载和日志抽查 |
| 5.3 画布第二批闭环 | 待验证 | 两类代表性节点的目录、添加、连接、定位、撤销和冲突保护已完成 |
| 5.4 最小评测与阶段验收 | 待验证 | 最小评测 runner 与三类重复用例已完成；待真实模型基线 |
| 6.1 运行状态与检查点持久化 | 已完成 | SQLite migration、run/event/artifact/checkpoint、历史与安全重试已落地 |
| 6.2 评测集与回归 | 已完成 | 黄金、历史失败、对抗评测与持续回归指标已落地 |
| 6.3 第三方工具协议扩展 | 已取消 | 当前没有真实第三方需求；按可选任务约束不空造 MCP 客户端 |
| 6.4 运行时独立进程迁移 | 待验证 | utilityProcess、版本握手、心跳、RPC、崩溃隔离与重启已落地 |
| 6.5 会话记忆与隐私管理 | 待验证 | 默认关闭、候选确认、scope/TTL/冲突/删除与渐进检索已落地 |
| 7.1 生成与模型目录能力全面接入 | 实现完成，待验证 | 生成准备/模型目录/可见任务状态与取消接入，模型 schema 按需发现 |
| 7.2 画布与项目能力全面接入 | 实现完成，待验证 | 项目、节点、边、批量 plan/preview/commit/undo 与 revision 冲突保护已接入 |
| 7.3 工具箱与素材能力全面接入 | 实现完成，待验证 | 工具箱、3D、分镜、图片编辑、素材稳定 ID 工具与审批已接入 |
| 7.4 跨工作区编排与撤销恢复 | 实现完成，待验证 | 3 个确定性工作流、步骤引用、暂停/恢复/取消、冲突与补偿已接入 |

## 最近更新

### 2026-07-24

- 收口第七阶段剩余工具注册：补齐 3D 工程重命名/删除、3D 对象复制/删除/更新、图片编辑预览/提交、分镜项目 list/get，并在 utility registry 中随同一网关注册。
- 新增共享图片编辑契约，标记类型、坐标、尺寸、样式和裁剪边界均在工具入口严格校验；不合法标记不再以宽松对象进入图片编辑业务。
- 3D/分镜项目查询不再返回完整 `sceneJson/nodesJson/edgesJson`；改为 schema/editor 摘要、稳定对象/镜头/节点/边 ID、计数、最多 32 项受限详情和字节数，避免大结果进入 Agent 上下文。
- 第七阶段 7.1～7.4 代码与自动化收口完成；`npm test -- --run` 80 个测试文件、363 个用例通过（2 个文件、6 个用例按环境跳过），`npm run test:assistant-eval` 9 个文件、33 个用例通过。
- `npm run lint`、Electron ESLint、renderer/Electron TypeScript、`npm run electron:build`、`npm run electron:assistant-utility-smoke`、`git diff --check` 均通过；真实 Provider、真实 Electron 鼠标交互、长稳和日志窗口/JSONL 抽查留待最终统一手动验收。
- 最终复验新增素材编辑工作流：`asset_edit_to_canvas` 不再只创建空上传节点，新增 `add_asset_to_canvas` 并通过 `assetId` 将生成素材绑定到画布节点；重新执行 Electron 构建、utility 握手、全量测试和助手评测均通过。

### 2026-07-23

- 启动第七阶段：已重新读取 7.1～7.4 与五份阶段记录；先建立现有领域能力矩阵和稳定宿主命令，所有写操作继续经过统一网关、schema、revision 与审批，不复制业务实现。
- 完成第六阶段代码：Agent run/thread/event/checkpoint/artifact/permission audit 使用版本化 SQLite 独立表；重启或进程崩溃后的活动 run 进入 `recovery_required`，未知副作用不自动重放。
- Runner、上下文、模型单步执行和工具网关已迁移到 `utilityProcess`；main 通过版本化 RPC 保留凭据、宿主工具和持久化边界，支持握手、心跳、取消、日志转发、崩溃隔离与自动拉起。
- 长期记忆默认关闭，写入采用候选 → 用户确认；拒绝秘密、完整日志/文件/提示词与安全注入，支持作用域、TTL、冲突替换审计、查看编辑删除清空，并只渐进披露少量相关条目。
- 回归评测扩展为黄金、历史失败、对抗三类，新增工具/嵌套参数/批准风险/安全/日志/费用指标；SQLite 真库测试 6/6、相关 Vitest 21/21、utilityProcess 构建与握手 smoke、renderer/Electron TypeScript 均通过。
- 第六阶段结束记录已更新；6.4 的 30～60 分钟长稳、6.5 的真实交互和真实 Provider 数据统一留到第八阶段最终测试，下一步进入第七阶段。
- 启动第六阶段：已重新读取 6.1～6.5、五份阶段记录和现有 DB/Runtime/PAL 边界；执行顺序固定为 6.1 → 6.2 → 6.4 → 6.5。
- 6.3 明确跳过：当前没有用户指定的第三方 Server、安装来源、授权范围或真实工具需求，按任务自身阻断规则不得为“完成阶段”空造 MCP 客户端。
- 完成第五阶段手动反馈收口：自然语言生成、诊断、画布和用户指令不再依赖不断扩张的关键词表，统一由 router 模型按完整语义分类；本地只保留带稳定标识的快捷命令、工具域映射、权限和安全边界。
- 修复 KIE `fetch failed` 可观测性与安全恢复：日志保留底层网络错误码，仅对能证明连接尚未建立的故障重试一次；连接状态不明确的 POST 绝不自动重试，避免创建重复计费任务。
- 助手输入区新增“每次询问 / 助手判断 / 充分访问”三种批准方式；R4 始终禁止，充分访问下 R3 仍要求确认。诊断只读查询在“助手判断”下可自动执行，并限制每个 run 一次、最多 16 条证据。
- 修复 AI SDK system message 警告：系统规则改走 `streamText.system`，历史消息不再携带 system role，且显式禁止 messages 中的 system。
- 对话结果启用 GFM Markdown，计划改为默认折叠的执行思路，工具活动压缩为单行；移除大结果占位卡，流式滚动不再使用 smooth，长对话增加布局与绘制隔离。
- 可见生成任务的返回语义改为 `submitted`，助手只能称“已提交/生成中”，不能在 Provider 完成前宣称生成成功。
- Seedance 2.0 系列生成音频默认值由开改为关：普通画面请求不再无意生成音轨并触发供应商音频审核；用户明确开启时仍按原字段请求。
- 定向测试 6 文件、35 用例及全量测试 75 文件、348 用例通过；renderer TypeScript、前后端 ESLint、颜色与模型 i18n 检查通过，Electron TypeScript 在补齐测试字面量类型后通过，真实窗口表现留待最终统一验收。
- 修复真实 run `8aa28be8` 的图片生成误路由：原目标中的“照片”未命中窄生成词表，DeepSeek router 输出又未通过结构校验，导致错误降级为只有能力目录的 `general/catalog` 路径。
- 图片、照片、插画、海报、头像、壁纸、封面、视频和音频等自然表达现在由本地确定性规则直接进入 `models + generation + navigation`；router 模型只分类 intent/complexity，工具域和执行路径由本地权威策略决定。
- 应用能力目录改为多词、媒体同义词和真实 category 枚举检索；目录发现的权威工具会在下一轮渐进披露，生成工作区尚未就绪时可先切换工作区继续，不再把暂不可用误报为“没有生成能力”。
- 模型目录查询明确区分内容 prompt 与模型筛选：剪纸风格、猫咪等内容不再作为模型 query；未指定模型时使用空 query + mediaType。助手评测 8 文件、30 用例，全量测试 73 文件、340 用例及前后端 lint、TypeScript、颜色/i18n、Electron build 全部通过。
- 将结构化模型偏好面板替换为自然语言“用户指令”：设置页统一使用 PromptEditor，主进程权威文件改为 `user-instructions.md`，每次 run 在 main 侧重新读取；开发期旧 `model-preferences.json` 已彻底退出读取与迁移链路，磁盘遗留文件不会影响运行。
- Agent 新增 `get_user_instructions` / `update_user_instructions` 受控工具；只有用户明确要求长期保存偏好或工作习惯时才能经 R2 审批更新，临时要求、敏感内容和模型推断不得自动写入。
- 明确当前尚无助手自动管理的长期记忆：run/checkpoint、thread history、用户主动指令、agent-managed memory 分层治理属于 6.1/6.5；记忆相关性检索与渐进披露在 6.5 实现，不在第五阶段建立临时平行系统。
- 用户指令作为最多 4000 字、带明确边界标记的 P3 上下文注入；优先级为“安全与真实能力硬约束 > 用户当前明确要求 > 持久化用户指令 > 通用模型描述与系统默认倾向”。用户指令只自动脱敏密钥、令牌、授权头、密码等秘密信息，普通路径、网址和自然语言完整传递。
- 现有确定性路由、最多 8 个 active tools、目录/schema 按需发现、历史压缩和大结果 offload 保持不变；P8 日志、文件和工具结果继续使用更严格的外部观察脱敏规则。
- 本轮定向测试 3 文件、18 用例，助手评测 7 文件、25 用例，全量测试 72 文件、335 用例通过；前后端 lint、TypeScript、颜色/i18n、`git diff --check` 和 Electron build 通过，设置与真实对话行为待用户手动验收。
- 完成第五阶段模型选择补充优化：65 个供应商模型统一引用 43 个跨供应商通用模型标识，模型定性描述集中到单一目录，供应商模型文件和模型 i18n 不再重复维护描述。
- 新增主进程权威的助手模型偏好文件、设置页、IPC/PAL/command 与受控 Agent 读写工具；用户当前要求、持久偏好、通用描述按固定优先级参与图片/视频/音频模型选择。
- 两套模型适配 Skill 已同步 `canonicalModelId` 与中央描述检查规则；全量测试 72 文件、332 用例，前后端 lint、TypeScript、模型/i18n、颜色、Skill 校验和 Electron build 均通过，真实设置与对话偏好仍待用户手动验收。
- 完成第五阶段 5.1～5.4 代码实现：生成/诊断闭环加固、C2 诊断外发审批、审批日志、画布节点目录与确定性添加/连接/定位/撤销、结果跳转和最小评测 runner 全部接线。
- 新增 `npm run test:assistant-eval`；7 个评测相关测试文件、22 个用例通过，全量 `npm test` 70 个文件、325 个用例通过，前后端 lint、TypeScript、颜色/i18n 与 Electron build 通过。
- 三类确定性评测各重复 3 次并达到 100%；真实 Provider 成功率/延迟/token、Electron 鼠标交互及 JSONL 脱敏抽查尚未执行，第五阶段保持“待验证”。
- 完成 4.1 实时排版性能修复：撤销 `scale3d` 尺寸预览，尺寸帧在 `requestAnimationFrame` 中直接更新助手局部 `width/height`；左右停靠同步更新工作区局部 padding，拖动期间文字、卡片与工作区均按真实尺寸实时排版。
- 高频尺寸值只保存在 ref 并直接批量写入必要 DOM，不触发 React 重渲染或 Zustand 持久化；助手继续使用布局/样式 containment，长对话块增加 `content-visibility: auto`，离屏计划、工具、审批与 Markdown 不参与每帧布局和绘制。
- 定向测试更新为真实布局与工作区避让约束；全量 `npm test` 通过（66 文件、312 用例），renderer/Electron TypeScript、lint、颜色/i18n、旧缩放路径静态检查、文件体积、`git diff --check` 与 `npm run electron:build` 全部通过。
- 按桌面交互约定未代替用户执行鼠标性能复验；4.1 与第四阶段恢复“待验证”。
- 收到第三轮交互反馈：合成层缩放会让内容在拖动期间变形，不符合实时排版要求；重新进入 4.1，研究局部真实尺寸更新、工作区边界隔离和长对话离屏布局优化。
- 完成 4.1 尺寸性能二次修复：移除每帧根级宽高变量和工作区 padding 更新；左右停靠与悬浮缩放期间只更新已提升为合成层的面板 `transform`，松手时才提交真实宽高、工作区避让和持久化状态。
- 面板增加布局/样式 containment，尺寸临时值继续保存在 ref；静态核对确认运行时代码已无 `--assistant-panel-width/height` 写入或消费。
- 修复后全量 `npm test` 通过（66 文件、312 用例），renderer/Electron TypeScript、lint、颜色/i18n、`git diff --check` 与 `npm run electron:build` 通过；实际鼠标跟手性待用户复验。
- 收到第二轮交互复验反馈：左右停靠和悬浮态调整尺寸时整个窗口严重卡顿；重新进入 4.1，重点隔离每帧根级样式失效与工作区布局重排。
- 完成 4.1 手动验收反馈修复：悬浮拖动不再在每个 `pointermove` 重渲染完整对话树，改为 `requestAnimationFrame` 合并事件并通过 `translate3d` 直接更新；交互结束后才提交持久化状态。
- 左右停靠新增内侧边缘宽度调整；悬浮态新增右边缘、下边缘和右下角宽度/高度/双向调整，尺寸实时驱动工作区避让并在结束后持久化。
- 修复后全量 `npm test` 通过（66 文件、311 用例），renderer/Electron TypeScript、lint、颜色/i18n、`git diff --check` 与 `npm run electron:build` 通过；鼠标手感与三种形态尺寸调整待用户复验。
- 收到第四阶段手动验收反馈：悬浮拖动存在轻微滞后，左右停靠缺少宽度调整，悬浮态缺少宽高调整；重新进入 4.1 交互优化。
- 完成第四阶段 4.1～4.3 实现：全局助手侧边栏、运行轨迹可视化、审批/预算/控制、事件快照恢复和受限错误诊断入口全部接线。
- 全量 `npm test` 通过（66 文件、309 用例），前后端 lint、renderer/Electron TypeScript、颜色/i18n、`git diff --check` 与 `npm run electron:build` 通过。
- 按桌面交互约定未代替用户执行停靠、拖拽、错误入口和真实 Provider 对话验收；第四阶段保持“待验证”，验收通过后进入 5.1。
- 启动第四阶段，已读取 4.1～4.3 任务定义、总览/实施方案/重要记录和五份阶段记录；先执行 4.1 全局侧边栏容器。
- 完成第三阶段 3.1～3.3：唯一 Runner、严格事件流、预算/取消/审批状态机、权威工具网关、首批 8 个工具、上下文构建与确定性优先路由全部接线。
- 全量 `npm test` 通过（62 文件、300 用例），前后端 lint、renderer/Electron TypeScript、颜色/i18n、`git diff --check` 与 `npm run electron:build` 通过。
- 第三阶段不新增可直接操作的 UI；真实 Agent 对话与执行可视化将在第四阶段接入，生成/诊断端到端由第五阶段验收。
- 启动第三阶段，已读取 3.1～3.3 任务定义和五份阶段记录；先执行 3.1 唯一 Runner 与事件流。
- 修复 DeepSeek capability smoke 将 JSON 对象能力误当成原生 JSON Schema 能力的问题；`json` 模式改发 `json_object`，`schema` 模式才发 `json_schema`。
- capability smoke 通过后会把实际验证成功的文本、流式、工具、结构化输出和 usage 同步到模型静态能力，避免动态通过但静态仍为“否/none”。
- 修复后全量 `npm test` 通过（56 文件、283 用例），前后端 lint、TypeScript、颜色/i18n 与 `npm run electron:build` 通过；待用户重启并重新点击真实模型验证。
- 完成第二阶段 2.1～2.4 实现：全局导航、宿主命令面、前端工具桥、AI SDK 单步适配、Agent 模型档案和 capability smoke。
- 全量 `npm test` 通过（55 文件、280 用例），前后端 lint、TypeScript、颜色/i18n 与 `npm run electron:build` 通过。
- `npm run electron:smoke` 唯一失败为用户历史外部媒体路径未在 `henji-media://` 白名单导致的 403，与本阶段改动无关；保留用户数据不处理。
- 按桌面交互约定未代替用户执行鼠标验收，也未用真实 API Key 发起付费 capability smoke。
- 启动第二阶段，读取阶段任务定义、第一阶段交付物索引和五份阶段记录，开始 2.1 导航状态收口。
- 完成第一阶段全部 4 个任务，任务总览已切换到第二阶段待开始。
- 新增四份设计交付物，并把跨任务决定同步到 `重要记录.md` 009～010。
- 完成 Markdown 链接、关键路径、关键术语和 `git diff --check` 验证。
- 启动第一阶段，建立阶段级任务记录。
- 开始读取 1.1～1.4 任务定义并核对代码基线。
