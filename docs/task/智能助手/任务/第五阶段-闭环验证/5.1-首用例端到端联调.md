# 5.1 首用例端到端联调

## 任务状态

- 状态：待验证
- 所属阶段：第五阶段 · 闭环验证
- 前置任务：3.2、3.3、4.2、4.3
- 关联任务：5.2、5.3、5.4、6.1、6.2
- 是否可以并行：否（收敛前四阶段成果）

## 任务目标

选定并打通首个真实用例的端到端闭环：用户在侧边栏用自然语言提出请求 → 意图路由 → Agent 循环 → 工具网关执行（前端 / 后端）→ 界面产生用户可见结果 → 侧边栏可视化。验证"模型 + Harness + 两侧工具 + 宿主改造"整条链路真实可用。

## 当前情况

- 前置能力已就绪：导航、宿主命令/快照、模型适配/Profile、Runner、网关、上下文、侧边栏和诊断。
- 记录 004 已确定本任务覆盖“生成可见记录 + 日志诊断”；画布由 5.3 单独验收，不再重复确认范围。
- MVP 工具集见 1.2 工具清单。

## 实施方案

端到端串联两个已定用例。生成必须走 2.2 的宿主命令并返回 taskId；诊断必须走 main 受限日志查询。联调同时核对 `requestId=runId/taskId=toolCallId` 的日志链路，不以“UI 看起来成功”替代服务状态和日志证据。

## 执行步骤

1. 从 1.2 取生成/诊断所需最小工具集，固定模型 Profile、输入和前置条件。
2. 冷启动真实 Electron，从侧边栏分别发起生成与诊断请求，逐段观察事件流、宿主命令、工具结果和日志。
3. 修补断点：确保意图路由选对链路、模型正确产生工具调用、网关校验 / 审批正常、前端 / 后端工具正确执行、结果在界面可见并可跳转。
4. 针对"生成页产生用户可见记录"的用例，核对生成记录与手动生成的一致性（记录结构 / 展示 / 可回放）。
5. 针对"读日志诊断"的用例，核对诊断结论与真实错误一致。
6. 用日志窗口/查询按 runId 串联：路由、模型、工具、frontend action、诊断、完成/失败；确认不含敏感原文。
7. 整理两个用例的标准操作脚本与期望结果，作为 5.4/6.2 黄金用例。
8. 写清真实 Electron 手动验证步骤交用户验证。

## 涉及内容

本任务以联调与缺口修补为主，新增文件应尽量少。

### 修改文件

- 视断点落在哪一层，可能小幅修补 2.2 / 3.1 / 3.2 / 3.3 / 4.2 的接线（不新增能力，只补缝）。

### 新增文件

- `docs/task/智能助手/任务/第五阶段-闭环验证/首用例操作脚本.md`：首用例的标准操作与期望结果（供 6.2 复用）。

## 验收标准

- [ ] 首用例可从侧边栏一句话请求端到端跑通，界面产生正确的用户可见结果。
- [ ] 若为生成用例：助手产生的生成记录与手动生成一致地出现在生成工作区并可跳转。
- [ ] 若含诊断用例：读日志诊断结论与真实错误一致。
- [x] 工具调用经统一网关、Runner 事件流与现有 Trace/结构化日志链路执行；侧边栏真实可视化待手动复验。
- [ ] 每个用例可按 runId 在现有日志系统还原路径，前后端 toolCallId 对得上且已脱敏。
- [ ] 首用例操作脚本已沉淀；用户手动验证通过；任务总览已同步更新。

## 执行记录

### 2026-07-23

- 完成内容：完成生成与诊断首用例的闭环补缝，统一真实可见生成任务、受限诊断、审批、日志和结果跳转；沉淀标准操作脚本。
- 实际改动：Runner 在同一模型批次内传递最新 scope revision；诊断日志外发按 C2/R2 逐次展示预览并审批；审批请求/决定/过期补齐结构化日志；结果引用支持画布节点与边。
- 验证结果：最小评测 7 个文件、22 个用例通过；全量 70 个文件、325 个用例通过；前后端 lint、TypeScript、颜色/i18n 和 Electron build 通过。
- 遗留问题：尚未使用真实 Provider 和真实 Electron 窗口执行生成、诊断、侧边栏事件可视化与 JSONL 关联验收，因此用户可见结果、结论准确性和日志脱敏项仍保持未勾选。
- 与原计划的偏差：未新建 Trace 或诊断通道，继续复用现有日志系统；自动化先覆盖确定性边界，付费/随机 Provider 验收交由用户手动执行。
- 对后续任务的影响：操作脚本和最小评测用例可直接由 5.4、6.2 复用。
- 相关提交：`8b3d6a4 feat: 完成智能助手第五阶段闭环验证`。
