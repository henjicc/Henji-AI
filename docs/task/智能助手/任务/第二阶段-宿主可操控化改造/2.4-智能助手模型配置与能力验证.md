# 2.4 智能助手模型配置与能力验证

## 任务状态

- 状态：实现完成，待真实模型与 UI 手动验收
- 所属阶段：第二阶段 · 宿主可操控化改造
- 前置任务：1.1、2.3
- 关联任务：3.1、3.3、4.2、5.4、6.2、8.1
- 是否可以并行：可与 2.2 后半段并行

## 任务目标

为智能助手建立独立、可配置、可验证的模型 Profile，覆盖 primary/router/summarizer/fallback，并记录真实 token usage、延迟和可证实费用。防止用户选择不支持工具的模型后进入不可解释失败。

## 当前情况

- `LlmConfigState` 只有提示词优化 profile；模型能力已有 text/image/video/audio/streaming/toolCall/jsonOutput，但内置默认 toolCall/jsonOutput 为 false。
- 设置页可编辑部分模型能力，但没有 Agent 角色、fallback、上下文长度、parallel tools、usage/cost 和 capability smoke。
- 现有 LLM Trace 主要记录输入/输出字符数，不能执行精确 token 预算。

## 实施方案

在现有 LLM 配置体系上新增 `AgentModelProfile`，不复制 Provider/API Key 管理。Profile 保存模型角色和运行策略，不保存密钥。静态能力来自配置，动态 smoke 记录实际验证结果、时间和错误；运行前两者共同判断。价格未知时显示“未知”并只执行 token/调用/时长预算，不伪造费用。

## 执行步骤

1. 定义 AgentModelProfile：primary/router/summarizer/fallback、模型参数、超时、重试、上下文/输出预算。
2. 扩展能力模型：toolCall、parallelTools、structuredOutputMode、reasoning、sampling、contextWindow、maxOutputTokens、usage。
3. 在设置页复用现有 Ui* 增加智能助手模型选择和能力状态；不新建平行 Provider/密钥配置。
4. 实现 capability smoke：最小文本、tool-call、Output.object、stream、usage、cancel；结果带 verifiedAt/provider/model/version。
5. 启动 run 前校验 primary 能力；不满足时自动选择已验证 fallback 或给出明确设置入口，不能静默降级成无工具聊天。
6. router/summarizer 缺失时允许复用 primary；确定性路由命中时不调用 router。
7. 采集 input/output/reasoning/cache token、调用次数和延迟；价格有可靠来源时估算费用，否则标记 unknown。
8. 接入现有日志：Profile 选择、能力验证、fallback 和 usage 使用 `requestId=runId` 或独立验证 requestId，带 modelId/providerId；不记录 API Key/测试 prompt 原文。
9. 写配置迁移、能力判断、fallback、usage 和设置 UI 测试。

## 涉及内容

### 新增文件

- AgentModelProfile 类型、校验、能力 smoke 与设置子组件，路径按现有 LLM 分层落地。

### 修改文件

- `src/core/llm/types.ts`、defaults、`LlmConfigService`：配置与兼容迁移。
- `src/components/Settings/sections/LlmSettingsSection.tsx`：Agent 模型角色和验证状态。
- 2.3 SDK adapter：暴露 usage/capability smoke 所需接口。

## 验收标准

- [x] primary/router/summarizer/fallback 可配置并复用现有 Provider/密钥。
- [x] run 前选择函数能阻止静态/动态能力不合格的模型，只选择已验证 fallback，否则返回明确设置入口。
- [ ] tool-call、结构化输出、流式、usage 和取消有实际 smoke 结果，不只按名称猜测。
- [x] smoke 会采集真实 token/延迟；费用未知时固定显示 unknown，不显示虚假精度。
- [x] Profile 更新、验证和 model-step usage 可进入现有日志系统且脱敏；fallback 返回显式原因供 3.1 记录。
- [ ] 配置迁移、静态检查和 UI 手动验证通过；总览已同步。

## 执行记录

### 2026-07-23

- 真实验收修复：DeepSeek 首次 smoke 的结构化输出因误发 `json_schema` 失败；适配层现已区分 `json/schema`，JSON 对象模式使用 `json_object` 并继续由 `Output.object()` 校验。
- 配置联动：smoke 通过后同步提升已实测的静态能力，解决“动态工具通过但静态仍为否、结构化仍为 none”的状态分叉。
- 修复验证：全量 56 个测试文件、283 个用例以及前后端 lint、TypeScript、颜色/i18n、Electron 构建均通过；真实 DeepSeek 结果待重启后由用户再次点击验证。
- 完成内容：新增 AgentModelProfile、四角色与运行策略；扩展静态能力编辑；实现文本/tool/object/stream/usage/cancel 真实 smoke 与结果持久化。
- 实际改动：设置页新增模型角色/验证状态区，复用现有供应商与密钥；新增配置迁移、角色继承、能力门槛和 fallback 纯函数。
- 验证结果：capability smoke 聚合、Profile/fallback、旧配置迁移共 6 个用例通过；全量 280 用例与完整构建通过。
- 遗留问题：未擅自使用用户 API Key 发起付费真实 smoke；该验收项和设置页交互由用户显式执行。
- 与原计划的偏差：费用模型没有可靠价格源，因此按方案只保存 `unknown`；router/summarizer 未配置时复用 primary。
- 对后续任务的影响：3.1 启动 run 前必须调用 `selectAgentExecutionModel()`，并记录 fallback 事件；5.4/6.2 再用真实评测校准预算。
- 相关提交：第二阶段统一提交。
