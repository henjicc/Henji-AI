# 2.4 工程模式接入与专业功能收敛

## 任务状态

- 状态：已完成（待主控复核）
- 所属阶段：第二阶段-简易模式界面
- 前置任务：2.2
- 关联任务：3.2（烘焙按钮挂在本任务的模式标识区）
- 是否可以并行：可与 2.3 并行

## 任务目标

打通"工程级模式"的完整用户路径：新建工程时选择简易/专业（默认简易）；编辑器顶栏显示模式标识；简易模式下收敛专业专属 UI（属性面板码表、时间轴专业入口）。完成后用户不再需要控制台命令即可进入简易模式。

## 当前情况

- 工程列表 `projects/CameraStageProjectList.tsx`（210 行）：新建工程入口，调 `createNewProject(name)`（`cameraStageProjectService.ts`）→ `newScene` + 保存。需要加模式选择（默认简易），`createNewProject` 增加 mode 参数。
- 编辑器顶栏 `CameraStageEditor.tsx`（459 行，已超 400——**新增 UI 尽量抽小组件文件**）：现有布局为 返回/撤销重做/视角切换/QuickAddGroup/gizmo/右侧截图导出。模式标识放顶栏（如项目名旁的小徽标"简易"/"专业"）。
- 属性面板码表：`panels/PropertyPanel.tsx`（221 行）与 `timeline/KeyframeStopwatch.tsx`；码表在属性行的渲染点执行时定位（搜 `KeyframeStopwatch` 引用），简易模式下隐藏（简易用户不打关键帧）。同理检查 `panels/CameraSettingsSection.tsx`、`panels/CharacterPoseSection.tsx` 内的码表。
- 时间轴槽位切换已在 2.2 完成（dock 内按 editorMode 渲染）。
- 工程列表徽标：工程记录 summary（`platform/contracts/cameraStageProjects.ts`）不含 sceneJson，列表上显示模式徽标需要动 DB 列或加载时解析——一期**不做列表徽标**（见重要记录 005，待确认），只在编辑器内显示模式。
- 旧工程（v10 及以下）加载后 editorMode='pro'（1.1 已处理），本任务只需保证 UI 正确跟随。

## 实施方案

- `createNewProject(name, mode: StageEditorMode = 'simple')`：simple 时 `newScene` 后自动加"片段 1"初始卡（调 2.1 的 addShot）。
- 工程列表新建流程：新建按钮弹出两选一（简易模式-推荐 / 专业模式），复用现有 `PanelTrigger`/`Dropdown` 交互模式。
- 顶栏模式徽标：小组件 `simple/EditorModeBadge.tsx`（显示"简易"或"专业"，简易时附"转为专业工程"入口占位——3.2 实装，本任务先放禁用项或隐藏）。
- 码表收敛：`KeyframeStopwatch` 或其渲染处按 `editorMode` 返回 null（改一处优于改多处——若码表统一经 KeyframeStopwatch 渲染，直接在该组件内读 store 判断）。
- FPS/时长设置：专业时间轴的 duration/fps 编辑在简易模式不可达（时间轴已换面板），无需额外处理；确认导出视频读取的 `animation.duration/fps` 在简易模式由编译器维护正确。

## 执行步骤

1. 搜 `KeyframeStopwatch` 全部引用点，确认收敛方案（组件内判断 or 调用处判断），实施并保证专业模式不变。
2. `cameraStageProjectService.ts`：`createNewProject` 加 mode 参数；简易初始卡。
3. `CameraStageProjectList.tsx`：新建入口加模式选择（默认简易，带一句说明文案）。
4. 新建 `simple/EditorModeBadge.tsx` 并接入 `CameraStageEditor.tsx` 顶栏。
5. 快速检查 + 写用户手动验证步骤：新建简易工程默认带片段 1 → 属性面板无码表 → 时间轴为镜头卡面板；新建专业工程/打开旧工程 → 一切如旧；重启应用重新打开简易工程模式保持。

## 涉及内容

### 新增文件

- `src/features/cameraStage/simple/EditorModeBadge.tsx`：顶栏模式徽标。

### 修改文件

- `src/features/cameraStage/projects/cameraStageProjectService.ts`：createNewProject 模式参数。
- `src/features/cameraStage/projects/CameraStageProjectList.tsx`：新建时模式选择。
- `src/features/cameraStage/CameraStageEditor.tsx`：顶栏徽标接线（控制增量，必要时抽子组件）。
- `src/features/cameraStage/timeline/KeyframeStopwatch.tsx` 及/或其调用处：简易模式隐藏。

## 验收标准

- [x] 新建工程可选模式、默认简易且自动带初始卡；模式随现有 v11 序列化保存。
- [x] 简易模式下属性面板无码表、时间轴复用镜头卡面板；专业与旧工程路径保持不变。
- [x] 顶栏模式徽标正确显示，并为 3.2 保留禁用的单向转换入口。
- [x] lint / tsc 通过；`CameraStageEditor.tsx` 仅增加 2 行接线；任务总览已同步更新。

## 执行记录

- `createNewProject(name, mode='simple')` 接入工程模式，简易模式利用既有 `setEditorMode` 自动创建“片段 1”，并补齐创建 start/completed/failed 结构化日志。
- 工程列表通过现有 `UiModal`/`UiOptionButton` 提供默认简易、可选专业的新建流程，不新增 primitive。
- `KeyframeStopwatch` 统一按 `editorMode` 隐藏，避免修改多个属性面板调用点。
- 新增 `EditorModeBadge`，简易模式的“转为专业工程”保持禁用，未实现 3.2 烘焙。
- 自动验证：40/40 单测、lint、前后端 tsc、颜色检查、原生控件检查、`git diff --check` 全通过。
- 真实 Electron 鼠标交互按项目约定未由 agent 执行，步骤见 `test-report.md`。
