# 1.3 摄像机运镜预设编译

## 任务状态

- 状态：已完成
- 所属阶段：第一阶段-数据与编译核心
- 前置任务：1.2
- 关联任务：2.3（细节层 UI 暴露预设选项）
- 是否可以并行：可与 1.4 并行

## 任务目标

实现摄像机"运镜行为预设"的编译：用户在过渡上声明 `环绕 / 推近 / 拉远 / 横移 / 升降`（相对取景目标），编译器将其展开为一串密集位置关键帧，替代两点直插。完成后镜头卡两卡之间可产出正确的弧线环绕、匀滑推拉，单元测试验证几何正确性。

## 当前情况

- 摄像机对象 `StageCameraObject`（`domain/sceneTypes.ts`）已有 `lookAt: { mode:'manual', target } | { mode:'object', objectId, fallbackTarget }`——运镜预设的"目标点"直接取 lookAt 解析结果，object 模式取目标对象在**对应时刻快照中的位置**（注意目标自身也可能在两卡间移动，一期按"取过渡起始卡的目标位置"简化，记入注释）。
- 摄像机朝向：渲染层由 lookAt 驱动（见 `scene/CameraModel.tsx` / `StageViewportCamera.tsx`，执行时确认），位置轨道动了之后朝向自动跟随 lookAt，所以**只需生成位置关键帧**；若发现摄像机朝向依赖 transform.rotation 轨道，先在执行记录中记录实际机制再决定是否需旋转关键帧。
- `StageCameraMove` 判别联合骨架由 1.1 定义，本任务定稿参数并实现编译。
- 编译入口挂点：1.2 的 `shotCompiler.ts` 已为 `cameraMoves[cameraId]` 留接口（direct 直插）。
- 数学工具：项目有 three（`@react-three/fiber`），但 domain 层禁止依赖 three——用纯数学实现（Vec3 加减/绕轴旋转），可放 `domain/cameraUtils.ts`（已存在，执行时查看现有内容决定扩展还是新建 `cameraMoveGeometry.ts`）。

## 实施方案

一期预设清单（与用户确认过的方向，参数尽量少）：

| 预设 | 参数 | 几何 |
|---|---|---|
| direct 直接 | 无 | 两点直插（1.2 已有，默认值） |
| orbit 环绕 | degrees（±90/180/270/360）、direction | 以目标为圆心、起始机位半径与高度，绕世界 Y 轴扫过指定角度；终点即环绕落点（覆盖 B 卡机位）或以 B 卡机位反解角度——一期取"角度驱动、终点由几何决定"，B 卡摄像机位置由编译回写语义见重要记录 003 |
| dollyIn/Out 推近/拉远 | distanceRatio（如 0.5=推到一半距离） | 沿"机位→目标"连线移动 |
| truck 横移 | offset（左右距离） | 垂直于视线方向平移 |
| crane 升降 | height（升降量） | 沿世界 Y 平移 |

采样策略：环绕每 ~15° 一个中间关键帧（linear 分段近似圆弧）；整体速度感通过对采样时间做缓动重映射（easeInOut 时中间点在时间上非均匀分布）；直线类预设只需首尾两点 + easing。

## 执行步骤

1. 定稿 `StageCameraMove` 各预设参数（更新 1.1 的 `shotTypes.ts` 类型），保持判别联合。
2. 新建 `src/features/cameraStage/domain/cameraMovePresets.ts`：每种预设一个纯函数 `(startState, targetPos, move, t01) → Vec3` 或直接输出采样点数组 `[{ time01, position }]`；含缓动时间重映射工具。
3. 在 `shotCompiler.ts` 接入：摄像机过渡存在非 direct 预设时，调用预设函数生成中间关键帧写入 position.x/y/z 轨道（分量 scalar，linear 分段）。
4. 执行时确认摄像机朝向机制（lookAt 驱动 or rotation 轨道），把结论写入执行记录；若需 fov 联动（推近变焦等）标记为二期不做。
5. 单元测试 `cameraMovePresets.test.ts`：
   - orbit 180°：中间采样点到目标距离恒定（半径不变）、起终角度差 180°、方向正确；
   - dollyIn 0.5：终点在连线中点；
   - 时间重映射：easeInOut 下中段采样时间间隔小于两端（或对应关系断言）；
   - 编译集成：含 orbit 的两卡编译产物轨道可被 sampleTrack 采样且中间时刻位置在圆弧上。
6. lint + 测试全绿。

## 涉及内容

### 新增文件

- `src/features/cameraStage/domain/cameraMovePresets.ts`：运镜预设几何与采样（纯函数）。
- `src/features/cameraStage/domain/cameraMovePresets.test.ts`：单元测试。

### 修改文件

- `src/features/cameraStage/domain/shotTypes.ts`：StageCameraMove 参数定稿。
- `src/features/cameraStage/domain/shotCompiler.ts`：接入预设编译分支。
- （可能）`src/features/cameraStage/domain/cameraUtils.ts`：复用/补充向量数学工具。

## 验收标准

- [x] 5 种预设（含 direct）编译产物几何正确，单元测试覆盖并通过。
- [x] 环绕全程与目标距离恒定，无"穿过中间"现象。
- [x] 预设函数纯净（无 three/store/UI 依赖）。
- [x] 摄像机朝向机制已确认并记录，环绕过程取景始终对准目标（该项如需真实视口验证，写明手动验证步骤留给 2.x 之后）。
- [x] lint / 类型检查通过；任务总览已同步更新。

## 执行记录

- 日期：2026-07-10
- **参数定稿**：`shotTypes.ts` 的 `StageCameraMove` 判别联合扩展为 5 种——`direct`（无参数）、`orbit: { degrees, direction: 'cw'|'ccw' }`、`dollyIn`/`dollyOut: { distanceRatio }`、`truck: { offset }`、`crane: { height }`。一期预设清单表格里的全部 5 种预设均已实现，未省略 truck/crane（时间允许，按任务文件"若时间允许就都做"的要求全量完成）。`normalizeCameraMove` 同步扩展解析与安全兜底默认值。
- **新建 `src/features/cameraStage/domain/shotCameraMovePresets.ts`**（207 行）：纯函数几何实现，统一入口 `compileCameraMoveSamples(move, fromPosition, targetPosition, segStart, segEnd, easing)`。orbit 按 `ORBIT_DEGREES_PER_KEYFRAME = 15` 度一个采样点、绕世界 Y 轴旋转（自实现 `rotateAroundY`，数学上等价 three.js `Matrix4.makeRotationY`，但零 three 依赖）近似圆弧；采样点的时间通过对缓动曲线做二分法数值反解（`invertEasing`）得到非均匀分布，实现"角度均匀采样保证圆弧精度 + 时间非均匀分布体现整体缓动速度感"的双重目标；dolly/truck/crane 首尾两点 + easing。全部为纯数学向量运算（`addVec3`/`subVec3`/`scaleVec3`），未导入 three、UI、store。
- **接入 `shotCompiler.ts`**：实际拦截点与 handoff.md 最初建议（在 `compileTransitionPoints` 内部按分量分支）不同——由于 orbit/truck 等几何需要 X/Y/Z 三分量整体向量（单分量 scalar 签名算不出正确结果），改为在 `compileObjectTransition` 的 `transform.position` 分组循环入口拦截（新增 `isCameraPositionMoveGroup` + `compileCameraPositionGroup`），一次性算出三分量采样点并写入 x/y/z 三条轨道。`compileTransitionPoints` 仍按建议加了 `propertyPath` 参数，但改用作防御性断言（摄像机运镜位置分量意外流入时直接抛错），不影响 fov/color 等其它属性的两点直插逻辑。新增 `resolveShotLookAtTarget` 解析过渡起始卡快照中的 lookAt 目标点（manual 直接用 target；object 模式取快照中的目标位置，朝向偏移对齐 `cameraUtils.getObjectLookAtPoint` 的胸口高度算法）。完整理由见 `decisions.md`。
- **环绕终点语义定稿（重要记录 003）**：选了非 direct 运镜预设后，终点由几何计算决定，覆盖/忽略 B 卡摆放的机位；统一应用到全部 5 种预设，不仅是 orbit。已回填 `重要记录.md` 记录 003 为"已确定"。
- **摄像机朝向机制验证**：验证方式——`Grep` 定位到 `src/features/cameraStage/scene/StageViewportCamera.tsx` 后通读全文件（71 行），确认渲染相机的朝向由 `camera.lookAt(lookAtTarget...)` 在位置采样回调（`registerPlaybackApplier('transform.position', ...)`）与初始 `useLayoutEffect` 中每次实时计算，不读取任何 `transform.rotation` 关键帧轨道；同时确认 `animatableProps.ts` 的 `TRANSFORM_GROUPS` 对摄像机的 `rotation`/`scale` 分组用 `notCamera` 过滤，摄像机对象在可动画属性注册表里本来就没有这两个分组。结论：handoff.md 给出的预判（"朝向由 lookAt 驱动，只需生成位置关键帧"）验证无误、未过时，因此本任务只生成位置关键帧，未涉及旋转轨道，也未对渲染层做任何改动。
- **新增测试**：`shotCameraMovePresets.test.ts`（165 行，10 用例，纯几何测试）+ `shotCompiler.test.ts` 新增 `摄像机运镜预设接入（1.3）` 描述块（2 个集成用例：orbit 编译产物几何验证 + B 卡机位覆盖验证、fov 变化不受运镜预设误伤）。
- **验证结果**：`npm run test` 25/25 全部通过（含 1.2 遗留 13 个用例，全部仍通过，无回归）；`npm run lint` 通过；`npx tsc --noEmit`（前端根 tsconfig）通过；`npx tsc -p tsconfig.electron.json --noEmit` 通过。
- **遗留问题**：无功能性遗留。5 种预设全部实现，未省略任何预设。
- **下一步（2.3）需要知道**：过渡细节层 UI 需要暴露 `StageCameraMove.kind` 选择器 + 对应参数（orbit 的 degrees/direction，dolly 的 distanceRatio，truck 的 offset，crane 的 height），并在 UI 上提示"启用运镜预设后终点由预设参数决定，与本卡摆放位置无关"（对齐重要记录 003）。详见 `handoff.md`。
