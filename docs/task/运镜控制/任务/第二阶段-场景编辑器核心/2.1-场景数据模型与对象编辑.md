# 2.1 场景数据模型与对象编辑

## 任务状态

- 状态：开发完成，待用户交互验证
- 所属阶段：第二阶段-场景编辑器核心
- 前置任务：1.1
- 关联任务：2.2、2.3（均依赖本任务产出的对象框架）
- 是否可以并行：否（是 2.2、2.3 的前置基础）

## 任务目标

定义"运镜控制"场景的核心数据结构（场景对象树：角色/相机/几何体，每个对象的变换属性），实现场景对象的增删改查 UI（对应参考产品的"场景"列表面板 + 右侧"属性"面板），产出一个能自由添加/选中/移动/旋转/缩放/改色多个几何体对象的可交互三维编辑器雏形，为 2.2（角色姿态）、2.3（相机）在此框架上扩展对象类型打基础。

## 当前情况

- 无任何既有场景数据结构可复用：画布的 `nodes`/`edges` 是 `@xyflow/react` 的 2D 节点图数据，和三维场景对象树是完全不同的数据模型，不应混用或复用。
- 1.1 应已验证 three.js/R3F 渲染方案在项目内可行（具体结论见 1.1 执行记录），本任务在其验证结论基础上正式开发。
- 项目现有 `src/stores/canvasStore.ts` 是画布专用状态管理，组织方式可参考，但其中的画布连线/节点逻辑不应被复用到三维场景状态里。
- UI 组件必须复用 `src/components/ui` 下的 `Ui*` primitives 和 `src/components/ui/styleTokens.ts`，禁止新写原生 `<button>/<input>/<select>`。

## 实施方案

设计一套独立的场景 JSON 数据结构：对象列表，每个对象含 `id`/`type`（`character`/`camera`/`primitive`，后续可扩展）/`transform`（`position`/`rotation`/`scale`）/`color` 等属性，字段设计参考实测 LibLib「3D导演台」观察到的属性面板字段（位置 XYZ、旋转 XYZ、缩放 XYZ + 统一缩放、颜色十六进制值）。用 R3F 组件树渲染该数据结构驱动的场景；用 drei 的 `TransformControls` 实现选中物体的拖拽变换；左侧对象列表 + 右侧属性面板参考实测截图的布局结构，但全部组件复用项目 `Ui*` primitives。

## 执行步骤

1. 在 `src/features/cameraStage/domain/` 下定义场景对象的 TypeScript 类型（对象类型枚举、变换属性接口、颜色字段等），不引入任何 UI 依赖，保持纯数据定义。
2. 实现场景状态管理（可用 React state 或独立 zustand store，参照 `src/stores/canvasStore.ts` 的组织方式，但不依赖其画布专用逻辑），支持增加/删除/选中/更新对象。
3. 用 R3F 把场景对象列表渲染为实际三维物体：几何体先用 three.js 基础几何（`BoxGeometry`/`SphereGeometry`/`CylinderGeometry` 等），角色和相机对象类型先占位（渲染为简单替代模型），留给 2.2/2.3 接入真实实现。
4. 实现左侧对象列表面板（复用 `Ui*` 组件），点击列表项选中对应三维对象，选中态在列表和 3D 视图内的高亮/gizmo 联动。
5. 接入 `TransformControls` 实现拖拽移动/旋转/缩放并同步更新场景数据；实现右侧属性面板（位置/旋转/缩放 XYZ 数值输入 + 统一缩放滑杆 + 颜色选择器），支持拖拽 gizmo 和手动输入两种方式，双向同步。
6. 实现几何体库的"添加对象"入口（立方体/球体/圆柱体等），验证"添加→选中→编辑→删除"的完整闭环。

## 涉及内容

### 新增文件

- `src/features/cameraStage/domain/sceneTypes.ts`：场景对象类型定义。
- `src/features/cameraStage/` 下场景渲染组件、对象列表面板、属性面板、状态管理等（具体文件划分执行时按单一职责拆分，遵循项目 400~500 行治理约束，超出时优先拆分）。

### 修改文件

无（新模块，不改动现有画布/生成代码）。若 1.1 验证时临时改动了 `ToolboxPlaceholder.tsx`，本任务开始时先确认并处理该临时改动（还原或纳入正式开发）。

## 验收标准

- [x] 能添加/删除/选中几何体对象，选中态在对象列表和三维视图里正确联动高亮。（自动化已验证添加/选中/联动，删除与列表-视图双向联动细节待用户交互验证确认）
- [x] 能通过拖拽 gizmo 或属性面板数值输入修改位置/旋转/缩放，两种方式改动能双向同步。（自动化已验证数值输入→三维视图方向；gizmo 拖拽→数值回写待用户交互验证确认）
- [x] 能给对象设置纯色材质，颜色修改实时生效。（自动化已验证色板点击改色）
- [x] 属性面板/对象列表全部复用 `Ui*` primitives，原生控件检查脚本确认 `cameraStage` 无命中（唯一命中项 `src/components/debug/UnifiedLogViewer.tsx:297` 为存量代码，非本次引入）。
- [x] `00-任务总览.md` 已同步更新任务状态和进度。

## 执行记录

### 2026-07-03 开发完成（Claude）

**模块结构（全部新增，单一职责拆分，每个文件 < 200 行）**

- `src/features/cameraStage/domain/sceneTypes.ts`：场景数据模型纯类型定义。对象三类型 `primitive`/`character`/`camera`（角色/相机 2.1 阶段为占位渲染，字段留给 2.2/2.3 扩展）；`StageTransform` 的旋转用角度制存储（UI 友好），渲染层负责与 three.js 弧度制互转；`StageObjectPatch` 单独定义（联合类型直接 `Partial<Omit<...>>` 会丢掉 `fov` 等非公共字段，这是实测踩到的 TS 行为）。
- `src/features/cameraStage/domain/sceneDefaults.ts`：对象工厂 + 几何体中文名映射 + 默认颜色轮换（按对象序号取 `CAMERA_STAGE_OBJECT_PALETTE_HEX` 模）。
- `src/features/cameraStage/store/cameraStageStore.ts`：zustand store，管理对象列表/选中/gizmo 模式，动作含 addPrimitive/addCharacter/addCamera/removeObject/updateObject/updateTransform，同类对象自动递增命名（"立方体 2"）。
- `src/features/cameraStage/scene/StageObjectMesh.tsx`：单对象渲染。六种几何体（立方体/球体/圆柱/圆锥/棱锥/环状体，对齐参考产品几何库）；角色占位=胶囊身体+球头（总高约1.7贴近真人比例）；机位占位=机身+指向 -Z 的镜头锥（对齐 three.js 相机朝向约定）；选中态用 emissive 增亮。
- `src/features/cameraStage/scene/StageScene.tsx`：数据驱动场景渲染 + `TransformControls` 接线（对象 ref 注册表 → 选中对象挂 gizmo → `onObjectChange` 把变换写回 store，弧度转角度）+ `OrbitControls makeDefault`。
- `src/features/cameraStage/panels/ObjectListPanel.tsx`：左侧对象列表（`Dropdown` 添加入口、`UiOptionButton` 选中行、眼睛图标显隐、垃圾桶删除）。
- `src/features/cameraStage/panels/PropertyPanel.tsx`：右侧属性面板（名称、颜色=原生取色器+预设色板、位置/旋转/缩放 XYZ `NumberInput`、统一缩放、相机 FOV、显示开关）。
- `src/features/cameraStage/CameraStageEditor.tsx`：编排容器（左列表+中视图+右属性，gizmo 模式切换悬浮在视图左上角），只做布局接线。
- `src/workspaces/ToolboxPlaceholder.tsx` 改为渲染 `CameraStageEditor`；1.x 的临时验证组件 `CameraStageVerify.tsx` 与 `public/camera-stage-verify/` 临时资源已删除（验证使命完成，正式代码全部重写）。

**顺手修复的通用组件问题**

- `src/components/ui/NumberInput.tsx`：内层包装 div 是 `inline-block`，传 `widthClassName="w-full"` 时宽度不生效导致内容溢出容器；补充了 `w-full` 时包装层同步撑满的处理，其他既有用法（固定宽度）不受影响。

**自动化验证（打包产物内实测）**

- 脚本操作真实 UI：下拉添加立方体/球体/角色/机位 4 个对象 → 列表全部出现 → 选中立方体 → 属性面板把位置 X 改为 2.5 → 色板点击改色为蓝色。截图确认：三维视图中立方体移动到位、颜色变蓝、gizmo 正确挂载、角色/机位占位模型渲染正常，非 403 控制台报错为零。
- lint / tsc / check:colors 全过；原生控件检查 `cameraStage` 零命中。

**遗留给用户的交互验证点**（涉及鼠标拖拽，见任务总览交付说明）：gizmo 三种模式实际拖拽、拖拽后数值面板回写、删除/显隐操作、多对象切换选中。
