# 3.1 迁移生成工作区提示词入口

## 任务状态

- 状态：待用户验证
- 所属阶段：第三阶段（核心入口迁移）
- 前置任务：2.3
- 关联任务：3.2、3.3、4.1
- 是否可以并行：是，可与 3.2、3.3 并行

## 任务目标

将生成工作区主提示词输入迁移到共享 `PromptEditor`，支持上传图片的缩略图引用、提示词优化整段替换和现有提交/粘贴/拖放行为，同时保持生成请求、预设和历史记录兼容旧字符串。

## 当前情况

- `InputArea.tsx` 使用 `ReferenceTextarea`，按 `uploadedImages` 数组下标构造 `image-ref-N/图N`，并传递图片粘贴、拖放、提交和复杂样式。
- `MediaGenerator/index.tsx` 持有 `ReferenceTextareaHandle`，提示词优化通过 `replaceValueWithUndo()` 写回并依赖撤销/重做 replacement 值。
- `useGenerationHandler.ts` 在调用 `onGenerate` 前用 `stripReferenceAtPrefix()` 将 `@图N` 转为 `图N`。
- `GenerationWorkspace.tsx`、预设和生成历史仍消费 prompt 字符串；本任务不能迫使模型 runtime 接收编辑器 JSON。

## 实施方案

在生成工作区启用“媒体引用”preset。调用方把当前上传媒体映射为 1.2 确定的稳定引用上下文，编辑器文档保存稳定 ID，NodeView 动态显示缩略图与当前 `图片N` 标签。生成、预设和历史边界统一调用核心 serializer 输出兼容字符串/模型文本；旧 `图N` 继续作为读取别名。

提示词优化返回字符串时通过 parser 构造结构化文档并作为一次可撤销 transaction 写入当前编辑器；不再维护一套 textarea 原生 undo replacement 协议。

## 执行步骤

1. 追踪 `input` 状态、预设载入、历史重编、提示词优化和模型切换的完整数据流，按 1.2 契约确定结构化字段的所有者。
2. 将上传图片映射为稳定媒体引用上下文；重排/删除时更新显示序号，不改写文档中的稳定 ID。
3. 用 `PromptEditor` 替换 `InputArea.tsx` 的主 `ReferenceTextarea`，迁移 placeholder、最小高度、左侧上传区避让、提交、paste/drop 和 focus handle。
4. 将提示词优化整段替换改为编辑器 transaction，确认一次撤销恢复优化前文档，重做恢复优化结果。
5. 在生成、预设保存/加载、历史重编边界接入统一 serializer/parser，移除本入口对 `stripReferenceAtPrefix()` 的直接依赖。
6. 验证图片新增、删除、重排、重复引用和失效引用时，显示标签与模型文本符合契约。
7. 增加生成工作区组件/集成测试，并记录真实 Electron 键鼠验收步骤交由用户执行。

## 涉及内容

### 新增文件

- 预计无需新增业务组件；仅在 `MediaGenerator` 内需要小型 adapter 时新增单一职责文件。

### 修改文件

- `src/components/MediaGenerator/components/InputArea.tsx`：替换主提示词输入并提供媒体上下文。
- `src/components/MediaGenerator/index.tsx`：切换 handle、结构化状态和提示词优化替换协议。
- `src/components/MediaGenerator/hooks/useGenerationHandler.ts`：改用统一模型 serializer。
- `src/workspaces/GenerationWorkspace.tsx`、预设相关模块：按 1.2 契约兼容读取/保存，不改变外部字符串行为。

### 可能废弃

- 生成工作区专用的 undo/redo replacement props 与状态：编辑器 transaction 验证通过后删除。

## 验收标准

- [ ] 主提示词输入使用共享编辑器，图片引用带缩略图并整体换行/删除（代码与组件测试通过，待真实 Electron 验证）。
- [x] 上传图片重排后引用仍绑定同一稳定媒体，显示序号和生成文本按当前上下文更新。
- [ ] 提示词优化替换可在当前输入中撤销/重做，不影响其他输入（transaction 已接入，待真实 Electron 验证）。
- [ ] 粘贴图片、拖放、提交快捷键、预设载入和历史重编行为未回退（待真实 Electron 验证）。
- [x] 实际发送给现有模型的 prompt 与迁移契约一致。
- [x] 相关测试、lint、TypeScript 检查通过，用户手动步骤和任务总览已更新。

## 执行记录

### 2026-07-22

- 完成内容：生成区主输入迁移到 `PromptEditor` 的媒体引用 preset；结构化文档成为编辑态事实来源，兼容字符串按当前引用上下文同步派生，模型请求统一使用 `toModelPromptText()`。
- 实际改动：上传图片进入状态时生成 `generation-upload:*` UUID，重排复用资源 ID；优化结果由 parser 转为文档并通过 `replaceDocument(..., { addToHistory: true })` 写入；预设双写 `promptDocument`、`promptMediaBindings`、`prompt` 和 `params.input`，旧预设/历史重编继续由 legacy parser 读取。生成入口已移除 `ReferenceTextarea`、本地 replacement history 和 `stripReferenceAtPrefix()` 直接依赖。
- 结构调整：抽出 84 行 `GeneratorConfigurationBar`，将 `MediaGenerator/index.tsx` 从 557 行降至 477 行；共享编辑器只新增通用 `editorShellClassName` 样式入口，没有复制输入控件或编辑器实现。
- 验证结果：`npm run lint`、`npx tsc --noEmit`、`npm run check:colors`、`npm test`、`npm run build` 全部通过；共 39 个测试文件、227 条用例。生产主 chunk 849.35 kB，编辑器继续保持 359.36 kB 独立按需 chunk。
- 首轮用户验证修正：对照迁移前样式后，移除生成区编辑器额外叠加的 `ring-2` 并恢复透明底色和弱边框；确认 `@` 菜单因 portal 无层级而被 `z-index: 20` 的浮动生成面板遮挡，候选 renderer 统一提升到 `z-index: 1000` 并增加回归测试。
- 第二轮用户验证修正：媒体引用建议取消空格前缀限制，允许在中文、英文及其他字符后直接输入 `@`；候选列表固定宽度并移除资源 ID/文件名副行，只显示缩略图和单行大标签。图片标签来源统一为 `图片N`，共享旧输入工具与 parser 同时兼容 `图N`；画布标准生成、分镜生成和分镜拆分的现存标签源也改用同一媒体标签函数。
- 第二轮修正验证：`npm run lint`、`npx tsc --noEmit`、`npm run check:colors`、`npm test`、`npm run build` 全部通过；共 40 个测试文件、231 条用例，包含任意字符后触发 `@`、候选不显示资源 ID，以及旧 `图N` 向当前 `图片N` 兼容升级的回归测试。
- 第三轮用户验证修正：共享编辑器插入媒体 atom 时不再追加真实空格，旧字符串/历史载入和识别粘贴时也压缩引用边界空格；模型 serializer 独立为引用与相邻正文智能补空格，例如编辑态 `参考[图片1]然后修改` 发送为 `参考 图片1 然后修改`。粘贴识别以当前实际 reference 表为白名单，支持 `图片N`、`图片 N`、旧 `图N`/`图 N`，不存在或不唯一的标签仍是普通文字。媒体候选宽度改为按最长内容收缩，未选中项取消卡片边框和常驻底色，仅选中项与鼠标悬浮项高亮。
- 第三轮修正验证：`git diff --check`、`npm run lint`、`npx tsc --noEmit`、`npm run check:colors`、`npm test`、`npm run build` 全部通过；共 40 个测试文件、234 条用例，新增紧凑编辑态、模型边界补空格、带空格标签粘贴升级和菜单自适应/未选中样式断言。
- 用户验证步骤：在真实 Electron 生成工作区上传两张容易区分的图片；分别在行首、中文后和英文后直接输入 `@`，确认菜单均出现且候选只显示缩略图与 `图片1/图片2` 单行标签；插入两项后检查整项选中/删除及跨行表现；重排图片后检查引用仍指向原图且 `图片1/图片2` 标签随顺序变化；粘贴和拖入图片；载入包含旧 `@图1` 的预设与历史“重新编辑”；执行一次提示词优化后用 Ctrl+Z/Ctrl+Y 验证整段替换历史；最后在测试模式下用 Enter 提交并确认生成任务中的 prompt 不含 `@`。
- 遗留问题：以上鼠标、拖拽、真实优化与提交链路按项目约定等待用户验证；通过后才能把本任务标记为已完成并进入 3.2。
- 与原计划的偏差：为遵守存量超长文件修改即拆分的约束，额外抽离模型/参数配置栏；未给生成历史新增结构化字段，继续遵循 1.2 已冻结的“历史只保存最终模型字符串”契约。
- 对后续任务的影响：3.2/3.3 可复用同一 parser/serializer、稳定 binding 与 editor transaction 模式；4.1 可在全部入口迁移后统一评估旧 `ReferenceTextarea` 和 `stripReferenceAtPrefix()` 的剩余消费者。
- 相关提交：本任务待用户验证提交。
