# 1.2 文档模型与兼容契约定型

## 任务状态

- 状态：已完成
- 所属阶段：第一阶段（技术验证与契约定型）
- 前置任务：1.1
- 关联任务：2.1、2.2、2.3、3.1、3.2、3.3
- 是否可以并行：否

## 任务目标

冻结第一版提示词文档、引用身份、序列化输出、存储兼容和撤销事务契约，让后续 AI 可以在不同模块并行实现而不会各自定义一套 value 结构。

## 当前情况

- 画布 `MediaGenNodeData.prompt`、`ImageEditNodeData.prompt`、分镜描述/备注均为字符串，并随整个 nodes JSON 写入画布项目。
- 生成工作区、预设、提示词优化 `systemPrompt/userTemplate` 也使用字符串，且最终模型 builder 读取普通 prompt。
- `ReferenceItem` 只有 `id/label/thumbnailSrc`；生成区 ID 由数组下标生成，画布引用通常由媒体数组顺序生成，稳定身份来源尚未统一。
- `referenceTokens.ts` 已定义当前 `@图N` 插入/删除/空格和生成前去 `@` 语义，旧项目兼容必须以它的现状为基线。

## 实施方案

在 `01-实施方案.md` 和 `重要记录.md` 中冻结 `PromptDocumentV1` 契约，不另建庞大设计文档。契约必须把“编辑态文档”“显示标签”“媒体身份”“生成文本”分离：稳定 ID 决定引用对象，`图N` 只是在当前媒体上下文中的动态标签，serializer 决定送给模型的字符串。

推荐迁移期采用“结构化字段优先 + 旧字符串回退”的双格式：读取旧字符串时构造文档，保存时保留可恢复字符串；具体字段名、各数据载体的落点和双写终止条件在本任务确认。

## 执行步骤

1. 重新扫描 `canvasNodes.ts`、`projectStore.ts`、预设类型、LLM 配置、项目包导入导出和生成入口，列出所有需要兼容的真实数据载体。
2. 定义 `PromptDocumentV1` 允许的节点、属性和版本字段；明确未知节点、损坏 JSON、缺失媒体和未识别旧 token 的降级行为。
3. 为媒体引用逐入口确认稳定 ID 来源：上传文件、画布内联媒体、上游节点媒体、分镜帧和未来资产库；无法确认的来源标记“待确认”，不得继续使用数组下标冒充稳定 ID。
4. 定义至少三种输出：兼容旧字符串、模型纯文本、可选 Markdown；明确 `@图N`、`图N`、`{{变量}}`、换行和未知引用的结果。
5. 定义组件受控接口、预设能力、只读/禁用、程序化替换、focus，以及编辑器 history 与画布 `historyGroup` 的事务边界。
6. 确定各载体采用新增结构化字段、包装类型还是边界即时转换，并制定读取升级、双写、版本回退和最终清理条件。
7. 将最终决定写入 `01-实施方案.md` 与 `重要记录.md`，同步调整后续任务的涉及文件和验收项。

## 涉及内容

本任务以计划契约为主要输出，不开始业务入口迁移。

### 修改文件

- `docs/task/结构化提示词编辑器升级/01-实施方案.md`：写入最终 schema、接口和迁移契约。
- `docs/task/结构化提示词编辑器升级/重要记录.md`：确定记录 002、004，并记录第一版 Markdown 决策。
- `docs/task/结构化提示词编辑器升级/任务/第二阶段-共享编辑器核心/*.md`：如实同步已确定的类型名和落点。

### 可能废弃

- 无。已有 `referenceTokens.ts` 此阶段继续作为旧格式基线。

## 验收标准

- [x] 文档节点、属性、版本和损坏数据降级规则明确，无 HTML 持久化。
- [x] 每类媒体引用的稳定身份来源已确认或明确标记待确认。
- [x] 旧字符串、模型文本和可选 Markdown 的序列化语义有具体示例。
- [x] 画布、预设、LLM 配置和项目包的迁移/回退方式均有落点。
- [x] 编辑器 history 与画布 history 的职责边界明确。
- [x] 实施方案、重要记录、后续任务和任务总览已同步更新。

## 执行记录

### 2026-07-22

- 完成内容：冻结 `PromptDocumentV1` 的 doc/paragraph/text/hardBreak/mediaReference/templateVariable 节点；确定媒体 `resourceId`、变量 `key`、fallback label、运行时 resolver、三种 serializer、损坏数据降级、公共组件接口和历史事务边界。
- 实际改动：在 `01-实施方案.md` 增加完整类型、身份来源、serializer 示例、读取优先级、数据载体双写矩阵和 history 规则；将 `重要记录.md` 的记录 002、003、004 更新为已确定。
- 数据载体扫描：画布节点/分镜帧随 `nodes_json/history_json` 保存；生成区 `presets.json` 同时保存 `prompt` 与 `params.input`；SQLite 参数预设保存动态 params；LLM profile 保存到 `llm-config.json`；项目包原样保存 nodes 但媒体收集器尚未覆盖内联 binding；任务历史继续保存已提交模型字符串。
- 身份结论：资产库用资产 ID；上游画布输出用 sourceNodeId/sourceHandle/outputIndex；分镜帧用 nodeId/frame.id；内联上传和生成区临时素材在进入状态时生成 UUID。数组下标与 URL 均不得继续充当稳定身份。
- 序列化结论：`toLegacyPromptString()` 保留 `@`，`toModelPromptText()` 移除媒体 `@`，`toMarkdown()` 首版只保留扩展接口并使用 `henji-media:` 自定义链接；未知旧 token 保持普通文本。
- 兼容结论：所有迁移载体结构化优先、旧字符串回退、同一事务双写；兼容字符串至少保留至 4.2。未知版本/损坏文档回退且记录不含正文的 `prompt_document.parse.fallback` 日志。
- 遗留问题：无。1.1 真实 Electron 硬性门槛已通过，文档与 serializer 契约正式生效。
- 与原计划的偏差：将生成任务历史明确排除为结构化编辑态来源；项目包无需新增顶层格式版本，只需在后续迁移时保留节点字段并扩展内联媒体收集。
- 对后续任务的影响：2.1/2.2/2.3 已同步具体类型、字段与输出名称；第三阶段各入口按双写矩阵迁移，禁止重新定义 value 或 stable ID。
- 相关提交：第一阶段完成后独立提交。
