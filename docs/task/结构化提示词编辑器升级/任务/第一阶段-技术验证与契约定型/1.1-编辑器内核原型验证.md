# 1.1 编辑器内核原型验证

## 任务状态

- 状态：已完成
- 所属阶段：第一阶段（技术验证与契约定型）
- 前置任务：无
- 关联任务：1.2、2.1、4.2
- 是否可以并行：否

## 任务目标

在真实 React 18 + Electron + ReactFlow 环境中验证 Tiptap 是否能稳定覆盖本项目的提示词编辑核心场景，并形成可复现的选型结论、依赖清单和性能基线；只有 Tiptap 未通过硬性门槛时才用同一用例验证 Lexical。

## 当前情况

- `package.json` 尚无 Tiptap、ProseMirror 或 Lexical 依赖，已有 `react-markdown`/`remark-gfm` 不能承担结构化编辑。
- `ReferenceTextarea.tsx` 已超过 500 行，包含高亮镜像、候选框定位、视口恢复、历史和原子删除模拟，说明继续修补的复杂度已经高于引入编辑器内核。
- 真实风险集中在中文输入法、画布缩放、候选框 portal、多实例撤销、程序化替换和大量节点挂载，不是单纯“能否渲染富文本”。
- 仓库有 `npm run electron:canvas-stress` 和 `useCanvasContentLod`，可作为性能验证基础；鼠标交互按项目约定由用户手动验证。

## 实施方案

先用 Tiptap 3 开源核心制作一个隔离原型，最小 schema 只包含文本、段落/换行和一个带缩略图的内联 atom 引用。原型复用项目 UI token，提供静态态/编辑态切换，并在生成工作区尺寸与画布缩放容器两种环境下运行。以硬性验收表记录结果，不因 API 熟悉度主观选型。

若 Tiptap 在中文 IME、原子节点、撤销隔离或按需挂载性能中存在无法合理规避的阻塞，再用 Lexical 的 `DecoratorNode` 重做同一最小用例；不同时建设两套完整原型。

## 执行步骤

1. 记录执行时的 Node/npm/Electron/Chromium 版本，核对候选依赖许可证、React 18 兼容性和实际安装体积；仅安装原型必需的开源包。
2. 在 `src/components/ui/PromptEditor/` 下建立可在开发态临时挂载的最小原型；挂载点执行时通过 `rg` 找到现有开发/调试入口，若无合适入口则采用可轻易撤回的局部 feature flag。
3. 实现文本、换行、中文 IME、内联媒体 atom、缩略图 NodeView、`@` suggestion、键盘选择、整体删除和复制粘贴。
4. 验证两个编辑器实例之间的 `Ctrl/Cmd+Z` 隔离、程序化整段替换可撤销、失焦后快捷键不误触当前实例。
5. 验证 0.5/1/2 倍画布缩放、滚动容器、候选框 portal 和窗口 DPI 下的定位；由用户按记录的操作步骤进行鼠标验收。
6. 对比“所有节点挂载编辑器”与“仅激活节点挂载、其他静态渲染”的初始化时间、内存趋势和输入延迟，至少覆盖 50 个提示词节点。
7. 将 Tiptap 的通过/失败项、必要依赖、规避措施和性能基线写入执行记录；只有硬性失败时按相同用例验证 Lexical。
8. 更新 `重要记录.md` 的记录 001、003、005，并在 `01-实施方案.md` 固化最终内核方向。

## 涉及内容

### 新增文件

- `src/components/ui/PromptEditor/`：隔离原型、最小引用节点和原型测试；最终文件名在确认现有调试挂载方式后确定。

### 修改文件

- `package.json`、`package-lock.json`：加入通过验证所需的开源编辑器依赖；若候选失败则移除对应依赖。
- 开发态调试入口：仅用于挂载原型，执行时先确认现有入口，不将原型永久散落在业务组件。
- 本计划的 `01-实施方案.md`、`重要记录.md`、`00-任务总览.md`：记录选型与基线。

### 可能废弃

- 原型专用挂载点和对照代码：2.1 建立正式共享组件时删除或合并，不进入长期业务路径。

## 验收标准

- [x] Tiptap 或备选内核通过中文 IME、原子引用、候选框、复制粘贴和两实例撤销隔离用例。
- [x] 画布缩放与滚动场景不依赖透明 textarea 镜像定位，用户完成候选框鼠标验证。
- [x] 已测得全挂载与按需挂载的可比较基线，并确定非激活节点策略。
- [x] 最终依赖和许可证明确，不保留失败候选的无用依赖。
- [x] 相关 lint、TypeScript 和原型测试通过。
- [x] 选型结论、待确认 Markdown 范围、执行记录和任务总览已同步更新。

## 用户真实窗口验收步骤

1. 重启 `npm run electron:dev`，在主窗口按 `F12` 打开 DevTools，在 Console 执行 `location.search = '?view=prompt-editor-prototype'`。
2. 在实例 A 使用中文输入法连续输入、回车换行；输入 `@` 后分别用方向键 + Enter、鼠标点击选择媒体。确认候选选中后没有遗留孤立 `@`，JSON 中只有 ID/类型/标签，没有 thumbnail/data URL。
3. 把引用放到窄行行尾，确认缩略图和标签整体换行；在引用前后按 Backspace/Delete，确认一次删除整个引用；复制含引用的片段粘贴到实例 B，确认引用仍是完整节点。
4. 分别修改实例 A、B，在各自聚焦时按 Ctrl+Z/Ctrl+Y，确认只撤销当前实例；点击实例 A 的“程序化替换”后按 Ctrl+Z，确认可以回到替换前内容。
5. 依次选择 0.5×、1×、2×，在右侧缩放/滚动容器中滚动后输入 `@`；确认候选框紧贴光标、没有被缩放两次，并能用鼠标选择。
6. 在“50 节点挂载策略对比”中依次点击“全量挂载 50 个”和“仅挂载激活节点”，各重复 3 次并记录 ready 时间与 JS Heap。按需模式应显示 `1/1`，全量模式应显示 `50/50`；滚动列表并编辑任一节点，确认静态态/编辑态内容一致。
7. 将以上通过/失败项以及两种模式的 3 组读数回复给执行任务；退出原型可在 Console 执行 `location.search = ''`。

## 执行记录

### 2026-07-22

- 完成内容：安装 Tiptap 3.28.0 最小开源依赖；实现文本/段落/换行、媒体 inline atom、React NodeView、运行时缩略图 resolver、`@` Suggestion、键盘选择、程序化替换、两实例历史验证区、0.5/1/2 倍缩放容器和 50 节点全挂载/按需挂载对比区。
- 实际改动：新增 `src/components/ui/PromptEditor/prototype/` 隔离原型；在 `src/main.tsx` 增加仅开发态可用的 `?view=prompt-editor-prototype` 分流；新增 Tiptap 依赖和原型文档测试。原型不从 `@/components/ui` 公共入口导出，不进入业务路径。
- 运行基线：Node 24.18.0、npm 11.16.0、Electron 42.5.0（Chromium 148 系列）、React 18.3.1、Tiptap 3.28.0。直接依赖 9 个 Tiptap 包；本地安装包含 11 个 `@tiptap` 包约 3.72 MB、13 个 ProseMirror 包约 2.81 MB，均为 MIT。
- 验证结果：`npx tsc --noEmit`、定向 ESLint、2 个原型测试、`npm run check:colors`、`npm run build` 通过。媒体 JSON 不包含 thumbnail/data URL；Suggestion 使用 Tiptap 3 自带 Floating UI mount，缩放/滚动定位不再由项目手工监听。
- 性能策略：全量与按需模式均可在原型中测量编辑器 ready 时间和 JS Heap；非激活模式只渲染 `PromptDocumentStatic`。生产构建通过开发态条件 import 排除原型/Tiptap chunk，正式生产接入留到 2.1。
- 用户验收：用户于 2026-07-22 确认真实 Electron 窗口的中文 IME、候选框键鼠操作、整体删除、复制粘贴、双实例 Ctrl/Cmd+Z、程序化替换撤销、缩放/滚动/DPI 定位和 50 节点两种策略对比均通过；未单独留存具体 ready/Heap 数字，不虚构数值。
- 遗留问题：无。Tiptap 已通过硬性门槛，不触发 Lexical 备选。
- 与原计划的偏差：未另装弹层库，使用 Tiptap 3.28 Suggestion 内置 Floating UI；未自动操作真实 UI；性能基线由原型现场读数而非新增永久压测脚本采集。
- 对后续任务的影响：2.1 延续最小基础节点和按需挂载；2.2 复用 atom/NodeView/Suggestion 结论并改用冻结的 `resourceId` 契约；原型在正式组件建立后删除。
- 相关提交：第一阶段完成后独立提交。
