# 1.3 动态工具激活与产物回读

## 任务状态

- 状态：已完成
- 所属阶段：第一阶段 · 运行正确性与能力可达性
- 前置任务：1.1
- 关联任务：2.2、3.2、4.2
- 是否可以并行：是，可与 2.1、4.1 并行；与 2.2 共用上下文契约时需协调

## 任务目标

让当前已经注册的工具能力不再受静态列表前 8 项限制，并让被 offload 的大结果可以由 Agent 在权限范围内分页回读。完成后，模型每轮只接收小而相关的工具集合，但任何未激活的应用能力都能通过能力搜索继续发现；Artifact 引用不再是数据黑洞，也不会扩大为任意文件读取。

## 当前情况

- `electron/main/services/agent-runtime/context/catalog.ts` 使用手写 `toolsByDomain` 数组，并对候选名称直接执行 `slice(0, 8)`。
- 当 route 已命中直接领域时，`search_application_capabilities` 不会默认加入；画布、素材、相机等列表第 9 项以后的工具可能永远无法暴露给模型。
- `rememberDiscovered()` 能记住能力搜索结果，但只有模型先获得并调用搜索工具时才生效。
- `electron/main/services/agent-runtime/context/offload.ts` 在结果超过 8 KiB 或 100 条时保存 Artifact 引用，内存 store 支持 `get()`。
- `electron/main/agent-utility.ts` 的 Artifact persistence 只实现 `save` RPC，`load` 固定返回 `null`；主进程 `AgentPersistenceStore.loadArtifact()` 已存在但没有 utility 回读通道。
- 工具注册表已经包含模型 schema、catalog、risk、permission、requiredContext、data class 和摘要，可作为动态目录的单一来源。
- 可继续复用：`search_application_capabilities`、注册表、Artifact SQLite 表、run/thread 所有权、数据分类、offload 事件和现有工具网关。

## 实施方案

将静态“领域数组前 8 个”改为注册表驱动的两层工具披露：始终提供小型能力发现入口，再根据 route、当前计划步骤、已发现工具、最近 observation 和宿主上下文选择本轮活动工具。活动集合仍有预算上限，但上限只控制单轮 schema 体积，不再决定能力能否到达；工具集合只在轮次保存点刷新。

新增受控 Artifact 查询能力：按 `artifactRef`、offset/cursor、limit 和可选字段范围读取摘要或分页内容；main 验证 Artifact 所属 run/thread、数据分类和最大输出，utility 通过版本化 RPC 请求，不接收任意路径。Artifact 回读本身作为只读工具通过注册表与网关，结果过大时仍可再次分页而不是递归 offload 无法消费。

## 执行步骤

1. 从注册表枚举工具 catalog 元数据，比较手写 `toolsByDomain` 与真实注册项，列出当前不可达或重复工具并加入回归夹具。
2. 定义动态激活输入：route、计划步骤、宿主 context、已发现名称、最近成功/失败 observation 和工具 schema 预算；明确保存点之外不改变当前模型请求。
3. 保留能力搜索工具作为常驻控制面，实现搜索结果到下一轮活动集合的确定性激活，并对名称冲突、未知工具、上下文不满足和预算超限给出诊断。
4. 删除“直接领域命中即没有发现入口”的行为；用测试证明画布和素材列表后半部工具可通过搜索后进入下一轮。
5. 为 Artifact 定义共享读取 schema、RPC 和 repository 查询；校验 run/thread 所有权、data class、分页上限、序列化大小和脱敏。
6. 注册只读 Artifact 查询工具，返回稳定 cursor、总量/是否有更多和受限内容；禁止接受本地文件路径、SQL 或任意 JSONPath 执行。
7. 覆盖 Utility 重启后 Artifact 回读、旧 Artifact 不存在、跨 thread 访问、C2/C3 数据、超大数组/对象和重复分页测试。
8. 运行上下文、工具目录、网关、持久化和 Utility smoke，记录工具 schema token 变化和 Artifact 回读上限；同步计划并提交。

## 涉及内容

### 新增文件

- `electron/main/services/agent-runtime/context/tool-activation.ts`：动态工具选择、预算和保存点激活；`catalog.ts` 保留发现编排并委托该模块。
- `src/core/assistant/artifacts.ts`、`electron/main/services/agent-runtime/persistence/artifact-store.ts`、`electron/main/services/agent-runtime/tools/builtin/artifacts.ts`：分别承载共享契约、持久分页读取和受控只读工具。

### 修改文件

- `electron/main/services/agent-runtime/context/catalog.ts`：改为注册表驱动的发现与活动集合，不再静态截断能力可达性。
- `electron/main/services/agent-runtime/context/builder.ts`、`runner/runner.ts`：在保存点使用活动工具集合并记录上下文证据。
- `electron/main/services/agent-runtime/tools/registry.ts`、`tools/types.ts`：若现有 catalog 元数据不足，增加配置驱动的发现字段。
- `electron/main/services/agent-runtime/context/offload.ts`：支持持久回读和分页语义。
- `electron/main/services/agent-runtime/persistence/store.ts`：按所有权读取 Artifact；必要时补索引，不改变 payload 事实。
- `src/core/assistant/utilityContracts.ts`、`electron/main/services/agent-runtime-manager/manager.ts`、`electron/main/agent-utility.ts`：增加 Artifact load RPC。
- `electron/main/services/agent-runtime/tools/builtin/backend.ts` 或经确认后的同职责文件：注册受控查询工具。
- `electron/main/services/agent-runtime/context/catalog.test.ts` 及相关网关/Utility 测试：覆盖可达性和权限。

### 可能废弃

- `toolsByDomain` 手写全量数组在注册表能够提供同等领域元数据后废弃；迁移前保留对照测试，避免漏工具。
- Utility 中 `load: () => null` 在 RPC 回读完成后删除。

## 特殊风险与回退要求

- 动态工具不能在同一模型请求中途变化，避免 schema 与模型输出不一致。
- Artifact 回读失败时只返回结构化错误，不得退化为直接访问数据库或文件系统。
- 工具 ranking 只能影响披露顺序，不能改变网关风险、审批、permission 或 requiredContext。

## 验收标准

- [x] 所有已注册且允许当前宿主上下文使用的工具，都能通过能力搜索在有限轮次内被发现和激活。
- [x] 画布、素材等超过 8 个工具的领域，列表后半工具有自动化可达性用例，不再依赖静态数组顺序。
- [x] 单轮模型工具 schema 仍受预算约束，活动集合变化只在保存点生效并有结构化事件/日志证据。
- [x] 超过 8 KiB 或 100 条的工具结果可按 Artifact 引用分页回读，Utility 重启后仍可读取。
- [x] 跨 run/thread、无权限、已删除、C2/C3 不允许外发和超出分页上限的请求被拒绝且可审计。
- [x] Artifact 工具不能读取任意路径、任意 SQL 或未登记资源。
- [x] 上下文、目录、网关、持久化和 Utility smoke 通过，无新增平行工具通道。
- [x] 本任务执行记录和任务总览已同步更新，并创建独立 Git 提交。

## 执行记录

### 2026-07-30

- 完成内容：删除手写 `toolsByDomain`，能力搜索改为常驻控制面；活动工具由注册表 category、route、最近 observation、已发现名称和宿主可用性排序，在每个模型请求前冻结为最多 8 项、最多 48 KiB schema。搜索分页覆盖完整目录，发现项按窗口轮换，dynamic 或未披露工具调用以 `TOOL_NOT_ACTIVE` 在网关前拒绝。
- 实际改动：新增共享 Artifact v1 契约、SQLite 所有权 repository、`artifact.describe` / `artifact.read` Utility RPC 和 `read_agent_artifact` 只读工具；正文按 UTF-8 稳定 cursor 每页最多 4 KiB，可筛选明确顶层字段，输出保持低于二次 offload 阈值。元数据预检取自主进程真实记录，C2 每页逐次审批并显示目的地，C3、跨 run/thread、缺失引用、错误 cursor 和越界参数默认闭锁；执行继续经过统一网关、权限审计和结构化日志。
- 验证结果：`npm run test:assistant-eval` 189/189、`npm run test:assistant-persistence` 18/18、`npm test` 636/636；`npx tsc -p tsconfig.electron.json --noEmit`、Electron/核心 ESLint、`npm run electron:build` 和构建后的 `npm run electron:assistant-utility-smoke` 全部通过。回归覆盖画布后半工具、`get_canvas_project`、目录第二页、有限轮换激活、schema 预算、dynamic/allowlist 拒绝、Utility 重建后 UTF-8 多页拼接、字段/cursor 绑定、跨所有权、C2/C3 和递归 offload。
- 遗留问题：真实 Provider 对大结果自主发现与多页阅读的行为质量、真实窗口中的 C2 审批文案和长稳表现按计划留到 4.2；不阻塞本任务代码完成。
- 与原计划的偏差：单页上限最终固定为 4 KiB，而非允许接近 8 KiB，目的是连同工具结果信封后仍严格低于 offload 阈值；跨 run 默认拒绝，后续 3.2 如需关联子 run 读取，必须通过显式 `parentRunId` 委托扩展，不能放宽为同 thread 任意读取。
- 对后续任务的影响：2.2 只应投影本轮冻结的 `activeToolNames`；2.3/3.2 需要把跨 run Artifact 委托建模为显式持久事实；4.2 应加入真实模型的大结果分页和 C2 逐页审批验收。
- 相关提交：本执行记录与 1.3 实现位于同一独立提交。

### 2026-07-30（任务开始）

- 完成内容：任务已正式开始；已核对本地 AI SDK 的 `prepareStep` / `activeTools` 语义，并完成工具披露和 Artifact 回读的独立只读审查。
- 实际改动：先更新任务状态；代码实施进行中。
- 验证结果：确认当前真实注册 75 个工具，静态领域表漏 `get_canvas_project`，且存在先截断后过滤、搜索入口被挤出、搜索分页最多只能覆盖前 20 项，以及未校验本轮活动工具 allowlist 等边界；确认 Artifact preview 必须通过 main 元数据 RPC 获取真实 data class，不能信任模型输入自报。
- 遗留问题：需在实现中冻结单轮工具集合、拒绝 dynamic/未披露调用，并为 Artifact describe/read 分离审批前元数据与审批后内容读取。
- 与原计划的偏差：新增“未披露工具调用拒绝”作为安全验收项；这是现状审查发现的执行边界，不扩大产品范围。
- 对后续任务的影响：2.2 必须只投影本轮已冻结工具契约；3.2 的异步结果引用可复用同一 Artifact 所有权与分页契约。
- 相关提交：前置任务 1.2 为 `275aff3`；本任务提交待完成后创建。

### 2026-07-30（计划建立）

- 完成内容：完成工具可达性和 Artifact 链路核实，实施尚未开始。
- 实际改动：仅创建本计划文件，无代码改动。
- 验证结果：已确认静态候选被截到前 8 项、直接领域场景缺少持续发现入口，Utility Artifact 回读固定为空。
- 遗留问题：动态 ranking 的初始权重和单轮 schema 预算需结合现有模型 context 数据确定。
- 与原计划的偏差：无。
- 对后续任务的影响：2.2 依赖稳定的工具/Artifact 投影，3.2 依赖可回读的异步结果引用。
- 相关提交：无。
