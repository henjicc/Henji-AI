# 1.2 工具执行安全与审批审计

## 任务状态

- 状态：待开始
- 所属阶段：第一阶段 · 运行正确性与能力可达性
- 前置任务：无
- 关联任务：2.3、3.1、4.1、4.2
- 是否可以并行：是，可与 1.1 并行

## 任务目标

封闭当前工具执行与审批链路中的高优先级安全缺口：模型输出被长度截断时绝不执行工具；用户批准的参数、预览、目标和 revision 在执行前不可漂移；批准、拒绝、过期、消费和失败都有可查询审计；审批模式的实际行为与界面说明一致。

## 当前情况

- `electron/main/services/agent-runtime/runner/runner.ts` 在模型返回 `toolCalls` 后直接调度执行，没有先拒绝 `finishReason=length` 或其他表明输出不完整的状态。
- `src/core/llm/modelStep.ts` 和 SDK 已能返回 `finishReason`，因此无需修改 Provider 才能先封闭该风险。
- `electron/main/services/agent-runtime/tools/approval.ts` 创建审批时保存 `argsDigest`、`previewDigest`、`targetIds`、`expectedRevisions` 和 TTL；消费时只核对工具、版本、参数摘要和 revision，没有重新绑定当前 preview 与目标。
- `electron/main/services/agent-runtime/tools/gateway.ts` 在消费审批前会重新生成 preview，但没有把新 preview 交给 `consume()` 校验。
- `electron/main/services/agent-runtime/persistence/migrations.ts` 已建立 `agent_permission_audit`，当前 `AgentPersistenceStore` 和运行时没有写入/查询闭环。
- `AssistantComposer.tsx` 将 `assistant_decides` 描述为“安全读取自动执行”，而 `requiresApproval()` 会自动放行所有 R1，包括可能持久写入的低风险工具。
- 可继续复用：Zod 输入校验、risk R0～R4、data class、scope revision、幂等 ledger、TTL、preview、supportsUndo 和现有结构化日志。

## 实施方案

在 Runner 的工具调度边界先判断模型步骤是否完整：凡 finish reason 表示长度截断、异常或无法确认完整性，全部工具调用转为不可执行的结构化失败/恢复提示，不允许挑选“看起来完整”的个别调用。

审批消费时使用最终解析参数重新生成 preview，并核对 `previewDigest`、目标 ID、可逆性、permission、工具版本和 revision；任何变换后再次执行 schema 与大小限制校验。审批生命周期写入现有 SQLite 审计表和结构化日志，但不得记录原始密钥或不必要的完整参数。

`assistant_decides` 的 R1 范围先完成产品确认。建议自动放行只读以及明确声明“低风险、可逆、目标稳定”的写操作，其余请求审批；若产品保留现状，则同步修正文案并让审计明确记录自动放行原因。

## 执行步骤

1. 按 `重要记录.md` 的记录 008 落实三种审批模式：`assistant_decides` 不再自动执行任何写操作。
2. 为 `length`、error、abort、多个工具调用混合、并行工具和正常 stop 编写 Runner 回归测试，先证明截断结果会触发当前执行路径。
3. 在工具调度前增加完整性裁决；对被拒绝调用生成统一错误码、日志和模型恢复消息，禁止进入网关、审批或幂等 ledger。
4. 扩展 `AgentApprovalManager.consume()` 输入，使用当前 preview 重算并比对摘要、目标和可逆性；最终参数再次 Zod 校验并重算 permission scope。
5. 为 `agent_permission_audit` 增加最小 repository：记录请求、自动放行、批准、拒绝、过期、消费、绑定失败和执行结果；元数据只保存摘要、风险、工具、目标计数和稳定关联 ID。
6. 将审批审计从 utility 安全传到 main 持久层，或在 main 网关边界统一写入；禁止让 renderer 直接写数据库。
7. 按确认后的策略同步网关测试、审批卡片和 Composer 文案，验证 ask、assistant_decides、full_access 三种模式。
8. 运行工具网关、Runner、安全审计和 UI 相关测试，更新任务记录、总览及独立提交。

## 涉及内容

### 新增文件

- `electron/main/services/agent-runtime/persistence/permission-audit-store.ts`：权限审计写入与按 run 查询，避免继续膨胀 `store.ts`。
- `electron/main/services/agent-runtime/persistence/permission-audit-store.test.ts`：覆盖自动放行、批准、拒绝、过期、消费、绑定失败和脱敏；本任务不新增独立审计 UI，继续复用现有日志/诊断入口。

### 修改文件

- `src/core/llm/modelStep.ts`：确认并规范可安全执行工具的 finish reason 语义。
- `electron/main/services/agent-runtime/runner/runner.ts`、`runner/model-execution.ts`：在调度前拒绝截断或异常工具调用并记录证据。
- `electron/main/services/agent-runtime/tools/approval.ts`：完整绑定当前 preview、目标、可逆性、参数和 revision。
- `electron/main/services/agent-runtime/tools/gateway.ts`：最终校验、审批消费和自动放行原因。
- `electron/main/services/agent-runtime/persistence/store.ts`、`migrations.ts`：复用审计表，只有索引/字段确实不足时追加 migration。
- `electron/main/services/agent-runtime/runtime.ts`、`electron/main/services/agent-runtime-manager/manager.ts`、`src/core/assistant/utilityContracts.ts`：按最终落点传递审计事实。
- `src/core/assistant/events.ts`：只有用户可见执行事实需要新事件时才扩展，避免把审计详情全部推给 UI。
- `src/features/assistant/conversation/AssistantComposer.tsx`、`ApprovalCard.tsx`：同步审批语义与说明。
- `electron/main/services/agent-runtime/tools/gateway.test.ts`、`runner/runner.test.ts`：新增安全回归。

### 可能废弃

- 不删除现有三种审批模式；只调整其可验证语义。
- 现有仅按 args/revision 消费审批的内部签名在全部调用方迁移后废弃。

## 特殊风险与回退要求

- 本任务属于安全边界改动，默认 fail-closed；不能以“模型通常不会截断”或“preview 通常不变化”作为兼容理由。
- 策略收紧可能增加审批次数。若体验不可接受，只能通过更精确的工具声明降低风险，不能绕过网关。
- 审计写入失败时高风险工具不得静默执行；低风险只读是否允许降级，需要在实现时明确并测试。

## 验收标准

- [ ] `finishReason=length` 或等价不完整结果中的全部工具调用均不进入网关，无论参数能否通过 schema。
- [ ] 正常 stop 的完整工具调用、并行调度和结果验证不受破坏。
- [ ] 审批后修改参数、preview、目标、可逆性、工具版本或 revision 任一项都会得到 `APPROVAL_INVALID`，工具不执行。
- [ ] 批准、拒绝、过期、消费、自动放行、绑定失败和执行结果都可按 runId/toolCallId 查询，且审计不含密钥和原始敏感正文。
- [ ] ask、assistant_decides、full_access 的网关行为、界面文案和自动化测试完全一致。
- [ ] 未知写副作用仍不自动重放，R3 仍需审批，R4 仍禁止执行。
- [ ] 相关测试、类型检查、Electron lint 和结构化日志检查通过。
- [ ] 本任务执行记录和任务总览已同步更新，并创建独立 Git 提交。

## 执行记录

### 2026-07-30

- 完成内容：完成安全缺口定位和实施计划，实施尚未开始。
- 实际改动：仅创建本计划文件，无代码改动。
- 验证结果：已确认 Runner 未拦截截断工具、审批消费未核对 `previewDigest`、权限审计表无实际写入以及 R1 文案存在策略差异。
- 遗留问题：无；三种审批模式的范围已在重要记录 008 确定。
- 与原计划的偏差：无。
- 对后续任务的影响：2.3 的保存点、3.1 的 busy 队列和 4.1 的模型错误契约都必须继承本任务的安全裁决。
- 相关提交：无。
