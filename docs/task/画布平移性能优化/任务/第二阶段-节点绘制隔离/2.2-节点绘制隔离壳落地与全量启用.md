# 2.2 节点绘制隔离壳落地与全量启用

## 任务状态

- 状态：待开始
- 所属阶段：第二阶段 · 节点绘制隔离
- 前置任务：1.1、1.2、2.1
- 关联任务：3.1（结论沉淀）
- 是否可以并行：否

## 任务目标

给画布节点加上一层「绘制隔离壳」：用透明内边距把节点的**绘制框**撑到能包住浮动标题、端口、resize 手柄等所有溢出装饰，再对该框启用 `contain: paint`。要求**外观逐像素不变**、**`.react-flow__node` 的测量尺寸不变**，并在 1.1 的基准上拿到可验证的帧率收益。

## 当前情况

### 为什么是这个方案

瓶颈是可见节点内容的逐帧重绘（隐藏节点内容立刻回到 58~60fps）。已排除的方案见 `01-实施方案.md` 第二节，其中最关键的两条：

- `will-change: transform` 会钉死合成层光栅倍率，画布常用倍率约 0.54，文字必糊——**不要重试**
- `content-visibility: auto`（ComfyUI 的做法）在我们的深层嵌套节点上是负优化，124 节点时掉到 8.4fps

`contain: paint` 是唯一既有大幅收益又能做到逐像素不变的方案：

| 配置 | 31 节点 | 124 节点 | 外观差异 |
|---|---|---|---|
| 现状 | 30~46 | 34~51 | — |
| 只加内边距（无 contain） | 44.3 | — | 0.000% |
| **内边距 + `contain: paint`** | **36.7~56.5** | **59.5** | **0.000%（最大差 1）** |
| 隐藏节点内容（上限） | 60 | 59.7 | — |

### 为什么不能直接加在 `.react-flow__node` 上

实测把 `box-sizing: content-box; padding: 36px 24px 20px; margin: -36px -24px -20px; contain: paint` 直接加到 `.react-flow__node`，节点盒会从 320×286 变成 368×342（其余类型同样 +48×+56）。xyflow 用 ResizeObserver 测量该元素，尺寸被污染会影响 minimap、fitView、框选判定。**这是本任务最重要的约束。**

### 需要覆盖的溢出量（来自 1.2 的探测，单位为流坐标 px）

- 顶部 32px：浮动标题（`NODE_HEADER_FLOATING_POSITION_CLASS = 'absolute -top-8 left-2 right-2 z-20'`，定义在 `src/features/canvas/ui/NodeHeader.tsx:51`）
- 左右各约 18px：`react-flow__handle` 端口
- 右下约 24px：`react-flow__resize-control` 手柄
- 额外：`storyboardGenNode` 正文超 34~58px、`imageNode` 正文超 32px
- 选择器节点的 843~1346px 由任务 2.1 决定处理方式

实际内边距取值以 1.2 探测出的最新数字为准，留出余量。

### 实施点

`src/features/canvas/nodes/index.ts` 的 `nodeTypes` 是**唯一注册点**，共 20 个类型。用一个包装高阶组件在这里统一套壳，**不需要改 11 个节点面板组件**。

节点当前 DOM 结构：

```text
.react-flow__node                     ← xyflow 拥有，尺寸被测量，不可改动其盒子
└── div（可见面板：border + bg + rounded + p-2，overflow-visible，宽高内联）
    ├── NodeHeader（absolute -top-8 …）
    ├── NodeLodPlaceholder
    ├── div.canvas-node-lod-detail
    ├── react-flow__handle
    └── react-flow__resize-control
```

隔离壳要插在 `.react-flow__node` 与可见面板之间。

### 已知风险

包装层用负外边距时，若 `.react-flow__node` 是**由内容撑开尺寸**（部分节点类型的 `style.height` 为空，见 1.2 的探测清单），负外边距可能反过来缩小父元素的测量尺寸。这一点必须在实现时逐类型验证，必要时改用不影响父盒尺寸的写法（例如壳自身 `position: absolute` 铺满父盒再向外扩展绘制区，面板保持在原文档流中）。具体写法在实现时确定，验收以「父盒尺寸不变 + 逐像素不变」为准。

## 实施方案

1. 新增一个薄组件 `CanvasNodePaintFrame`：只负责绘制隔离，不含任何业务逻辑，不引入新的视觉令牌。
2. 新增一个 HOC `withNodePaintFrame(Component, options)`，在 `nodes/index.ts` 组装 `nodeTypes` 时统一套上；`options` 支持按类型覆盖溢出量或整体跳过（供 2.1 结论 B 使用）。
3. 隔离壳的尺寸与 `contain` 走 `storyboard.css` 中的一个类 + CSS 变量，数值集中在一处便于调整。
4. 提供统一开关常量，关闭后行为与现状完全一致（**是否需要暴露为用户设置项：待确认**）。

## 执行步骤

1. 在 `src/features/canvas/ui/` 新增 `CanvasNodePaintFrame.tsx`，渲染一层包裹 `children` 的 `div`，挂 `canvas-node-paint-frame` 类，溢出量通过 CSS 变量传入（默认值取 1.2 的探测结果 + 余量）。
2. 在 `src/features/canvas/storyboard.css` 增加 `.canvas-node-paint-frame` 规则：透明内边距 + 等量负外边距 + `contain: paint`，并写清注释（为什么不用 `will-change`、为什么不用 `content-visibility`、为什么不能加在 `.react-flow__node` 上）。
3. 在 `src/features/canvas/nodes/index.ts` 新增 `withNodePaintFrame`，用它包装 `nodeTypes` 的每一项；对 2.1 判定需跳过的类型不包装。
4. 逐类型验证父盒尺寸：用 1.2 的脚本对比改动前后全部 20 个类型的 `.react-flow__node` 盒尺寸，必须完全一致。若某类型被负外边距影响，改用不影响父盒的写法并重新验证。
5. 用 1.2 的脚本做逐像素比对：静止态与手势中态都要跑，`changedPct` 必须为 0.000、`maxDelta` ≤ 2。
6. 用 1.2 的脚本确认 minimap 节点矩形与连线 `d` 属性一致。
7. 手工验证清单（**涉及鼠标操作，交给用户执行**，本文件需写清步骤与判定点）：
   - 拖动画布、拖动单个节点、框选多个节点
   - 拉伸带 resize 手柄的节点
   - 展开/折叠模型选择器节点，打开节点内各类下拉与面板
   - 连线拖拽、连线吸附到端口
   - 节点右键菜单、节点操作工具条
   - 缩放到 LOD 阈值上下（`canvasLodLevel` 的 0.25 / 0.4 / 0.6 附近）
8. 用 `npm run electron:pan-bench` 在 `BENCH_MULT=1` 与 `BENCH_MULT=4` 下各跑一次，与 1.1 记录的现状数字对比，把收益写入执行记录。
9. 运行 `npm run lint`、`npx tsc -p tsconfig.electron.json --noEmit`、`npm run check:colors`、`npm run check:surface:strict`、`npm run check:icons:strict`。
10. 为本任务创建一次独立 Git 提交。

## 涉及内容

### 新增文件

- `src/features/canvas/ui/CanvasNodePaintFrame.tsx`：绘制隔离壳组件

### 修改文件

- `src/features/canvas/nodes/index.ts`：新增 `withNodePaintFrame` 并包装 `nodeTypes` 的 20 个类型
- `src/features/canvas/storyboard.css`：新增 `.canvas-node-paint-frame` 规则与说明注释

### 可能废弃

- 无

## 验收标准

- [ ] 全部 20 个节点类型的 `.react-flow__node` 盒尺寸在改动前后**完全一致**
- [ ] `npm run check:canvas-visual` 通过：静止态与手势中态 `changedPct` 均为 0.000、`maxDelta` ≤ 2
- [ ] minimap 节点矩形尺寸与连线 `d` 属性在改动前后一致
- [ ] `npm run electron:pan-bench` 在 `BENCH_MULT=1` 与 `BENCH_MULT=4` 下均较现状有明确提升，具体数字写入执行记录（目标口径**待确认**，建议 31 节点 fps 中位 ≥ 55）
- [ ] 全部 20 个节点类型逐一确认无内容被裁（含展开态、下拉打开态）
- [ ] 步骤 7 的手工验证清单已交给用户执行并回收结论
- [ ] `npm run lint`、`npx tsc -p tsconfig.electron.json --noEmit`、`npm run check:colors`、`npm run check:surface:strict`、`npm run check:icons:strict` 全部通过
- [ ] 已创建独立 Git 提交，`00-任务总览.md` 已同步更新

## 回退方式

隔离壳带统一开关，关闭后 `nodeTypes` 不套壳，行为与现状完全一致；CSS 类不生效即可恢复。回退不涉及数据结构与持久化，无迁移成本。

## 执行记录

<!-- 执行时按 YYYY-MM-DD 追加 -->

