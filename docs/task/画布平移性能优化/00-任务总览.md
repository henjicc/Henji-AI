# 画布平移性能优化 · 任务总览

## 任务目标

画布在**持续朝一个方向拖动、不断露出新内容**时跑不满 60fps（真实 TEST 项目实测 30~46fps，掉帧明显）。历史上曾用 `will-change: transform` 提升视口合成层来缓解，但它会让拖动过程中的文字发虚，于是在 commit `9c07487` / `51e894f` 中被移除——**移除是对的，但没有任何东西补上**，所以现在是"字清楚但顿挫"。

本次要达到的结果：在**外观逐像素不变**的前提下，把真实项目的连续平移帧率提到接近渲染上限（实测上限约 59~60fps），并留下一套可信、可重复的性能与外观回归测量手段，避免以后再用错误的测法得出错误结论。

**属于本次任务**：

- 画布平移（拖动画布）的渲染性能
- 节点层的绘制隔离（`contain: paint` + 扩展绘制框）
- 阻塞该方案落地的节点布局问题（模型选择器节点内容超框）
- 可信的性能基准脚本与外观/功能回归校验脚本
- 结论沉淀（避免后人重走 `will-change` 死路）

**不属于本次任务**：

- 缩放（zoom）性能、节点拖拽性能、框选性能
- 减少单个节点的 DOM 元素数量（当前每节点约 77 个元素）——这是更大的结构工程，若第二阶段后仍不达标再单独立项
- 画布 LOD 等级体系的调整（`useCanvasContentLod` / `canvasLodLevel` 已存在且工作正常）
- 连线渲染、毛玻璃密度降级（`useCanvasGlassPerformance` 已存在）
- 智能助手、生成链路等非画布渲染的部分

## 当前进度

- 当前阶段：全部阶段已完成
- 当前任务：无
- 整体状态：代码、自动化验收与规范沉淀已完成；鼠标交互回归待用户按 2.2 步骤确认
- 已完成任务数量：5
- 总任务数量：5
- 当前阻塞问题：无

## 阶段列表

| 阶段 | 阶段目标 | 状态 |
|---|---|---|
| 第一阶段 · 建立可信性能基准 | 有一套能稳定复现顿挫、且不会被预热假象误导的测量手段；有一套能判定"外观是否逐像素不变"的校验手段 | 已完成 |
| 第二阶段 · 节点绘制隔离 | 让 `contain: paint` 可以安全套用到全部节点类型，并实际拿到帧率收益 | 已完成（待手工交互回归） |
| 第三阶段 · 收尾与沉淀 | 结论写进项目约定，修正测不出问题的旧压测场景 | 已完成 |

## 任务清单

| 编号 | 任务名称 | 状态 | 前置任务 | 任务文件 |
|---|---|---|---|---|
| 1.1 | 真实内容连续扫掠性能基准脚本 | 已完成 | 无 | [1.1](任务/第一阶段-建立可信性能基准/1.1-真实内容连续扫掠性能基准脚本.md) |
| 1.2 | 外观与功能回归校验脚本 | 已完成 | 无 | [1.2](任务/第一阶段-建立可信性能基准/1.2-外观与功能回归校验脚本.md) |
| 2.1 | 排查并处理模型选择器节点内容超框 | 已完成 | 1.2 | [2.1](任务/第二阶段-节点绘制隔离/2.1-排查并处理模型选择器节点内容超框.md) |
| 2.2 | 节点绘制隔离壳落地与全量启用 | 已完成（待手工回归） | 1.1、1.2、2.1 | [2.2](任务/第二阶段-节点绘制隔离/2.2-节点绘制隔离壳落地与全量启用.md) |
| 3.1 | 性能结论沉淀与旧压测场景修正 | 已完成 | 2.2 | [3.1](任务/第三阶段-收尾与沉淀/3.1-性能结论沉淀与旧压测场景修正.md) |

## 重要说明

1. **`will-change: transform` 在本项目是死路，不要再试。** 原因不是"分数像素重采样"，而是它把合成层的光栅倍率钉死：画布视口的 `scale` 通常不等于 1（常用工作倍率约 0.54），图层按 `0.54 × DPR` 光栅再放大回屏幕，文字必糊。已用"把位移强制吸附到整设备像素后再对比"的实验排除了分数像素这个解释。详见 `重要记录.md` 记录 001。
2. **性能测量必须用"连续扫掠 + 同启动内交替采样"**，否则结论不可信。原地小幅来回拖是 60fps，测不出任何问题；按配置顺序测会产生"第一个配置永远最慢"的预热假象。详见 `重要记录.md` 记录 004。
3. **外观必须逐像素不变**，这是用户明确的硬约束。任何方案落地前后都要用 1.2 的脚本比对截图。
4. **性能基准怎么跑**：`npm run electron:pan-bench`（默认 `BENCH_MULT=1`）跑真实项目原样；`BENCH_MULT=4 npm run electron:pan-bench` 是**判定收益的主场景**。×1 两次独立运行的中位数就能差 2.9fps，3fps 以内的差异在 ×1 下不构成结论。详见 `重要记录.md` 记录 007。
5. **最终性能**：同启动交替 A/B 下，×4 从关闭隔离的 23.1fps 提升到 43.8fps（+89.6%），p95 63.4 → 35.2ms，>50ms 严重长帧中位数 7 → 0；距离建议的 45fps 线尚差 1.2fps。模型选择器无需改布局；进一步减少单节点 DOM 量不在本轮范围内。详见 `重要记录.md` 记录 006、009。

## 最近记录

### 2026-07-29（全部任务完成）

- 21 种节点统一启用两级绘制隔离；全类型像素、节点盒、minimap、连线几何自动回归通过，未检测到可见外观变化。
- ×4 连续扫掠从 23.1 提升到 43.8fps，严重长帧归零；结果显著改善但如实保留与 45fps 建议线的 1.2fps 差距。
- `CLAUDE.md`、`henji-ui-surface` 性能参考和旧 stress 脚本已沉淀测量边界与禁用方案，任务文档回填最终数据。
- 自动化验收已完成；按项目约定，节点拖拽、resize、框选、端口连线与模型选择器滚动等鼠标交互由用户手工确认。

### 2026-07-29（任务 1.1 完成）

- 交付 `npm run electron:pan-bench`，能稳定复现顿挫：`TEST` ×4 现状 **26.9fps**，隐藏节点内容的渲染上限 **56.3fps**。
- 落地过程中推翻了计划里的 5 处设定（不能沿用源项目视口、副本改横向单排、扫掠长度按内容封顶、反向扫掠改精确复位、可见节点判据改取首尾较小值），原因写在任务 1.1 的执行记录里。
- 新结论：绘制成本主要随**视口内节点总量**增长，而不是随屏幕可见像素量增长（见 `重要记录.md` 记录 007），为第二阶段的 `contain: paint` 方案提供了独立佐证。
- 发现 `重要记录.md` 记录 005 的"临时项目已全部删除"不属实：`__panlab13_x4` 仍在真实数据库中，已备份后删除。

### 2026-07-29（计划创建）

- 完成画布平移性能的完整调研，定位机制并选定方案，创建本计划。
- 调研过程中曾两次得出错误结论并自行推翻：一次是把冷启动预热当成配置差异；一次是扫掠驱动 bug 让视口偏移指数爆炸到 `1e237`，导致几轮测量实际是在空画布上进行。两次教训已写入 `重要记录.md` 记录 004。
- 调研期间曾误改真实 TEST 项目的视口并已还原；临时测试项目已清理；**未提交任何代码改动**。
