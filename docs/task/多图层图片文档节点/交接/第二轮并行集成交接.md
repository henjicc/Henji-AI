# 第二轮并行集成交接

## 集成结论

3.2“导出普通图片节点并接入应用能力”和 4.1“补齐复制删除项目包与旧数据迁移”已按顺序合入 `main`，并收口为同一套多图层文档节点 application 服务、组合根和画布事务。当前计划为 7/8 完成：第二、第三阶段已完成，第四阶段进行中，下一任务固定为 4.2，不在本次集成中提前删除旧轻量界面。

## 合入提交

| 来源 | 原提交 | `main` 提交 | 内容 |
|---|---|---|---|
| 3.2 | `abaf06f6` | `c6415aaa` | 独立导出普通图片节点、UI 动作与应用能力 |
| 3.2 | `cef4c0aa` | `d504528a` | 3.2 交接与计划回填 |
| 4.1 | `a018c358` | `c9f00e17` | 文档 fork、引用感知清理、项目包与 V1 迁移 |
| 4.1 | `b1cd5ced` | `c62c533f` | 4.1 交接与计划回填 |
| 集成收口 | — | `a2d9572f` | 消除组合后的模块初始化环与无效占位实现 |

基线为 `4db50a20`。代码收口提交已推送；本文件和总览记录随独立文档提交推送。

## 冲突与解决

4.1 实现提交与已合入的 3.2 在 5 个文件发生冲突：

- `docs/task/多图层图片文档节点/00-任务总览.md`
- `docs/task/多图层图片文档节点/01-实施方案.md`
- `docs/task/多图层图片文档节点/重要记录.md`
- `src/features/canvas/application/multiLayerDocumentNodeApplicationService.test.ts`
- `src/features/canvas/application/multiLayerDocumentNodeGenerationAdapter.ts`

解决时未采用整文件 `ours/theirs`：

- 计划状态合并为 1.1、1.2、2.1、2.2、3.1、3.2、4.1 共 7 项完成；记录 014 保留独立导出决策，4.1 所有权决策顺延为记录 015。
- application service 测试同时保留保存/物化/CAS、独立导出、fork、释放候选和 V1 迁移用例。
- GenerationAdapter 同时装配投影、导出画布和生命周期窄端口，只创建一份 `MultiLayerDocumentNodeApplicationService`。

组合测试首次运行发现真实集成问题：`generationAdapter -> canvasAdapter -> canvasBatchService -> canvasMutationService -> generationAdapter` 在模块初始化期形成循环，导致投影画布端口尚未初始化。`a2d9572f` 将复制、回滚和删除候选登记改为事务执行时延迟加载同一 GenerationAdapter；没有新增服务、能力入口或业务协议，普通节点复制逻辑也未改变。同期移除合并后已无调用者的 `unavailable` 占位函数。

## 统一接口与事务语义

- 唯一服务继续是 `MultiLayerDocumentNodeApplicationService`，同时具备 `saveMaterializedProjection`、`exportTarget`、`forkDocument`、`markReleaseCandidate` 和 `migrateLegacyDocument`。
- 3.2 的 `createExportedImageNode` 创建普通 `exportImageNode` 与源到结果边；普通节点不保存 `imageEditSession`、`layerStackDocument` 或其他 V3 引用。失败回滚节点、边、选择/弹窗状态并释放未接管 PNG。
- 4.1 的复制统一走 `commitCanvasNodeDuplication` 与文档 fork；副本持有独立 `documentRef` 和历史头，fork 未被节点接管时按精确 revision 补偿。
- `LayerStackResultNode` 对新 V3 节点直接打开完整编辑器；V1 仅在首次打开时幂等迁移，成功后进入同一个完整编辑器，不回退旧轻量界面。
- 正式能力 `export_image_edit_target_to_canvas` 的定义、handler、Effect 与稳定引用均保留；UI 和助手共享 `exportMultiLayerDocumentTargetToCanvas`，未新增 `HostCommand`、`HostQuery` 或第二处理入口。
- Electron repository/IPC/preload/PAL 同时保留 fork、standalone materialization、条件删除和 GC 契约；项目包继续强制一节点一文档，并保留导入补偿及 history/dialog/project-operation lease。

## 组合验证

- 3.2 精确矩阵：9 个文件、67 个用例通过；相对原分支 64 项，多出的 3 项为 4.1 合入后的同文件生命周期/迁移用例。
- 4.1 精确矩阵：18 个文件、115 个用例通过；相对原分支 113 项，多出的 2 项为 3.2 合入后的同文件导出/能力用例。
- 去重组合矩阵：24 个文件、147 个用例全部通过，覆盖冲突服务、画布适配器、导出、复制、释放、迁移、项目包、UI 与能力处理器。
- `npm run check:assistant-capabilities` 通过：结构检查通过、coverage 6/6、invariants 29 个文件 165 项通过。
- Reality 场景目录静态测试 `scripts/lib/uiInspection.test.cjs`：21/21 通过。
- renderer 与 Electron 两套 TypeScript 检查通过；全部受影响 TS/TSX ESLint 通过；`npm run lint:electron -- --quiet` 通过。
- `npm run check:main-imports` 通过，扫描 492 个 main/preload 文件；`git diff --check` 通过。

本次没有重跑 `canvas-multi-layer-document-editor` Reality：集成收口只修复复制/释放路径的模块初始化时机，没有修改 3.2 的真实导出事务、UI handler、稳定选择映射或“导出后编辑器保持打开”逻辑。3.2 已有的完整 Reality 证据继续有效；4.2 按任务文件负责最终双尺寸 Electron 综合场景。未执行真实付费模型调用，也未使用助手 `full_access`。

`assistant-status.md` 未更新：本次没有改变能力“通/不通”，没有新增或删除 KNOWN 欠账，也未推翻既有完成结论。

## 4.2 准确边界

4.2 只负责旧界面清理与最终综合验收：

1. 全仓确认无调用者后，删除 `LayerStackSpecialEditor` 及 `specialEditorRegistry` 的 `layers` 注册，并统一节点面向用户的“多图层图片/编辑”文案。
2. 必须保留 `LayerStackDocumentV1` 读取、`layerStackV1Adapter`、`migrateLegacyMultiLayerDocumentNode`、正式 fork/候选清理、项目包校验，以及 3.2 的导出按钮和应用能力。
3. 扩展真实 Electron 场景，串联 V1 首次/二次打开、实时保存与关闭刷新、目标导出、复制独立编辑、Alt 拖拽、删除撤销/重做和项目包往返；使用两个合理窗口尺寸并目视检查。
4. 不重新实现文档服务、导出事务、复制事务或媒体下载旁路；发现问题时只在既有唯一入口修根因。
5. 真实供应商付费烟测仍需用户明确授权；未授权不阻塞本地代码任务完成，但必须继续标为待确认。

## 开发环境

- 启动前未发现当前仓库或 3.2/4.1 worktree 的 Electron 开发实例，因此没有结束其他项目进程。
- 当前仅保留一个属于主工作区 `/Users/henji/Documents/VibeCode/Henji-AI` 的实例，命令为 `npm run electron:dev -- --background --dev-skip-onboarding --dev-surface=tool.image_edit --dev-media=docs/ref/test01.jpg`。
- 主实例已成功构建 main/preload，渲染服务监听 `[::1]:5173`，Electron 已以图片编辑页面和 `docs/ref/test01.jpg` 启动。
- 另一个监听 `127.0.0.1:5173` 的进程属于 `/Users/henji/Documents/VibeCode/动画测试`，按隔离规则未触碰。
