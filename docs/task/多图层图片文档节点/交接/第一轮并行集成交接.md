# 第一轮并行集成交接

## 集成结论

1.2 与 3.1 已安全合入 `main`。新图层分离结果会在幂等检查通过后创建独立 V3 文档，再原子交接给原占位节点；稳定的图层、组与标注引用可经同一 application 服务窄端口物化为原画布尺寸的透明受管 PNG，不修改源文档。

当前计划进度为 3/8：1.1、1.2、3.1 已完成，第一阶段已完成，第三阶段尚待 3.2 收口。下一执行任务是 2.1。

## 合入提交

- 1.2 实现：原提交 `35b5cb84`，合入 `main` 后为 `fd57601b`。
- 1.2 交接：原提交 `842e4928`，合入 `main` 后为 `14c2b885`。
- 3.1 实现：原提交 `b56185ce`，合入 `main` 后为 `42233deb`。
- 3.1 交接：原提交 `34ee590f`，合入 `main` 后为 `6d2988b7`。
- 3.1 同步记录：原提交 `194ae84a`，合入 `main` 后为 `dfaf64bd`。
- 集成收口：`chore(canvas): 汇合多图层文档首轮并行实现`（本交接所在提交）。

## 冲突与解决原则

- 唯一手工冲突文件为 `docs/task/多图层图片文档节点/00-任务总览.md`。没有选择任一分支整体覆盖，而是合并双方任务状态与最近记录：1.1/1.2/3.1 已完成、完成数 3/8、第一阶段完成、第三阶段进行中、下一任务 2.1。
- `01-实施方案.md`、`重要记录.md`、应用服务、platform/commands 和 Electron 物化链路均自动加法合并；集成后仍逐项审查了语义，没有把自动合并当作正确性证明。
- 保留 1.2 的 `deleteDocumentIfRevision` 精确补偿链路；它只用于新文档尚未交接所有权时的失败回滚，不替代 4.1 的引用感知正常删除。
- 保留 3.1 的流式受管栅格 `publication` 语义，并在跨层结果上增加 `document-preview` / `standalone-image` 字面量判别字段；省略入参仍默认为 `document-preview`，现有调用行为不变。

## 统一最终接口

- `MultiLayerDocumentNodePort` 仍是宿主文档、像素和资源能力的唯一完整契约，包含 `createFromLayerStack`、`inspectDocument`、`saveAndMaterialize`、`forkDocument`、`markReleaseCandidate`、`materializeExportTarget` 和 `releaseExportRaster`。
- 1.2 的生成组合根 `multiLayerDocumentNodeGenerationAdapter.ts` 现在直接展开 `createMultiLayerDocumentExportPort()`，创建与独立导出不再使用两套端口或两个 application 服务。尚未到实施阶段的检查、保存物化、fork 和释放候选仍显式拒绝，由 2.1/2.2/4.1 扩展这一组合根，不能新建平行服务。
- `materializeImageEditorV3Raster` 继续使用 `document-preview`，成功时返回带 `publication: 'document-preview'` 的预览引用；`materializeImageEditorV3StandaloneRaster` 显式使用 `standalone-image`，成功时返回可补偿受管路径。两类结果以判别联合互斥。

## 组合验证

验证级别为 L2：本轮合并了公共契约、文件 I/O、IPC/preload 与异步事务，需要同时验证渲染层和 Electron 工程。

- 精确组合 Vitest：16 个文件、95 个用例全部通过；覆盖 1.1 状态/服务、1.2 生成适配/原子落图/图层分离/恢复轮询/条件删除，以及 3.1 目标派生视图/物化适配/command/主进程/IPC。
- 集成修复后再运行直接相关的 4 个测试文件、14 个用例，全部通过。
- `npx tsc -p tsconfig.json --noEmit`：通过。
- `npx tsc -p tsconfig.electron.json --noEmit`：通过。
- 从 `c51b10ec` 起所有改动的渲染层 TypeScript/TSX 定向 ESLint：通过。
- `npm run lint:electron -- --quiet`：通过。
- `npm run check:main-imports`：通过，扫描 492 个 main/preload 文件。
- `git diff --check`：通过。
- 未运行 Reality、全量测试或付费烟测：两个子任务已各自完成 L2，本次集成没有 UI 行为、视觉几何或真实供应商请求的升级条件。

## 遗留风险

- 2.1/2.2 尚未实现 `inspectDocument` 与 `saveAndMaterialize`；当前生成路径只接管创建和独立渲染能力。
- 3.2 尚未实现 `createExportedImageNode`，因此 3.1 只产出可被画布事务接管的 PNG，不会当前就在画布创建普通图片节点。
- 浮点/HDR 文档首版明确拒绝 PNG 独立导出；不做静默降位。
- 真实付费供应商烟测仍需用户明确授权。

## 2.1 准确接入点

1. 新节点已是 `editable-v3` 投影；在 `LayerStackResultNode.tsx` 的双击/编辑动作中进入完整 V3 编辑宿主，不得再从 `layerStackDocument` 摄取新文档。
2. 复用 `CanvasEditToolEditorV3Host.tsx` 和现有 `ImageMarkV3PersistenceQueue`，但打开前必须先经 `MultiLayerDocumentNodeApplicationService.openAndValidate`。
3. 扩展 `multiLayerDocumentNodeGenerationAdapter.ts` 当前的唯一 `MultiLayerDocumentNodePort` 组合根，接入 `inspectDocument`；不得新建第二套文档节点服务或绕过 application 层直连仓库。
4. 关闭语义只做“禁止新交互 → flush 实时保存队列”；物化与节点原位投影由 2.2 实现，2.1 不提前新建派生节点。
5. `NodeToolDialog.tsx` 的通用“应用后创建派生节点”流程不适用于该特殊节点；旧 `LayerStackSpecialEditor` 仍仅保留给 V1 兼容，最终收口属于 4.2。

## 主分支与远端状态

- 基线：集成前 `main` 与 `origin/main` 均为 `c51b10ec`，工作区干净。
- 最终 `main` HEAD：本交接所在的集成收口提交；精确哈希与推送后 ahead/behind 证据见任务交付回复。
- 集成提交完成后执行 `git pull --rebase`，推送 `origin/main`，并以 ahead/behind 各为 0、根工作区干净作为完成条件。
