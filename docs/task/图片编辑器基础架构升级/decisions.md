# 执行决定

本文件记录执行期间形成、会影响后续任务的决定；稳定的全局决定同时同步到 `重要记录.md`。

## 决定 001：阶段测试统一后置

- 日期：2026-07-25
- 状态：已确定
- 决定：第一、第二阶段不运行自动测试或鼠标验收，只维护 `test-report.md`；所有任务完成后统一执行。
- 影响：阶段任务可以完成代码实现与静态审查，但测试相关验收项保持待验证。

## 决定 002：先保持现有宿主兼容入口

- 日期：2026-07-25
- 状态：已确定
- 决定：第一、第二阶段只建立核心契约和共享编辑器，保留 `ImageMarkDoc`、`ImageMarkSession`、`MarkEditor` 及现有宿主调用方式；第三阶段再迁移宿主。
- 影响：1.1、1.2、2.1、2.2、2.3、3.1、3.2、3.3。

## 决定 003：V2 使用有序操作文档

- 日期：2026-07-25
- 状态：已确定
- 决定：V2 使用 `version: 2` 与有序 `operations`；V1 的朝向、标注、裁剪按原像素顺序映射为三个内置操作。编辑器内部使用 V2，旧入口通过投影适配。
- 影响：1.1、1.2、2.1、2.3、3.1、3.2、3.3。

## 决定 004：当前执行端口按完整文档一次合成

- 日期：2026-07-25
- 状态：已确定
- 决定：操作注册表逐项校验能力和参数，执行器接收校验后的完整文档。当前浏览器执行器把内置操作投影为 V1 后一次调用现有 Canvas 合成，不逐操作生成中间 data URL。
- 影响：1.2、2.3、3.2、3.3；未来 Electron 执行器实现同一端口即可。

## 决定 005：右侧检查器使用图片编辑专属 shell

- 日期：2026-07-25
- 状态：已确定
- 决定：当前只需一个固定右侧检查器，不新增 `src/components/ui` 通用 Primitive，也不引入 Dockview；使用 `src/features/imageEdit/editor/ImageEditorShell.tsx` 和专属 UI store。
- 选择原因：避免把单面板需求扩大为多 Tab 停靠系统，同时不违反通用 UI Primitive 单点约束；未来需要多面板自由停靠时再评估 Dockview。
- 影响：2.2、2.3、3.1、4.1。

## 待形成决定

- 无架构路线待确认；真实窗口下的最终视觉尺寸属于验收项，不改变当前 shell 技术选择。

## 决定 006：第三阶段按宿主适配层迁移

- 日期：2026-07-25
- 状态：已确定
- 决定：工具箱、查看器、画布和智能助手只消费 `src/core/imageEdit` 的文档、注册表和执行端口；宿主保留现有公开入口、导航 ID、工具名及结果语义。
- 选择原因：避免三套图片编辑规则继续分叉，同时不把 React、画布节点或助手协议反向放入核心契约。
- 影响：3.1、3.2、3.3、4.1。

## 决定 007：生成工作区写入 V2 会话并兼容旧会话读取

- 日期：2026-07-25
- 状态：已确定
- 决定：查看器和生成工作区内存/落盘状态使用 `ImageEditSession`；任务重放统一通过 `coerceImageEditSession()` 接受旧 `ImageEditState`、`ImageMarkSession` 和 V2 会话。
- 影响：3.1、4.1。

## 决定 008：画布编辑 options 兼容双写

- 日期：2026-07-25
- 状态：已确定
- 决定：画布编辑工具新增字符串字段 `document` 保存 V2 文档，同时继续写入旧 `markDoc`；读取优先 `document`，缺失时回退 `markDoc`。
- 影响：3.2、4.1；旧画布数据无需批量迁移。

## 决定 009：助手预览引用采用有界成功释放策略

- 日期：2026-07-25
- 状态：已确定
- 决定：助手图片编辑预览最多保留 64 项，超限淘汰最早引用；仅提交成功后释放引用，失败时保留以支持重试，重复成功提交按未知引用处理。
- 影响：3.3、4.1。

## 阶段交接

- 2026-07-25：第一阶段代码实现提交为 `d85a15c`，1.1/1.2 等待统一验证。
- 2026-07-25：第二阶段开始，当前执行 2.1。
