# 测试记录

## 测试策略

- 状态：统一后置
- 执行时机：全部任务完成后
- 当前阶段只登记测试范围、命令和人工验收项，不运行测试。

## 待执行自动测试

### 第一阶段

- V2 文档合法解析、序列化和默认值。
- V1 文档、纯标注数组、旧 `ImageEditState`、`ImageMarkSession` 兼容读取。
- 损坏 JSON、未知版本、非法操作参数和未知操作的确定回退。
- 操作注册重复 ID、默认参数、校验与执行 handler 分发。

### 第二阶段

- 统一历史、撤销重做和瞬时交互状态清理。
- 标注交互与右侧工具选择互不覆盖。
- 面板宽度约束、折叠、恢复、键盘调整和偏好持久化。
- 工具注册排序、可见性、参数面板切换和几何操作接入。

## 待执行检查命令

- 定向 Vitest。
- `npm run check:colors`
- `npm run lint`
- `npx tsc -p tsconfig.electron.json --noEmit`
- `npx eslint electron --ext ts --report-unused-disable-directives --max-warnings 0`
- 最终阶段按需执行 `npm run electron:build`、`npm run electron:smoke`、`npm run electron:dpi-check`。

## 待人工验收

- 标注选择、绘制、文字编辑、裁剪、旋转、镜像与撤销重做。
- 右侧面板拖动、键盘调整、折叠、恢复和小窗口布局。
- 顶部标注切换与右侧参数工具切换互不丢失状态。

## 当前结果

- 尚未执行测试。
- 第一阶段代码已完成，1.1、1.2 因统一后置策略保持“待验证”。
- 第二阶段新增的会话、历史、外壳和工具注册测试项会继续追加，当前不执行。
- 第二阶段代码已完成，2.1、2.2、2.3 因统一后置策略保持“待验证”。
- 第三阶段代码已完成，3.1、3.2、3.3 因统一后置策略保持“待验证”。
- 本阶段未运行自动测试、TypeScript、ESLint、Electron 或鼠标交互验收。

## 第三阶段待执行测试项

### 3.1 工具箱与查看器

- 新版 `ImageEditor` 宿主接入、旧会话恢复、每图会话隔离和任务重放。
- 工具箱打开/粘贴/拖入/复制/另存为/加入资产库、取消与保存。
- 查看器取消不写回、保存新资产、再次编辑和损坏会话回退。
- 结构化日志覆盖打开、导出和保存的 `start/completed/failed`。

### 3.2 画布工具适配

- 编辑/切割注册策略、未知工具和非法 options 的确定错误。
- `NodeToolDialog` 按注册信息渲染，`CanvasToolProcessor` 按 handler 分发。
- 旧 `options.markDoc` 读取、共享编辑器保存、结果节点和连线语义。

### 3.3 智能助手协议

- 两个助手工具名称、作用域、输入输出和错误码兼容。
- 助手操作到核心文档转换、旋转后裁剪、全部标注类型和边界校验。
- 预览引用未知/重复提交、执行失败重试和成功释放。
- 助手路径与编辑器路径的同序列文档/执行结果一致性。
