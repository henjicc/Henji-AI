# 交接说明

## 当前执行位置

- 第一阶段代码与自动测试已完成，1.1、1.2 状态均为待验证；提交为 `d85a15c`。
- 第二阶段代码与自动测试已完成，2.1、2.2、2.3 状态均为待验证；提交为 `36d4dc6`。
- 第三阶段代码与自动测试已完成，3.1、3.2、3.3 均为待验证；提交为 `a4b2ac4`。
- `src/core/imageEdit/` 已包含 V2 文档、V1 兼容、操作注册和执行端口。
- 第二阶段已收口；`ImageEditor` 通过 V2 会话接入 shell，旧 `MarkEditor` 默认保持 legacy 布局。
- 第四阶段 4.1 已完成兼容收口和全部自动检查，状态为待验证；只剩真实 Electron 鼠标交互验收。

## 必须保留的兼容边界

- 旧 `ImageMarkDoc V1`、纯标注数组、旧 `ImageEditState` 和 `ImageMarkSession` 必须继续可读。
- `markDoc`、`imageMark` 导航 ID 与智能助手工具名暂不改动。
- 工具箱、查看器、画布和智能助手已经迁移，但旧公开名称和存量数据读取路径仍需保留到最终验证完成。

## 已冻结核心契约

- `ImageEditDocument.version` 固定为 `2`。
- `operations` 顺序即执行顺序。
- V1 内置映射顺序为朝向、标注、裁剪。
- 旧数据读取时迁移，不做后台批量重写。
- 生成工作区新写入 `ImageEditSession`；旧会话由 `coerceImageEditSession()` 恢复。
- 画布编辑 options 优先读取 V2 `document`，并继续双写旧 `markDoc`。
- 助手对外工具名与输入输出协议不变，内部预览保存 V2 文档。

## 验证状态

- 图片编辑定向测试 5 个文件、17 个用例全部通过。
- 全量 Vitest 94 个文件、404 个用例通过；2 个文件、7 个用例按仓库条件跳过。
- 颜色检查、前端/主进程 ESLint、Electron TypeScript、构建、smoke、6 组 DPI 检查和 `git diff --check` 全部通过。
- 真实 Electron 中的绘制、拖拽、点击、剪贴板和画布连线按项目约定未由执行 AI 操作，详见 `test-report.md`。

## 本轮工作约束

- 四个阶段的代码工作均已结束，后续只进行最终人工验收或处理验收发现的问题。
- 不实现黑柔、白柔、灰光或调色算法。
- 不新增正式图片效果节点、空 IPC 或空 Electron 服务。
- 不重复运行已经通过的自动检查；新增改动时仅补充受影响范围的验证记录。
