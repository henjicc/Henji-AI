# 2.2 接通后台任务与跨域保存

## 任务状态

- 状态：已完成
- 所属阶段：第二阶段：可靠写入与后台任务
- 前置任务：2.1
- 关联任务：3.1、3.2
- 是否可以并行：本任务开始前仍串行；内部生成与图片编辑调查可并行，公共操作存储、App 根节点及构建验收不得并发修改。

## 任务目标

让生成、三维渲染、图片编辑和标注通过 MCP 完成真实任务闭环，解决 MCP 所需的页面依赖、原目标恢复、跨域保存和媒体结果交付。

## 当前情况

- `src/workspaces/GenerationWorkspace.tsx` 挂载任务状态、历史加载和 `useTaskGeneration`，后者在 Hook 中注册可见生成任务处理器。
- `src/features/generation/application/generationPreparationService.ts`、`generationApplicationService.ts` 可复用；`src/core/services/GenerationService.ts` 是页面调用的正式生成服务，继续走既有 AI 运行链。
- `electron/main/services/camera-stage-render.ts`、`camera-stage-render-task-registry.ts` 提供已有三维后台任务；不重新实现渲染。
- `src/features/imageEdit/v3/application/imageEditLiveSessionRegistry.ts` 和 `imageEditPersistenceOperations.ts` 绑定原文档总线及保存所有者；工具箱与画布附着文档不能互换。
- `src/features/assistant/applicationCapabilities/generationResultCanvasApplicationService.ts` 与 `src/features/canvas/application/generationOutputApplicationService.ts` 已有结果进入画布的领域桥梁。
- 媒体存在应用内协议和稳定引用，外部客户端的实际读取能力尚未验证。

## 实施方案

沿现有领域入口提升任务生命周期，React 订阅和展示，后台工作不依赖生成页面。所有提交先取得 2.1 的操作身份，长任务的状态和结果由领域存储及任务记录提供，不能依赖助手轮询续跑。图片会话必须真实存在；只移除非必要页面依赖。

阅读[实施方案](../../01-实施方案.md)后执行；若实际代码与本文件不同，先更新路径和方案再开发，不重做无关全仓调查。

## 执行步骤

1. 梳理生成创建、排队、供应商提交、状态查询、历史保存和取消的直接消费者；把页面持有的必要执行状态提升到应用级服务或根生命周期，原页面改为消费同一实现。
2. 给生成及三维渲染补原操作关联和持久任务引用；提交与查询分开，超时后查询原任务，不按名称猜结果、不重新付费。应用重启只核对及查既有状态。
3. 接通图片文档和标注读写，分别验证工具箱宿主与画布节点附着宿主；缺失会话返回原目标恢复入口，已修改但保存失败保留事实并提供仅重试保存。
4. 验证跨工程的画布与三维修改；可后台操作不切页，真实会话型动作声明前置条件。不能为了通过测试默认把目标切成当前工程。
5. 复用现有媒体解析和导出能力，提供授权范围内的图片预览及视频／音频结果获取；兼容不支持富媒体的客户端，不暴露任意本地路径或任意 URL 代理。
6. 沿正式业务回读验证生成结果进入画布、图片附着文档双域保存和三维结果引用；导航作为单独明确调用，导航失败不能更改业务成功事实。
7. 先执行非付费集成与隔离 Electron 生命周期测试。真实付费供应商行为留到 3.2 记录具体预算与清理范围后，沿用户本次自主授权验证，清楚标记本任务证据边界。

## 涉及内容

### 新增文件

- 预计在 `src/features/generation/application/` 或现有工作区 application 目录补应用级任务协调模块；选择由真实职责决定，不复制 GenerationService。
- MCP 媒体结果投影、受控资源读取适配和跨域恢复测试，沿 1.2 的协议模块扩展。

### 修改文件

- `src/workspaces/GenerationWorkspace.tsx`、其 `hooks/useTaskGeneration.ts`、`hooks/useTaskHistory.ts`、`application/visibleGenerationTaskCommand.ts`、`src/App.tsx`：生命周期和持久确认。
- `src/features/generation/application/`、`src/services/database/DatabaseService.ts`：任务关联与正式保存。
- `electron/main/services/camera-stage-render.ts`、`camera-stage-render-task-registry.ts`：查询、取消和重启核对。
- `src/features/imageEdit/v3/application/`、`src/features/imageMark/application/`、`src/features/canvas/application/`：原会话、跨域保存与结果应用。
- `src/features/navigation/application/surfaceNavigationService.ts` 及现有媒体服务目录：必要的调用来源与受控结果获取。

### 可能废弃

- 生成页面独占的后台执行注册改为消费共享生命周期；不丢弃页面功能，不新写供应商适配，不删除助手模块。

## 验收标准

- [x] 离开生成页面、最小化、渲染层重载后可查询原任务，已提交业务不重复创建。（最小化未单独取证，见执行记录「证据边界」）
- [x] 应用退出停止本地调度；重启能核对旧任务，不自动生成或发起模型请求。
- [x] 三维后台渲染及结果应用真实可查；断线后沿稳定引用继续查询。
- [x] 图片编辑和标注分别覆盖真实会话；附着画布文档保存失败可恢复原保存，不用另一宿主伪造成功。
- [x] 支持从结果引用获取实际允许的媒体内容；只支持文本的客户端仍能理解状态和继续操作。
- [x] 外部只读、禁止导航或已撤销权限得到尊重；拒绝不造成意外写入。
- [x] 按 L2 执行领域、恢复及跨域集成；必须依赖 Electron 的结论用统一 Reality 验证，并检查截图与日志。
- [x] 执行记录列明各领域实际覆盖、付费未测部分及相关提交，总览同步更新。

## 执行记录

### 2026-09-12（实现与验收）

- 完成内容：生成生命周期提升到应用根节点；生成提交与三维渲染任务落到持久账本；图片编辑保存宿主身份化并接通仅重试保存；画布与三维支持后台工程执行和跨工程保存；新增按稳定引用分块读取媒体的 MCP 工具；写入范围、能力清单与连接权限扩展到八写域并单独拆出付费授权。
- 实际改动（按职责）：
  - 生成生命周期：新增 `src/features/generation/application/generationLifecycle.ts` 与 `GenerationLifecycleProvider.tsx`，由 `src/App.tsx` 挂载；`GenerationWorkspace.tsx` 改为消费同一实例，切页不再卸载执行器。`hooks/useTaskHistory.ts` 把历史保存从「整表重写」改成按任务的唯一写入入口并可等待落盘，`useTaskState.ts`／`useTaskGeneration.ts`／`continuePollingTask.ts` 的每次状态推进都等待持久确认。
  - 生成不重复付费：新增 `electron/main/services/ai-runtime/generation-submissions.ts`，先记意图再允许供应商调用；`runtime.ts` 分 provider→media→completed 三阶段封存回执，收尾失败只重试媒体保存，不重新调用 SDK，也不再删除已落盘的真实结果。`pending-results.ts` 的读取不再消费回执。
  - 三维后台任务：新增 `camera-stage-render-task-storage.ts`，`camera-stage-render-task-registry.ts` 支持重启恢复——非终态任务标为「应用已退出，原渲染已停止」，不自动重排队；已确认任务保留身份防重。
  - 跨工程执行：新增 `src/features/canvas/application/canvasProjectRuntime.ts` 与 `src/features/cameraStage/application/cameraStageProjectRuntime.ts`／`cameraStageProjectExecution.ts`，`canvasStore`／`cameraStageStore` 拆出可实例化工厂；未打开的工程用独立实例执行并保存，当前编辑实例不被替换，也不把目标切成当前工程。
  - 图片编辑：`ImageEditPersistenceOwnerV3` 带 `ownerId`，失败事实与 `retry_image_edit_document_save` 的恢复入口一起记录；宿主被替换时返回 `RECOVERY_SESSION_LOST`，不借用新宿主伪造成功。
  - 媒体结果：新增 `electron/main/services/mcp/mediaResources.ts` 与 MCP 工具 `read_application_media`，只接受 `generation.result`／`asset`／`canvas.node` 稳定引用，realpath 后仍需落在已授权媒体根内，失败信息不回传本地路径；分块返回 base64，纯文本客户端可只用状态字段继续操作。
  - 账本与权限：`operationCoordinator.ts` 扩展到八写域并按能力定义解析持久目标；新增 `persistedOperationRecovery.ts` 用正式历史主键核对外部生成操作。连接新增独立的「允许付费生成」授权（`connections.ts`、`McpSection.tsx`、i18n），未授权时付费工具既不出现在清单里也拒绝直呼。
- 验证结果（全部本地实跑，命令与数量见交接记录）：两工程 `tsc --noEmit` 通过；本次 86 个改动文件 ESLint `--max-warnings 0` 通过；相关精确单测 308 文件 1631 测试通过；原生 Electron SQLite 运行器 4 文件 19 测试通过；`npm run test:suites` 4/4 通过；`npm run test:reality -- --build --suite ui --only mcp-read-lifecycle,mcp-write-recovery,mcp-background-crossdomain --size 1440x900` 于 2026-09-12T03:47:46Z 三场景零失败，Agent 已目视三张截图，evidence.json 中 `browserErrors`／`logErrors` 均为空，唯一 warning 是本场景刻意构造的媒体拒绝。
- 修复的既有缺陷：三处工具清单断言写死数量，读取能力扩容后会整体变红，改成「必须含读取工具、不得含写入工具」的名单断言；`assistantHarness.writeLoop` 的三维真相源断言仍编码旧的「写入顺手 openProject」行为，改为盯持久化并新增「不得把目标切成当前工程」的反向断言。
- 证据边界：
  - 窗口最小化未单独取证。生成执行器现在挂在 App 根节点、状态推进逐条等待落盘，渲染层重载后可查询已由 Reality 与精确测试覆盖；最小化路径按同一生命周期推导，未实测。
  - 应用真实退出重启未在 Reality 中执行，用注册表与操作账本的精确／原生测试代替（非终态任务重启后标为已停止、不自动重排队；unknown 操作重启后仍阻断）。
  - 未调用任何付费供应商。真实付费生成、跨客户端矩阵与完整桌面退出重启矩阵留给 3.2。
- 遗留问题：后台三维工程实例缓存上限 32 且只释放已保存实例，保存失败时内存占用不回收（刻意，不丢未保存内容）；`generation_submissions` 账本按设计不清理，避免同 requestId 重复扣费；`readDomainSnapshot` 对未打开工程读持久化快照，后台实例存在未保存内容时读到的是上次保存值。
- 与原计划的偏差：执行步骤 5 的媒体交付没有新建协议模块，而是在 1.2 的 MCP 服务里直接加一个工具，复用现有媒体根授权与画布引用解码；执行步骤 3 的「缺失会话恢复入口」由 2.1 已有的 `retry_application_operation_save` 承接，只补 `ownerId` 绑定，未新增恢复通道。
- 对后续任务的影响：3.1 需要把新增的读写工具、`read_application_media` 与付费授权一起纳入能力发现与客户端兼容收敛；3.2 需要用真实付费连接验收生成闭环与真实退出重启矩阵。
- 相关提交：见本文件所在目录的 Git 历史（标题 `feat(mcp): 接通后台任务、跨工程保存与媒体读取`）。

### 2026-09-12（计划编制）

- 完成内容：调查当前实现并编制本任务；尚未开始开发。
- 实际改动：仅创建本计划任务文件，没有修改上述业务代码。
- 验证结果：代码路径和依赖关系已做只读核查；开发验收项均未执行。
- 遗留问题：按实施方案「待确认事项」在本任务所属范围内核定。
- 与原计划的偏差：无；本文件为首次基线。
- 对后续任务的影响：前置任务未完成前不开始依赖实现。
- 相关提交：本轮计划提交可用版本历史查询；尚无本任务开发提交。
