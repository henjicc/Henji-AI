# 重要记录

## 记录 001：能力开放优先于旧助手兼容

- 日期：2026-09-12
- 状态：已确定
- 背景：用户希望结束通用 Agent 引擎的持续扩张，把主要投入转向各种外部智能体都能调用的软件能力。
- 最终决定：本次只建设 MCP 及其必要的应用基础；保留助手源码和历史，可隐藏入口。助手运行功能即使受影响也不要求修复，不以助手回归通过为交付条件。正常应用、业务数据与安全边界仍需保护。
- 选择原因：把未来更换 Agent 的成本限制在连接和薄适配层，避免再次带入旧助手完善计划。
- 影响任务：全部；1.1 负责同步与之冲突的规则及构建检查边界。当前已进入实施，按任务修改相关规则。

## 记录 002：复用应用能力，协议适配不复制业务

- 日期：2026-09-12
- 状态：已确定
- 背景：主分支已有反射、通用事务及领域服务，但部分协调能力位于助手目录且依赖助手上下文。
- 最终决定：迁移或抽离必要共享职责，由调用方无关的执行入口提供能力，MCP 为首个外部消费者。禁止伪造助手运行记录接入，禁止让客户端先调用模型或必须编写 Henji Script。
- 选择原因：复用既有投入，同时切断 MCP 与旧助手是否可用的依赖。
- 影响任务：1.1、1.2、2.1、3.1。

## 记录 003：本机连接与协议版本已核定

- 日期：2026-09-12
- 状态：已确定
- 背景：仓库没有正式 MCP 服务；候选客户端的协议及扩展支持可能不同。
- 最终决定：1.2 使用官方 SDK 精确版本 1.30.0，仅回环 Streamable HTTP、JSON 响应，默认关闭；设置 → 通用 → 外部智能体连接进行独立只读授权与撤销，凭据复用现有安全存储。SDK 实际协商通过 2025-11-25、2025-06-18、2025-03-26；尚未接入现代 v2／2026-07-28。没有 stdio 必要性证据，不新增桥接。真正 DeepSeek／Codex Agent 客户端矩阵仍在 3.1／3.2 验收。
- 选择原因：让多个客户端连接同一个真实应用实例，不复制业务进程；避免只按最新规范设计而目标客户端连不上。
- 影响任务：1.2、3.1、3.2。

## 记录 004：旧助手重构不是实施基线

- 日期：2026-09-12
- 状态：已确定
- 背景：独立助手分支停在 395bd833，另有 53 个未提交文件；主分支为 d421b85b。
- 最终决定：本计划以主分支真实代码为调查基线，旧分支只按需审查和选取可独立复用的资产，不整体合并，不清理其工作区，不恢复原六阶段任务。
- 选择原因：避免未验证的生命周期改动及助手专属逻辑变成 MCP 的隐性前置。
- 影响任务：全部，尤其 2.1、2.2。

## 记录 005：业务记录独立于连接和对话

- 日期：2026-09-12
- 状态：已确定
- 背景：现有主分支网关和渲染桥的幂等缓存均不能独自承担重启恢复。
- 最终决定：执行前持久记录操作身份；超时不推断失败，未知不重放。生成与渲染通过提交、查询、取消、取结果组成基础链路；应用重启不自动发起剩余修改或模型调用。
- 选择原因：确保断线和更换客户端时可查询事实，不重复修改、生成或扣费。
- 影响任务：2.1、2.2、3.1、3.2。

## 记录 006：已授权顺序委派实施及自主决策

- 日期：2026-09-12
- 状态：已确定
- 背景：用户现要求主代理作为管理者，按任务顺序派发子代理，全部执行，不再提问。
- 最终决定：开始完整实施；每任务独立验证、提交并提供中文交接记录。真实模型验收仍须先记录具体场景、费用上限、写入范围及清理清单，在已有授权和可用条件内执行，不编造缺失证据。
- 选择原因：授权已由计划编制更新为完整开发；保留可控执行和真实验收边界。
- 影响任务：全部。
