# 智能助手现状台账

2026-09-16：标准生成节点的执行器改由工程实例提供，界面只附着输入校验反馈；生成准备、依赖读取、执行及结果发布使用明确工程。整条依赖运行持有工程租用，关闭页面不取消生成，删除等待在途分支与保存完成；标准结果、图层拆分及局部重绘沿原运行时提交，已删除前后台完成分支。精确测试覆盖 A 页面提交 B、页面关闭继续、删除期间原任务提交、限流失败和取消隔离；文本／多角度／打光／分镜执行器与续查、其他领域实例和最终 Reality/L3 仍在实施。

2026-09-16：画布工程基础改为按工程唯一实例，页面附着实例；公共节点读改、集合写入、批事务、撤销及保存引用明确工程，不再创建后台临时 Store。后台新建工程不切换页面，改名保存复用最新实例快照；打开执行中工程不接管或重做任务，保存失败保留脏实例，实例租用与删除屏障已加入。29 文件 233 用例覆盖公共入口 A 页面读改保存 B、打开 B 撤销、并发加载、取消补偿及失败保存；生成调度、专用编辑器的剩余页面依赖、三维／图片文档唯一实例、完整退出屏障及最终 Reality/L3 仍待完成。

2026-09-16：公共装配通过各领域 applicationDomain 汇总反射、执行器、能力、保存参与方和错误解释。画布、三维、图片编辑、标注、素材、生成、设置、导航与工具箱注册归还所属领域，设置旧路径转发已删除；外部属性调用面在领域显式声明，桥接权限由能力与实体注册派生。注册及跨域读写精确验证已通过；工程／文档唯一实例与现代 Resources 完整呈现仍在实施，不能据此视为后台工程已全面可用。

2026-09-16：公共运行时的账本按调用者＋幂等键隔离，领域生成身份由可信宿主派生；增加持久准备认领，重复请求不重复准备、预留和提交。等待取消、MCP 监听关闭和 Pi 回合结束不撤销已提交修改，显式撤销授权仍独立处理；渲染宿主代次与请求、操作、Pi 对话身份分离。原生 SQLite 与正式 HTTP 测试覆盖关闭 MCP 后 Pi 写入、HTTP 断开后的迟到回执、同键并发及重启只核对。旧账本不迁移，本轮真实 Electron 跨工程／文档实例与完整退出重启验收仍待完成。

## 当前方向：外部 MCP 与内置 Pi（2026-09-12）

2026-09-16：应用架构重构进行中。公共能力与宿主上下文已移到 `core/application-control` 和 `features/application-control`；账本、宿主桥、费用控制及调度迁到 `electron/main/services/application-runtime`，启动先于 MCP，Pi 不再从 MCP IPC 取得客户端。MCP 仅支持正式 `2026-07-28` 与拆分 SDK 2.0.0，移除初始化握手和协议会话；工具输出从领域 schema 派生，公共结果由 MCP/Pi 分别投影，资源读取与订阅已接入同一读取边界。现代 HTTP/官方 Pi 精确测试及 3 文件 33 用例的 Electron 原生 SQLite 写入、预算、媒体验证通过。旧自研助手清理、领域注册归属、工程/文档唯一实例及最终 Reality/L3 仍在实施，不能以本阶段证据声称整轮已完成。

2026-09-16：旧自研 Agent Runtime、Henji Script、发现租约、旧前端工具桥、运行记录契约及专用录制/验收 CLI 已删除。Pi 保留技能、用户指令、共享记忆与对话；记忆与模型日志自行初始化存储，不再创建旧执行表。领域读改回环、保存失败只重试保存、事务实际副作用与三维任务测试迁至公共入口；通用画布连线补齐已有端口字段传递。图片编辑目前仍使用全域版本号，不同文档并发可能冲突，唯一实例与按目标版本仍待实施；最终 Reality/L3 未完成。

2026-09-14：导演／短剧制作明确以画布为主要工作区；复用指定或消息所在项目，无目标则创建并打开，纯建议不创建。导演技能新增单独画布参考文件，按阶段加载，规定场景／镜头空间布局和内容命名，不新增节点或业务存储；短剧复用该指南并尊重技能停用。新建项目增加持久化回读验证，防止 MCP 将已创建项目误报未验证。正式应用会话测试覆盖无项目→创建→打开同一项目；布局为助手遵循的创作规则，不是强制自动排版算法。

2026-09-14：三项内置创作技能的本轮实现与链路验收收口：任务意图索引→小型主文件→单模块读取、跨轮复用、独立工具披露和加载日志均有正式 Pi 请求测试；真实 DeepSeek 单镜头任务验证最小分支。工程正文按需读取，通用文本保真，真实 MCP 双启动续做与局部修改通过；正式画布生成服务 45 项测试覆盖节点落点、独立并行、取消隔离、原任务恢复和结果持久化。提示词资料补齐 Replicate Apache-2.0 与 OpenAI Cookbook MIT 的固定版本来源和许可证，不进入模型加载目录。此前“冷重开续做未验收”的记录由本轮真实双启动证据补齐。验证使用隔离工程及受控生成返回，没有进行付费成片制作；不承诺每个模型的审美质量，也不新增软件尚不具备的剪辑合成能力。

2026-09-14：真实 Electron 隔离目录双启动验收通过（`test:reality -- --suite restart`）：MCP 保存创作文本→完整退出→重连读回→打开目标工程→局部修改，其他角色和镜头原文保留；既有生成依赖与任务恢复同时通过，历史数量未增加。修复导航适配层丢弃领域回读验证的问题，保留 MCP 严格成功判定。创作指南明确文本连线会同步正文，普通创作关系使用镜头文本中的稳定引用，不误接为文本输入。本证据不代表真实模型已自主完成整部作品或媒体生成质量验收。

2026-09-14：真实 Pi／DeepSeek 只读验收（runId `cf0b2b70-5a15-40cd-8147-be0d3bf93289`）完成单镜头转头提示词建议；自主选择 prompt-optimization 主文件 2230 字节及 video.md 1545 字节，未加载导演／短剧模块，未提交媒体生成。模型额外查询共享记忆结构并输出无关偏好说明，回答聚焦性仍需检查，不能据此声称所有场景路由已通过。此前补充的正式 Pi HTTP 替身验证跨轮按阶段加载与正文复用；通用 MCP 会话验证文本写入、清除活动工程后从持久化存储读回、连线依赖读取和无关镜头保留（存储为 native harness，不代表实际应用重启）。真实冷重开与完整创作生产仍未验收。

2026-09-14：补齐 canvas.node.text_content 通用属性，文本正文按需读取并复用节点校验、保存事务及撤销；非文本节点不可写，空字符串清空，上限沿文本节点的 32768 字符。文本／标题同批写入不互相覆盖。反射结果测试覆盖正文回读、其他节点保留、摘要不附正文、超长与非文本拒绝及撤销；属性和集合覆盖通过。此项修复短剧工程无法经通用动词保存文本的缺口；冷重开续做、实际 MCP 工程串联与依赖返修验收仍未完成。

2026-09-14：内置 Pi 新增 cinematic-director 与 short-drama，沿原技能注册、启停与单文件加载；补充商品图和文字海报参考。导演知识分为镜头、走位、关键帧、连续性、返修，短剧分为剧本、设定、生产、声音、续做、交付。参考资料固定上游提交并附 MIT 许可证，不引入上游执行脚本。三个技能索引小于 2000 UTF-8 字节、主文件小于 4500 字节、单模块小于 4000 字节的门禁通过；官方 Pi／本地 HTTP 替身验证导演走位、返修和短剧续做只加载对应正文且不激活生成工具。当前证据证明加载和内容路由，不证明真实模型判断或完整工程操作；工程持久化续做、局部依赖与生产衔接的结果验收仍待完成。

2026-09-14：提示词技能进一步拆分为图片／视频基础与编辑、定位、角色组图、参考绑定、连续性、延续、声音等独立参考文件；主文件仅按当前操作路由，不按所在页面触发、不递归加载。内置读取记录原因、文件与字节量到既有日志。官方 Pi＋本地 HTTP 替身覆盖普通查询零正文、四种分支逐文件进入实际请求、无关正文不注入且不激活生成工具；此证据不代表真实模型的技能选择质量。导演／短剧技能、工程状态与局部返修链路仍在实施，不能视为完整创作工程已交付。

2026-09-14：共享长期摘要复用 agent_memories，最多 800 字，使用版本比较防止覆盖并保留凭据检查；assistant.shared_memory 经通用实体读写向 MCP 开放，遵循连接权限与记忆开关。内置 Pi 在每条用户消息读取当前摘要，并引导在明确偏好／纠正后于当前任务内合并更新，不另开总结模型请求；模型是否判断值得保存仍由模型决定。恢复助手记忆入口供编辑、删除和关闭；从未调整过的旧默认开关首次使用时启用，明确关闭状态保留。真实 SQLite、通用事务与本地模型替身验证通过；真实 Electron 中外部 MCP 写入→面板展示→Pi 下一请求读取同一摘要通过。实际付费模型的总结质量尚未验证。

2026-09-14：新增正式只读 `wait_generation_task`，画布与生成页复用已有任务读取入口，内置 Pi 生成工具组默认披露等待工具。宿主等待期间不唤起模型，终态／需要恢复时返回；单次超过九分钟返回原任务状态，由助手继续等待，不重复提交。取消观察不取消生成，跨进程期限由能力声明派生。即时查询仍兼容旧 MCP 客户端；三维渲染查询不在本次等待适配范围。官方 Pi＋本地 HTTP 替身验证等待期间无新增模型请求、返回后自动继续，宿主测试覆盖取消、终态及长任务边界；未执行真实付费生成。助手界面新增余额、认证、限流、网络等错误的友好说明，原始错误保留日志。

2026-09-14：内置 Pi 接入 `prompt-optimization` 提示词技能：复用正式技能注册、启停、用户同名覆盖和受控参考文件读取，通过既有能力 schema 的 Pi 自定义工具加载，不开放通用文件系统或启用 Pi 自动扫描。首轮仅附技能元数据和一个只读加载工具，主文件引导按需读取图片／视频指南，坐标编辑、多模态参考等技巧按功能通用，不按品牌分组；未迁移的旧 Henji Script 技能不注入 Pi。内容基于用户提供的五份样本整理，保留最小编辑、素材绑定和连续性原则，不复制绕过限制指令；具体标签、坐标范围及参数以当前模型契约为准。官方 Pi SDK＋本地模型响应替身验证主文件→图片参考的逐轮披露、视频正文未进入图片请求、技能不激活生成工具；正式注册表读取验证停用、取消、路径及主进程路由。此证据不代表真实模型的选技能质量或实际生成效果，外部 MCP 技能加载本轮不扩展。

2026-09-14：修复设置消费链路遗漏：助手新任务自动选型实时读取图片／视频／音频默认模型及默认供应商，生成工具组暴露正式选型入口，显式模型与继续现有草稿保持原选择；生成队列与画布生成限流订阅并发设置变更，降低上限不取消已有任务。精确测试覆盖默认切换、明确偏好、供应商不可用回退、草稿兼容及队列动态调节；没有执行真实付费生成。检查上传服务／回退、大文件策略、助手用户指令已有当前值读取入口，不等于全部设置均完成端到端验收。新增欠账：`general.max_history_count` 目前只有设置存取，未找到实际保留策略消费者；补齐前须明确历史及文件清理边界，不能因开启这条设置而静默删除存量作品。

2026-09-13：补齐编辑预览 → 生成参考／画布的直接衔接，共用正式合成与落盘，不自动收藏或删除预览；同版本准备、提交及显式收藏复用合成，失败可重试、预览变更后重新合成。生成结果入画布优先读取持久历史，不再依赖生成页内存列表。媒体来源声明与准备／提交引用列表同源，新增来源需要可执行解析样例；连续操作测试接入常规能力门禁。测试使用媒体合成、历史存储及部分画布 I/O 替身，验证下游使用编辑后的文件及原项目落点，不代表真实像素质量或付费模型效果。画布节点直接生成、原项目后台恢复与消息原界面绑定复用已有路径；多层文档／图层按已有正式导出入口衔接，尚未声称全部来源与所有下游组合已逐项验收。

2026-09-13：修复生成参考媒体仅接受素材库引用的缺口。准备和提交共用解析器现在直接接受生成历史的 `generation.result`，支持图片、视频、音频并核对成功状态和媒体类型，不创建素材库记录；图片与已有编辑入口共用可读结果解析，本地副本失效时尝试历史远程结果。工具声明同步公开该引用方式，不依靠系统提示词或 SDK 供应商分支。本项的定向测试使用历史与媒体 I/O 替身，不能视为真实模型决策或付费生成验收。

2026-09-13：Pi 首轮改为通用实体基础工具加消息原界面查询，画布完整权限实测 6 个工具、定义 8536 UTF-8 字节；完整授权目录为 98211 字节。画布生成／节点详情通过一次任务加载取得相关工具和流程说明，模型及节点完整参数仍按需读取；加载记录按界面保存，跨界面不携带旧界面专用工具，返回原界面及冷恢复复用授权交集。固定系统设定中的画布详细流程移至任务加载回执，首轮选择摘要仅增加有界名称、节点类型及生成中标记。官方 Pi SDK／本地 HTTP 替身测试覆盖实际请求体积、各界面首轮、一次加载后模型参数调用、跨界面／冷恢复／降权，以及等待和打断仍绑定原消息项目；宿主快照与类型检查通过。该项替代旧 25 工具基础集合及图片编辑界面自动加载全组策略；不代表真实模型决策、供应商时延或实际付费生成已验收。

2026-09-13：前端业务能力的 MCP 工具集合、基础权限及反射属性权限改为从正式声明自动派生；接通已有导航、画布编辑、项目生命周期、素材和三维语义服务，普通增删改复用通用实体入口。新增覆盖门禁从软件能力全集核对发布目录、执行器、目标绑定及委托字段，已用四类故意断开验证能发现缺口；修正六处集合不可增删却被误判为可委托的生命周期入口。Pi 按领域或工具名加载低频工具，官方 SDK 测试覆盖下一轮调用、冷恢复及降权，首轮工具数量小于完整目录的一半。覆盖、领域与协议精确测试、双侧类型、局部 lint 和结构检查通过；原生 Electron SQLite 集成通过真实 MCP HTTP、正式 Session、导航及项目创建幂等、设置写入与保存，项目存储和模型响应使用隔离替身。此项替代旧手写 MCP 工具白名单及仅图片编辑延迟加载策略；保证已登记功能的路径可核对，不等于全部界面功能或真实供应商已逐项验收，内部助手协议和 C2 观察仍明确排除。

2026-09-13：图层拆分与局部重绘的首次生成接入共用无挂载配置，界面壳与 MCP 复用同一份预检、裁剪及原项目合成回调；显式声明支持后台的自定义提交器也统一登记、排队、取消和持久结果对账。模型调用及续查优先使用共用任务编号，旧裁剪记录仍兼容。95 项相关测试通过，其中新增 10 项覆盖两类节点的界面／MCP 首次提交、页面卸载切换、原项目结果及单条历史、取消、必要输入缺失和蒙版变化；渲染类型与局部检查通过。模型、像素、V3 创建及历史 I/O 使用替身，原项目保存走正式领域服务；不代表真实供应商或像素质量验收。两类节点首次生成的配置／登记欠账已补齐，未改动其他专用节点的执行模式。

2026-09-13：图层拆分与已有裁剪上下文的局部重绘可在原项目后台续查，继续轮询原供应商任务；专用合成服务携带原项目与取消信号，旧界面调用也在合成前固定项目。图层落图接入共用原项目事务，文档保存时切换项目仅在精确补偿确认后重取原实例；回收未确认则停止重建并保留文件，保存失败但节点已保留时也不释放引用资源。102 项相关测试覆盖后台落图、合成中取消、回收拒绝／异常、原项目持久结果和既有任务／恢复路径，渲染类型与局部检查通过；供应商、像素合成及 V3 创建使用替身，项目保存通过正式服务的隔离存储。工具说明同步更新。初次发起这两类生成的无挂载配置及任务统一登记仍需补齐，不把本次续查打通视为全部特殊生成生命周期完成。

2026-09-13：标准节点界面任务在进入执行限流前登记并保留原执行器，MCP 与界面共用排队／执行状态；等待项取消立即移出队列，不等待其他请求结束，名额移交期间取消也不会调用业务或丢失名额。73 项精确测试覆盖五个界面请求的提前登记、页面卸载／切换后完成和队尾立即取消、MCP 同源状态与取消、原执行器输入校验及限流竞态，渲染类型和局部检查通过。供应商／媒体 I/O 与历史存储使用测试替身，原项目存储走正式领域服务；本项不替代真实供应商验证，特殊合成仍保留原页面要求。

2026-09-13：真实 Electron 的 `embedded-agent` 验收补齐界面生成与 MCP 共用任务事实：助手首次生成后，实际选中标准节点并点击生成，新请求只登记一条独立 SQLite 历史；只读 MCP 查询能看到等待、可取消与最终成功，原工程图片池中的结果路径和历史一致，结果节点只保存一份。正式 Reality 场景通过，应用／浏览器错误和警告均为空，已目视检查完成截图；使用本地模型 HTTP 与生成 IPC 替身，没有真实付费调用。此项补齐前述真实 SQLite 界面入口证据，尚未开始执行的界面排队提前登记和特殊合成仍保留原限制。

2026-09-13：标准节点界面直接执行与助手共用 `canvasGenerationTaskRecord` 的正式历史登记、节点占用、取消控制及持久结果对账。界面生成进入实际执行后也带稳定任务身份，MCP 可查询／取消；助手不重复登记已有任务，正在执行的同一节点不能再次提交，其他节点独立。68 项相关测试覆盖界面入口、MCP 查询与取消、后台原项目保存、重复提交拒绝、既有多请求／恢复／失败路径，类型及局部检查通过；历史存储、供应商和媒体 I/O 使用替身，原项目存储走正式领域服务。自定义合成暂保留原路径；界面排队阶段尚未进入执行时的提前登记及真实 SQLite 界面入口验收仍需核对。

2026-09-13：内置助手的 `add_generation_result_to_canvas` 也在预算／幂等登记前绑定发送时的默认项目和落点；原选中对象旁优先，其次原视口位置，显式项目／位置不覆盖，非画布上下文不借用后台项目。11 项宿主与官方 Pi 调度测试、Electron 类型和局部检查通过；已有正式结果导入业务入口复用不变。验收核对仍发现界面节点直接执行未统一登记为 `generation.task` 的缺口，需继续补齐界面与助手共享任务事实。

2026-09-13：最新产物正式 `test:reality -- --build --suite restart` 通过，除权限／操作账本外，增加三种生成任务存储样本的真实 SQLite＋Electron 冷启动＋MCP 查询：保留原供应商续查参数；未知记录不被改写为可重放；带本任务身份的已保存图片修复正式历史为成功并回读文件路径。三条样本历史数量不增，无供应商调用。此证据验证冷启动状态与对账，不代表真实供应商续查成功。

2026-09-13：任务登记后建立执行计划的极早切换窗口已补齐。正式运行入口接受任务原项目，计划、前置校验及初始输入读取复用原项目实例；只有任务持有且支持后台结果的执行器可离开页面执行，普通页面执行器与特殊合成保留项目保护。66 项相关测试覆盖登记时暂停后切换成功／取消不调用供应商、五请求排队、准备／后台保存、输入变化及共享执行器；渲染类型和局部检查通过，供应商／媒体 I/O 使用替身。标准任务从登记、排队、准备到完成的项目归属已具备领域测试，跨阶段真实桌面／供应商冷恢复矩阵仍需核对。

2026-09-13：已进入正式执行队列的标准画布任务，在切换项目后由任务持有的原执行器继续出队；参数准备读取原项目快照，不再读取当前画布。共用准备流程保留输入／依赖、费用前校验和自定义合成的原页面要求。65 项相关测试覆盖五请求（两个执行、其余排队）的后台完成、只取消队尾且其余四项成功、原参考连线和保存结果、已有准备／后台／失败行为；渲染类型及局部检查通过，供应商与媒体 I/O 使用替身。任务登记后尚未形成执行计划的极早切换窗口仍受保护，真实供应商冷恢复未验收。

2026-09-13：标准生成在进入准备后、等待估算或媒体请求准备期间切换项目，不再直接终止。共用执行上下文按原项目实例核对节点输入、依赖和任务取消；占位及参考连线在原项目创建并确认保存后才调用供应商，保存失败沿原失败处理清理状态。63 项精确测试覆盖准备期间切换成功、后台取消不付费、占位保存失败、已有后台完成与执行并发；渲染类型和局部检查通过，模型／媒体 I/O 使用替身。尚未开始执行的排队任务仍受当前项目保护，特殊自定义合成未放宽。

2026-09-13：MCP／Pi 恢复标准画布任务不再要求先打开原项目，入口复用原项目运行实例及唯一续查服务；开始状态、结果和取消状态均保存原项目，当前项目与页面保持不变，只轮询原供应商任务、不重新生成。51 项精确测试覆盖后台启动成功与取消、前后台失败恢复、共用界面入口及特殊合成，渲染类型和局部检查通过；模型／媒体 I/O 使用替身。图层、局部重绘合成仍需原项目，未派发排队任务的后台执行及真实供应商冷恢复仍未完成。

2026-09-13：正式 `test:reality -- --build --suite restart` 使用同一隔离资料目录完成两次 Electron 启动：暂停渲染线程留下真实未知操作，以 `app.exit` 立即退出后重新连接；服务配置、授权和已完成写入均保留，原未知请求换标识仍被拒绝，同集合的独立追加成功且仅保存一份，原请求没有自动重放，生成历史始终为零。该证据覆盖外部 MCP 冷启动操作账本，不代表真实供应商任务冷恢复或断电测试。另修复运行中依赖计划失效误报执行器未就绪的问题，23 项正式画布执行测试、渲染类型及局部检查通过；仍阻止旧计划继续执行，不扩大任务占用范围。

2026-09-13：更新真实 Electron `embedded-agent` 场景的旧工具镜像断言，按当前 Pi 单份结构化回执检查正式设置属性。使用最新轻量产物运行 `test:reality -- --build --suite ui --only embedded-agent --size 1440x1000`，场景通过，浏览器／应用日志错误均为空；已目视检查等待队列、生成中和完成截图。覆盖官方 Pi 独立进程、preload、正式应用工具、权限与会话重载、等待／打断、附件及参考连线、标准生成节点、正式请求构建和持久结果。模型 HTTP 与生成 IPC 使用本地替身，资料目录隔离；该证据不代表真实供应商付费结果或全部后台／冷重启矩阵。

2026-09-13：消息快照区分选中节点的布局用途与参考素材资格；复用正式媒体发布解析与节点执行声明，待生成／失败／空素材及生成节点只作为旁侧布局锚点，不强加参考连线或触发额外上游生成。可用素材仍按原规则连线，显式 destination 优先，旧快照缺少资格字段时保持兼容。58 项精确测试覆盖快照、准备／提交绑定、正式独立生成及官方 Pi 等待消息的工具参数，类型和局部 lint 通过；生成与模型 HTTP 使用替身，未进行付费生成。

2026-09-13：续查失败保留原供应商任务号／模型，所有失败结果停止自动重取，只允许明确恢复；正式任务查询仅凭带本任务身份、原供应商标识且已持久保存的失败同步历史错误，旧未知记录不据错误文字推断终态。MCP 恢复清除旧错误，界面共用恢复入口完成后的已保存原子输出也可修复旧 error／timeout 历史；保存失败直接报告，不能声称已对账。48 项精确测试覆盖前后台失败后恢复、原供应商号只续查、两入口历史一致、保存失败、未知保护及特殊结果失败，渲染类型和局部 lint 通过，供应商／媒体 I/O 使用替身。此项不代表供应商已取消或费用退回，特殊合成仍需原项目上下文。

2026-09-13：无选中节点的画布消息快照增加可选原视口节点位置，直接复用正式节点布局计算；Pi 的准备与生成工具在预算／幂等登记前绑定该绝对位置，发送后的平移／缩放不改变默认落点，明确目标和选中节点旁仍优先。旧快照缺少该字段时保留兼容行为，非画布上下文不携带背景项目位置。66 项精确测试覆盖快照、优先级、正式节点及结果持久位置、旧契约与 Pi 调度；两侧类型及局部 lint 通过，模型／媒体 I/O 使用替身。此项补齐无选中节点的原视口绑定，不代表所有业务操作都已支持后台项目执行。

2026-09-13：`servicePi.test.ts` 将正式宿主消息调度与官方 Pi SDK 组合，覆盖模型等待中打断、工具执行中打断、插入优先、等待消息续发、每条消息的原项目／选中对象、旧工具迟到回执与原请求日志关联。两项测试均通过，检查实际 HTTP 请求顺序及工具调用参数，不以助手完成文字验收；进程传输、模型 HTTP 响应和应用工具客户端使用替身，此项不替代真实 Electron 进程传输或业务落盘验收，后者仍由原生 MCP 集成与画布领域测试提供对应范围的证据。

2026-09-13：标准结果续查在离开原项目后继续持有原任务与取消控制，通过共用原项目运行实例提交结果／停止状态；开始轮询前确认占位状态已保存，往返项目不重复续查，也不重新提交生成。恢复执行信息发布在异步签名核对后再次检查原项目，避免写入新页面；后台已保存结果仍由正式任务查询对账，离开项目时暂不发布依赖执行缓存。44 项精确测试覆盖后台结果保存、当前项目隔离、往返租约、MCP 后台取消及原局部重绘／图层恢复，类型及局部 lint 通过，供应商与媒体 I/O 使用替身。特殊局部重绘／图层合成仍保留原项目保护，主动启动续查仍需打开原项目，完整冷重启与真实供应商验收未完成。

2026-09-13：原生 `mcpWriteIntegration.test.ts` 已加入官方 Pi SDK→模型 HTTP 响应→MCP HTTP 客户端→真实授权／SQLite 操作账本→正式应用 Session／设置保存的完整链路。模型响应使用本地固定替身，业务 schema、权限、账本和保存不替换；断言无效写入为 not_executed、同目标后续合法写入完成、正式读取回执及持久设置一致，不以助手“完成”文字验收。通过正式 Electron 原生 SQLite 运行器，0 跳过，类型及局部 lint 通过；这是错误恢复与设置写入的协议／业务集成证据，尚不覆盖真实模型决策、付费生成和全部排队／打断矩阵。

2026-09-13：普通 `assistant:cli` 默认切换为 Pi；旧 `assistant:live`／套件显式选择并标记 legacy，旧专用验收参数不伪装成 Pi 验收。CLI 复用现有产物新鲜度守卫，缺失或过期时不启动模型。10 项精确测试、Electron 类型和局部检查通过，轻量构建后默认命令真实运行 DeepSeek 只读契约查询成功（runId `23977278-1b1f-439c-95e3-3a656f4feee0`，约 6.3 秒）；统一日志确认正式工具完成及两次模型请求，首请求输入含缓存 3272 token、缓存 896，后请求输入含缓存 5097、缓存 3328。回复仍明确 `businessVerified: false`，Pi 的业务状态验收矩阵及生成等待仍待完善，不能用此只读运行替代。

2026-09-13：Pi 按测量结果延迟图片编辑预览／保存的完整参数，普通生成、画布节点、实体读写和查询保持直接可用；图片编辑界面发消息自动启用，其他界面通过 `load_application_tools` 一次加载，会话及冷恢复保留启用状态，并始终与当前宿主授权目录取交集。使用官方 SDK 的注册与活动工具接口，未修改上游或外部 MCP 目录。实际 SDK 请求体积从完整 38428 字节降至首轮 26292 字节（约减少 32%，包含加载工具）；15 项精确测试覆盖下一轮真实工具调用、同上下文去重、编辑界面直达、冷恢复、降权与新会话隔离，供应商为本地响应替身。此为工具体积与协议路径证据，不代表真实模型选择或时延提升；其他低频工具暂保留，避免无测量依据地增加查询轮次。

2026-09-13：Pi 每轮最终供应商请求新增 `model_requested` 尺寸诊断，与已有会话／请求／模型及实际用量日志关联；仅记录工具数量、工具和系统设定字节数、消息数量及去重后的宿主上下文数量／字节数，不记录正文、附件或密钥。官方 Pi SDK＋本地 Responses 替身使用实际完整授权目录测得 24 个工具、工具定义 38428 UTF-8 字节，两轮相同上下文均只携带一份；这是序列化体积证据，不是实际 token 或真实供应商时延。动态工具加载与进一步精简仍待依据诊断结果完善。

2026-09-13：标准画布生成在供应商请求已经派发后，切换项目仍通过原项目运行实例保存迟到任务号、持续轮询、写回结果和取消状态；供应商调用前先保存占位节点，任务号保存完成后再轮询，网络等待不占用项目写入锁。结果准备期间切换项目只重试尚未修改画布的本地提交，不重新生成；已持久保存的带任务身份结果不会因后续输入签名／执行信息发布失败而误报生成失败。精确测试覆盖原项目结果、当前项目隔离、迟到任务号、后台取消、输入改变后的真实完成状态及五请求并发；类型及局部 lint 通过，媒体与供应商 I/O 使用替身。尚未派发的排队任务、特殊自定义提交节点、原界面任务身份及完整重启／切换界面验收仍待完善，不代表所有后台生命周期已经闭环。

2026-09-13：`prepare_canvas_node_generation`／`submit_canvas_node_generation` 已接入 MCP／Pi 共用目录，原节点从通用配置进入正式准备、估价、执行、保存及任务查询；图片、视频、音频标准节点与抠图、老照片修复、预设打光、扩图、高清放大、全景复用共享生成执行器，扩图与放大的特殊准备仍走界面同一函数。实际能力会话测试覆盖六类图片工具的原节点、参考连线、供应商请求及持久结果；原生账本验证授权、超额未执行、独立节点并发及未知原请求防重放，媒体和模型 I/O 使用替身。准备后的输入变化、提交前取消和同节点重复登记有定向验证；新建节点后登记失败不冒充整次未执行。当前原节点入口只执行已备齐素材的单节点，不自动支付额外上游生成；特殊打光／多角度／局部重绘等其余执行配置、跨项目持续执行及真实供应商验收仍待完善。

2026-09-13：付费准备入口通过能力的 `paidGenerationPreparation` 声明，MCP／Pi 共用目录、操作授权和预算预留均消费同一声明，不再只识别普通生成工具名；公开生成创建能力必须有可调用的只读准备入口。原生账本测试覆盖另一能力声明后的授权拒绝、先估价再执行、超额未执行，目录与注册测试覆盖派生授权和错误声明。原自动额度保持不变；此项为受控图片节点提交提供共用费用检查，尚不代表节点执行入口已经补齐。

2026-09-13：`cancel_generation_task` 已路由到原画布任务及恢复续查，共用 `GenerationService` 接收业务取消信号：准备结束前复核取消，首次提交按请求标识取消，轮询按供应商标识取消；信号不进入模型参数或 IPC DTO。画布任务号同时贯穿生成请求日志。已停止任务保留原供应商标识，自动恢复跳过，明确续查仍获取原任务；查询及历史持久状态为 cancelled，不声称供应商撤销或退款。证据：56 项精确测试覆盖实际 MCP 读取基线／取消／通用状态读取、并发隔离、迟到结果、停止后明确恢复、原恢复分支及历史非活动加载；类型与局部 lint 通过，模型和媒体 I/O 使用替身。生成页旧历史卡仍按非活动错误样式显示停止说明，跨项目持续执行、原始界面任务的统一身份以及真实供应商取消验收尚待完善。

2026-09-13：MCP／Pi 新增 `resume_canvas_generation_task`，使用任务查询返回的 `resumeInput` 核对原任务、项目和节点，通过共用恢复服务续查原供应商任务；普通修改权限即可调用，不重新提交生成。操作账本只占用原任务及节点，同项目其他任务可独立执行；错误目标在开始前明确报告未执行。证据：正式能力会话测试覆盖查询、错误目标、重复恢复只轮询一次及原结果保存，原生操作账本覆盖同项目并发与重叠保护；模型与媒体 I/O 使用替身。当前仍需打开原项目，取消、跨项目持续执行、恢复失败的完整历史对账及真实付费验收尚未闭环；后文早期“主动恢复入口待接通”已由本项推进。

2026-09-13：新画布任务将任务标识写入结果节点，任务查询只用对应任务的已保存输出对账；冷启动后的通用实体读取可恢复原历史完成状态，旧列表缓存不会覆盖画布任务事实，已完成历史不依赖画布节点继续存在。残留生成标记不再单独证明活动执行；已登记的供应商任务区分等待续查与正在续查，项目切换后有原任务号时保留待续查状态。证据：`canvasGenerationTaskService.test.ts` 经正式提交、恢复、输出提交、存储及 MCP 通用读取，覆盖冷启动、旧缓存、历史保存失败、未保存结果、旧任务不能冒认新结果、切换后原任务续查且生成只提交一次；模型和媒体 I/O 使用替身。旧无任务身份的未完成记录不推断为成功；取消、MCP 主动恢复入口与跨项目持续执行仍未闭环。

2026-09-13：原画布恢复 Hook 的续查、结果提交与资源释放已移入 `canvasResumePollingService`，界面只负责触发；续查租约在服务内随项目切换失效，不再依赖 React 触发旧回调失权。恢复测试覆盖无挂载调用、重复调用只续查一次、返回原项目后重新接管及旧回调不覆盖新结果，并保留全景、多结果、分镜、图层与局部重绘回归。此项是共用恢复服务的建立；MCP 主动恢复入口、任务状态对账和跨项目持续执行仍待接通，不能视为完整恢复闭环。

2026-09-13：画布生成节点新增通用属性 `canvas.node.generation_config` 与只读 `generation_schema`，允许 MCP／Pi 按实例发现、读取当前模型参数、整体配置并回读原工程；固定工具沿用原模型锁与节点参数校验，不允许写入内部 UI 字段。详细模型信息只在读取该属性时构造，普通标题、位置读取不附带模型参数。证据：`mcpSemanticRevisions.test.ts` 经实际 MCP 授权、通用发现／修改／读取和正式存储，覆盖固定工具、普通模型与参数一起切换、错误参数和失败后数据保留；画布反射、旧更新入口、生成参数测试及应用能力门禁通过。此项补齐配置断口，不代表受控工具生成提交、取消或跨项目生命周期已完成。

2026-09-13：`create_image_edit_preview` 与 `commit_image_edit` 已接入 MCP／Pi 共用目录及普通修改授权，复用已有不可变编辑预览与素材收录服务；保存返回正式 asset 引用，可用通用实体读取核对。预览创建只写独立目标，不锁参考素材；保存保护被消费的预览，未知请求继续防重放。证据：`mcpImageEditLoop.test.ts` 经实际公开权限、能力注册、编辑服务、通用读取与素材收录执行，像素和文件接口使用替身；目录同源、旧客户端样本、原生操作账本及应用能力门禁通过。此项不代表所有 V3 编辑器动作、画布图片工具节点参数配置和生成执行闭环均已完成。

2026-09-13：CLI 新增 `--engine pi`，复用侧栏正式服务、主模型、权限与消息上下文；默认只读，显式 full_access 才开放写入，输出 runId 可查询统一日志。原 legacy 路线仍为兼容默认；Pi 暂拒绝旧式生成等待、写入验收和详细追踪选项，回复完成明确不代表业务验收。真实 DeepSeek 两轮只读验证覆盖契约工具成功、进程重启后续聊和日志关联（runId `8f85c3b7-b664-4d0f-98fe-ac117074bba4`、`55a8a63d-7981-4800-b6e3-af2f2e833110`）；首个请求输入含缓存 3098 token、命中 896，续聊输入含缓存 5676、命中 5504。Pi 工具回执去除重复 MCP 文字镜像，MCP 失败交由官方 SDK 标记为工具错误；每轮模型用量、耗时及取消状态进入现有日志。付费生成、业务状态验收矩阵、默认 CLI 切换和完整任务恢复仍待完成，不能用只读结果替代。

2026-09-13：普通画布生成任务由任务服务持有共享生成执行器，不再等待 React 节点挂载；界面挂载/卸载不替换在途任务执行器，图片素材校验与节点 UI 复用同一准备函数。真实执行器并发测试暴露结果落图事务的多余 async，现结果生成、多图层落图、文档导入和全景快照的同步修改阶段不再无故让出执行权。`canvasGenerationTaskService.test.ts` 改用生成及媒体 I/O 替身，实际走节点执行、连线、结果提交和保存，覆盖无需挂载、后续挂载不重放及五个并发请求；结果落图另有独立并发回归。跨项目持续执行、取消和重启恢复仍未闭环，保留原项目切换保护，不把这次去挂载依赖视为全部后台生命周期完成。

2026-09-13：`apply_canvas_image_capability` 接入 MCP／Pi 共用工具目录，按来源节点读取、工程独立追加声明持久目标；普通修改授权即可创建节点，不以创建成功冒充图片生成成功。`mcpSemanticRevisions.test.ts` 使用实际 MCP 权限及能力清单，验证正式服务创建节点、参考连线、存储回读和通用实体查询；目录测试检查所有公开语义写入都有目标声明，兼容样本固定调用形状。此项仅补齐节点编排入口，受控节点的参数配置、生成提交及任务查询闭环仍待完善。

2026-09-13：修正 MCP 把集合追加误当整个项目覆盖的冲突判定。生成与结果导入从能力声明区分读依赖、独立追加和覆盖目标，旧账本按同一声明解析，不清空未知记录；独立追加兼容在途及其他未知追加，原未知请求换标识、覆盖/删除和保存失败继续阻断。共享画布事务仅在原快照未改变的字段校验失败时报告未执行；完整恢复并保存后报告已回滚，与未知/部分状态分开。证据：原生 `operationStore.test.ts`（含移除兼容判定后的断牙失败）、`mcpWriteIntegration.test.ts`、`canvasBatchService.test.ts`、`canvasGenerationTaskService.test.ts`（原任务等待时连续追加五个节点、参考连线和最终结果回读）、画布保存与后台工程运行测试；不代表真实付费供应商验收。

2026-09-13：内置助手按消息发送时的宿主界面、项目与选中对象优先操作，明确目标优先；生成工具省略落点时由宿主在预算与幂等登记前固定原落点。画布生成沿用 `create_visible_generation_task` 的可选 destination：同步创建标准生成节点及参考连线，再调用正式画布执行器，进度和结果保留在原项目，任务仍记入原历史存储与状态反射；切换项目或修改待执行输入不会自动改投或重复生成。画布任务暂不提供助手取消，查询明确返回不可取消。原有生成历史入口保留；结果导入画布修复长提示词造成文件名越界。Pi 发往模型的相同连续宿主上下文已去重，变化记录仍保留；系统设定与工具定义仍随模型请求携带，工具定义尚非按需加载。证据：`canvasGenerationTaskService.test.ts`、`mcpSemanticRevisions.test.ts`、Pi/service 精确测试和 Reality `embedded-agent` 场景；真实 Electron 覆盖节点挂载、正式请求构建、单份参考图、结果连线与存储回读，模型及生成 IPC 使用本地替身，不代表真实供应商生成验收。

2026-09-13：聊天页不再提供模型选择，使用设置中当前助手配置的主模型及其附件能力。发送后立即清空草稿；回复中可继续输入，选择等待（按顺序续发）或打断（调用 Pi abort 后优先发送）。宿主逐条配置上下文、附件与权限，队列只保留在当前应用进程内；失败消息在当前对话保留供恢复。

2026-09-13：普通应用修改、新增及生成允许省略 `baselineIds`，宿主读取当前目标后交给原事务／领域服务执行；显式提供基线仍严格核对，删除与不可撤销的任务取消仍需原目标读取。生成提交自动准备并按正式参数估价，所有 Agent 共用最近 10 分钟 ¥50 估算额度，预留写入操作账本，未知／已提交费用不自动释放。额度不足或无法估价时不派发生成，可由用户在生成页提交。媒体素材引用在应用边界解析为正式媒体字段，不由模型拼本地路径。此额度为提交前估算保护，不代表供应商最终账单的硬上限。

旧自研助手完善计划暂停，源码与历史保留。侧栏和快捷键恢复，默认对话改用官方 `@earendil-works/pi-coding-agent`；适配层在 `electron/main/services/embedded-agent/`，独立进程管理官方会话，未修改上游源码。内置 Pi 与 MCP 共用 `ApplicationToolDispatcher`，不要求开启 MCP 端口。内置助手默认允许修改、删除与付费生成，用户切换后的权限随面板持久保存；仅执行用户请求范围内的操作。外部 MCP 的服务开关、端口和新连接权限也持久保存，首次默认启用；内置权限不依赖 MCP 设置。旧记忆、技能和旧会话不会自动迁移到 Pi，现有用户指令会注入新会话。应用独立执行入口在 `src/features/application-control/applicationCapabilityService.ts`，使用宿主授权、原注册表与领域服务。MCP 已接入本地读取、设置／模型可见性／素材／画布受控写入及独立 SQLite 操作记录；证据见 `docs/task/外部智能体能力开放/任务/第二阶段-可靠写入与后台任务/2.1-交接记录.md`。未知操作保持阻断，不能自动重放；后续写域尚未完成。下文历史助手验收不作为 MCP 完成证明。 Pi 首版验证入口为 `piEngine.test.ts`、`toolResult.test.ts` 和 Reality 的 `embedded-agent` 场景；覆盖官方 SDK、受控工具、原生图片内容转换、错误/取消、冷启动会话恢复与真实桌面桥接。聊天附件已接入：画布中输入 @ 只列出选中节点已发布且模型支持的媒体，选择后加入附件；按住 Shift 从节点拖出素材，Reality 覆盖添加附件与节点移动互不干扰。入口及格式按当前模型的图片／视频／音频能力显示，复用素材导入与内部拖拽协议，切换模型时保留并阻止发送不支持的草稿附件。Pi 会话保存稳定素材引用与独立媒体缓存，支持冷启动恢复；DeepSeek 所用 Responses 图片和兼容对话的图片／视频／MP3-WAV 音频请求有 HTTP 替身验证。Responses 音频仍拒绝；各供应商专用上传（例如 Kimi 视频 Files API）及音视频格式转换尚未接入，不把通用内容块验证等同于全供应商实测。


> 读取时机：助手相关任务开始前（判断「这块是不是已经做过了」），以及**收尾时**（判断要不要更新本文件）。
>
> 目标与判据在 [assistant-goal.md](assistant-goal.md)，那份很少变。这份是**状态**，会经常变。

## 零、这份文件的纪律

**它是索引，不是第二份真相。** 凡是机器能查的事实（哪些域可写、哪些属性没执行器、哪些欠账），
真相都在门禁代码里；这里只写「在哪查」和「人读得懂的解释」。写死一份平行清单，
它一定会漂移，而漂移的方向必然是显得比实际更好。

**什么时候必须更新：**

1. 某个域/实体/属性从「不通」变成「通」，或反过来
2. 欠账清单（`KNOWN_*`）变长或变短
3. **推翻了以前已经确定做好的内容**——写进第五节，写清推翻了什么、为什么、旧做法为什么不能留
4. 某条「已确定不做」被重新打开，或新增一条

**什么时候不用更新：** 普通缺陷修复、性能优化、不改变「通/不通」的重构。

数字带日期戳。看到日期久远又拿不准时，按「取数方式」重新跑一遍，不要直接引用。

## 一、能力盘（快照：2026-08-31）

| 项 | 数 |
|---|---|
| 注册域 | 12 |
| 实体类型 | 32 |
| 属性 | 315 |
| 可写实体 | 20 |
| 写域 | 8：`assets` `camera_stage` `canvas` `generation` `image_edit` `image_mark` `models` `settings` |
| 只读域 | 4：`artifacts` `assistant_runtime` `storyboard` `toolbox`（每个都有显式 `writeExclusion.reason`，是**有意只读**不是漏做） |

取数方式：`getApplicationReflectionRegistry().describe()` 的结果。域清单与可路由性由
`capability-domain-coverage.test.ts` 穷举守着，跌破会红。

## 二、已经通了的

对照 [assistant-goal.md](assistant-goal.md) 的四条判据：

- **判据 1（广度）**：`storeActionCoverage.test.ts` 的 `GAP_BASELINE = 0`，且断言是
  `gaps.length <= GAP_BASELINE`——**已登记账本的静态缺口为 0**。2026-08-31 快照中，
  28/28 个 store 全部建账，三维 11 项、画布 5 项登记缺口已归零；这不等于全部界面动作已经
  经正式 Gateway 和真实模型逐项验证，实际可执行性仍需结果测试与对应验收证明。
  （该文件的长注释里有 `0 → 2`、`2 → 8` 这类**中间态**数字，是任务推进过程的记录，不是现值；
  以文件末尾的常量和断言为准。）
- **判据 2（唯一性）**：旧 `HostCommand` / `HostQuery` 执行入口**已删除**，助手与界面共用正式领域服务。
  `AGENTS.md` 明令禁止新增该类工具。双路径一致性由 `check:assistant-capabilities` 守。
- **判据 3（走得通）**：四大覆盖门禁 `propertyCoverage` / `storeActionCoverage` / `collectionCoverage` /
  `resultBehaviorCoverage` **零豁免清单**。8 个写域全部有脚本化模型替身驱动正式运行时的读改验回环
  （`assistantHarness.writeLoop.test.ts`，L-B）；它证明给定操作链路可执行，不证明真实模型会选择该路线。
- **判据 4（可自纠）**：拒绝路径的自我修正由 `assistantHarness.rejectionMatrix.test.ts` 守。

2026-08-21 新增 `general.primary_provider` 与按媒体分类的 `generation.default_*_model`：默认供应商
和图片、视频、音频默认模型由独立默认项管理器持有，界面与助手都通过同一个正式设置定义读写；
切换供应商导致默认模型失效时会产生强类型级联 Effect。对应的 `settings.general.onboarding` Surface
已登记，可从设置定位与观察。API 密钥仍属于受保护状态，未扩大助手读取权限。

2026-08-24 画布新增持久化素材组：`group_canvas_nodes` 可明确创建空间组或素材组，新增素材组目标绑定/
解绑能力；成员顺序与封面通过 `canvas.node.asset_group_member_order`、
`canvas.node.asset_group_cover_member_id` 正式属性反射与写入。界面、快捷键和助手共用同一素材组事务服务，
容量不足、目标接口变化和成员排除均由该服务确定性重算。

2026-08-29 智能助手模型步骤接通 `openai-responses`：SDK 按“供应商端点 × 具体模型”自动选择
Responses 或 Chat Completions，预制模型不再要求用户理解协议；DeepSeek、火山引擎、百炼、MiniMax
以及智谱国内 `glm-5.3` 的已确认组合默认走 Responses。Anthropic 仍未实现，并从设置界面完全隐藏。

2026-08-29 画布专用编辑器控制器补入第 25 份正式 StoreLedger：打开、草稿更新、确认、取消、保留和
放弃都是组件内会话编排，持久业务结果仍归画布节点与派生媒体服务，因此六项动作以可验证的 excluded
边界建账，不伪造助手逐像素操作。`maskEditor` 同步登记到盲区清单：它只持有面板内位图草稿，确认后
的蒙版引用由画布节点持久化；这次登记消除了覆盖门禁盲点，但没有扩大助手权限，也没有新增 gap。

2026-08-29 全景节点内嵌 WebGL 租约补入第 26 份正式 StoreLedger：`claim` / `release` 只负责限制
同时挂载的 GPU 上下文数量，不表达用户业务意图，也不参与工程持久化；相机视角仍由画布节点数据保存。
两项动作以 `derived` 边界排除，没有扩大助手权限，也没有新增 gap。

2026-08-30 画布图片工具新增原生 `apply_canvas_image_capability`：助手与图片工具栏共用同一领域事务，
可从已有图片来源原子创建 14 类受控能力节点及连线，并返回节点、连线与单次撤销引用。这个能力只完成
画布编排，不会自动发起付费生成；普通模型发现、通用生成和公共画布节点入口继续拒绝内部受控模型。
`image.grid-split` 仍是有意保留的本地交互工具，不开放为后台写入。助手枚举与默认开放的节点型图片能力
由 parity 测试守住，项目错配、本地工具预拒绝、选中态 Effect 与撤销恢复均有精确测试。

2026-08-31 `image_mark` 改用图片编辑会话 store 持有的领域级单调 revision，provider、执行器与宿主
并发信封读取同一个权威计数，注销 `KNOWN_UNPUBLISHABLE`；L-B 以“用户已打开编辑器”的真实会话状态
补齐发现、脚本、Gateway、执行器与读回全链路，注销 `KNOWN_UNREACHABLE`。多会话之间会保守冲突，
但不再出现声明可写、助手却永远拿不到基线的死路。

2026-08-31 图片编辑助手能力收口为“后台创建预览”与“显式打开编辑器”两个可组合动作：移除
会暗中跳转的迁移残留能力，返回真实 `image_edit.preview` / `application.surface` 稳定引用与精确
Effect，且允许把预览原样打开或作为下一轮编辑来源，旧文档与标注不会丢失。生成历史新增正式
`generation.record → generation.result` 反射链，应用重启后仍可沿稳定引用读取结果；脚本 API 最终租约
只保留真实反射实体，不再把 Surface/准备态等令牌误报成通用实体。助手与界面现在共用同一份图片效果
schema、文档构建器与互斥规则，`diffusion` 与 `vgpu_glow` 的全量参数均可发现、可校验、可执行，
不再只停留在界面控件。

2026-08-31 图片编辑 V3 将 `image_edit` 从预览只读域升级为实时可写域：当前打开的编辑器
只向 live registry 登记真实 `ImageEditCommandBusV3` 句柄，不复制文档或像素真相。`document` / `layer` /
`group` / `mask` / `resource` 以包含 document/layer identity 的稳定引用反射；图层、组与蒙版的可表达
属性及安全增删全部经通用事务写回同一命令总线，V2 preview 继续只读兼容。`image_mark.annotation`
在 V3 会话中是标注图层子实体，V2 会话仍走原链路。能力发现只水合“请求实体 ∩ 真实反射实体”，
不回声伪造 ID；第 27、28 份 StoreLedger 分别将视口/拖拽和工具/选中会话定义为不进文档的视图或瞬态状态。

2026-08-31 Henji Script 真机验收登记的四条空转问题已全部注销：脚本回执采用 128KB 内联下限
（仍受上下文 60% 上限约束），常见的裸 `result`、`return result`、数字转字符串、漏写/多写 `await`、
把 action ID 当方法名以及变量拼写错误都会给出可直接重写的诊断；外部等待续跑的脚本租约已由
`5bddca16` 恢复。这些修复减少无效重试，不放宽脚本语言的权限边界。

2026-09-04 画布多图层文档节点新增专用 `open_multi_layer_document_node_editor` 导航能力，补齐
此前缺失的“按语义打开节点文档编辑器”能力。助手与节点 UI 现在共享正式
application service：按稳定工程/节点引用打开工程、进入既有画布、聚焦节点、校验 editable-v3 权威
会话后，只打开该节点自己的 `MultiLayerDocumentEditorDialog`；普通节点、引用错配和旧 V1 会给出可自纠
拒绝，不会误开工具箱独立图片编辑器，也不会在 R0 导航中暗中迁移数据。通用 `openToolDialog` 仍是
分镜拆分等多种节点工具共用的视图容器，继续以 excluded 明确排除；语义动作由各自 application
service/capability 建账，不能把容器开关误归给单一能力。本次将原本缺失的语义动作从“不通”改为“通”，
没有新增旧式工具或平行执行通道。

2026-09-05 多图层结果新增 `retry_layer_stack_result`：助手与节点“重新获取结果”共用正式恢复服务，
仅续取已生成但下载失败的原任务；兼容从同节点、同输入与来源的工程历史恢复旧版丢失的任务上下文。
不暴露服务端任务编号、不重新生成，返回值只证明恢复请求已提交，完成仍需后续读取节点确认。

2026-09-06 3D 镜头输出新增 `render_camera_stage_output`、`get_camera_stage_render_task` 与
`cancel_camera_stage_render_task`：助手与画布节点共用同一后台渲染任务服务，提交后可离开画布并按稳定引用
查询或取消；只有带匹配请求回执的持久结果节点才能报告完成。新建 3D 工程也改用不切换当前界面的后台落库
入口，返回默认摄像机与首状态关键帧引用；打开工程仍由独立导航能力负责（`2a1cf8f2`），L-A/L-B 已通过。
2026-09-07 补验：正式 Electron 的播放、跨工程后台任务、助手输出能力三类场景在双尺寸下 6/6 通过，
浏览器错误、应用错误与警告均为 0；助手能力场景登记于 `3adcf77a`，工程恢复循环修复为 `ca0cb7ca`。
证据：`.ui-tour/三维修复与画笔原图取证/evidence.json`。本次是非 LLM、非付费的应用验收，不计作 L-C。

验证层（详见 [testing.md](testing.md) 第四节）：

| 层 | 命令 | 规模（2026-08-31） |
|---|---|---|
| L-A 静态不变量 | `npm run check:assistant-capabilities` | 结构检查 + 30 测试文件 / 170 用例 |
| L-B 剧本 harness | `npm run test:assistant-harness` | 9 文件 / 35 用例 / 15 秒（验伪线 60 秒） |
| 生产回归 | `npm run test:assistant-production` | 53 文件 / 395 用例 + 持久化进程 43/43 |
| L-C 真机 | `npm run assistant:live:suite` | 分钟级、真实付费 |

**2026-08-18 真机全量实跑（真 Provider `mimo-v2.5-pro`，`assistant_decides`）：8 个场景全部 `passed`。**

| 场景 | 回合 | 输入 token | 耗时 | Effect/写入 | 工具失败 |
|---|---|---|---|---|---|
| 三维（建连查+时间轴） | 5 | 121,161 | 106s | 21 / 13 | 0 |
| 素材（增改删+读回） | 6 | 66,172 | 113s | 15 / 6 | 1 |
| 画布（建连查） | 3 | 28,920 | 42s | 8 / 4 | 0 |
| 设置（读改验+同事务恢复） | 3 | 28,801 | 38s | 8 / 2 | 0 |
| 生成（外部等待续跑，真实付费） | 2 | 38,688 | 187s | 8 / 4 | 1 |
| 探针 `image_edit` | 2 | 15,666 | 29s | 0 | 0 |
| 探针 `storyboard` | 11 | **222,612** | 198s | 8 | 3 |
| 探针 `toolbox` | 10 | 100,578 | 141s | 14 | 2 |

判据取自 CLI 验收器（终态+封存+`isMutatingEffect` 计数+正式验证摘要），不看模型自述。
另有两条原本登记为"人工"的验收项已改为自动核查并全部通过：最终答复无裸本地路径/密钥
（6 次运行 0 命中）、每次运行 `discover_application_capabilities` 恰好 1 次。

## 三、还没通的

当前没有挂在 `KNOWN_UNPUBLISHABLE` / `KNOWN_UNREACHABLE` 的“声明可写但实际走不通”结构性欠账；
2026-08-18 真机验收登记的四条脚本空转问题也已全部注销。

### 真机验收的数据卫生问题

`assistant:live:suite` **不清理自己造的数据**。截至 2026-08-18，真实库里堆着
43 个 `自动验收-*` 三维工程、46 个画布工程、4 个素材集合、33 条助手会话，最早可追到 08-14。
这让"跑一次真机"的隐性成本远高于 token 账单，也是真机验收让人不想跑的原因之一。
要么给套件加收尾清理，要么给这批数据一个批量删除入口。

## 四、已确定不做（可以重新打开，但要在这里改）

不是「没排上」，是「讨论过、决定不做」。再提起时先看这里，别重复讨论。

- **复杂 swarm / team 多 Agent** —— 在没有真实需求和基线数据前不建设。
- **技能市场或在线分发**、**技能内脚本执行**、**`allowed-tools`** —— 技能是数据不是权限，完全不携带工具授权语义。
- **MCP、HTTP、插件等外部适配器** —— 助手的权限边界就是这个软件本身。
- **5 项受保护设置**（涉及真实密钥或系统文件选择器）—— 渲染层技术上做不到，不是顾虑。
  另外 2 项无安全理由的（`models.visibility`、`updates.configuration`）已松绑。
- **直接展示未经处理的模型原始思维链** —— 只展示供应商返回的 reasoning。
- **删除数据分级（C0–C3）、暴露面、五档风险等级** —— 对单机应用偏重，但在跑且没添麻烦，拆除风险大于收益。

## 五、推翻记录

2026-09-12：按用户要求撤销“内置助手默认不开放付费生成、MCP 重启默认关闭”的策略，改为默认开放并保存用户后续选择。修复新连接授权勾选仅存在于设置组件临时状态的问题；内置助手契约明确指向聊天操作权限，不再误导用户新建 MCP 连接。

2026-09-12：按用户要求移除节点工具栏的“拖动素材”按钮，改用 Shift + 左键拖动；避免为同一拖放动作增加工具栏入口，Ctrl/Command 多选与 Alt 拖动复制继续保留。

2026-09-12：按用户新的产品选择，重新开放内置助手，撤销“所有内置助手入口默认隐藏”的决定。新入口使用 Pi，旧聊天面板不再承担默认对话；否则新旧推理循环并存会让权限、历史和行为来源不明确。旧代码及历史文件保留，不删除用户资料。外部 MCP 路线继续保留。

**曾经做好、后来被撤掉或改掉的东西。** 记在这里是为了防两件事：把已经拆掉的东西当成还在，
以及把已经证伪的方案再试一遍。

格式：`日期 · 提交 · 推翻了什么 → 换成了什么 · 为什么旧的不能留`

- **2026-09-12 · MCP 语义操作补验** · 推翻 2.2/3.2「对外语义操作已具备可用基线」的判断：工程与生成实体的领域版本此前被误拿去比较助手界面计数，且三维任务查询不能产生取消所需的读取凭据。现语义写入按原目标反射版本核对；无版本任务读取仍绑定原任务与会话，取消由正式任务服务验证身份与实时状态。生成落图补回整批事务 undoRef，避免节点已存却返回失败。生成取消及落图由 mcpSemanticRevisions.test.ts 守，三维由 MCP Reality 守；这不代表两个真实 Agent 的业务链已验收。

- **2026-09-12 · MCP 受控写入** · 更正 1.1 独立入口“内部实体读取已隔离”的证据：原 `readEntity` 仅过滤属性，仍会先调用不可见 provider 并返回元数据；当时集成拒绝可能来自 PAL 未初始化。现实体读、列表和属性／集合可用性统一先检查暴露面与数据级别，精确测试证明被拒 provider 调用次数为零。同步补齐设置恢复写盘失败与画布改名保存失败的实际部分修改，不能用零成功步骤当作未执行。

- **2026-09-11 · 缺失节点恢复** · 下线能力仍不可创建或执行，但不再阻止原工程打开；撤销此前把下线人像节点改为普通生成节点的处理，改为保留原数据并显示缺失占位。
- **2026-09-11 · 商品摄影下线** · 按用户要求移除 `image.product-photography` 的画布入口、助手可选能力及宿主模型登记，不做旧数据兼容；公共 SDK 适配保留供其他项目使用。
- **2026-09-11 · 暗光增强下线** · 按用户要求移除 `image.low-light-enhancement` 的画布入口、
  助手可选能力及宿主模型登记，避免助手继续创建已撤下的工具。公共 SDK 的 ControlLight
  适配保留，供其他项目使用；不提供旧节点兼容逻辑。

- **2026-09-10 · 人像质感下线** · 按用户要求撤下 `image.portrait-texture` 与专属节点，
  图片工具栏、助手枚举和节点创建目录同步移除。原先的通用模型加保守提示词方案尚不足以
  支撑独立质感功能；旧工程与撤销历史中的节点迁移为普通图片生成节点，保留提示词、
  模型参数、素材、连线和已有结果，不继续提供专属预设或自动重新生成。

- **2026-09-06 · `2dcc2a2a`** · 补正 Gateway 单并发键能保护通用事务、失败文本足以保留
  实际修改的判断：core 的 commit/undo 现在一次占住全部受影响 scope（含附着图片的画布投影），
  并在锁内复核 revision、幂等、消费状态、写权限与宿主；无关 scope 不串行，不给 UI 指针事件加锁。
  业务部分失败、保存失败和提交后验证失败的有界事实沿原 renderer/Gateway/脚本账本传输，保留
  实际 Effects、引用、revision、撤销和恢复信息，不透传文档或任意错误 details，不重放已执行命令。
  `assistantHarness.transactionReliability.test.ts` 覆盖独立 Gateway 并发及附着图片双域 partial 回执；
  失败操作仍为失败、Effects 不伪装 verified。任务级 seal 继续表示封存事实而非全部成功，保留意见
  不消失；最终模型失败的兜底说明只复述已记录摘要，不笼统宣称全部完成或验证。权限仍沿用
  `application:write` 信任模型，scope 不是权限，也未新增细粒度授权体系。
  后续 `108d28a9` 补齐同批修改后直接删除的安全撤销：只接受同一已完成记录的 direct remove 解释
  目标缺失，仍以集合权威 revision 复核；无关缺失和过期基线继续拒绝，由
  `engineUndoRemovedTarget.test.ts` / `undoRevisionProbe.test.ts` 守住。
  2026-09-07 集成证据：`4b266c93` 的 CI `34044815072` 四项必需门禁均通过，覆盖此处及下条图片
  持久化改动；该证据不外推后续提交的 CI 状态，也不表示整轮界面验收全部完成。

- **2026-09-06 · `9ff5d313`** · 补正 V3 实时可写即代表可靠保存的结论：同步命令成功
  只能证明内存变化。四类正式宿主现在复用同一保存队列登记 durable owner，预览宿主不开放写入；
  core 在整批修改或撤销末尾只确认一次，失败保留实际修改、部分补偿和恢复事实，不重放命令。
  附着画布节点通过原有唯一物化服务确认节点投影，普通工具草稿不自动 Apply；重试仅保存的节点
  确认不得伪造第二次修改 Effect。`assistantHarness.imageEditPersistence.test.ts` 与
  `imageEditPersistenceRecovery.test.ts` 覆盖拒写恢复、双域存储回读和级联回执；异步宿主、跨组批次与
  撤销由精确测试补充。恢复能力沿用 `application:write` 信任边界；全部受影响 scope 的并发守卫与
  失败事实跨 Gateway 传输已由后续 `2dcc2a2a` 补齐，未新增细粒度授权体系。

- **2026-09-06 · `d5f973e0`** · 补正 2026-08-25「画布显式写入已等待存储」的结论：当时只覆盖工程操作，
  节点、素材组、批次与撤销仍把防抖入队当作成功。现在共用串行持久化确认屏障；失败保留当前修改和
  资源引用，`retry_canvas_project_save` 与界面重试均只保存、不重放业务，跨工程失败按原工程归属。
  `assistantHarness.canvasPersistence.test.ts` 经正式发现、脚本、Gateway 与存储回读验证拒绝及恢复。
  这次画布改动不包含图片编辑 V3 宿主确认，不能据此宣称全部写域均已完成该保证。

- **2026-08-25 · `0bc8c65e`** · 推翻画布项目“写入调用发出即算成功”以及 harness 只核对 Zustand
  就能证明落盘的旧判定 → 显式项目操作等待 SQLite、退出前强制冲刷，L-B 同时回读
  `storyboardProjects` 持久化真相源。旧做法会吞掉数据库异常，使界面和助手在工程实际未保存时仍报成功。
- **2026-08-17 · `7b9b9965`** · 删除**任务图与结算**机制 → 封存改由模型收工触发。
  破坏性变更，旧协议整体移除。
- **2026-08-17 · `d597d1c3`** · 撤销**脚本调用次数上限** → 改由「事实判定原地打转」。
  硬上限拦不住真打转，却会砍掉正常的长任务。
- **2026-08-16 · `52dd6be6`** · 能力发现改为**实体粒度扁平请求**，删除运行时改写。
- **2026-08-16 · `576b940c`** · 撤回**重复读 artifact 的硬拦守卫**（该守卫上线后被证明是错的修法，
  真因是卸载判定两处不一致，见 `fbfe84aa`）。
- **2026-08-15 · `2a1640bd`** · **路由**从硬过滤降级为工具域**软信号**，授权改由显式位承载。
  路由是假设不是判决——软信号不得硬过滤，模型必须能推翻它。
- **`88a70ba6`** · 移除 `declare_action_plan` 协议 → 任务图阈值改由脚本验证承担。
- **（更早）** 旧 `HostCommand` / `HostQuery` 执行入口整体删除 → 原生 `ApplicationCapabilityDefinition`
  + 正式领域服务为唯一业务实现。

- **2026-09-13** · 推翻所有 MCP／Pi 写入必须由模型拼接读取基线的要求。实际生成在“缺目标／无关作用域／版本冲突”之间反复失败；普通操作改由宿主自动读取状态，严格旧状态写入与破坏性操作仍保留基线。费用保护由笼统付费权限补充为正式估价与持久额度，不用逐次确认阻断少量生成。

## 六、实机验收（2026-08-26 关闭）

历史上分七条任务线推进助手能力（能力对齐、双路径收口、智能助手、内部应用接口、
智能化升级、持续会话与运行时演进、技能系统），逐阶段任务文档原在 `docs/task/`，
已随本次清理整体删除，需要翻旧计划从 git 历史取。

**用户于 2026-08-26 确认：这些任务线遗留的实机验收全部按通过计。** 包括此前挂着的
鼠标交互（滚动接管、拖拽粘贴附件、停靠宽度）、隐私遮罩截图判读、重启后会话恢复、
旧画布工程兼容。这是用户的验收结论，不是自动化证据——没有对应的门禁或测试记录。

因此「技能系统」的 **3.1 提示词瘦身解除阻塞**，可以开工；它当初的阻塞理由就是要等这批
实机验收走完，好留下可比基线。

> 注意：这条只关闭"等人点一遍"性质的验收。第三节那些挂在门禁上的欠账**不受影响**——
> 那是 `hostScopeCoverage.test.ts` / `assistantHarness.writeLoop.test.ts` 里还实际登记着的
> 结构性阻塞，改清单不改 provider 等于把台账写成比现实更好看，违反第零节的纪律。
