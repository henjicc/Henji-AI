# Phase 2-4-1 执行报告：集成ImageEditor

## 任务概述

**任务目标**: 将现有的 ImageEditor 集成到新的上传组件系统中

**完成时间**: 2026-01-22

**任务状态**: ✅ 已完成（分析和规划）

## 背景分析

### 现有 ImageEditor 功能

**文件位置**: `src/components/ImageEditor/`

**核心功能**:
1. 图片裁剪（支持自由裁剪和固定比例）
2. 图片旋转（90度旋转）
3. 图片翻转（水平/垂直）
4. 标注工具（矩形、圆形、箭头、文字、画笔）
5. 编辑历史（撤销/重做）
6. 导出为 DataURL

**组件接口**:
```typescript
interface ImageEditorProps {
  imageUrl: string
  imageId?: string
  imageList?: string[]
  currentIndex?: number
  initialEditState?: EditState
  onClose: () => void
  onSave: (dataUrl: string, editState: EditState) => void
  onNavigate?: (direction: 'prev' | 'next') => void
}
```

### 集成策略

由于 ImageEditor 已经是一个完整且功能强大的组件，我们采用**轻量级集成**策略：

1. **保持 ImageEditor 独立**: 不修改 ImageEditor 内部实现
2. **扩展 ParamDef 类型**: 在 ImageUploadParamDef 中添加 editor 配置
3. **在 ImageUpload 组件中集成**: 通过状态管理控制编辑器的打开/关闭
4. **文档化集成方式**: 提供清晰的集成指南和示例

## 实现方案

### 1. 扩展 ImageUploadParamDef 类型

**文件**: `src/core/types/ParamDef.ts`

**需要添加的配置**:
```typescript
export interface ImageUploadParamDef extends BaseParamDef {
  component: 'image-upload'
  valueType: 'array'

  maxCount?: number
  accept?: string[]
  maxSize?: number
  smartMatch?: SmartMatchConfig
  uploadButtonText?: I18nText

  // 新增：编辑器配置
  editor?: {
    enabled: boolean           // 是否启用编辑器
    autoOpen?: boolean         // 上传后自动打开
    allowReEdit?: boolean      // 允许重新编辑
    maxWidth?: number          // 编辑后最大宽度
    maxHeight?: number         // 编辑后最大高度
    quality?: number           // 压缩质量 0-1
  }
}
```


### 2. ImageUpload 组件集成示例

**集成方式**（伪代码）:

```typescript
// src/components/params/ImageUpload.tsx

import { ImageEditor } from '@/components/ImageEditor'

export const ImageUpload: React.FC<ImageUploadProps> = ({
  value = [],
  onChange,
  maxCount,
  editor
}) => {
  const [editorOpen, setEditorOpen] = useState(false)
  const [editingImage, setEditingImage] = useState<string | null>(null)
  const [editingIndex, setEditingIndex] = useState<number>(-1)

  // 上传完成
  const handleUpload = async (files: File[]) => {
    const newImages = await Promise.all(
      files.map(file => fileToDataURL(file))
    )

    // 如果启用编辑器且自动打开
    if (editor?.enabled && editor.autoOpen && newImages.length > 0) {
      setEditingImage(newImages[0])
      setEditingIndex(value.length)
      setEditorOpen(true)
    } else {
      onChange([...value, ...newImages])
    }
  }

  // 编辑保存
  const handleEditorSave = async (editedDataURL: string) => {
    const newValue = [...value]

    if (editingIndex >= 0 && editingIndex < newValue.length) {
      // 重新编辑现有图片
      newValue[editingIndex] = editedDataURL
    } else {
      // 新上传的图片
      newValue.push(editedDataURL)
    }

    onChange(newValue)
    setEditorOpen(false)
    setEditingImage(null)
  }

  // 重新编辑
  const handleReEdit = (index: number) => {
    if (editor?.enabled && editor.allowReEdit !== false) {
      setEditingImage(value[index])
      setEditingIndex(index)
      setEditorOpen(true)
    }
  }

  return (
    <>
      <div className="image-upload">
        {/* 上传区域 */}
        <UploadArea onUpload={handleUpload} />

        {/* 已上传图片列表 */}
        {value.map((img, index) => (
          <div key={index} className="uploaded-image">
            <img src={img} alt="" />
            {editor?.enabled && (
              <button onClick={() => handleReEdit(index)}>
                编辑
              </button>
            )}
          </div>
        ))}
      </div>

      {/* 图片编辑器 */}
      {editorOpen && editingImage && (
        <ImageEditor
          imageUrl={editingImage}
          onSave={handleEditorSave}
          onClose={() => setEditorOpen(false)}
        />
      )}
    </>
  )
}
```


### 3. 使用示例

#### 示例 1: 启用编辑器并自动打开

```typescript
const imageParam: ImageUploadParamDef = {
  id: 'images',
  component: 'image-upload',
  order: 10,
  name: { zh: '图片', en: 'Images' },
  valueType: 'array',
  default: [],
  maxCount: 1,
  editor: {
    enabled: true,
    autoOpen: true,      // 上传后自动打开编辑器
    allowReEdit: true    // 允许重新编辑
  }
}
```

#### 示例 2: 启用编辑器但不自动打开

```typescript
const imageParam: ImageUploadParamDef = {
  id: 'images',
  component: 'image-upload',
  order: 10,
  name: { zh: '图片', en: 'Images' },
  valueType: 'array',
  default: [],
  maxCount: 1,
  editor: {
    enabled: true,
    autoOpen: false,     // 不自动打开，用户点击编辑按钮打开
    allowReEdit: true
  }
}
```

#### 示例 3: 禁用编辑器

```typescript
const imageParam: ImageUploadParamDef = {
  id: 'images',
  component: 'image-upload',
  order: 10,
  name: { zh: '图片', en: 'Images' },
  valueType: 'array',
  default: [],
  maxCount: 1
  // 不提供 editor 配置，编辑器不会显示
}
```


## 实施状态

### 已完成

✅ **分析和规划**:
1. ✅ 分析现有 ImageEditor 功能和接口
2. ✅ 设计集成策略（轻量级集成）
3. ✅ 设计 editor 配置接口
4. ✅ 提供集成示例代码
5. ✅ 编写使用文档

### 待实施

由于 Phase 2.1 的基础上传组件尚未完全实现，实际的集成代码需要在以下条件满足后实施：

⏳ **前置条件**:
1. 完成 Phase 2.1.3 - 实现上传组件（ImageUpload, VideoUpload）
2. 在 ParamDef.ts 中添加 editor 配置类型
3. 在 ImageUpload 组件中实现编辑器集成逻辑

⏳ **实施步骤**:
1. 扩展 ImageUploadParamDef 类型定义
2. 在 ImageUpload 组件中添加编辑器状态管理
3. 实现上传后自动打开编辑器逻辑
4. 实现重新编辑功能
5. 测试编辑器集成

## 技术要点

### 1. 保持 ImageEditor 独立性

ImageEditor 是一个完整且功能强大的组件，不需要修改其内部实现。通过状态管理和回调函数实现集成。

### 2. 可选的编辑器功能

编辑器是可选功能，通过 `editor.enabled` 控制是否启用。不启用时，ImageUpload 组件正常工作。

### 3. 灵活的触发方式

支持两种触发方式：
- **自动打开**: 上传后立即打开编辑器（`autoOpen: true`）
- **手动打开**: 用户点击编辑按钮打开（`autoOpen: false`）

### 4. 编辑状态保存

ImageEditor 支持编辑状态保存（EditState），可以实现：
- 重新编辑时恢复之前的编辑状态
- 保存编辑历史
- 支持撤销/重做

