# 任务 3-1-1：设计数据库表结构

## 任务信息

- **阶段**: Phase 3 - 数据与存储
- **优先级**: P1（高优先级）
- **预计工时**: 4-6 小时
- **依赖**: 无
- **前置条件**: SQLite 依赖已安装

## 目标

设计完整的 SQLite 数据库表结构，用于替换当前的 JSON 文件存储系统（history.json）。

## 背景

当前系统使用 JSON 文件存储历史记录和预设，存在以下问题：

1. **Base64 膨胀问题**: history.json 可能包含 Base64 图片数据，导致文件急剧膨胀
2. **查询效率低**: 无法高效查询和筛选历史记录
3. **并发写入风险**: 多次生成可能导致文件损坏
4. **数据完整性**: 缺乏数据验证和约束

## 实施步骤

### 步骤 1: 分析现有数据结构

**位置**: `src/types/index.ts`, `history.json`

**当前 HistoryItem 接口**:
```typescript
interface HistoryItem {
  id: string
  provider: string
  model: string
  type: 'image' | 'video' | 'audio'
  prompt: string
  result: {
    url?: string          // ⚠️ 可能包含 base64
    filePath?: string     // 实际文件路径
    taskId?: string
  }
  timestamp: number
  settings: Record<string, any>  // 生成时的参数
}
```

**问题分析**:
- `result.url` 可能包含巨大的 base64 数据
- `settings` 存储为 JSON，难以查询
- 缺少索引和外键约束

### 步骤 2: 设计表结构

创建 SQL 设计文档：

**文件**: `D:\VibeCode\Henji-AI\docs\REFACTOR2026\database-schema.sql`

```sql
-- ==================== 生成历史表 ====================
CREATE TABLE IF NOT EXISTS history (
  -- 主键
  id TEXT PRIMARY KEY,

  -- 模型信息
  provider_id TEXT NOT NULL,          -- 'ppio', 'fal', 'kie', 'modelscope'
  model_id TEXT NOT NULL,             -- 'wan-2.6', 'kling-v2.6', etc.
  type TEXT NOT NULL,                 -- 'image', 'video', 'audio'

  -- 生成参数
  prompt TEXT,                        -- 提示词
  params TEXT,                        -- JSON 格式的完整参数

  -- 结果（只存储文件路径，不存储 base64/url）
  file_path TEXT,                     -- 本地文件路径（相对于 AppLocalData）
  task_id TEXT,                       -- API 任务 ID（用于追踪）

  -- 状态
  status TEXT NOT NULL DEFAULT 'completed',  -- 'completed', 'failed', 'timeout'
  error_message TEXT,                 -- 错误信息（如果失败）

  -- 元数据
  cost REAL,                          -- 生成成本（元）
  duration INTEGER,                   -- 生成耗时（秒）

  -- 时间戳
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,

  -- 索引提示
  -- INDEX: provider_id, model_id, type, created_at
  CHECK (type IN ('image', 'video', 'audio')),
  CHECK (status IN ('completed', 'failed', 'timeout'))
);

-- 索引：按时间倒序查询（最常用）
CREATE INDEX IF NOT EXISTS idx_history_created_at
  ON history(created_at DESC);

-- 索引：按模型查询
CREATE INDEX IF NOT EXISTS idx_history_model
  ON history(provider_id, model_id);

-- 索引：按类型查询
CREATE INDEX IF NOT EXISTS idx_history_type
  ON history(type, created_at DESC);

-- 全文搜索索引（用于提示词搜索）
CREATE VIRTUAL TABLE IF NOT EXISTS history_fts USING fts5(
  id UNINDEXED,
  prompt,
  content=history,
  content_rowid=rowid
);

-- 触发器：自动更新 updated_at
CREATE TRIGGER IF NOT EXISTS update_history_timestamp
  AFTER UPDATE ON history
BEGIN
  UPDATE history SET updated_at = CURRENT_TIMESTAMP WHERE id = NEW.id;
END;

-- ==================== 预设表 ====================
CREATE TABLE IF NOT EXISTS presets (
  -- 主键
  id TEXT PRIMARY KEY,

  -- 预设信息
  name TEXT NOT NULL,                 -- 预设名称
  description TEXT,                   -- 预设描述

  -- 模型绑定（可选）
  model_id TEXT,                      -- 绑定的模型 ID（NULL 表示全局预设）

  -- 参数
  params TEXT NOT NULL,               -- JSON 格式的参数对象

  -- 元数据
  is_favorite INTEGER DEFAULT 0,     -- 是否收藏（0/1）
  use_count INTEGER DEFAULT 0,       -- 使用次数

  -- 时间戳
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,

  CHECK (is_favorite IN (0, 1))
);

-- 索引：按名称搜索
CREATE INDEX IF NOT EXISTS idx_presets_name
  ON presets(name);

-- 索引：按模型查询
CREATE INDEX IF NOT EXISTS idx_presets_model
  ON presets(model_id);

-- 索引：收藏优先 + 使用次数排序
CREATE INDEX IF NOT EXISTS idx_presets_favorite_usage
  ON presets(is_favorite DESC, use_count DESC, created_at DESC);

-- 触发器：自动更新 updated_at
CREATE TRIGGER IF NOT EXISTS update_presets_timestamp
  AFTER UPDATE ON presets
BEGIN
  UPDATE presets SET updated_at = CURRENT_TIMESTAMP WHERE id = NEW.id;
END;

-- ==================== 应用设置表 ====================
CREATE TABLE IF NOT EXISTS settings (
  -- 键值对
  key TEXT PRIMARY KEY,
  value TEXT NOT NULL,

  -- 元数据
  type TEXT DEFAULT 'string',         -- 'string', 'number', 'boolean', 'json'
  description TEXT,                   -- 设置说明

  -- 时间戳
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,

  CHECK (type IN ('string', 'number', 'boolean', 'json'))
);

-- 触发器：自动更新 updated_at
CREATE TRIGGER IF NOT EXISTS update_settings_timestamp
  AFTER UPDATE ON settings
BEGIN
  UPDATE settings SET updated_at = CURRENT_TIMESTAMP WHERE key = NEW.key;
END;

-- ==================== 自定义模型表（Phase 3 预留）====================
CREATE TABLE IF NOT EXISTS custom_models (
  -- 主键
  id TEXT PRIMARY KEY,

  -- 基础信息
  name TEXT NOT NULL,
  provider_id TEXT NOT NULL,          -- 'modelscope' 等
  base_model TEXT,                    -- 基础模型 ID

  -- 配置
  config TEXT NOT NULL,               -- JSON 格式的完整模型配置

  -- 元数据
  is_enabled INTEGER DEFAULT 1,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,

  CHECK (is_enabled IN (0, 1))
);

-- 触发器：自动更新 updated_at
CREATE TRIGGER IF NOT EXISTS update_custom_models_timestamp
  AFTER UPDATE ON custom_models
BEGIN
  UPDATE custom_models SET updated_at = CURRENT_TIMESTAMP WHERE id = NEW.id;
END;
```

### 步骤 3: 设计数据访问层接口

**文件**: `src/services/database/types.ts`

```typescript
// 历史记录类型
export interface HistoryRecord {
  id: string
  providerId: string
  modelId: string
  type: 'image' | 'video' | 'audio'
  prompt: string | null
  params: Record<string, any>
  filePath: string | null
  taskId: string | null
  status: 'completed' | 'failed' | 'timeout'
  errorMessage: string | null
  cost: number | null
  duration: number | null
  createdAt: string
  updatedAt: string
}

// 预设类型
export interface PresetRecord {
  id: string
  name: string
  description: string | null
  modelId: string | null  // null = 全局预设
  params: Record<string, any>
  isFavorite: boolean
  useCount: number
  createdAt: string
  updatedAt: string
}

// 设置类型
export interface SettingRecord {
  key: string
  value: string
  type: 'string' | 'number' | 'boolean' | 'json'
  description: string | null
  updatedAt: string
}

// 查询选项
export interface HistoryQueryOptions {
  providerId?: string
  modelId?: string
  type?: 'image' | 'video' | 'audio'
  status?: 'completed' | 'failed' | 'timeout'
  limit?: number
  offset?: number
  searchPrompt?: string
}

export interface PresetQueryOptions {
  modelId?: string | null
  onlyFavorites?: boolean
  limit?: number
  offset?: number
}

// 数据库接口
export interface DatabaseService {
  // 历史记录
  insertHistory(record: Omit<HistoryRecord, 'createdAt' | 'updatedAt'>): Promise<void>
  getHistory(options?: HistoryQueryOptions): Promise<HistoryRecord[]>
  getHistoryById(id: string): Promise<HistoryRecord | null>
  deleteHistory(id: string): Promise<void>
  clearHistory(olderThan?: Date): Promise<number>  // 返回删除数量

  // 预设
  insertPreset(preset: Omit<PresetRecord, 'createdAt' | 'updatedAt' | 'useCount'>): Promise<void>
  getPresets(options?: PresetQueryOptions): Promise<PresetRecord[]>
  getPresetById(id: string): Promise<PresetRecord | null>
  updatePreset(id: string, updates: Partial<PresetRecord>): Promise<void>
  deletePreset(id: string): Promise<void>
  incrementPresetUsage(id: string): Promise<void>

  // 设置
  getSetting(key: string): Promise<string | null>
  setSetting(key: string, value: string, type?: SettingRecord['type']): Promise<void>
  deleteSetting(key: string): Promise<void>

  // 工具方法
  vacuum(): Promise<void>  // 数据库优化
  backup(): Promise<string>  // 备份数据库，返回备份文件路径
}
```

### 步骤 4: 数据库位置规划

**位置**: `src/config/paths.ts`

```typescript
import { appLocalDataDir, join } from '@tauri-apps/api/path'

export async function getDatabasePath(): Promise<string> {
  const appDataDir = await appLocalDataDir()
  return join(appDataDir, 'Henji-AI', 'database.db')
}

export async function getBackupDatabasePath(timestamp: string): Promise<string> {
  const appDataDir = await appLocalDataDir()
  return join(appDataDir, 'Henji-AI', 'Backups', `database_${timestamp}.db`)
}
```

### 步骤 5: 文档数据库迁移计划

**文件**: `D:\VibeCode\Henji-AI\docs\REFACTOR2026\phase3-6\migration-plan.md`

```markdown
# 数据迁移计划

## 迁移流程

1. **备份现有数据**
   - 备份 `history.json` 到 `history.json.backup`
   - 记录文件大小和条目数量

2. **创建 SQLite 数据库**
   - 执行 schema.sql 创建表结构
   - 验证表和索引创建成功

3. **迁移历史记录**
   - 读取 `history.json`
   - 逐条解析并插入数据库
   - **关键**: 过滤掉 `result.url` 字段，只保留 `filePath`
   - 验证数据完整性

4. **迁移预设（如果存在）**
   - 从 localStorage 读取预设
   - 转换格式并插入数据库

5. **验证迁移**
   - 对比条目数量
   - 随机抽样验证数据准确性
   - 检查文件大小（应显著减小）

6. **清理旧文件**
   - 删除 `history.json`（保留备份）
   - 更新应用配置指向新数据库

## 数据清洗规则

### 历史记录清洗

```typescript
// ❌ 错误：保留 base64
{
  result: {
    url: "data:image/png;base64,iVBORw0KGgo...",  // 巨大
    filePath: "/path/to/image.png"
  }
}

// ✅ 正确：只保留 filePath
{
  filePath: "/path/to/image.png",
  taskId: "task-123"
}
```

### 多文件处理

```typescript
// 旧格式：使用 ||| 分隔符
filePath: "/path/1.png|||/path/2.png|||/path/3.png"

// 新格式：存储为 JSON 数组
params: {
  outputFiles: ["/path/1.png", "/path/2.png", "/path/3.png"]
}
```
```

## 涉及文件

### 新建文件

- `D:\VibeCode\Henji-AI\docs\REFACTOR2026\database-schema.sql` - SQL 表结构定义
- `D:\VibeCode\Henji-AI\docs\REFACTOR2026\phase3-6\migration-plan.md` - 迁移计划文档
- `D:\VibeCode\Henji-AI\src\services\database\types.ts` - TypeScript 类型定义

### 修改文件

- `D:\VibeCode\Henji-AI\src\config\paths.ts` - 添加数据库路径配置

## 验收标准

- [ ] SQL schema 文件完整，包含所有表、索引、触发器
- [ ] 表结构支持以下功能：
  - [ ] 历史记录存储（不含 base64）
  - [ ] 预设存储（支持全局和模型级）
  - [ ] 应用设置存储
  - [ ] 自定义模型配置（预留）
- [ ] TypeScript 类型定义完整，与 SQL schema 一致
- [ ] 索引设计合理，覆盖常用查询场景
- [ ] 约束和检查完整（CHECK, NOT NULL, 外键等）
- [ ] 触发器正确（自动更新时间戳）
- [ ] 全文搜索索引配置正确
- [ ] 数据库路径配置正确
- [ ] 迁移计划文档清晰，包含数据清洗规则

## 注意事项

1. **数据安全**
   - 绝对不能在数据库中存储 base64 数据
   - 只存储相对文件路径（相对于 AppLocalData）
   - 确保备份机制

2. **性能考虑**
   - 历史记录可能有数千条，需要索引优化
   - 全文搜索索引大小控制
   - 定期 VACUUM 清理碎片

3. **兼容性**
   - SQLite 版本兼容性（Tauri 内置版本）
   - 跨平台路径处理（Windows/macOS）

4. **扩展性**
   - 预留自定义模型表
   - 预留统计分析字段（cost, duration）
   - 支持未来新增字段（ALTER TABLE）

## 后续任务

完成此任务后，继续：
- **任务 3-1-2**: 实现数据库操作层
- **任务 3-1-3**: 迁移历史数据
