# 任务 3-1-2：实现数据库操作层

## 任务信息

- **阶段**: Phase 3 - 数据与存储
- **优先级**: P1（高优先级）
- **预计工时**: 8-12 小时
- **依赖**: 任务 3-1-1（数据库表结构设计）
- **前置条件**: SQLite schema 已定义

## 目标

实现完整的数据库操作层，提供类型安全的 API 用于访问 SQLite 数据库。

## 背景

当前系统使用 JSON 文件存储，缺乏结构化查询能力。新的数据库操作层需要：

1. **类型安全**: 使用 TypeScript 确保编译时类型检查
2. **异步操作**: 所有数据库操作返回 Promise
3. **错误处理**: 统一的错误处理机制
4. **事务支持**: 批量操作使用事务确保一致性
5. **跨平台**: 通过 Tauri 插件在 Windows/macOS 上正常工作

## 实施步骤

### 步骤 1: 安装 SQLite 依赖

**文件**: `package.json`

```bash
npm install @tauri-apps/plugin-sql
```

**配置 Tauri 插件** (`src-tauri/Cargo.toml`):

```toml
[dependencies]
tauri-plugin-sql = { version = "2.x", features = ["sqlite"] }
```

**注册插件** (`src-tauri/src/main.rs`):

```rust
use tauri_plugin_sql::{Builder, Migration, MigrationKind};

fn main() {
    let migrations = vec![
        Migration {
            version: 1,
            description: "初始化数据库",
            sql: include_str!("../migrations/001_initial.sql"),
            kind: MigrationKind::Up,
        }
    ];

    tauri::Builder::default()
        .plugin(
            Builder::default()
                .add_migrations("sqlite:henji.db", migrations)
                .build(),
        )
        .run(tauri::generate_context!())
        .expect("启动 Tauri 应用失败");
}
```

### 步骤 2: 实现数据库服务类

**文件**: `src/services/database/DatabaseService.ts`

```typescript
import Database from '@tauri-apps/plugin-sql'
import type {
  DatabaseService as IDatabaseService,
  HistoryRecord,
  PresetRecord,
  SettingRecord,
  HistoryQueryOptions,
  PresetQueryOptions,
} from './types'

class DatabaseService implements IDatabaseService {
  private db: Database | null = null
  private dbPath = 'sqlite:henji.db'

  // 初始化数据库连接
  async init(): Promise<void> {
    if (this.db) return

    try {
      this.db = await Database.load(this.dbPath)
      console.log('[Database] 连接成功')
    } catch (error) {
      console.error('[Database] 连接失败:', error)
      throw new Error(`数据库初始化失败: ${error}`)
    }
  }

  // 确保数据库已连接
  private ensureConnected(): Database {
    if (!this.db) {
      throw new Error('数据库未初始化，请先调用 init()')
    }
    return this.db
  }

  // ==================== 历史记录操作 ====================

  async insertHistory(
    record: Omit<HistoryRecord, 'createdAt' | 'updatedAt'>
  ): Promise<void> {
    const db = this.ensureConnected()

    await db.execute(
      `INSERT INTO history (
        id, provider_id, model_id, type, prompt, params,
        file_path, task_id, status, error_message, cost, duration
      ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)`,
      [
        record.id,
        record.providerId,
        record.modelId,
        record.type,
        record.prompt,
        JSON.stringify(record.params),
        record.filePath,
        record.taskId,
        record.status,
        record.errorMessage,
        record.cost,
        record.duration,
      ]
    )
  }

  async getHistory(options: HistoryQueryOptions = {}): Promise<HistoryRecord[]> {
    const db = this.ensureConnected()

    let sql = 'SELECT * FROM history WHERE 1=1'
    const params: any[] = []

    if (options.providerId) {
      sql += ' AND provider_id = ?'
      params.push(options.providerId)
    }

    if (options.modelId) {
      sql += ' AND model_id = ?'
      params.push(options.modelId)
    }

    if (options.type) {
      sql += ' AND type = ?'
      params.push(options.type)
    }

    if (options.status) {
      sql += ' AND status = ?'
      params.push(options.status)
    }

    if (options.searchPrompt) {
      sql += ' AND id IN (SELECT id FROM history_fts WHERE prompt MATCH ?)'
      params.push(options.searchPrompt)
    }

    sql += ' ORDER BY created_at DESC'

    if (options.limit) {
      sql += ' LIMIT ?'
      params.push(options.limit)
    }

    if (options.offset) {
      sql += ' OFFSET ?'
      params.push(options.offset)
    }

    const rows = await db.select<any[]>(sql, params)

    return rows.map(this.mapHistoryRow)
  }

  async getHistoryById(id: string): Promise<HistoryRecord | null> {
    const db = this.ensureConnected()

    const rows = await db.select<any[]>(
      'SELECT * FROM history WHERE id = ?',
      [id]
    )

    return rows.length > 0 ? this.mapHistoryRow(rows[0]) : null
  }

  async deleteHistory(id: string): Promise<void> {
    const db = this.ensureConnected()
    await db.execute('DELETE FROM history WHERE id = ?', [id])
  }

  async clearHistory(olderThan?: Date): Promise<number> {
    const db = this.ensureConnected()

    if (olderThan) {
      const result = await db.execute(
        'DELETE FROM history WHERE created_at < ?',
        [olderThan.toISOString()]
      )
      return result.rowsAffected
    } else {
      const result = await db.execute('DELETE FROM history')
      return result.rowsAffected
    }
  }

  // ==================== 预设操作 ====================

  async insertPreset(
    preset: Omit<PresetRecord, 'createdAt' | 'updatedAt' | 'useCount'>
  ): Promise<void> {
    const db = this.ensureConnected()

    await db.execute(
      `INSERT INTO presets (
        id, name, description, model_id, params, is_favorite
      ) VALUES (?, ?, ?, ?, ?, ?)`,
      [
        preset.id,
        preset.name,
        preset.description,
        preset.modelId,
        JSON.stringify(preset.params),
        preset.isFavorite ? 1 : 0,
      ]
    )
  }

  async getPresets(options: PresetQueryOptions = {}): Promise<PresetRecord[]> {
    const db = this.ensureConnected()

    let sql = 'SELECT * FROM presets WHERE 1=1'
    const params: any[] = []

    if (options.modelId !== undefined) {
      if (options.modelId === null) {
        sql += ' AND model_id IS NULL'
      } else {
        sql += ' AND (model_id = ? OR model_id IS NULL)'
        params.push(options.modelId)
      }
    }

    if (options.onlyFavorites) {
      sql += ' AND is_favorite = 1'
    }

    sql += ' ORDER BY is_favorite DESC, use_count DESC, created_at DESC'

    if (options.limit) {
      sql += ' LIMIT ?'
      params.push(options.limit)
    }

    if (options.offset) {
      sql += ' OFFSET ?'
      params.push(options.offset)
    }

    const rows = await db.select<any[]>(sql, params)
    return rows.map(this.mapPresetRow)
  }

  async getPresetById(id: string): Promise<PresetRecord | null> {
    const db = this.ensureConnected()

    const rows = await db.select<any[]>(
      'SELECT * FROM presets WHERE id = ?',
      [id]
    )

    return rows.length > 0 ? this.mapPresetRow(rows[0]) : null
  }

  async updatePreset(
    id: string,
    updates: Partial<PresetRecord>
  ): Promise<void> {
    const db = this.ensureConnected()

    const fields: string[] = []
    const params: any[] = []

    if (updates.name !== undefined) {
      fields.push('name = ?')
      params.push(updates.name)
    }

    if (updates.description !== undefined) {
      fields.push('description = ?')
      params.push(updates.description)
    }

    if (updates.params !== undefined) {
      fields.push('params = ?')
      params.push(JSON.stringify(updates.params))
    }

    if (updates.isFavorite !== undefined) {
      fields.push('is_favorite = ?')
      params.push(updates.isFavorite ? 1 : 0)
    }

    if (fields.length === 0) return

    params.push(id)

    await db.execute(
      `UPDATE presets SET ${fields.join(', ')} WHERE id = ?`,
      params
    )
  }

  async deletePreset(id: string): Promise<void> {
    const db = this.ensureConnected()
    await db.execute('DELETE FROM presets WHERE id = ?', [id])
  }

  async incrementPresetUsage(id: string): Promise<void> {
    const db = this.ensureConnected()
    await db.execute(
      'UPDATE presets SET use_count = use_count + 1 WHERE id = ?',
      [id]
    )
  }

  // ==================== 设置操作 ====================

  async getSetting(key: string): Promise<string | null> {
    const db = this.ensureConnected()

    const rows = await db.select<any[]>(
      'SELECT value FROM settings WHERE key = ?',
      [key]
    )

    return rows.length > 0 ? rows[0].value : null
  }

  async setSetting(
    key: string,
    value: string,
    type: SettingRecord['type'] = 'string'
  ): Promise<void> {
    const db = this.ensureConnected()

    await db.execute(
      `INSERT INTO settings (key, value, type)
       VALUES (?, ?, ?)
       ON CONFLICT(key) DO UPDATE SET value = ?, type = ?`,
      [key, value, type, value, type]
    )
  }

  async deleteSetting(key: string): Promise<void> {
    const db = this.ensureConnected()
    await db.execute('DELETE FROM settings WHERE key = ?', [key])
  }

  // ==================== 工具方法 ====================

  async vacuum(): Promise<void> {
    const db = this.ensureConnected()
    await db.execute('VACUUM')
  }

  async backup(): Promise<string> {
    const db = this.ensureConnected()
    const timestamp = new Date().toISOString().replace(/[:.]/g, '-')
    const backupPath = `sqlite:henji_backup_${timestamp}.db`

    // Tauri SQL 插件不直接支持备份，需要通过复制实现
    // 这里返回备份路径，实际复制由调用方处理
    return backupPath
  }

  // ==================== 私有辅助方法 ====================

  private mapHistoryRow(row: any): HistoryRecord {
    return {
      id: row.id,
      providerId: row.provider_id,
      modelId: row.model_id,
      type: row.type,
      prompt: row.prompt,
      params: JSON.parse(row.params || '{}'),
      filePath: row.file_path,
      taskId: row.task_id,
      status: row.status,
      errorMessage: row.error_message,
      cost: row.cost,
      duration: row.duration,
      createdAt: row.created_at,
      updatedAt: row.updated_at,
    }
  }

  private mapPresetRow(row: any): PresetRecord {
    return {
      id: row.id,
      name: row.name,
      description: row.description,
      modelId: row.model_id,
      params: JSON.parse(row.params || '{}'),
      isFavorite: row.is_favorite === 1,
      useCount: row.use_count,
      createdAt: row.created_at,
      updatedAt: row.updated_at,
    }
  }
}

// 单例
export const databaseService = new DatabaseService()
```

### 步骤 3: 实现数据库初始化 Hook

**文件**: `src/hooks/useDatabase.ts`

```typescript
import { useEffect, useState } from 'react'
import { databaseService } from '@/services/database/DatabaseService'

export function useDatabase() {
  const [isReady, setIsReady] = useState(false)
  const [error, setError] = useState<Error | null>(null)

  useEffect(() => {
    databaseService
      .init()
      .then(() => {
        setIsReady(true)
        console.log('[useDatabase] 数据库初始化成功')
      })
      .catch((err) => {
        setError(err)
        console.error('[useDatabase] 数据库初始化失败:', err)
      })
  }, [])

  return { isReady, error }
}
```

### 步骤 4: 实现错误处理

**文件**: `src/services/database/errors.ts`

```typescript
export class DatabaseError extends Error {
  constructor(
    message: string,
    public code: string,
    public originalError?: any
  ) {
    super(message)
    this.name = 'DatabaseError'
  }
}

export class DatabaseNotInitializedError extends DatabaseError {
  constructor() {
    super('数据库未初始化', 'DB_NOT_INITIALIZED')
  }
}

export class DatabaseQueryError extends DatabaseError {
  constructor(query: string, originalError: any) {
    super(
      `查询执行失败: ${query}`,
      'DB_QUERY_ERROR',
      originalError
    )
  }
}

export class DatabaseConstraintError extends DatabaseError {
  constructor(constraint: string, originalError: any) {
    super(
      `约束违反: ${constraint}`,
      'DB_CONSTRAINT_ERROR',
      originalError
    )
  }
}
```

### 步骤 5: 编写单元测试

**文件**: `src/services/database/__tests__/DatabaseService.test.ts`

```typescript
import { describe, it, expect, beforeAll, afterAll } from 'vitest'
import { databaseService } from '../DatabaseService'
import type { HistoryRecord } from '../types'

describe('DatabaseService', () => {
  beforeAll(async () => {
    await databaseService.init()
  })

  afterAll(async () => {
    await databaseService.clearHistory()
  })

  describe('历史记录操作', () => {
    it('应该能插入历史记录', async () => {
      const record: Omit<HistoryRecord, 'createdAt' | 'updatedAt'> = {
        id: 'test-1',
        providerId: 'ppio',
        modelId: 'wan-2.6',
        type: 'video',
        prompt: '测试提示词',
        params: { duration: 5 },
        filePath: '/test/video.mp4',
        taskId: 'task-123',
        status: 'completed',
        errorMessage: null,
        cost: 3.0,
        duration: 45,
      }

      await databaseService.insertHistory(record)

      const retrieved = await databaseService.getHistoryById('test-1')
      expect(retrieved).not.toBeNull()
      expect(retrieved?.prompt).toBe('测试提示词')
    })

    it('应该能查询历史记录', async () => {
      const results = await databaseService.getHistory({
        type: 'video',
        limit: 10,
      })

      expect(results).toBeInstanceOf(Array)
      expect(results.length).toBeGreaterThan(0)
    })

    it('应该能全文搜索提示词', async () => {
      const results = await databaseService.getHistory({
        searchPrompt: '测试',
      })

      expect(results.length).toBeGreaterThan(0)
      expect(results[0].prompt).toContain('测试')
    })
  })

  describe('预设操作', () => {
    it('应该能插入预设', async () => {
      await databaseService.insertPreset({
        id: 'preset-1',
        name: '测试预设',
        description: '描述',
        modelId: 'wan-2.6',
        params: { duration: 10 },
        isFavorite: true,
      })

      const retrieved = await databaseService.getPresetById('preset-1')
      expect(retrieved).not.toBeNull()
      expect(retrieved?.name).toBe('测试预设')
    })

    it('应该能更新预设', async () => {
      await databaseService.updatePreset('preset-1', {
        name: '更新后的名称',
      })

      const retrieved = await databaseService.getPresetById('preset-1')
      expect(retrieved?.name).toBe('更新后的名称')
    })

    it('应该能增加使用次数', async () => {
      const before = await databaseService.getPresetById('preset-1')
      await databaseService.incrementPresetUsage('preset-1')
      const after = await databaseService.getPresetById('preset-1')

      expect(after!.useCount).toBe(before!.useCount + 1)
    })
  })
})
```

## 涉及文件

### 新建文件

- `D:\VibeCode\Henji-AI\src\services\database\DatabaseService.ts` - 数据库服务实现
- `D:\VibeCode\Henji-AI\src\services\database\errors.ts` - 错误类型定义
- `D:\VibeCode\Henji-AI\src\services\database\index.ts` - 导出文件
- `D:\VibeCode\Henji-AI\src\hooks\useDatabase.ts` - 数据库初始化 Hook
- `D:\VibeCode\Henji-AI\src\services\database\__tests__\DatabaseService.test.ts` - 单元测试
- `D:\VibeCode\Henji-AI\src-tauri\migrations\001_initial.sql` - 初始化迁移脚本

### 修改文件

- `D:\VibeCode\Henji-AI\package.json` - 添加依赖
- `D:\VibeCode\Henji-AI\src-tauri\Cargo.toml` - 添加 Rust 插件
- `D:\VibeCode\Henji-AI\src-tauri\src\main.rs` - 注册插件

## 验收标准

- [ ] DatabaseService 类实现完整，包含所有接口方法
- [ ] 所有数据库操作使用 TypeScript 类型安全
- [ ] 错误处理机制完善，自定义错误类型
- [ ] 单例模式实现正确
- [ ] 查询支持过滤、分页、排序
- [ ] 全文搜索功能正常
- [ ] 预设支持全局和模型级查询
- [ ] 使用次数自动统计
- [ ] 时间戳自动更新（触发器）
- [ ] 单元测试覆盖率 > 80%
- [ ] 性能测试通过（1000 条记录插入 < 1s）
- [ ] 跨平台测试通过（Windows + macOS）

## 注意事项

1. **类型转换**
   - SQLite 的 INTEGER 0/1 需要转换为 TypeScript boolean
   - JSON 字段需要序列化/反序列化
   - 日期字段使用 ISO 8601 格式

2. **性能优化**
   - 批量插入使用事务
   - 查询使用索引
   - 避免 SELECT *，只查询需要的字段

3. **错误处理**
   - 捕获所有数据库异常
   - 提供有意义的错误信息
   - 记录错误日志

4. **SQL 注入防护**
   - 所有查询使用参数化语句
   - 不使用字符串拼接

## 后续任务

完成此任务后，继续：
- **任务 3-1-3**: 迁移历史数据
- **任务 3-2-1**: 重构预设存储
