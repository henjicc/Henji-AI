# Phase 3-1-2 执行报告：实现数据库操作层

## 任务概述

**任务目标**: 实现完整的数据库操作层，提供类型安全的 API 用于访问 SQLite 数据库

**完成时间**: 2026-01-22

**任务状态**: ✅ 已完成

## 实现内容

### 1. DatabaseService 类 (DatabaseService.ts)

**文件路径**: `src/services/database/DatabaseService.ts`

**代码行数**: 约 480 行

**主要功能**:
- 数据库连接管理
- 历史记录 CRUD 操作
- 预设 CRUD 操作
- 应用设置管理
- 自定义模型管理
- 工具方法（vacuum, backup）

#### 1.1 初始化和连接管理

```typescript
class DatabaseService implements IDatabaseService {
  private db: Database | null = null
  private dbPath = 'sqlite:henji.db'

  async init(): Promise<void> {
    if (this.db) return

    try {
      this.db = await Database.load(this.dbPath)
      console.log('[Database] Connected successfully')
    } catch (error) {
      console.error('[Database] Connection failed:', error)
      throw new Error(`Database initialization failed: ${error}`)
    }
  }

  private ensureConnected(): Database {
    if (!this.db) {
      throw new Error('Database not initialized. Please call init() first.')
    }
    return this.db
  }
}
```

#### 1.2 历史记录操作

**插入记录**:
```typescript
async insertHistory(
  record: Omit<HistoryRecord, 'createdAt' | 'updatedAt'>
): Promise<void> {
  const db = this.ensureConnected()

  await db.execute(
    `INSERT INTO history (
      id, provider_id, model_id, type, prompt, params,
      file_path, task_id, status, error_message, cost, duration
    ) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)`,
    [
      record.id,
      record.providerId,
      record.modelId,
      record.type,
      record.prompt,
      JSON.stringify(record.params),
      record.filePath,
      record.taskId,
      record.status,
      record.errorMessage,
      record.cost,
      record.duration,
    ]
  )
}
```

**查询记录（支持多条件过滤）**:
```typescript
async getHistory(options: HistoryQueryOptions = {}): Promise<HistoryRecord[]> {
  const db = this.ensureConnected()

  let sql = 'SELECT * FROM history WHERE 1=1'
  const params: any[] = []

  if (options.providerId) {
    sql += ' AND provider_id = ?'
    params.push(options.providerId)
  }

  if (options.modelId) {
    sql += ' AND model_id = ?'
    params.push(options.modelId)
  }

  if (options.type) {
    sql += ' AND type = ?'
    params.push(options.type)
  }

  if (options.status) {
    sql += ' AND status = ?'
    params.push(options.status)
  }

  if (options.searchPrompt) {
    sql += ' AND id IN (SELECT id FROM history_fts WHERE prompt MATCH ?)'
    params.push(options.searchPrompt)
  }

  sql += ' ORDER BY created_at DESC'

  if (options.limit) {
    sql += ' LIMIT ?'
    params.push(options.limit)
  }

  if (options.offset) {
    sql += ' OFFSET ?'
    params.push(options.offset)
  }

  const rows = await db.select<any[]>(sql, params)
  return rows.map(this.mapHistoryRow)
}
```

#### 1.3 预设操作

**查询预设（支持全局和模型级）**:
```typescript
async getPresets(options: PresetQueryOptions = {}): Promise<PresetRecord[]> {
  const db = this.ensureConnected()

  let sql = 'SELECT * FROM presets WHERE 1=1'
  const params: any[] = []

  if (options.modelId !== undefined) {
    if (options.modelId === null) {
      sql += ' AND model_id IS NULL'
    } else {
      sql += ' AND (model_id = ? OR model_id IS NULL)'
      params.push(options.modelId)
    }
  }

  if (options.onlyFavorites) {
    sql += ' AND is_favorite = 1'
  }

  sql += ' ORDER BY is_favorite DESC, use_count DESC, created_at DESC'

  if (options.limit) {
    sql += ' LIMIT ?'
    params.push(options.limit)
  }

  if (options.offset) {
    sql += ' OFFSET ?'
    params.push(options.offset)
  }

  const rows = await db.select<any[]>(sql, params)
  return rows.map(this.mapPresetRow)
}
```

**更新预设（部分更新）**:
```typescript
async updatePreset(
  id: string,
  updates: Partial<PresetRecord>
): Promise<void> {
  const db = this.ensureConnected()

  const fields: string[] = []
  const params: any[] = []

  if (updates.name !== undefined) {
    fields.push('name = ?')
    params.push(updates.name)
  }

  if (updates.description !== undefined) {
    fields.push('description = ?')
    params.push(updates.description)
  }

  if (updates.params !== undefined) {
    fields.push('params = ?')
    params.push(JSON.stringify(updates.params))
  }

  if (updates.isFavorite !== undefined) {
    fields.push('is_favorite = ?')
    params.push(updates.isFavorite ? 1 : 0)
  }

  if (fields.length === 0) return

  params.push(id)

  await db.execute(
    `UPDATE presets SET ${fields.join(', ')} WHERE id = ?`,
    params
  )
}
```

#### 1.4 设置操作

**Upsert 模式（插入或更新）**:
```typescript
async setSetting(
  key: string,
  value: string,
  type: SettingRecord['type'] = 'string'
): Promise<void> {
  const db = this.ensureConnected()

  await db.execute(
    `INSERT INTO settings (key, value, type)
     VALUES (?, ?, ?)
     ON CONFLICT(key) DO UPDATE SET value = ?, type = ?`,
    [key, value, type, value, type]
  )
}
```

#### 1.5 数据映射（SQL → TypeScript）

**类型转换逻辑**:
```typescript
private mapHistoryRow(row: any): HistoryRecord {
  return {
    id: row.id,
    providerId: row.provider_id,        // snake_case → camelCase
    modelId: row.model_id,
    type: row.type,
    prompt: row.prompt,
    params: JSON.parse(row.params || '{}'),  // JSON string → object
    filePath: row.file_path,
    taskId: row.task_id,
    status: row.status,
    errorMessage: row.error_message,
    cost: row.cost,
    duration: row.duration,
    createdAt: row.created_at,
    updatedAt: row.updated_at,
  }
}

private mapPresetRow(row: any): PresetRecord {
  return {
    id: row.id,
    name: row.name,
    description: row.description,
    modelId: row.model_id,
    params: JSON.parse(row.params || '{}'),
    isFavorite: row.is_favorite === 1,  // INTEGER 0/1 → boolean
    useCount: row.use_count,
    createdAt: row.created_at,
    updatedAt: row.updated_at,
  }
}
```

### 2. 错误处理 (errors.ts)

**文件路径**: `src/services/database/errors.ts`

**代码行数**: 约 40 行

**错误类型**:
```typescript
export class DatabaseError extends Error {
  constructor(
    message: string,
    public code: string,
    public originalError?: any
  ) {
    super(message)
    this.name = 'DatabaseError'
  }
}

export class DatabaseNotInitializedError extends DatabaseError {
  constructor() {
    super('Database not initialized', 'DB_NOT_INITIALIZED')
  }
}

export class DatabaseQueryError extends DatabaseError {
  constructor(query: string, originalError: any) {
    super(
      `Query execution failed: ${query}`,
      'DB_QUERY_ERROR',
      originalError
    )
  }
}

export class DatabaseConstraintError extends DatabaseError {
  constructor(constraint: string, originalError: any) {
    super(
      `Constraint violation: ${constraint}`,
      'DB_CONSTRAINT_ERROR',
      originalError
    )
  }
}

export class DatabaseConnectionError extends DatabaseError {
  constructor(originalError: any) {
    super(
      'Failed to connect to database',
      'DB_CONNECTION_ERROR',
      originalError
    )
  }
}
```

### 3. React Hook (useDatabase.ts)

**文件路径**: `src/hooks/useDatabase.ts`

**代码行数**: 约 25 行

**功能**: 在组件挂载时自动初始化数据库

```typescript
export function useDatabase() {
  const [isReady, setIsReady] = useState(false)
  const [error, setError] = useState<Error | null>(null)

  useEffect(() => {
    databaseService
      .init()
      .then(() => {
        setIsReady(true)
        console.log('[useDatabase] Database initialized successfully')
      })
      .catch((err) => {
        setError(err)
        console.error('[useDatabase] Database initialization failed:', err)
      })
  }, [])

  return { isReady, error }
}
```

### 4. 导出文件 (index.ts)

**文件路径**: `src/services/database/index.ts`

**代码行数**: 约 5 行

```typescript
export { databaseService } from './DatabaseService'
export * from './types'
export * from './errors'
```

## 文件统计

| 文件 | 路径 | 行数 | 类型 |
|------|------|------|------|
| DatabaseService.ts | src/services/database/ | ~480 | 服务类 |
| types.ts | src/services/database/ | ~130 | 类型定义 |
| errors.ts | src/services/database/ | ~40 | 错误类 |
| index.ts | src/services/database/ | ~5 | 导出 |
| useDatabase.ts | src/hooks/ | ~25 | React Hook |

**总计**: 新增约 680 行代码

## 技术亮点

### 1. 完整的类型安全

**编译时类型检查**:
```typescript
// ✅ 正确：类型匹配
const record: Omit<HistoryRecord, 'createdAt' | 'updatedAt'> = {
  id: 'test-1',
  providerId: 'ppio',
  modelId: 'wan-2.6',
  type: 'video',
  // ...
}

// ❌ 错误：类型不匹配（编译时报错）
const record = {
  type: 'invalid-type'  // Error: Type '"invalid-type"' is not assignable
}
```

### 2. 参数化查询（防止 SQL 注入）

**安全的查询构建**:
```typescript
// ✅ 正确：使用参数化查询
await db.execute(
  'SELECT * FROM history WHERE id = ?',
  [userId]
)

// ❌ 错误：字符串拼接（易受 SQL 注入攻击）
// await db.execute(`SELECT * FROM history WHERE id = '${userId}'`)
```

### 3. 灵活的查询构建

**动态 SQL 构建**:
```typescript
let sql = 'SELECT * FROM history WHERE 1=1'
const params: any[] = []

if (options.providerId) {
  sql += ' AND provider_id = ?'
  params.push(options.providerId)
}

if (options.modelId) {
  sql += ' AND model_id = ?'
  params.push(options.modelId)
}

// 只添加提供的过滤条件
```

### 4. 单例模式

**全局唯一实例**:
```typescript
// 导出单例
export const databaseService = new DatabaseService()

// 使用
import { databaseService } from '@/services/database'
await databaseService.init()
```

### 5. 自动类型转换

**SQL 类型 → TypeScript 类型**:
- `INTEGER 0/1` → `boolean`
- `TEXT (JSON)` → `Record<string, any>`
- `snake_case` → `camelCase`
- `NULL` → `null`

## 使用示例

### 示例 1: 初始化数据库

```typescript
import { databaseService } from '@/services/database'

// 应用启动时初始化
await databaseService.init()
```

### 示例 2: 插入历史记录

```typescript
await databaseService.insertHistory({
  id: 'task-123',
  providerId: 'ppio',
  modelId: 'wan-2.6',
  type: 'video',
  prompt: '一只可爱的猫咪',
  params: { duration: 5, quality: '720P' },
  filePath: '/Media/video-123.mp4',
  taskId: 'ppio-task-456',
  status: 'completed',
  errorMessage: null,
  cost: 3.0,
  duration: 45,
})
```

### 示例 3: 查询历史记录

```typescript
// 查询所有视频记录
const videos = await databaseService.getHistory({
  type: 'video',
  limit: 20,
})

// 查询特定模型的记录
const wanVideos = await databaseService.getHistory({
  providerId: 'ppio',
  modelId: 'wan-2.6',
  limit: 10,
})

// 全文搜索提示词
const searchResults = await databaseService.getHistory({
  searchPrompt: '猫咪',
  limit: 50,
})
```

### 示例 4: 预设管理

```typescript
// 插入预设
await databaseService.insertPreset({
  id: 'preset-1',
  name: '高质量视频',
  description: '720P 高质量设置',
  modelId: 'wan-2.6',
  params: { duration: 10, quality: '720P' },
  isFavorite: true,
})

// 获取特定模型的预设（包括全局预设）
const presets = await databaseService.getPresets({
  modelId: 'wan-2.6',
})

// 增加使用次数
await databaseService.incrementPresetUsage('preset-1')
```

### 示例 5: 在 React 组件中使用

```typescript
import { useDatabase } from '@/hooks/useDatabase'

function App() {
  const { isReady, error } = useDatabase()

  if (error) {
    return <div>数据库初始化失败: {error.message}</div>
  }

  if (!isReady) {
    return <div>正在初始化数据库...</div>
  }

  return <div>应用已就绪</div>
}
```

## 总结

### 完成情况

✅ 所有任务已完成：

1. ✅ 实现 DatabaseService 类
2. ✅ 实现历史记录 CRUD 操作
3. ✅ 实现预设 CRUD 操作
4. ✅ 实现设置管理
5. ✅ 实现自定义模型管理
6. ✅ 实现错误处理
7. ✅ 实现 React Hook
8. ✅ 完整的类型定义

### 架构优势

1. **类型安全**: 完整的 TypeScript 类型定义，编译时检查
2. **防 SQL 注入**: 所有查询使用参数化语句
3. **灵活查询**: 支持多条件动态查询
4. **单例模式**: 全局唯一数据库连接
5. **自动转换**: SQL 类型自动转换为 TypeScript 类型
6. **错误处理**: 自定义错误类型，便于调试

### 后续工作

1. 完成 Phase 3-1-3: 迁移历史数据
2. 编写单元测试
3. 性能测试和优化
4. 添加数据库版本管理（migrations）

### 相关文件

- 服务实现: `src/services/database/DatabaseService.ts`
- 类型定义: `src/services/database/types.ts`
- 错误处理: `src/services/database/errors.ts`
- React Hook: `src/hooks/useDatabase.ts`
- SQL Schema: `docs/REFACTOR2026/database-schema.sql`

---

**执行人**: Claude Code
**完成时间**: 2026-01-22
**任务状态**: ✅ 已完成
