# 3-2-1 重构预设存储

## 目标

重构预设系统，从 localStorage 迁移到 SQLite 存储，支持模型级和全局预设，消除手动 setter 映射。

## 背景

### 当前问题

**当前预设系统**（`src/config/presetStateMapping.ts`）：

```typescript
// 588 行的手动映射
export interface PresetSetters {
  setInput: (v: string) => void
  setVideoDuration: (v: number) => void
  setPpioKling26AspectRatio: (v: string) => void
  // ... 数十个 setter
}

export function createPresetSetterMap(setters: PresetSetters) {
  return {
    input: setters.setInput,
    videoDuration: setters.setVideoDuration,
    ppioKling26AspectRatio: setters.setPpioKling26AspectRatio,
    // ... 手动维护映射
  }
}
```

**问题分析**：
1. 每个新参数需要手动添加 setter 映射
2. 存储在 localStorage，缺乏查询和管理能力
3. 没有版本兼容性机制
4. 预设格式与模型耦合
5. 无法区分全局预设和模型级预设

### 新架构方案

**核心思想**：预设直接存储参数对象，无需 setter 映射

```typescript
// 新预设格式
interface Preset {
  id: string
  name: string
  description?: string
  modelId?: string  // null = 全局预设
  params: Record<string, any>  // 直接是参数对象
  isFavorite: boolean
  useCount: number
}

// 加载预设
function loadPreset(preset: Preset) {
  const { setParams } = useModelParams(preset.modelId)
  setParams(preset.params)  // 一次性设置所有参数
}
```

**优势**：
- 无需维护 setter 映射
- 预设格式与模型无关
- 支持任意参数组合
- 自动忽略无效参数

## 前置依赖

- [x] 3-1-1: 设计数据库表结构
- [x] 3-1-2: 实现数据库操作层
- [x] 1-3-1: 实现 useModelParams Hook

## 实施步骤

### 步骤 1: 定义预设类型系统

**文件**: `src/core/types/Preset.ts`

```typescript
/**
 * 预设类型定义
 */
export interface Preset {
  /** 唯一标识 */
  id: string

  /** 预设名称 */
  name: string

  /** 预设描述（可选） */
  description?: string

  /** 绑定的模型 ID（null = 全局预设） */
  modelId: string | null

  /** 参数对象 */
  params: PresetParams

  /** 是否收藏 */
  isFavorite: boolean

  /** 使用次数 */
  useCount: number

  /** 创建时间 */
  createdAt: string

  /** 更新时间 */
  updatedAt: string
}

/**
 * 预设参数类型
 */
export type PresetParams = Record<string, any>

/**
 * 创建预设输入
 */
export interface CreatePresetInput {
  name: string
  description?: string
  modelId?: string | null
  params: PresetParams
  isFavorite?: boolean
}

/**
 * 更新预设输入
 */
export interface UpdatePresetInput {
  name?: string
  description?: string
  params?: PresetParams
  isFavorite?: boolean
}

/**
 * 预设查询选项
 */
export interface PresetQueryOptions {
  /** 只查询指定模型的预设 */
  modelId?: string | null

  /** 只查询收藏的预设 */
  onlyFavorites?: boolean

  /** 限制返回数量 */
  limit?: number

  /** 偏移量 */
  offset?: number

  /** 搜索关键词（名称或描述） */
  search?: string
}
```

### 步骤 2: 实现预设服务

**文件**: `src/services/presets/PresetService.ts`

```typescript
import { DatabaseService } from '../database/DatabaseService'
import {
  Preset,
  CreatePresetInput,
  UpdatePresetInput,
  PresetQueryOptions
} from '@/core/types/Preset'
import { nanoid } from 'nanoid'

/**
 * 预设管理服务
 */
export class PresetService {
  private db: DatabaseService

  constructor(database: DatabaseService) {
    this.db = database
  }

  /**
   * 创建预设
   */
  async createPreset(input: CreatePresetInput): Promise<Preset> {
    const preset: Preset = {
      id: nanoid(),
      name: input.name,
      description: input.description || null,
      modelId: input.modelId || null,
      params: input.params,
      isFavorite: input.isFavorite || false,
      useCount: 0,
      createdAt: new Date().toISOString(),
      updatedAt: new Date().toISOString()
    }

    await this.db.insertPreset(preset)
    return preset
  }

  /**
   * 获取预设列表
   */
  async getPresets(options?: PresetQueryOptions): Promise<Preset[]> {
    return await this.db.getPresets(options)
  }

  /**
   * 获取指定模型的预设（包括全局预设）
   */
  async getPresetsForModel(modelId: string): Promise<Preset[]> {
    // 获取模型专用预设
    const modelPresets = await this.db.getPresets({ modelId })

    // 获取全局预设
    const globalPresets = await this.db.getPresets({ modelId: null })

    // 合并并按收藏和使用次数排序
    return [...modelPresets, ...globalPresets].sort((a, b) => {
      if (a.isFavorite !== b.isFavorite) {
        return a.isFavorite ? -1 : 1
      }
      return b.useCount - a.useCount
    })
  }

  /**
   * 获取单个预设
   */
  async getPresetById(id: string): Promise<Preset | null> {
    return await this.db.getPresetById(id)
  }

  /**
   * 更新预设
   */
  async updatePreset(id: string, updates: UpdatePresetInput): Promise<void> {
    await this.db.updatePreset(id, updates)
  }

  /**
   * 删除预设
   */
  async deletePreset(id: string): Promise<void> {
    await this.db.deletePreset(id)
  }

  /**
   * 切换收藏状态
   */
  async toggleFavorite(id: string): Promise<void> {
    const preset = await this.getPresetById(id)
    if (preset) {
      await this.updatePreset(id, { isFavorite: !preset.isFavorite })
    }
  }

  /**
   * 应用预设（增加使用次数）
   */
  async applyPreset(id: string): Promise<Preset | null> {
    await this.db.incrementPresetUsage(id)
    return await this.getPresetById(id)
  }

  /**
   * 导出预设为 JSON
   */
  async exportPreset(id: string): Promise<string> {
    const preset = await this.getPresetById(id)
    if (!preset) {
      throw new Error('Preset not found')
    }

    return JSON.stringify({
      name: preset.name,
      description: preset.description,
      modelId: preset.modelId,
      params: preset.params,
      version: '1.0'
    }, null, 2)
  }

  /**
   * 从 JSON 导入预设
   */
  async importPreset(json: string): Promise<Preset> {
    const data = JSON.parse(json)

    // 验证格式
    if (!data.name || !data.params) {
      throw new Error('Invalid preset format')
    }

    return await this.createPreset({
      name: data.name,
      description: data.description,
      modelId: data.modelId,
      params: data.params
    })
  }

  /**
   * 清理未使用的预设
   */
  async cleanupUnused(minUseCount: number = 0, daysOld: number = 30): Promise<number> {
    const allPresets = await this.getPresets()
    const threshold = new Date()
    threshold.setDate(threshold.getDate() - daysOld)

    let deletedCount = 0

    for (const preset of allPresets) {
      if (
        !preset.isFavorite &&
        preset.useCount <= minUseCount &&
        new Date(preset.createdAt) < threshold
      ) {
        await this.deletePreset(preset.id)
        deletedCount++
      }
    }

    return deletedCount
  }
}

// 单例导出
let presetServiceInstance: PresetService | null = null

export function getPresetService(db: DatabaseService): PresetService {
  if (!presetServiceInstance) {
    presetServiceInstance = new PresetService(db)
  }
  return presetServiceInstance
}
```

### 步骤 3: 实现预设 Hook

**文件**: `src/hooks/usePresets.ts`

```typescript
import { useState, useEffect, useCallback } from 'react'
import { Preset, CreatePresetInput, PresetQueryOptions } from '@/core/types/Preset'
import { getPresetService } from '@/services/presets/PresetService'
import { getDatabaseService } from '@/services/database/DatabaseService'

/**
 * 预设管理 Hook
 */
export function usePresets(options?: PresetQueryOptions) {
  const [presets, setPresets] = useState<Preset[]>([])
  const [loading, setLoading] = useState(true)
  const [error, setError] = useState<Error | null>(null)

  const service = getPresetService(getDatabaseService())

  // 加载预设列表
  const loadPresets = useCallback(async () => {
    try {
      setLoading(true)
      setError(null)
      const data = await service.getPresets(options)
      setPresets(data)
    } catch (err) {
      setError(err as Error)
    } finally {
      setLoading(false)
    }
  }, [options])

  useEffect(() => {
    loadPresets()
  }, [loadPresets])

  // 创建预设
  const createPreset = useCallback(async (input: CreatePresetInput) => {
    const preset = await service.createPreset(input)
    setPresets(prev => [preset, ...prev])
    return preset
  }, [])

  // 更新预设
  const updatePreset = useCallback(async (id: string, updates: Partial<Preset>) => {
    await service.updatePreset(id, updates)
    setPresets(prev => prev.map(p => p.id === id ? { ...p, ...updates } : p))
  }, [])

  // 删除预设
  const deletePreset = useCallback(async (id: string) => {
    await service.deletePreset(id)
    setPresets(prev => prev.filter(p => p.id !== id))
  }, [])

  // 切换收藏
  const toggleFavorite = useCallback(async (id: string) => {
    await service.toggleFavorite(id)
    setPresets(prev => prev.map(p =>
      p.id === id ? { ...p, isFavorite: !p.isFavorite } : p
    ))
  }, [])

  // 应用预设
  const applyPreset = useCallback(async (id: string) => {
    const preset = await service.applyPreset(id)
    if (preset) {
      setPresets(prev => prev.map(p => p.id === id ? preset : p))
    }
    return preset
  }, [])

  return {
    presets,
    loading,
    error,
    createPreset,
    updatePreset,
    deletePreset,
    toggleFavorite,
    applyPreset,
    reload: loadPresets
  }
}

/**
 * 指定模型的预设 Hook
 */
export function useModelPresets(modelId: string | null) {
  return usePresets({ modelId })
}
```

### 步骤 4: 实现预设应用逻辑

**文件**: `src/hooks/usePresetLoader.ts`

```typescript
import { useCallback } from 'react'
import { Preset } from '@/core/types/Preset'
import { useModelParams } from './useModelParams'
import { registry } from '@/core/ModelRegistry'

/**
 * 预设加载 Hook
 */
export function usePresetLoader(currentModelId: string) {
  const { setParams, params } = useModelParams(currentModelId)

  /**
   * 应用预设到当前模型
   */
  const applyPreset = useCallback((preset: Preset) => {
    // 检查预设是否兼容当前模型
    if (preset.modelId && preset.modelId !== currentModelId) {
      console.warn(`Preset is for model ${preset.modelId}, but current model is ${currentModelId}`)
    }

    // 获取当前模型的参数定义
    const schema = registry.getSchema(currentModelId)
    const validParamIds = new Set(schema.map(p => p.id))

    // 过滤出有效参数（忽略无效参数）
    const validParams: Record<string, any> = {}
    for (const [key, value] of Object.entries(preset.params)) {
      if (validParamIds.has(key)) {
        validParams[key] = value
      } else {
        console.debug(`Ignoring invalid parameter: ${key}`)
      }
    }

    // 应用参数
    setParams(validParams)

    return {
      applied: Object.keys(validParams).length,
      ignored: Object.keys(preset.params).length - Object.keys(validParams).length
    }
  }, [currentModelId, setParams])

  /**
   * 从当前参数创建预设
   */
  const createPresetFromCurrent = useCallback((name: string, description?: string) => {
    return {
      name,
      description,
      modelId: currentModelId,
      params: { ...params }
    }
  }, [currentModelId, params])

  return {
    applyPreset,
    createPresetFromCurrent
  }
}
```

### 步骤 5: 实现预设 UI 组件

**文件**: `src/components/Presets/PresetManager.tsx`

```typescript
import React, { useState } from 'react'
import { usePresets } from '@/hooks/usePresets'
import { usePresetLoader } from '@/hooks/usePresetLoader'
import { CreatePresetInput } from '@/core/types/Preset'

interface PresetManagerProps {
  currentModelId: string
  onClose: () => void
}

export function PresetManager({ currentModelId, onClose }: PresetManagerProps) {
  const { presets, loading, createPreset, deletePreset, toggleFavorite, applyPreset } = usePresets()
  const { applyPreset: applyToModel } = usePresetLoader(currentModelId)
  const [showCreateDialog, setShowCreateDialog] = useState(false)

  const handleApply = async (presetId: string) => {
    const preset = presets.find(p => p.id === presetId)
    if (preset) {
      const result = applyToModel(preset)
      await applyPreset(presetId)  // 增加使用次数

      // 显示应用结果
      console.log(`Applied ${result.applied} params, ignored ${result.ignored} params`)
      onClose()
    }
  }

  return (
    <div className="preset-manager">
      <div className="preset-header">
        <h2>预设管理</h2>
        <button onClick={() => setShowCreateDialog(true)}>新建预设</button>
      </div>

      {loading ? (
        <div>加载中...</div>
      ) : (
        <div className="preset-list">
          {presets.map(preset => (
            <div key={preset.id} className="preset-item">
              <div className="preset-info">
                <h3>{preset.name}</h3>
                {preset.description && <p>{preset.description}</p>}
                <span className="preset-meta">
                  {preset.modelId || '全局预设'} · 使用 {preset.useCount} 次
                </span>
              </div>

              <div className="preset-actions">
                <button onClick={() => toggleFavorite(preset.id)}>
                  {preset.isFavorite ? '★' : '☆'}
                </button>
                <button onClick={() => handleApply(preset.id)}>应用</button>
                <button onClick={() => deletePreset(preset.id)}>删除</button>
              </div>
            </div>
          ))}
        </div>
      )}

      {showCreateDialog && (
        <CreatePresetDialog
          currentModelId={currentModelId}
          onSave={createPreset}
          onClose={() => setShowCreateDialog(false)}
        />
      )}
    </div>
  )
}
```

### 步骤 6: 迁移旧预设数据

**文件**: `src/services/presets/migration.ts`

```typescript
import { getPresetService } from './PresetService'
import { getDatabaseService } from '../database/DatabaseService'

/**
 * 从 localStorage 迁移旧预设
 */
export async function migratePresetsFromLocalStorage(): Promise<number> {
  const service = getPresetService(getDatabaseService())

  // 检查 localStorage 中的旧预设
  const oldPresetsJson = localStorage.getItem('presets')
  if (!oldPresetsJson) {
    return 0
  }

  try {
    const oldPresets = JSON.parse(oldPresetsJson)
    let migratedCount = 0

    for (const oldPreset of oldPresets) {
      // 转换格式
      await service.createPreset({
        name: oldPreset.name,
        description: oldPreset.description,
        modelId: oldPreset.modelId || null,
        params: oldPreset.settings || oldPreset.params,
        isFavorite: false
      })
      migratedCount++
    }

    // 备份并删除旧数据
    localStorage.setItem('presets_backup', oldPresetsJson)
    localStorage.removeItem('presets')

    console.log(`Migrated ${migratedCount} presets from localStorage`)
    return migratedCount
  } catch (error) {
    console.error('Failed to migrate presets:', error)
    return 0
  }
}
```

### 步骤 7: 删除旧的预设映射系统

**待删除文件**:
- `src/config/presetStateMapping.ts` (588 行)

**修改文件**: `src/components/MediaGenerator/index.tsx`

```typescript
// 删除旧代码
import { createPresetSetterMap } from '@/config/presetStateMapping'

// 删除 handleSchemaChange 中的 setterMap 逻辑
const setterMap = createPresetSetterMap({
  setInput: state.setInput,
  // ... 数十个 setter
})
```

## 涉及文件

### 新建文件
- `src/core/types/Preset.ts` - 预设类型定义
- `src/services/presets/PresetService.ts` - 预设服务
- `src/services/presets/migration.ts` - 迁移工具
- `src/hooks/usePresets.ts` - 预设管理 Hook
- `src/hooks/usePresetLoader.ts` - 预设加载 Hook
- `src/components/Presets/PresetManager.tsx` - 预设管理 UI
- `src/components/Presets/CreatePresetDialog.tsx` - 创建预设对话框

### 修改文件
- `src/components/MediaGenerator/index.tsx` - 移除旧预设逻辑
- `src/App.tsx` - 添加预设迁移调用

### 删除文件
- `src/config/presetStateMapping.ts` - 588 行的手动映射（完全删除）

## 验收标准

- [ ] PresetService 实现完整，包含 CRUD 操作
- [ ] 支持模型级预设和全局预设
- [ ] 预设应用时自动忽略无效参数
- [ ] 收藏和使用次数功能正常
- [ ] 预设导入导出功能正常
- [ ] usePresets Hook 可以正常加载和管理预设
- [ ] usePresetLoader 可以正确应用预设到模型
- [ ] PresetManager UI 功能完整（列表、创建、删除、应用）
- [ ] 旧预设数据成功迁移到 SQLite
- [ ] 删除 presetStateMapping.ts 后无编译错误

## 测试方法

### 测试用例 1: 创建和应用模型级预设

```typescript
// 1. 创建预设
const { createPreset } = usePresets()
const preset = await createPreset({
  name: '测试预设',
  modelId: 'wan-2.6',
  params: {
    duration: 10,
    resolution: { aspectRatio: '16:9', quality: '1080P' },
    audio: true
  }
})

// 2. 应用预设
const { applyPreset } = usePresetLoader('wan-2.6')
const result = applyPreset(preset)

// 验证
expect(result.applied).toBe(3)
expect(result.ignored).toBe(0)
```

### 测试用例 2: 全局预设在不同模型间使用

```typescript
// 1. 创建全局预设（只包含 prompt）
const preset = await createPreset({
  name: '通用提示词',
  modelId: null,  // 全局
  params: {
    prompt: 'A beautiful sunset over the ocean'
  }
})

// 2. 在模型 A 应用
const { applyPreset: applyToA } = usePresetLoader('wan-2.6')
applyToA(preset)  // 只应用 prompt

// 3. 在模型 B 应用
const { applyPreset: applyToB } = usePresetLoader('kling-v2.6')
applyToB(preset)  // 同样只应用 prompt

// 验证：参数应用正确，不影响其他参数
```

### 测试用例 3: 版本兼容（忽略无效参数）

```typescript
// 1. 创建包含旧参数的预设
const preset = await createPreset({
  name: '旧版预设',
  modelId: 'wan-2.6',
  params: {
    duration: 10,
    oldParameter: 'old-value',  // 已废弃的参数
    anotherOldParam: 123
  }
})

// 2. 应用预设
const { applyPreset } = usePresetLoader('wan-2.6')
const result = applyPreset(preset)

// 验证
expect(result.applied).toBe(1)  // 只有 duration 有效
expect(result.ignored).toBe(2)  // oldParameter 和 anotherOldParam 被忽略
console.log('Ignored parameters are logged but not applied')
```

### 测试用例 4: 预设导出导入

```typescript
// 1. 导出预设
const service = getPresetService(getDatabaseService())
const json = await service.exportPreset('preset-id')

// 2. 验证 JSON 格式
const exported = JSON.parse(json)
expect(exported).toHaveProperty('name')
expect(exported).toHaveProperty('params')
expect(exported).toHaveProperty('version')

// 3. 导入预设
const imported = await service.importPreset(json)
expect(imported.name).toBe(exported.name)
expect(imported.params).toEqual(exported.params)
```

### 测试用例 5: 旧数据迁移

```typescript
// 1. 模拟旧格式数据
localStorage.setItem('presets', JSON.stringify([
  {
    name: '旧预设1',
    modelId: 'wan-2.6',
    settings: { duration: 10 }
  },
  {
    name: '旧预设2',
    settings: { prompt: 'test' }
  }
]))

// 2. 执行迁移
import { migratePresetsFromLocalStorage } from '@/services/presets/migration'
const count = await migratePresetsFromLocalStorage()

// 3. 验证
expect(count).toBe(2)
const presets = await service.getPresets()
expect(presets).toHaveLength(2)
expect(localStorage.getItem('presets')).toBeNull()
expect(localStorage.getItem('presets_backup')).toBeTruthy()
```

## 风险与注意事项

### 风险

1. **数据丢失风险**
   - 旧预设迁移失败
   - 缓解：迁移前备份到 localStorage

2. **兼容性问题**
   - 旧版本应用无法识别新预设格式
   - 缓解：保留备份，提供回滚机制

3. **参数忽略可能导致困惑**
   - 用户不理解为什么某些参数没有应用
   - 缓解：在 UI 显示应用结果（X 个应用，Y 个忽略）

### 注意事项

1. **性能优化**
   - 预设列表可能很长，需要分页或虚拟滚动
   - 使用次数更新应该异步，不阻塞 UI

2. **用户体验**
   - 应用预设时提供明确的反馈
   - 收藏预设应该排在前面
   - 提供搜索和筛选功能

3. **数据一致性**
   - 确保 SQLite 事务正确
   - 删除预设时确认操作

4. **测试覆盖**
   - 测试所有边界情况
   - 测试迁移逻辑的健壮性

## 实现参考

### 完整的创建预设对话框

```typescript
// src/components/Presets/CreatePresetDialog.tsx
import React, { useState } from 'react'
import { usePresetLoader } from '@/hooks/usePresetLoader'
import { CreatePresetInput } from '@/core/types/Preset'

interface CreatePresetDialogProps {
  currentModelId: string
  onSave: (input: CreatePresetInput) => Promise<void>
  onClose: () => void
}

export function CreatePresetDialog({
  currentModelId,
  onSave,
  onClose
}: CreatePresetDialogProps) {
  const [name, setName] = useState('')
  const [description, setDescription] = useState('')
  const [isGlobal, setIsGlobal] = useState(false)
  const { createPresetFromCurrent } = usePresetLoader(currentModelId)

  const handleSave = async () => {
    const presetData = createPresetFromCurrent(name, description)

    await onSave({
      ...presetData,
      modelId: isGlobal ? null : currentModelId
    })

    onClose()
  }

  return (
    <div className="preset-dialog">
      <h3>创建预设</h3>

      <div className="form-group">
        <label>预设名称</label>
        <input
          type="text"
          value={name}
          onChange={e => setName(e.target.value)}
          placeholder="输入预设名称"
        />
      </div>

      <div className="form-group">
        <label>描述（可选）</label>
        <textarea
          value={description}
          onChange={e => setDescription(e.target.value)}
          placeholder="描述预设用途"
        />
      </div>

      <div className="form-group">
        <label>
          <input
            type="checkbox"
            checked={isGlobal}
            onChange={e => setIsGlobal(e.target.checked)}
          />
          全局预设（适用于所有模型）
        </label>
        <p className="help-text">
          {isGlobal
            ? '只会保存通用参数（如提示词），不保存模型特定参数'
            : `只适用于当前模型（${currentModelId}）`
          }
        </p>
      </div>

      <div className="dialog-actions">
        <button onClick={onClose}>取消</button>
        <button onClick={handleSave} disabled={!name.trim()}>保存</button>
      </div>
    </div>
  )
}
```

## 回滚方案

如果新预设系统出现问题，按以下步骤回滚：

1. **恢复旧文件**
   ```bash
   git checkout HEAD -- src/config/presetStateMapping.ts
   ```

2. **恢复 localStorage 数据**
   ```typescript
   const backup = localStorage.getItem('presets_backup')
   if (backup) {
     localStorage.setItem('presets', backup)
   }
   ```

3. **还原 MediaGenerator 组件**
   - 恢复 handleSchemaChange 中的 setterMap 逻辑

4. **禁用新预设系统**
   - 注释掉 PresetManager 组件的引用
   - 回退数据库表更改（如果需要）
